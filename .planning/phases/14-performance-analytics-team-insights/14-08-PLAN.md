---
phase: 14-performance-analytics-team-insights
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/analytics/PerformanceDashboard.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "PerformanceDashboard displays real teacher data in all tabs"
    - "Teacher selection dropdown shows real teacher names"
    - "PerformanceMetricsGrid receives teachers array with data"
    - "GradeTrendChart receives actual trend data"
  artifacts:
    - path: "src/components/analytics/PerformanceDashboard.tsx"
      provides: "Dashboard with real data integration"
      contains: "teachers.map|data.map"
  key_links:
    - from: "src/components/analytics/PerformanceDashboard.tsx"
      to: "PerformanceMetricsGrid, GradeTrendChart, MultiSubjectChart"
      via: "Data props"
      pattern: "<PerformanceMetricsGrid teachers=|<GradeTrendChart data="

---

<objective>
Update PerformanceDashboard to accept and display real data from analytics page

Purpose: Remove empty array placeholders and wire real data to all dashboard components and tabs
Output: PerformanceDashboard that displays real analytics data across all 4 tabs
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-performance-analytics-team-insights/14-performance-analytics-team-insights-VERIFICATION.md
@.planning/phases/14-performance-analytics-team-insights/14-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PerformanceDashboard props to accept real data</name>
  <files>src/components/analytics/PerformanceDashboard.tsx</files>
  <action>
    Update src/components/analytics/PerformanceDashboard.tsx component to accept real data props instead of using empty arrays:

    1. Add props interface to define data parameters:
       ```typescript
       interface PerformanceDashboardProps {
         teachers: TeacherWithMetrics[]
         gradeTrendData: GradeTrendData[]
         comparisonData: TeacherComparisonData[]
         counselingStats: CounselingStats | null
       }
       ```

    2. Update component function signature:
       ```typescript
       export default function PerformanceDashboard({
         teachers,
         gradeTrendData,
         comparisonData,
         counselingStats
       }: PerformanceDashboardProps)
       ```

    3. Remove hardcoded empty arrays from state:
       - Remove: `const [teachers, setTeachers] = useState([])`
       - Remove: `const [data, setData] = useState([])`
       - Data is now passed as props from analytics page (Plan 14-07)

    4. Update individual tab:
       - Replace `<PerformanceMetricsGrid teachers={[]} />` with `<PerformanceMetricsGrid teachers={teachers} />`

    5. Update trend tab:
       - Replace `<GradeTrendChart data={[]} />` with `<GradeTrendChart data={gradeTrendData} />`

    6. Update comparison tab:
       - Replace placeholder comparison data with actual comparisonData
       - If comparisonData has items, map over them
       - Otherwise show empty state message

    7. Update summary tab:
       - Display counselingStats if available
       - Show totalSessions, averageSatisfaction from counselingStats
       - Aggregate statistics across all teachers (total students, average grade change)

    8. Remove placeholder teacher selection options:
       - Find Select component with options ["선생님 1", "선생님 2", "선생님 3"]
       - Replace with dynamic options: `teachers.map((t) => ({ value: t.id, label: t.name }))`
       - Handle case when teachers array is empty

    Reference: 14-05-SUMMARY.md shows component structure. Lines 112, 121, 142 in VERIFICATION.md have empty arrays that need replacement.

    Verification gap: VERIFICATION.md states "uses empty arrays (teachers=[], data=[]) in all tabs" and "Placeholder Select options".
  </action>
  <verify>
    - Check PerformanceDashboardProps interface defines teachers, gradeTrendData, comparisonData, counselingStats
    - Verify component uses props data instead of empty arrays
    - Check Select options map over teachers array (not hardcoded strings)
    - Verify all 4 tabs receive and use real data
    - Run TypeScript build: `npm run build` passes
  </verify>
  <done>
    PerformanceDashboard accepts real data as props, all tabs display actual analytics data, teacher selection shows real teacher names from database
  </done>
</task>

</tasks>

<verification>
1. Navigate to /analytics page
2. Verify individual tab shows teacher cards with real data
3. Verify trend tab displays grade trend chart with actual data points
4. Verify comparison tab shows teacher comparison data
5. Verify summary tab shows counseling statistics and aggregated metrics
6. Verify teacher dropdown shows real teacher names (not "선생님 1", "선생님 2")
</verification>

<success_criteria>
PerformanceDashboard displays real analytics data across all 4 tabs when receiving props from analytics page
</success_criteria>

<output>
After completion, create `.planning/phases/14-performance-analytics-team-insights/14-08-SUMMARY.md`
</output>
