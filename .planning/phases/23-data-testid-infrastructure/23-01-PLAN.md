---
phase: 23-data-testid-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/students/page.tsx
  - src/components/students/student-detail.tsx
  - src/components/students/student-form.tsx
  - src/components/students/tabs/analysis-tab.tsx
  - src/components/students/saju-analysis-panel.tsx
  - src/components/students/mbti-analysis-panel.tsx
  - src/components/students/name-analysis-panel.tsx
  - src/app/(dashboard)/admin/llm-settings/page.tsx
  - src/app/(dashboard)/admin/llm-settings/provider-card.tsx
  - src/app/(dashboard)/admin/llm-usage/page.tsx
  - src/app/(dashboard)/admin/llm-usage/usage-charts.tsx
  - src/app/(dashboard)/admin/llm-usage/cost-alerts.tsx
autonomous: true

must_haves:
  truths:
    - "학생 카드에 data-testid='student-card'과 내부 student-name, student-grade, student-school이 있어 E2E에서 셀렉트 가능하다"
    - "분석 탭에서 data-testid='saju-tab', 'mbti-tab', 'analysis-loading'이 존재하여 탭 전환과 로딩 상태를 테스트 가능하다"
    - "LLM 설정 페이지에 data-testid='current-provider', 'provider-select'가 존재한다"
    - "LLM 사용량 페이지에 data-testid='usage-chart', 'total-tokens', 'estimated-cost'가 존재한다"
  artifacts:
    - path: "src/app/(dashboard)/students/page.tsx"
      provides: "student-card, student-name, student-grade, student-school data-testid"
      contains: "data-testid=\"student-name\""
    - path: "src/components/students/tabs/analysis-tab.tsx"
      provides: "analysis-loading data-testid"
      contains: "data-testid=\"analysis-loading\""
    - path: "src/app/(dashboard)/admin/llm-settings/page.tsx"
      provides: "current-provider data-testid"
      contains: "data-testid=\"current-provider\""
    - path: "src/app/(dashboard)/admin/llm-usage/page.tsx"
      provides: "total-tokens, estimated-cost data-testid"
      contains: "data-testid=\"total-tokens\""
  key_links:
    - from: "tests/e2e/student.spec.ts"
      to: "src/app/(dashboard)/students/page.tsx"
      via: "data-testid selectors"
      pattern: "data-testid=\"student-card\""
    - from: "tests/e2e/analysis.spec.ts"
      to: "src/components/students/tabs/analysis-tab.tsx"
      via: "data-testid selectors"
      pattern: "data-testid=\"saju-tab\""
    - from: "tests/e2e/admin.spec.ts"
      to: "src/app/(dashboard)/admin/llm-settings/page.tsx"
      via: "data-testid selectors"
      pattern: "data-testid=\"current-provider\""
---

<objective>
학생, 분석, Admin 페이지 컴포넌트에 data-testid 속성을 추가하여 E2E 테스트 셀렉터와 정합성을 맞춥니다.

Purpose: E2E 테스트(student.spec.ts, analysis.spec.ts, admin.spec.ts)에서 `[data-testid="..."]` 셀렉터로 안정적으로 요소를 찾을 수 있도록 합니다.
Output: STU-01, ANL-01, ADM-01, ADM-02 요구사항 충족
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-data-testid-infrastructure/23-RESEARCH.md
@tests/utils/selectors.ts
@tests/e2e/student.spec.ts
@tests/e2e/analysis.spec.ts
@tests/e2e/admin.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 학생 목록 + 학생 상세 + 분석 탭에 data-testid 추가 (STU-01, ANL-01)</name>
  <files>
    src/app/(dashboard)/students/page.tsx
    src/components/students/tabs/analysis-tab.tsx
    src/components/students/saju-analysis-panel.tsx
    src/components/students/mbti-analysis-panel.tsx
    src/components/students/name-analysis-panel.tsx
    src/components/students/student-detail.tsx
    src/components/students/student-form.tsx
  </files>
  <action>
    **학생 목록 (students/page.tsx):**
    - student-card는 이미 존재함. 내부에 data-testid 추가:
      - `data-testid="student-name"` -> 학생 이름 h3 태그에
      - `data-testid="student-grade"` -> 학년 표시 영역에
      - `data-testid="student-school"` -> 학교 표시 영역에
    - 학년/학교 정보를 개별 span/div로 분리하여 각각 data-testid 부여 (현재 한 줄에 합쳐져 있음 - `{student.school} {student.grade}학년`을 분리)

    **분석 탭 (tabs/analysis-tab.tsx):**
    - 로딩 상태에 `data-testid="analysis-loading"` 추가
    - 현재 analysis-tab.tsx는 사주/MBTI/성명학 탭이 별도로 분리되어 있지 않음. 이 파일은 통합 분석 탭임.
    - 학생 상세 페이지의 탭 구조에서 분석 탭 버튼에 접근 가능한지 확인하고, 필요시 data-testid 추가

    **사주 분석 패널 (saju-analysis-panel.tsx):**
    - 분석 결과가 표시되는 컨테이너에 `data-testid="saju-result"` 추가
    - 사주 결과 내부: 사주 테이블의 년주/월주/일주/시주에 각각:
      - `data-testid="year-pillar"`, `data-testid="month-pillar"`, `data-testid="day-pillar"`, `data-testid="hour-pillar"`
    - 오행 분석 섹션에 `data-testid="ohang-analysis"` 추가
    - 수리 분석 섹션에 `data-testid="suri-analysis"` 추가

    **MBTI 분석 패널 (mbti-analysis-panel.tsx):**
    - 탭 버튼에 `data-testid="mbti-tab"` (또는 MBTI 패널 진입점에)
    - MBTI 결과 카드에 `data-testid="mbti-result-card"` 추가
    - MBTI 설명/강점/약점 영역에 `data-testid="mbti-description"`, `data-testid="mbti-strengths"`, `data-testid="mbti-weaknesses"` 추가
    - 학습 스타일 영역에 `data-testid="learning-style"` 추가

    **성명학 분석 패널 (name-analysis-panel.tsx):**
    - name-tab 탭이 있으면 `data-testid="name-tab"` 추가

    **학생 상세 페이지 구조 확인:**
    - 학생 상세 페이지(students/[id]/page.tsx)의 탭 구조를 확인하고, 분석 관련 탭 트리거에 `data-testid="saju-tab"`, `data-testid="mbti-tab"` 추가
    - 만약 탭이 학생 상세 페이지 수준에서 정의되어 있다면 거기에 추가

    **주의:**
    - shadcn/ui Card 컴포넌트는 `...props` spreading을 지원하므로 data-testid 직접 전달 가능
    - Tabs/TabsTrigger 컴포넌트도 data-testid를 HTML 속성으로 전달 가능 (Radix UI 기반)
    - 기존 UI/스타일을 변경하지 않고 data-testid만 추가
  </action>
  <verify>
    `cd /home/gon/projects/ai/ai-afterschool && npx next build` 에러 없이 빌드 통과.
    `grep -r 'data-testid="student-name"' src/` 결과가 나옴.
    `grep -r 'data-testid="analysis-loading"' src/` 결과가 나옴.
    `grep -r 'data-testid="saju-tab"' src/` 결과가 나옴.
    `grep -r 'data-testid="mbti-tab"' src/` 결과가 나옴.
  </verify>
  <done>
    - student-card 내부에 student-name, student-grade, student-school data-testid가 존재
    - 분석 탭에서 saju-tab, mbti-tab data-testid가 TabsTrigger에 존재
    - 분석 로딩 상태에 analysis-loading data-testid 존재
    - 사주 분석 결과에 saju-result, year/month/day/hour-pillar, ohang-analysis, suri-analysis data-testid 존재
    - MBTI 결과에 mbti-result-card, mbti-description, mbti-strengths, mbti-weaknesses, learning-style data-testid 존재
    - 빌드 에러 없음
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin LLM 설정 + 사용량 페이지에 data-testid 추가 (ADM-01, ADM-02)</name>
  <files>
    src/app/(dashboard)/admin/llm-settings/page.tsx
    src/app/(dashboard)/admin/llm-settings/provider-card.tsx
    src/app/(dashboard)/admin/llm-usage/page.tsx
    src/app/(dashboard)/admin/llm-usage/usage-charts.tsx
    src/app/(dashboard)/admin/llm-usage/cost-alerts.tsx
  </files>
  <action>
    **LLM 설정 페이지 (admin/llm-settings/page.tsx):**
    - 현재 활성 제공자를 표시하는 영역이 없음. enabledProviders 중 첫 번째(또는 활성화+검증된 제공자)를 `data-testid="current-provider"`로 표시하는 div 추가
    - 또는 "현재 활성 제공자" 표시 섹션을 헤더 영역에 추가: 활성화된 제공자 이름을 표시하는 `<div data-testid="current-provider">{provider name}</div>`

    **LLM 제공자 카드 (provider-card.tsx):**
    - Select 컴포넌트가 현재 없음 (각 제공자별 카드 형태). 테스트에서 `[data-testid="provider-select"]`를 기대하므로:
    - 가장 자연스러운 방법: LLM 설정 페이지의 헤더 영역에 "기본 제공자 선택" Select를 추가하고 `data-testid="provider-select"` 부여
    - 또는 각 ProviderCard의 Switch에 `data-testid="provider-select"` 대신, 활성화/비활성화 토글 스위치에 각각 `data-testid="provider-toggle-{name}"`을 추가
    - **테스트 기대 사항 확인:** admin.spec.ts에서 `await page.click('[data-testid="provider-select"]')` 후 `await page.click('text=Claude (Anthropic)')` 하므로 Select 드롭다운 형태 필요
    - 따라서 llm-settings/page.tsx에 "기본 제공자" Select 컴포넌트를 추가하고 `data-testid="provider-select"` 부여
    - ProviderCard의 API 키 마스킹 표시에 `data-testid="api-key-display"` 추가

    **LLM 사용량 페이지 (admin/llm-usage/page.tsx):**
    - 총 토큰 카드에 `data-testid="total-tokens"` 추가 (현재 "총 토큰" CardContent 내부 div에)
    - 총 비용 카드에 `data-testid="estimated-cost"` 추가 (현재 "총 비용" CardContent 내부 div에)
    - 날짜 범위 선택기에 `data-testid="date-range-selector"` 추가 (현재 없음 - CostAlerts나 별도 컴포넌트에)

    **사용량 차트 (usage-charts.tsx):**
    - DailyCostChart 또는 TokenUsageChart의 Card에 `data-testid="usage-chart"` 추가
    - ProviderDistributionChart에 `data-testid="model-breakdown"` 추가
    - FeatureUsageChart에 `data-testid="feature-breakdown"` 추가

    **비용 알림 (cost-alerts.tsx):**
    - 확인 후 필요한 data-testid 추가 (usage-settings 등)

    **주의:**
    - admin.spec.ts의 테스트 시나리오와 정확히 매칭되는 data-testid 값 사용
    - 기존 UI 레이아웃 최소 변경. data-testid 추가만 우선
    - Select 컴포넌트 추가 시 실제 동작하는 provider 변경 기능까지 구현하지 않아도 됨 (Phase 23은 data-testid 인프라만 - 동작은 추후 Phase에서 보강)
    - 단, 추가하는 UI 요소가 빌드 에러를 일으키지 않도록 최소한의 상태 관리 포함
  </action>
  <verify>
    `cd /home/gon/projects/ai/ai-afterschool && npx next build` 에러 없이 빌드 통과.
    `grep -r 'data-testid="current-provider"' src/` 결과가 나옴.
    `grep -r 'data-testid="provider-select"' src/` 결과가 나옴.
    `grep -r 'data-testid="usage-chart"' src/` 결과가 나옴.
    `grep -r 'data-testid="total-tokens"' src/` 결과가 나옴.
    `grep -r 'data-testid="estimated-cost"' src/` 결과가 나옴.
    `grep -r 'data-testid="model-breakdown"' src/` 결과가 나옴.
    `grep -r 'data-testid="feature-breakdown"' src/` 결과가 나옴.
  </verify>
  <done>
    - LLM 설정 페이지에 current-provider, provider-select data-testid 존재
    - ProviderCard에 api-key-display data-testid 존재
    - LLM 사용량 페이지에 total-tokens, estimated-cost data-testid 존재
    - usage-chart, model-breakdown, feature-breakdown data-testid 존재
    - 빌드 에러 없음
  </done>
</task>

</tasks>

<verification>
1. `npx next build` - 전체 빌드 에러 없음
2. 다음 data-testid가 소스에 존재하는지 grep 확인:
   - student-card (이미 존재), student-name, student-grade, student-school
   - saju-tab, mbti-tab, analysis-loading
   - saju-result, ohang-analysis, suri-analysis
   - year-pillar, month-pillar, day-pillar, hour-pillar
   - current-provider, provider-select
   - usage-chart, total-tokens, estimated-cost
   - model-breakdown, feature-breakdown
</verification>

<success_criteria>
- STU-01 충족: 학생 카드와 내부 요소에 data-testid 추가 완료
- ANL-01 충족: 분석 탭과 로딩 상태에 data-testid 추가 완료
- ADM-01 충족: LLM 설정 페이지에 data-testid 추가 완료
- ADM-02 충족: LLM 사용량 페이지에 data-testid 추가 완료
- 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/23-data-testid-infrastructure/23-01-SUMMARY.md`
</output>
