---
phase: 23-data-testid-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/counseling/ReservationCalendarView.tsx
  - src/components/counseling/CounselingPageTabs.tsx
  - src/components/counseling/CounselingSessionCard.tsx
  - src/components/counseling/CounselingSessionModal.tsx
  - src/app/(dashboard)/counseling/page.tsx
  - src/components/compatibility/compatibility-score-card.tsx
  - src/components/compatibility/fairness-metrics-panel.tsx
  - src/app/(dashboard)/matching/fairness/page.tsx
  - src/components/analytics/PerformanceMetricsGrid.tsx
  - src/components/analytics/TeacherPerformanceCard.tsx
  - src/components/analytics/PerformanceDashboard.tsx
autonomous: true

must_haves:
  truths:
    - "상담 캘린더 뷰에 data-testid='calendar-view'가 존재하여 E2E 테스트에서 캘린더 렌더링을 확인 가능하다"
    - "상담 상세 모달에 data-testid='counseling-detail-modal'이 존재하여 모달 표시를 검증 가능하다"
    - "상담 기록 카드에 data-testid='counseling-session'이 존재하여 세션 목록을 검증 가능하다"
    - "궁합 점수 표시에 data-testid='compatibility-score'가 존재하여 점수를 검증 가능하다"
    - "공정성 메트릭 페이지에 heading data-testid가 존재한다"
    - "성과 대시보드 메트릭 카드에 data-testid='metric-card'가 존재하여 성과 지표를 확인 가능하다"
  artifacts:
    - path: "src/components/counseling/ReservationCalendarView.tsx"
      provides: "calendar-view data-testid"
      contains: "data-testid=\"calendar-view\""
    - path: "src/components/counseling/CounselingSessionModal.tsx"
      provides: "counseling-detail-modal data-testid"
      contains: "data-testid=\"counseling-detail-modal\""
    - path: "src/components/compatibility/compatibility-score-card.tsx"
      provides: "compatibility-score data-testid"
      contains: "data-testid=\"compatibility-score\""
    - path: "src/components/analytics/TeacherPerformanceCard.tsx"
      provides: "metric-card data-testid"
      contains: "data-testid=\"metric-card\""
  key_links:
    - from: "tests/e2e/counseling.spec.ts"
      to: "src/components/counseling/ReservationCalendarView.tsx"
      via: "data-testid selectors"
      pattern: "data-testid=\"calendar-view\""
    - from: "tests/e2e/matching.spec.ts"
      to: "src/components/compatibility/compatibility-score-card.tsx"
      via: "data-testid selectors"
      pattern: "data-testid=\"compatibility-score\""
    - from: "tests/e2e/performance.spec.ts"
      to: "src/components/analytics/TeacherPerformanceCard.tsx"
      via: "data-testid selectors"
      pattern: "data-testid=\"metric-card\""
---

<objective>
상담, 매칭/궁합, 성과 대시보드 컴포넌트에 data-testid 속성을 추가하여 E2E 테스트 셀렉터와 정합성을 맞춥니다.

Purpose: E2E 테스트(counseling.spec.ts, matching.spec.ts, performance.spec.ts)에서 `[data-testid="..."]` 셀렉터로 안정적으로 요소를 찾을 수 있도록 합니다.
Output: CNS-01, CNS-02, MAT-01, MAT-02, PRF-01 요구사항 충족
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-data-testid-infrastructure/23-RESEARCH.md
@tests/utils/selectors.ts
@tests/e2e/counseling.spec.ts
@tests/e2e/matching.spec.ts
@tests/e2e/performance.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 상담 캘린더/세션 + 상담 통계 컴포넌트에 data-testid 추가 (CNS-01, CNS-02)</name>
  <files>
    src/components/counseling/ReservationCalendarView.tsx
    src/components/counseling/CounselingPageTabs.tsx
    src/components/counseling/CounselingSessionCard.tsx
    src/components/counseling/CounselingSessionModal.tsx
    src/app/(dashboard)/counseling/page.tsx
  </files>
  <action>
    **캘린더 뷰 (ReservationCalendarView.tsx):**
    - 최상위 div에 `data-testid="calendar-view"` 추가 (현재 `<div className="space-y-4">`에)
    - 로딩 상태 div에 `data-testid="calendar-loading"` 추가

    **상담 페이지 탭 (CounselingPageTabs.tsx):**
    - 캘린더 탭 트리거에 `data-tab="calendar"` 속성 추가 (테스트에서 `[data-tab="calendar"]`로 셀렉트함)
    - 이미 TabsTrigger를 사용 중이라면 `data-tab` 속성 추가

    **상담 세션 카드 (CounselingSessionCard.tsx):**
    - 카드 최상위에 `data-testid="counseling-session"` 추가

    **상담 세션 모달 (CounselingSessionModal.tsx):**
    - DialogContent에 `data-testid="counseling-detail-modal"` 추가
    - shadcn/ui Dialog 컴포넌트의 DialogContent는 `...props` spreading을 지원하므로 직접 전달 가능

    **상담 페이지 (counseling/page.tsx):**
    - 상담 기록 리스트의 각 세션 div에 `data-testid="counseling-session"` 추가 (CounselingHistoryContent 함수 내 sessions.map 반복부)
    - 필터 폼에 `data-testid="counseling-form"` 추가 (form 태그에)
    - 통계 카드들에 의미있는 data-testid 추가 (선택적이지만 유용):
      - 이번 달 상담 횟수 카드: `data-testid="monthly-sessions"`
      - 전체 상담 횟수 카드: `data-testid="total-sessions"`
      - 평균 상담 시간 카드: `data-testid="avg-duration"`
      - 후속 조치 예정 카드: `data-testid="followup-count"`

    **주의:**
    - counseling.spec.ts에서 사용하는 셀렉터: `[data-testid="calendar-view"]`, `[data-testid="counseling-detail-modal"]`, `[data-testid="counseling-form"]`, `[data-testid="counseling-session"]`
    - 기존 컴포넌트의 UI/동작 변경 없이 data-testid만 추가
    - CounselingPageTabs의 탭 구조를 확인하고 캘린더 탭 접근 방법 확인 필요
  </action>
  <verify>
    `cd /home/gon/projects/ai/ai-afterschool && npx next build` 에러 없이 빌드 통과.
    `grep -r 'data-testid="calendar-view"' src/` 결과가 나옴.
    `grep -r 'data-testid="counseling-detail-modal"' src/` 결과가 나옴.
    `grep -r 'data-testid="counseling-session"' src/` 결과가 나옴.
  </verify>
  <done>
    - ReservationCalendarView에 calendar-view data-testid 존재
    - CounselingSessionModal에 counseling-detail-modal data-testid 존재
    - 상담 세션 목록 항목에 counseling-session data-testid 존재
    - CounselingPageTabs의 캘린더 탭에 data-tab="calendar" 존재
    - 빌드 에러 없음
  </done>
</task>

<task type="auto">
  <name>Task 2: 궁합/공정성 + 성과 대시보드에 data-testid 추가 (MAT-01, MAT-02, PRF-01)</name>
  <files>
    src/components/compatibility/compatibility-score-card.tsx
    src/components/compatibility/fairness-metrics-panel.tsx
    src/app/(dashboard)/matching/fairness/page.tsx
    src/components/analytics/PerformanceMetricsGrid.tsx
    src/components/analytics/TeacherPerformanceCard.tsx
    src/components/analytics/PerformanceDashboard.tsx
  </files>
  <action>
    **궁합 점수 카드 (compatibility-score-card.tsx):**
    - 전체 점수 표시 영역에 `data-testid="compatibility-score"` 추가
    - 현재 점수는 CompatibilityBar 내부에 있음. 전체 카드에 추가하되, 테스트에서 `.textContent()`로 점수를 읽을 수 있도록 점수 값이 표시되는 위치에 data-testid 배치
    - Card 최상위에 `data-testid="compatibility-score"` 추가 (테스트에서 `[data-testid="compatibility-score"]`로 셀렉트)
    - MBTI 항목에 `data-testid="mbti-compatibility"` 추가 (CompatibilityBar의 value 표시)
    - 학습 스타일에 `data-testid="learning-style-compatibility"` 추가

    **공정성 메트릭 패널 (fairness-metrics-panel.tsx):**
    - 각 메트릭 카드에 `data-testid="fairness-metric"` 추가
    - DisparityIndexCard, ABROCACard, DistributionBalanceCard 각각의 Card에 data-testid 추가
    - 값 표시 span에 `data-testid="metric-value"` 추가
    - 라벨에 `data-testid="metric-label"` 추가
    - 추천 사항 섹션에 `data-testid="fairness-suggestions"` 추가

    **공정성 페이지 (matching/fairness/page.tsx):**
    - 헤더 h1에 `data-testid="fairness-heading"` 추가 (MAT-02 요구사항)
    - 또는 전체 페이지 컨테이너에 `data-testid="fairness-page"` 추가

    **성과 메트릭 그리드 (PerformanceMetricsGrid.tsx):**
    - 기존 컴포넌트는 TeacherPerformanceCard를 반복 렌더링하는 구조

    **선생님 성과 카드 (TeacherPerformanceCard.tsx):**
    - Card 최상위에 `data-testid="metric-card"` 추가 (performance.spec.ts에서 `[data-testid="metric-card"]`로 셀렉트)
    - 현재 `<Card className={...}>` 에 data-testid 추가

    **성과 대시보드 (PerformanceDashboard.tsx):**
    - 파일 내용을 확인하고, 차트나 주요 섹션에 data-testid 추가
    - improvement-chart 관련 차트에 `data-testid="improvement-chart"` 추가
    - 전체 대시보드 컨테이너에 `data-testid="performance-dashboard"` 추가

    **주의:**
    - matching.spec.ts에서 기대하는 주요 셀렉터: compatibility-score, mbti-compatibility, learning-style-compatibility, fairness-metric, metric-value, metric-label, fairness-suggestions
    - performance.spec.ts에서 기대하는 주요 셀렉터: metric-card, improvement-chart
    - CompatibilityBar 내부 구조를 변경하지 않고, 필요한 위치에만 data-testid 추가
    - shadcn/ui Card는 data-testid를 직접 받을 수 있음
  </action>
  <verify>
    `cd /home/gon/projects/ai/ai-afterschool && npx next build` 에러 없이 빌드 통과.
    `grep -r 'data-testid="compatibility-score"' src/` 결과가 나옴.
    `grep -r 'data-testid="fairness-metric"' src/` 결과가 나옴.
    `grep -r 'data-testid="metric-card"' src/` 결과가 나옴.
    `grep -r 'data-testid="fairness-suggestions"' src/` 결과가 나옴.
  </verify>
  <done>
    - compatibility-score-card에 compatibility-score, mbti-compatibility, learning-style-compatibility data-testid 존재
    - fairness-metrics-panel의 각 카드에 fairness-metric data-testid 존재
    - fairness 페이지 heading에 data-testid 존재
    - TeacherPerformanceCard에 metric-card data-testid 존재
    - PerformanceDashboard에 improvement-chart data-testid 존재
    - 빌드 에러 없음
  </done>
</task>

</tasks>

<verification>
1. `npx next build` - 전체 빌드 에러 없음
2. 다음 data-testid가 소스에 존재하는지 grep 확인:
   - calendar-view, counseling-detail-modal, counseling-session
   - compatibility-score, mbti-compatibility, learning-style-compatibility
   - fairness-metric, metric-value, metric-label, fairness-suggestions
   - metric-card, improvement-chart
</verification>

<success_criteria>
- CNS-01 충족: 상담 캘린더 뷰와 상세 모달에 data-testid 추가 완료
- CNS-02 충족: 상담 통계 카드와 세션 목록에 data-testid 추가 완료
- MAT-01 충족: 궁합 점수 표시 UI에 data-testid 추가 완료
- MAT-02 충족: 공정성 지표 페이지에 heading data-testid 추가 완료
- PRF-01 충족: 성과 대시보드 metric 카드에 data-testid 추가 완료
- 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/23-data-testid-infrastructure/23-02-SUMMARY.md`
</output>
