---
phase: 18-reservation-management-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/badge.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Badge 컴포넌트에 예약 상태용 variants가 추가된다"
    - "scheduled, completed, cancelled, noShow variants가 올바른 색상으로 표시된다"
  artifacts:
    - path: "src/components/ui/badge.tsx"
      provides: "상태 배지 variants"
      contains: "badgeVariants cva with status variants"
  key_links:
    - from: "src/components/counseling/ReservationCard.tsx"
      to: "src/components/ui/badge.tsx"
      via: "Badge component with variant prop"
      pattern: "Badge.*variant.*status"
---

<objective>
예약 상태를 컬러 배지로 표시하기 위한 Badge 컴포넌트 variants 추가

Purpose: Phase 18의 다른 컴포넌트들(예약 카드, 목록)에서 상태를 시각적으로 표시할 수 있도록 badge.tsx에 예약 상태 전용 variants를 추가합니다.
Output: badge.tsx 파일에 scheduled, completed, cancelled, noShow variants가 추가됩니다.
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-reservation-management-ui/18-CONTEXT.md
@.planning/phases/18-reservation-management-ui/18-RESEARCH.md
@.planning/phases/17-reservation-server-actions/17-04-SUMMARY.md

# Prior phase patterns
@src/components/ui/badge.tsx
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: 예약 상태 variants 추가</name>
  <files>src/components/ui/badge.tsx</files>
  <action>
    badge.tsx의 badgeVariants cva에 예약 상태 variants 추가:

    1. variants 객체에 status variants 추가:
       - scheduled: "border-transparent bg-blue-100 text-blue-800" (예약 상태 - 파랑)
       - completed: "border-transparent bg-green-100 text-green-800" (완료 - 초록)
       - cancelled: "border-transparent bg-gray-100 text-gray-800" (취소 - 회색)
       - noShow: "border-transparent bg-orange-100 text-orange-800" (노쇼 - 주황)

    2. TypeScript 타입 자동 확장 (CVA가 타입 추론)

    참고: 기존 default, secondary, destructive, outline variants는 유지
  </action>
  <verify>
    1. badge.tsx 파일에서 variants 객체 확인
    2. scheduled, completed, cancelled, noShow 키가 존재하는지 확인
    3. 색상 클래스가 올바른지 확인 (blue-100, green-100, gray-100, orange-100)
  </verify>
  <done>
    Badge 컴포넌트에 4개의 상태 variants가 추가되어 &lt;Badge variant="scheduled"&gt; 형식으로 사용 가능
  </done>
</task>

</tasks>

<verification>
1. TypeScript 컴파일: `npm run build` 또는 `npx tsc --noEmit`
2. CVA 타입 추론 확인: variant prop에 새 variants가 자동완성되는지 확인
</verification>

<success_criteria>
1. badge.tsx에 4개의 상태 variants가 추가됨
2. 각 variant가 올바른 Tailwind CSS 색상 클래스를 사용함
3. TypeScript 타입 시스템이 새 variants를 인식함
4. 기존 default, secondary, destructive, outline variants가 정상 작동함 (회귀 없음)
</success_criteria>

<output>
After completion, create `.planning/phases/18-reservation-management-ui/18-01-SUMMARY.md`
</output>
