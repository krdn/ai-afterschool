---
phase: 18-reservation-management-ui
plan: 04
type: execute
wave: 3
depends_on: [18-01, 18-03]
files_modified:
  - src/components/counseling/ReservationCard.tsx
  - src/components/counseling/ReservationList.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "예약 카드에서 학생 이름, 학부모 관계, 예약 시간, 상태, 주제가 표시된다"
    - "상태가 컬러 배지로 표시된다"
    - "SCHEDULED 상태에서만 상태 변경 버튼이 표시된다"
    - "상태 변경 버튼 클릭 시 AlertDialog가 표시된다"
    - "예약 목록에서 상태별 필터링과 학생 이름 검색이 가능하다"
  artifacts:
    - path: "src/components/counseling/ReservationCard.tsx"
      provides: "예약 카드 컴포넌트"
      min_lines: 80
    - path: "src/components/counseling/ReservationList.tsx"
      provides: "예약 목록 컴포넌트"
      min_lines: 80
  key_links:
    - from: "src/components/counseling/ReservationCard.tsx"
      to: "src/components/ui/badge.tsx"
      via: "import Badge"
      pattern: "Badge.*variant.*status"
    - from: "src/components/counseling/ReservationCard.tsx"
      to: "src/lib/actions/reservations.ts"
      via: "complete/cancel/noShow action"
      pattern: "completeReservationAction|cancelReservationAction|markNoShowAction"
    - from: "src/components/counseling/ReservationList.tsx"
      to: "src/components/counseling/ReservationCard.tsx"
      via: "import ReservationCard"
      pattern: "ReservationCard"
    - from: "src/components/counseling/ReservationList.tsx"
      to: "src/lib/actions/reservations.ts"
      via: "getReservationsAction for filtering"
      pattern: "getReservationsAction"
---

<objective>
예약 카드와 예약 목록 컴포넌트 구현 (상태별 필터링 및 검색 포함)

Purpose: 예약 정보를 카드형으로 표시하는 컴포넌트와 예약 목록을 관리하는 컴포넌트를 생성합니다. 상태 변경 액션(완료/취소/노쇼)과 확인 다이얼로그를 포함합니다. 예약 목록에서 상태별 필터링과 학생 이름 검색 기능을 제공합니다.
Output: ReservationCard.tsx와 ReservationList.tsx 컴포넌트가 생성됩니다.
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-reservation-management-ui/18-CONTEXT.md
@.planning/phases/18-reservation-management-ui/18-RESEARCH.md
@.planning/phases/17-reservation-server-actions/17-04-SUMMARY.md
@.planning/phases/18-reservation-management-ui/18-01-SUMMARY.md

# Prior phase patterns
@src/components/ui/badge.tsx
@src/components/ui/card.tsx
@src/components/ui/button.tsx
@src/lib/actions/reservations.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn/ui AlertDialog 컴포넌트 설치</name>
  <files>src/components/ui/alert-dialog.tsx</files>
  <action>
    AlertDialog 컴포넌트 설치:
    ```bash
    npx shadcn@latest add alert-dialog
    ```

    참고: Research.md에서 확인한 shadcn/ui AlertDialog 패턴 사용
  </action>
  <verify>
    1. src/components/ui/alert-dialog.tsx 파일이 생성됨
    2. AlertDialog, AlertDialogAction, AlertDialogCancel 등 export 확인
  </verify>
  <done>
    AlertDialog 컴포넌트가 설치되어 확인 다이얼로그에 사용 가능
  </done>
</task>

<task type="auto">
  <name>Task 2: ReservationCard 컴포넌트 생성</name>
  <files>src/components/counseling/ReservationCard.tsx</files>
  <action>
    ReservationCard.tsx 컴포넌트 생성:

    1. 타입 정의 (Prisma 포함):
       ```typescript
       import type { ParentCounselingReservation, Student, Parent, Teacher, ReservationStatus } from "@prisma/client"

       type ReservationWithRelations = ParentCounselingReservation & {
         student: Student
         parent: Parent
         teacher: Teacher
       }

       interface ReservationCardProps {
         reservation: ReservationWithRelations
       }
       ```

    2. 상태 관리 (useState):
       - dialogOpen: boolean (확인 다이얼로그)
       - dialogType: 'complete' | 'cancel' | 'noShow' | null

    3. 상태 라벨/변환 함수:
       ```typescript
       function getStatusLabel(status: ReservationStatus): string {
         const labels = {
           SCHEDULED: "예약",
           COMPLETED: "완료",
           CANCELLED: "취소",
           NO_SHOW: "노쇼",
         }
         return labels[status]
       }

       function getStatusVariant(status: ReservationStatus): "scheduled" | "completed" | "cancelled" | "noShow" {
         // 상태를 Badge variant로 변환
       }
       ```

    4. 핸들러 함수:
       - handleComplete: completeReservationAction 호출
       - handleCancel: cancelReservationAction 호출
       - handleNoShow: markNoShowAction 호출
       - 각 핸들러는 toast 알림 후 router.refresh()

    5. 카드 레이아웃 (Card 컴포넌트 사용):
       - Header: 날짜/시간 + 상태 배지
       - Body: 학생 이름 · 학부모 관계, 주제
       - Footer: 상태 변경 버튼 (SCHEDULED 상태일 때만)

    6. 상태 배지 (Badge 컴포넌트):
       - variant={getStatusVariant(reservation.status)}
       - Plan 18-01에서 추가한 status variants 사용

    7. 상태 변경 버튼:
       - 완료: bg-green-50 text-green-700 hover:bg-green-100
       - 취소: bg-gray-50 text-gray-700 hover:bg-gray-100
       - 노쇼: bg-orange-50 text-orange-700 hover:bg-orange-100

    8. AlertDialog (확인 다이얼로그):
       - 상태 변경 전 확인
       - description에 학생 이름과 상태 내용 포함
       - 확인 버튼: 실제 상태 변경 핸들러 호출

    9. 날짜/시간 포맷:
       - date-fns format 사용
       - "M월 d일 E요일 HH:mm" 형식
       - locale={ko}

    10. 참고: Research.md의 Pattern 4 구현
  </action>
  <verify>
    1. ReservationCard.tsx 파일이 생성됨
    2. Badge 컴포넌트가 import됨
    3. Server Actions가 호출됨 (complete/cancel/noShow)
    4. AlertDialog가 구현됨
    5. TypeScript 컴파일: `npx tsc --noEmit`
  </verify>
  <done>
    ReservationCard 컴포넌트가 예약 정보 카드와 상태 변경 기능 제공
  </done>
</task>

<task type="auto">
  <name>Task 3: ReservationList 컴포넌트 생성 (필터링/검색 포함)</name>
  <files>src/components/counseling/ReservationList.tsx</files>
  <action>
    ReservationList.tsx 컴포넌트 생성 (필터링/검색 포함):

    1. 인터페이스 정의:
       ```typescript
       interface ReservationListProps {
         reservations: ReservationWithRelations[]
         onRefresh?: () => void
       }
       ```

    2. useState로 필터 상태 관리:
       - statusFilter: ReservationStatus | "ALL" (상태 필터)
       - searchQuery: string (학생 이름 검색)
       - dateFilter: Date | undefined (날짜 필터)

    3. 필터링 로직 (useMemo):
       ```typescript
       const filteredReservations = useMemo(() => {
         return reservations.filter(reservation => {
           // 상태 필터
           if (statusFilter !== "ALL" && reservation.status !== statusFilter) {
             return false
           }

           // 학생 이름 검색
           if (searchQuery.trim()) {
             const studentName = reservation.student.name.toLowerCase()
             if (!studentName.includes(searchQuery.toLowerCase())) {
               return false
             }
           }

           // 날짜 필터 (선택 시 해당 날짜의 예약만)
           if (dateFilter) {
             const reservationDate = new Date(reservation.scheduledAt)
             const filterDate = new Date(dateFilter)
             filterDate.setHours(0, 0, 0, 0)
             const filterDateEnd = new Date(dateFilter)
             filterDateEnd.setHours(23, 59, 59, 999)

             if (reservationDate < filterDate || reservationDate > filterDateEnd) {
               return false
             }
           }

           return true
         })
       }, [reservations, statusFilter, searchQuery, dateFilter])
       ```

    4. 정렬 (useMemo):
       - scheduledAt 기준 내림차순 (최신 순)

    5. 필터 UI:
       - 상태 필터: Select 컴포넌트 (shadcn/ui)
         - 옵션: 전체, 예약, 완료, 취소, 노쇼
         - 위치: 목록 상단
       - 검색 입력: Input 컴포넌트 (shadcn/ui)
         - placeholder: "학생 이름 검색..."
         - debounce 처리 (300ms)
       - 필터 리셋 버튼: "필터 초기화"

    6. 빈 상태 처리:
       - filteredReservations.length === 0일 때
       - 검색/필터 중인 경우: "검색 결과가 없습니다."
       - 아닌 경우: 아이콘 + "예약된 상담이 없습니다. 새 예약을 등록해주세요." + CTA 버튼

    7. 목록 렌더링:
       - space-y-3 간격
       - 각 예약을 ReservationCard로 렌더링

    8. 날짜 클릭 핸들러 (CONTEXT.md 의사결정):
       - 참고: "날짜 클릭 시 해당 날의 예약 목록 표시"는 Calendar 컴포넌트(Plan 18-02)나 페이지(Plan 18-05)에서 처리
       - ReservationList는 dateFilter prop을 받아 해당 날짜의 예약만 표시

    9. 로딩 상태 (옵션):
       - Skeleton UI 또는 로딩 스피너

    10. 참고: 기존 CounselingSessionList 패턴 재사용
  </action>
  <verify>
    1. ReservationList.tsx 파일이 생성됨
    2. ReservationCard가 import됨
    3. 빈 상태 처리가 구현됨
    4. 필터 UI가 구현됨 (상태 필터, 검색 입력)
    5. 필터링 로직이 동작함
    6. TypeScript 컴파일: `npx tsc --noEmit`
  </verify>
  <done>
    ReservationList 컴포넌트가 예약 목록, 필터링, 검색, 빈 상태 처리 제공
  </done>
</task>

</tasks>

<verification>
1. TypeScript 컴파일: `npx tsc --noEmit`
2. AlertDialog 설치 확인: alert-dialog.tsx 파일 존재
3. Badge variants 사용 확인: getStatusVariant 함수가 status variants 반환
4. 필터링 확인: 상태 필터와 검색 입력이 예약 목록을 필터링함
</verification>

<success_criteria>
1. ReservationCard가 예약 정보를 카드형으로 표시함
2. 상태가 컬러 배지로 표시됨 (파랑/초록/회색/주황)
3. SCHEDULED 상태에서만 상태 변경 버튼이 표시됨
4. 상태 변경 버튼 클릭 시 AlertDialog가 표시됨
5. 확인 후 Server Action이 호출되고 toast 알림이 표시됨
6. ReservationList가 예약 목록과 빈 상태를 렌더링함
7. 상태별 필터링이 가능함 (전체/예약/완료/취소/노쇼)
8. 학생 이름 검색이 가능함
9. 검색/필터 결과가 실시간으로 반영됨
</success_criteria>

<output>
After completion, create `.planning/phases/18-reservation-management-ui/18-04-SUMMARY.md`
</output>
