---
phase: 18-reservation-management-ui
plan: 05
type: execute
wave: 4
depends_on: [18-01, 18-02, 18-03, 18-04]
files_modified:
  - src/app/(dashboard)/counseling/page.tsx
autonomous: false
user_setup: []

must_haves:
  truths:
    - "/counseling 페이지에 탭이 2개로 나뉜다 (상담 기록 | 예약 관리)"
    - "예약 관리 탭에서 예약 목록이 표시된다"
    - "새 예약 등록 버튼 클릭 시 예약 폼이 표시된다"
    - "예약 등록 후 목록으로 자동 복귀한다"
    - "날짜 클릭 시 해당 날의 예약 목록이 표시된다"
  artifacts:
    - path: "src/app/(dashboard)/counseling/page.tsx"
      provides: "상담/예약 관리 페이지"
      contains: "Tabs component with 상담 기록 and 예약 관리 tabs, date filter state"
  key_links:
    - from: "src/app/(dashboard)/counseling/page.tsx"
      to: "src/components/counseling/ReservationList.tsx"
      via: "import ReservationList"
      pattern: "ReservationList"
    - from: "src/app/(dashboard)/counseling/page.tsx"
      to: "src/components/counseling/ReservationForm.tsx"
      via: "import ReservationForm"
      pattern: "ReservationForm"
    - from: "src/app/(dashboard)/counseling/page.tsx"
      to: "src/lib/actions/reservations.ts"
      via: "getReservationsAction"
      pattern: "getReservationsAction"
    - from: "src/app/(dashboard)/counseling/page.tsx"
      to: "src/components/counseling/ReservationCalendar.tsx"
      via: "onSelect callback for date filtering"
      pattern: "ReservationCalendar.*onSelect"
---

<objective>
기존 상담 페이지에 예약 관리 탭 통합 및 전체 워크플로우 연결 (날짜 클릭 필터링 포함)

Purpose: 기존 /counseling 페이지를 확장하여 상담 기록과 예약 관리를 탭으로 분리하고, 예약 등록/목록/상태 변경 전체 워크플로우를 연결합니다. CONTEXT.md 의사결정에 따라 날짜 클릭 시 해당 날의 예약 목록을 표시하는 기능을 구현합니다.
Output: /counseling 페이지가 탭 기반으로 수정되어 예약 관리 기능이 통합되며, 날짜 클릭 시 해당 날의 예약 목록을 필터링합니다.
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-reservation-management-ui/18-CONTEXT.md
@.planning/phases/18-reservation-management-ui/18-RESEARCH.md
@.planning/phases/17-reservation-server-actions/17-02-SUMMARY.md
@.planning/phases/18-reservation-management-ui/18-04-SUMMARY.md

# User decision from CONTEXT.md (locked):
"날짜 클릭 시 해당 날의 예약 목록 표시"

# Prior phase patterns
@src/app/(dashboard)/counseling/page.tsx
@src/components/ui/tabs.tsx
@src/components/ui/card.tsx
@src/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 페이지 레이아웃 탭 기반으로 수정</name>
  <files>src/app/(dashboard)/counseling/page.tsx</files>
  <action>
    기존 /counseling 페이지를 탭 기반으로 리팩토링:

    1. useState로 탭 상태 관리:
       ```typescript
       const [activeTab, setActiveTab] = useState<"history" | "reservations">("history")
       ```

    2. 기존 상담 기록 내용을 TabsContent로 감싸기:
       - value="history"
       - 기존 필터, 목록 등 모두 보존

    3. 탭 트리거 추가:
       - TabsList 컴포넌트
       - TabsTrigger: "상담 기록" (value="history")
       - TabsTrigger: "예약 관리" (value="reservations")

    4. 예약 관리 탭 추가 (Tasks 2-4에서 구현)
       - TabsContent value="reservations"
       - 예약 목록, 등록 버튼 등

    5. 기존 코드 보존:
       - 필터 폼
       - 통계 카드
       - 상담 기록 목록
       - 모든 기능 유지

    6. 스타일링:
       - 탭 리스트: 상단에 배치
       - 탭 콘텐츠: mt-4 (margin-top)
  </action>
  <verify>
    1. Tabs 컴포넌트가 import됨
    2. activeTab 상태가 관리됨
    3. 기존 상담 기록이 "상담 기록" 탭에 포함됨
    4. TypeScript 컴파일: `npx tsc --noEmit`
  </verify>
  <done>
    페이지가 탭 기반으로 구성되어 상담 기록과 예약 관리가 분리됨
  </done>
</task>

<task type="auto">
  <name>Task 2: 예약 관리 탭 콘텐츠 구현 (목록 + 날짜 필터)</name>
  <files>src/app/(dashboard)/counseling/page.tsx</files>
  <action>
    예약 관리 탭에 예약 목록 구현:

    1. useState로 날짜 필터 상태 관리 (CONTEXT.md 의사결정):
       ```typescript
       const [selectedDate, setSelectedDate] = useState<Date | undefined>(undefined)
       ```

    2. Server Component로 예약 목록 조회:
       ```typescript
       const result = await getReservationsAction({
         status: undefined // 전체 상태
       })

       const reservations = result.success ? result.data : []
       ```

    3. 예약 목록 렌더링:
       - ReservationList 컴포넌트 사용
       - reservations prop 전달
       - selectedDate prop 전달 (날짜 필터링용)

    4. 날짜 선택 UI (CONTEXT.md "날짜 클릭 시 해당 날의 예약 목록 표시"):
       - ReservationCalendar 컴포넌트 표시
       - onSelect callback으로 setSelectedDate 호출
       - 선택된 날짜 하이라이트

    5. 날짜 필터 해제 버튼:
       - selectedDate가 있을 때만 표시
       - 클릭 시 setSelectedDate(undefined)

    6. 새 예약 등록 버튼:
       - "새 예약 등록" 버튼
       - 클릭 시 formView 상태를 "form"으로 변경
       - 버튼 위치: 탭 헤더 옆 또는 목록 상단

    7. 빈 상태 처리:
       - ReservationList 내부에서 처리
       - CTA 버튼으로 새 예약 등록 유도
  </action>
  <verify>
    1. getReservationsAction이 호출됨
    2. ReservationList가 렌더링됨
    3. selectedDate 상태가 관리됨
    4. ReservationCalendar의 onSelect이 setSelectedDate를 호출함
    5. 새 예약 등록 버튼이 표시됨
    6. TypeScript 컴파일: `npx tsc --noEmit`
  </verify>
  <done>
    예약 관리 탭에 예약 목록, 날짜 선택, 등록 버튼이 표시됨
  </done>
</task>

<task type="auto">
  <name>Task 3: 예약 등록 폼 뷰 구현 (목록 ↔ 폼 전환)</name>
  <files>src/app/(dashboard)/counseling/page.tsx</files>
  <action>
    예약 등록 폼 표시/숨김 전환 구현:

    1. useState로 뷰 상태 관리:
       ```typescript
       type FormView = "list" | "form"
       const [formView, setFormView] = useState<FormView>("list")
       ```

    2. 목록 뷰 (formView === "list"):
       - ReservationCalendar (날짜 선택)
       - ReservationList (selectedDate prop 전달)
       - 새 예약 등록 버튼

    3. 폼 뷰 (formView === "form"):
       - ReservationForm 표시
       - onCancel={() => setFormView("list")}
       - onSuccess={() => setFormView("list")}
       - router.refresh()로 목록 갱신

    4. 페이지 내 전환:
       - URL 변경 없음
       - 상태 기반 뷰 전환
       - SPA 느낌 제공

    5. 폼 제출 후 자동 복귀:
       - ReservationForm의 onSuccess에서 setFormView("list")
       - router.refresh()로 최신 목록 로드
  </action>
  <verify>
    1. formView 상태가 관리됨
    2. formView에 따라 ReservationList 또는 ReservationForm이 표시됨
    3. 취소 버튼 클릭 시 목록으로 복귀함
    4. 제출 성공 시 목록으로 복귀함
    5. TypeScript 컴파일: `npx tsc --noEmit`
  </verify>
  <done>
    목록과 폼 간 전환 기능이 구현됨
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    전체 예약 관리 UI 워크플로우가 구현됨:
    - 탭 기반 페이지 레이아웃 (상담 기록 | 예약 관리)
    - 예약 목록 표시
    - 날짜 선택 시 해당 날의 예약 목록 필터링
    - 새 예약 등록 폼
    - 목록 ↔ 폼 전환
  </what-built>
  <how-to-verify>
    1. /counseling 페이지 접속
    2. "예약 관리" 탭 클릭
    3. 예약 목록이 표시되는지 확인
    4. 캘린더에서 날짜 클릭
    5. 해당 날짜의 예약 목록만 필터링되어 표시되는지 확인
    6. 날짜 필터 해제 버튼 클릭 시 전체 목록이 표시되는지 확인
    7. "새 예약 등록" 버튼 클릭
    8. 예약 폼이 표시되는지 확인
    9. 날짜 선택 (DatePicker)
    10. 시간 선택 (TimeSlotGrid)
    11. 학생/학부모 선택
    12. 주제 입력 후 제출
    13. 예약이 생성되고 목록으로 복귀하는지 확인
    14. 새 예약 카드에서 상태 변경 버튼 클릭
    15. AlertDialog 확인 후 상태가 변경되는지 확인
    16. 상태별 필터링이 동작하는지 확인
    17. 학생 이름 검색이 동작하는지 확인
  </how-to-verify>
  <resume-signal>
    확인 완료 후 "approved" 입력 또는 이슈 설명
  </resume-signal>
</task>

</tasks>

<verification>
1. TypeScript 컴파일: `npx tsc --noEmit`
2. 빌드: `npm run build`
3. 전체 워크플로우 테스트 (위 how-to-verify 참조)
</verification>

<success_criteria>
1. /counseling 페이지에 2개 탭이 표시됨 (상담 기록 | 예약 관리)
2. 예약 관리 탭에서 예약 목록이 표시됨
3. 캘린더에서 날짜 클릭 시 해당 날의 예약 목록만 표시됨 (CONTEXT.md 의사결정 준수)
4. 날짜 필터 해제 버튼으로 전체 목록 복원 가능
5. 새 예약 등록 버튼 클릭 시 예약 폼이 표시됨
6. 예약 등록 후 목록으로 자동 복귀함
7. 예약 카드에서 상태 변경이 가능함
8. 상태별 필터링과 학생 이름 검색이 동작함
9. 기존 상담 기록 기능이 정상 작동함 (회귀 없음)
</success_criteria>

<output>
After completion, create `.planning/phases/18-reservation-management-ui/18-05-SUMMARY.md`
</output>
