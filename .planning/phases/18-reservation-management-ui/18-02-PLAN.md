---
phase: 18-reservation-management-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/counseling/ReservationCalendar.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "react-day-picker 패키지가 설치된다"
    - "ReservationCalendar 컴포넌트가 한국어 로케일로 렌더링된다"
    - "날짜 선택 시 onSelect 콜백이 호출된다"
  artifacts:
    - path: "src/components/counseling/ReservationCalendar.tsx"
      provides: "DatePicker 컴포넌트"
      min_lines: 40
  key_links:
    - from: "src/components/counseling/ReservationForm.tsx"
      to: "src/components/counseling/ReservationCalendar.tsx"
      via: "import ReservationCalendar"
      pattern: "ReservationCalendar.*onSelect"
---

<objective>
react-day-picker 기반 예약 날짜 선택 DatePicker 컴포넌트 생성

Purpose: 예약 등록 폼에서 사용할 날짜 선택용 캘린더 컴포넌트를 생성합니다. 한국어 로케일 설정과 Tailwind CSS 스타일링을 포함합니다.
Output: ReservationCalendar.tsx 컴포넌트와 react-day-picker 패키지 의존성이 추가됩니다.
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-reservation-management-ui/18-CONTEXT.md
@.planning/phases/18-reservation-management-ui/18-RESEARCH.md
@.planning/phases/17-reservation-server-actions/17-04-SUMMARY.md

# Prior phase patterns
@src/components/ui/badge.tsx
@src/components/ui/tabs.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: react-day-picker 설치</name>
  <files>package.json</files>
  <action>
    npm 패키지 설치:
    ```bash
    npm install react-day-picker
    ```

    참고: date-fns는 이미 설치됨 (v4.1.0)
  </action>
  <verify>
    1. package.json에서 react-day-picker 의존성 확인
    2. npm ls react-day-picker로 설치 확인
  </verify>
  <done>
    react-day-picker가 package.json에 추가되고 node_modules에 설치됨
  </done>
</task>

<task type="auto">
  <name>Task 2: ReservationCalendar 컴포넌트 생성</name>
  <files>src/components/counseling/ReservationCalendar.tsx</files>
  <action>
    ReservationCalendar.tsx 컴포넌트 생성:

    1. "use client" 지시문 추가 (Next.js App Router)

    2. 인터페이스 정의:
       ```typescript
       interface ReservationCalendarProps {
         selected: Date | undefined
         onSelect: (date: Date | undefined) => void
         disabled?: (date: Date) => boolean
       }
       ```

    3. DayPicker import:
       ```typescript
       import { DayPicker } from "react-day-picker"
       import "react-day-picker/style.css"
       import { ko } from "date-fns/locale"
       ```

    4. 컴포넌트 구현:
       - mode="single" (단일 날짜 선택)
       - selected, onSelect props 전달
       - disabled prop 전달 (과거 날짜 비활성화 등)
       - locale={ko} (한국어 로케일)
       - className으로 Tailwind 스타일링
       - classNames prop으로 DayPicker 내부 요소 스타일링

    5. 스타일링 가이드 (Research.md 참조):
       - nav: flex, items-center, space-x-1
       - month: space-y-4
       - caption: flex, justify-center, pt-1
       - table: w-full border-collapse
       - head_row: flex
       - head_cell: w-9 text-sm font-medium text-muted-foreground
       - row: flex w-full mt-2
       - cell: p-0 relative
       - day: h-9 w-9 p-0 font-normal flex items-center justify-center

    6. 접근성: ARIA 라벨, 키보드 네비게이션 (DayPicker 기본 제공)
  </action>
  <verify>
    1. ReservationCalendar.tsx 파일이 생성됨
    2. DayPicker import가 존재함
    3. locale={ko} 설정이 포함됨
    4. TypeScript 컴파일: `npx tsc --noEmit`
  </verify>
  <done>
    ReservationCalendar 컴포넌트가 생성되어 날짜 선택 기능 제공
  </done>
</task>

</tasks>

<verification>
1. 패키지 설치: `npm ls react-day-picker`
2. TypeScript 컴파일: `npx tsc --noEmit`
3. 스타일 import 확인: "react-day-picker/style.css"가 컴포넌트에서 import됨
</verification>

<success_criteria>
1. react-day-picker 패키지가 package.json에 추가됨
2. ReservationCalendar 컴포넌트가 생성됨
3. 한국어 로케일이 적용됨 (요일 표시가 한글로 표시됨)
4. 날짜 선택 시 onSelect 콜백이 호출됨
5. Tailwind CSS로 스타일링이 적용됨
</success_criteria>

<output>
After completion, create `.planning/phases/18-reservation-management-ui/18-02-SUMMARY.md`
</output>
