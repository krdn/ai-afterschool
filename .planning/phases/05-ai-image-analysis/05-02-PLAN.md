---
phase: 05-ai-image-analysis
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - prisma/schema.prisma
  - src/lib/actions/ai-image-analysis.ts
  - src/lib/db/face-analysis.ts
  - src/lib/db/palm-analysis.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "FaceAnalysis와 PalmAnalysis 모델이 Prisma 스키마에 추가되었다"
    - "데이터베이스 마이그레이션이 완료되었다"
    - "analyzeFaceImage Server Action이 작동한다"
    - "analyzePalmImage Server Action이 작동한다"
    - "AI 분석 결과가 DB에 저장된다"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "FaceAnalysis, PalmAnalysis 모델"
      contains: "model FaceAnalysis", "model PalmAnalysis"
    - path: "src/lib/actions/ai-image-analysis.ts"
      provides: "AI 이미지 분석 Server Actions"
      exports: ["analyzeFaceImage", "analyzePalmImage", "getFaceAnalysis", "getPalmAnalysis"]
    - path: "src/lib/db/face-analysis.ts"
      provides: "관상 분석 DAL 함수"
      exports: ["createFaceAnalysis", "getFaceAnalysisByStudentId"]
    - path: "src/lib/db/palm-analysis.ts"
      provides: "손금 분석 DAL 함수"
      exports: ["createPalmAnalysis", "getPalmAnalysisByStudentId"]
  key_links:
    - from: "src/lib/actions/ai-image-analysis.ts"
      to: "src/lib/ai/claude.ts"
      via: "Claude API 호출"
      pattern: "anthropic\\.messages\\.create"
    - from: "src/lib/actions/ai-image-analysis.ts"
      to: "prisma.schema.FaceAnalysis"
      via: "Prisma 쿼리"
      pattern: "prisma\\.faceAnalysis\\.(upsert|findUnique)"
    - from: "src/lib/actions/ai-image-analysis.ts"
      to: "next/after"
      via: "비동기 처리"
      pattern: "after\\(async\\(\\)"
---

# Plan 05-02: DB 스키마 및 Server Actions 구현

## 목표

AI 이미지 분석 결과를 저장하는 데이터베이스 스키마를 추가하고, Claude Vision API를 호출하는 Server Actions를 구현합니다.

## 목표 달성 조건

- [ ] FaceAnalysis와 PalmAnalysis 모델이 Prisma 스키마에 추가된다
- [ ] Prisma 마이그레이션이 성공적으로 실행된다
- [ ] analyzeFaceImage Server Action이 Claude API를 호출하고 결과를 저장한다
- [ ] analyzePalmImage Server Action이 Claude API를 호출하고 결과를 저장한다
- [ ] after() API로 비동기 처리가 구현되어 UI가 블로킹되지 않는다
- [ ] 에러 처리 및 재시도 로직이 구현된다

## 실행 컨텍스트

@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md

## 컨텍스트

@.planning/phases/05-ai-image-analysis/05-CONTEXT.md
@.planning/phases/05-ai-image-analysis/05-RESEARCH.md
@.planning/phases/05-ai-image-analysis/05-01-PLAN.md
@/mnt/data/projects/ai/ai-afterschool/prisma/schema.prisma
@/mnt/data/projects/ai/ai-afterschool/src/lib/actions/students.ts

## 작업

<task type="auto">
  <name>작업 1: Prisma 스키마에 AI 분석 모델 추가</name>
  <files>prisma/schema.prisma</files>
  <action>
    prisma/schema.prisma의 Student 모델에 관계 추가:

    ```prisma
    model Student {
      // ... 기존 필드 ...
      mbtiSurveyDraft                MbtiSurveyDraft?
      mbtiAnalysis                   MbtiAnalysis?
      nameAnalysis                   NameAnalysis?
      sajuAnalysis                   SajuAnalysis?
      faceAnalysis                   FaceAnalysis?      // 추가
      palmAnalysis                   PalmAnalysis?      // 추가
      teacher                        Teacher            @relation(fields: [teacherId], references: [id])
      images                         StudentImage[]

      @@index([teacherId])
      @@index([name])
      @@index([school])
    }
    ```

    schema.prisma 끝에 모델 추가:

    ```prisma
    model FaceAnalysis {
      id           String   @id @default(cuid())
      studentId    String   @unique
      imageUrl     String
      result       Json
      status       String   @default("complete")
      errorMessage String?
      version      Int      @default(1)
      analyzedAt   DateTime
      createdAt    DateTime @default(now())
      updatedAt    DateTime @updatedAt
      student      Student  @relation(fields: [studentId], references: [id], onDelete: Cascade)

      @@index([studentId])
    }

    model PalmAnalysis {
      id           String   @id @default(cuid())
      studentId    String   @unique
      hand         String
      imageUrl     String
      result       Json
      status       String   @default("complete")
      errorMessage String?
      version      Int      @default(1)
      analyzedAt   DateTime
      createdAt    DateTime @default(now())
      updatedAt    DateTime @updatedAt
      student      Student  @relation(fields: [studentId], references: [id], onDelete: Cascade)

      @@index([studentId])
    }
    ```

    참고: RESEARCH.md 섹션 5의 스키마 정의 활용
  </action>
  <verify>
    ```bash
    npx prisma validate
    ```
  </verify>
  <done>
    FaceAnalysis와 PalmAnalysis 모델이 스키마에 추가되고 prisma validate가 통과한다
  </done>
</task>

<task type="auto">
  <name>작업 2: Prisma 마이그레이션 실행</name>
  <files>prisma/migrations</files>
  <action>
    Prisma 마이그레이션 생성 및 실행:

    ```bash
    npx prisma migrate dev --name add_ai_image_analysis
    ```

    참고: 개발 환경이므로 migrate dev 사용 (프로덕션에서는 migrate deploy)
  </action>
  <verify>
    ```bash
    npx prisma migrate status
    ```
  </verify>
  <done>
    마이그레이션이 성공적으로 실행되고 데이터베이스에 테이블이 생성된다
  </done>
</task>

<task type="auto">
  <name>작업 3: DAL (Data Access Layer) 함수 생성</name>
  <files>src/lib/db/face-analysis.ts, src/lib/db/palm-analysis.ts</files>
  <action>
    src/lib/db/face-analysis.ts 생성:

    ```typescript
    import { prisma } from './index'

    export async function createFaceAnalysis(data: {
      studentId: string
      imageUrl: string
      result: any
      status: string
      errorMessage?: string
    }) {
      return prisma.faceAnalysis.create({
        data: {
          studentId: data.studentId,
          imageUrl: data.imageUrl,
          result: data.result,
          status: data.status,
          errorMessage: data.errorMessage,
          analyzedAt: new Date()
        }
      })
    }

    export async function upsertFaceAnalysis(data: {
      studentId: string
      imageUrl: string
      result: any | null
      status: string
      errorMessage?: string
    }) {
      return prisma.faceAnalysis.upsert({
        where: { studentId: data.studentId },
        create: {
          studentId: data.studentId,
          imageUrl: data.imageUrl,
          result: data.result,
          status: data.status,
          errorMessage: data.errorMessage,
          analyzedAt: new Date()
        },
        update: {
          imageUrl: data.imageUrl,
          result: data.result,
          status: data.status,
          errorMessage: data.errorMessage,
          analyzedAt: new Date(),
          version: { increment: 1 }
        }
      })
    }

    export async function getFaceAnalysisByStudentId(studentId: string) {
      return prisma.faceAnalysis.findUnique({
        where: { studentId }
      })
    }
    ```

    src/lib/db/palm-analysis.ts 생성:

    ```typescript
    import { prisma } from './index'

    export async function createPalmAnalysis(data: {
      studentId: string
      hand: 'left' | 'right'
      imageUrl: string
      result: any
      status: string
      errorMessage?: string
    }) {
      return prisma.palmAnalysis.create({
        data: {
          studentId: data.studentId,
          hand: data.hand,
          imageUrl: data.imageUrl,
          result: data.result,
          status: data.status,
          errorMessage: data.errorMessage,
          analyzedAt: new Date()
        }
      })
    }

    export async function upsertPalmAnalysis(data: {
      studentId: string
      hand: 'left' | 'right'
      imageUrl: string
      result: any | null
      status: string
      errorMessage?: string
    }) {
      return prisma.palmAnalysis.upsert({
        where: { studentId: data.studentId },
        create: {
          studentId: data.studentId,
          hand: data.hand,
          imageUrl: data.imageUrl,
          result: data.result,
          status: data.status,
          errorMessage: data.errorMessage,
          analyzedAt: new Date()
        },
        update: {
          hand: data.hand,
          imageUrl: data.imageUrl,
          result: data.result,
          status: data.status,
          errorMessage: data.errorMessage,
          analyzedAt: new Date(),
          version: { increment: 1 }
        }
      })
    }

    export async function getPalmAnalysisByStudentId(studentId: string) {
      return prisma.palmAnalysis.findUnique({
        where: { studentId }
      })
    }
    ```

    참고: 기존 src/lib/db/student-analysis.ts 패턴 따르기
  </action>
  <verify>
    ```bash
    npx tsc --noEmit src/lib/db/face-analysis.ts src/lib/db/palm-analysis.ts
    ```
  </verify>
  <done>
    DAL 함수들이 export되고 Prisma 타입과 호환된다
  </done>
</task>

<task type="auto">
  <name>작업 4: AI 이미지 분석 Server Actions 구현</name>
  <files>src/lib/actions/ai-image-analysis.ts</files>
  <action>
    src/lib/actions/ai-image-analysis.ts 생성:

    ```typescript
    "use server"

    import { revalidatePath } from "next/cache"
    import { after } from "next/server"
    import { anthropic } from "@/lib/ai/claude"
    import { FACE_READING_PROMPT, PALM_READING_PROMPT } from "@/lib/ai/prompts"
    import { verifySession } from "@/lib/dal"
    import { db } from "@/lib/db"
    import { upsertFaceAnalysis, upsertPalmAnalysis } from "@/lib/db"

    export async function analyzeFaceImage(studentId: string, imageUrl: string) {
      const session = await verifySession()

      // 학생 접근 권한 확인
      const student = await db.student.findFirst({
        where: { id: studentId, teacherId: session.userId }
      })

      if (!student) {
        return { success: false, error: "학생을 찾을 수 없어요." }
      }

      // 즉시 응답하고 백그라운드에서 분석 실행
      after(async () => {
        try {
          // Cloudinary에서 이미지 다운로드
          const imageResponse = await fetch(imageUrl)
          const imageBuffer = Buffer.from(await imageResponse.arrayBuffer())
          const base64Image = imageBuffer.toString('base64')

          // Claude Vision API 호출
          const response = await anthropic.messages.create({
            model: 'claude-3-5-sonnet-20241022',
            max_tokens: 2048,
            messages: [{
              role: 'user',
              content: [
                {
                  type: 'image',
                  source: {
                    type: 'base64',
                    media_type: 'image/jpeg',
                    data: base64Image
                  }
                },
                {
                  type: 'text',
                  text: FACE_READING_PROMPT
                }
              ]
            }]
          })

          // JSON 응답 파싱
          const content = response.content[0]
          if (content.type !== 'text') {
            throw new Error('Unexpected response type from Claude')
          }

          const result = JSON.parse(content.text)

          // DB에 저장
          await upsertFaceAnalysis({
            studentId,
            imageUrl,
            result,
            status: 'complete'
          })

          revalidatePath(`/students/${studentId}`)

        } catch (error) {
          console.error('Face analysis error:', error)

          // 에러 상태 저장
          await upsertFaceAnalysis({
            studentId,
            imageUrl,
            result: null,
            status: 'failed',
            errorMessage: error instanceof Error ? error.message : '알 수 없는 에러'
          })

          revalidatePath(`/students/${studentId}`)
        }
      })

      return {
        success: true,
        message: "분석을 시작했어요. 잠시 후 결과가 표시됩니다."
      }
    }

    export async function analyzePalmImage(
      studentId: string,
      imageUrl: string,
      hand: 'left' | 'right'
    ) {
      const session = await verifySession()

      const student = await db.student.findFirst({
        where: { id: studentId, teacherId: session.userId }
      })

      if (!student) {
        return { success: false, error: "학생을 찾을 수 없어요." }
      }

      after(async () => {
        try {
          const imageResponse = await fetch(imageUrl)
          const imageBuffer = Buffer.from(await imageResponse.arrayBuffer())
          const base64Image = imageBuffer.toString('base64')

          const response = await anthropic.messages.create({
            model: 'claude-3-5-sonnet-20241022',
            max_tokens: 2048,
            messages: [{
              role: 'user',
              content: [
                {
                  type: 'image',
                  source: {
                    type: 'base64',
                    media_type: 'image/jpeg',
                    data: base64Image
                  }
                },
                {
                  type: 'text',
                  text: PALM_READING_PROMPT
                }
              ]
            }]
          })

          const content = response.content[0]
          if (content.type !== 'text') {
            throw new Error('Unexpected response type from Claude')
          }

          const result = JSON.parse(content.text)

          await upsertPalmAnalysis({
            studentId,
            hand,
            imageUrl,
            result,
            status: 'complete'
          })

          revalidatePath(`/students/${studentId}`)

        } catch (error) {
          console.error('Palm analysis error:', error)

          await upsertPalmAnalysis({
            studentId,
            hand,
            imageUrl,
            result: null,
            status: 'failed',
            errorMessage: error instanceof Error ? error.message : '알 수 없는 에러'
          })

          revalidatePath(`/students/${studentId}`)
        }
      })

      return {
        success: true,
        message: "분석을 시작했어요. 잠시 후 결과가 표시됩니다."
      }
    }

    export async function getFaceAnalysis(studentId: string) {
      const session = await verifySession()

      const analysis = await db.prisma.faceAnalysis.findUnique({
        where: { studentId },
        include: {
          student: {
            select: { teacherId: true }
          }
        }
      })

      if (!analysis || analysis.student.teacherId !== session.userId) {
        return null
      }

      return analysis
    }

    export async function getPalmAnalysis(studentId: string) {
      const session = await verifySession()

      const analysis = await db.prisma.palmAnalysis.findUnique({
        where: { studentId },
        include: {
          student: {
            select: { teacherId: true }
          }
        }
      })

      if (!analysis || analysis.student.teacherId !== session.userId) {
        return null
      }

      return analysis
    }
    ```

    참고: RESEARCH.md 섹션 5의 Server Action 예제 코드 활용
  </action>
  <verify>
    ```bash
    npx tsc --noEmit src/lib/actions/ai-image-analysis.ts
    ```
  </verify>
  <done>
    Server Action 4개가 export되고 타입체크가 통과한다
  </done>
</task>

## 통합 포인트

- **의존:** Plan 05-01 (Claude 클라이언트, 프롬프트)
- **출력:** DAL 함수, Server Actions
- **사용처:** Plan 05-03/05-04 (UI 컴포넌트)

## 검증

1. **Prisma 마이그레이션 확인:**
   ```bash
   psql -d ai_afterschool -c "\dt" | grep -i analysis
   ```

2. **타입체크:**
   ```bash
   npx tsc --noEmit
   ```

3. **Server Action import 테스트:**
   ```typescript
   import { analyzeFaceImage, analyzePalmImage } from '@/lib/actions/ai-image-analysis'
   ```

## 주의사항

- after() API는 Next.js 15+에서만 사용 가능 (현재 프로젝트는 Next.js 15.5.10)
- AI 분석은 5-15초 소요될 수 있으므로 반드시 비동기 처리
- 에러 발생 시 DB에 에러 메시지 저장하여 UI에서 표시
- Claude API 호출 실패 시 재시도 로직 (선택 사항)

## 출력

완료 후 `.planning/phases/05-ai-image-analysis/05-02-SUMMARY.md` 생성
