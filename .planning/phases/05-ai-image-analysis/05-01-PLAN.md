---
phase: 05-ai-image-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .env.example
  - src/lib/ai/claude.ts
  - src/lib/ai/prompts.ts
  - src/lib/ai/validation.ts
autonomous: true
user_setup:
  - service: anthropic
    why: "AI 이미지 분석을 위한 Claude Vision API"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Dashboard -> API Keys"

must_haves:
  truths:
    - "@anthropic-ai/sdk 패키지가 설치되었다"
    - "ANTHROPIC_API_KEY 환경변수가 설정되었다"
    - "Claude 클라이언트가 초기화되었다"
    - "관상/손금 분석 프롬프트 템플릿이 작성되었다"
    - "이미지 검증 유틸리티가 준비되었다"
  artifacts:
    - path: "src/lib/ai/claude.ts"
      provides: "Claude Vision API 클라이언트"
      exports: ["anthropic"]
    - path: "src/lib/ai/prompts.ts"
      provides: "관상/손금 분석 프롬프트"
      exports: ["FACE_READING_PROMPT", "PALM_READING_PROMPT"]
    - path: "src/lib/ai/validation.ts"
      provides: "이미지 품질 검증 함수"
      exports: ["validateImage", "detectBlur"]
  key_links:
    - from: "src/lib/ai/claude.ts"
      to: "process.env.ANTHROPIC_API_KEY"
      via: "환경변수 참조"
      pattern: "process\\.env\\.ANTHROPIC_API_KEY"
---

# Plan 05-01: AI Vision 인프라 구축

## 목표

Claude Vision API를 통합하여 AI 기반 관상/손금 분석을 위한 기반을 구축합니다.

## 목표 달성 조건

- [ ] @anthropic-ai/sdk 패키지가 설치되고 TypeScript 타입이 정상적으로 작동한다
- [ ] ANTHROPIC_API_KEY 환경변수가 안전하게 설정되고 .env.example에 문서화된다
- [ ] Claude 클라이언트가 싱글톤으로 초기화되고 재사용 가능하다
- [ ] 관상 분석용 프롬프트가 전통 관상학 기반으로 작성된다
- [ ] 손금 분석용 프롬프트가 전통 손금학 기반으로 작성된다
- [ ] 이미지 블러 감지용 Sharp 기반 검증 함수가 작동한다

## 실행 컨텍스트

@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md

## 컨텍스트

@.planning/phases/05-ai-image-analysis/05-CONTEXT.md
@.planning/phases/05-ai-image-analysis/05-RESEARCH.md
@.planning/phases/02-file-infrastructure/02-02-PLAN.md
@/mnt/data/projects/ai/ai-afterschool/package.json

## 작업

<task type="auto">
  <name>작업 1: Anthropic SDK 설치 및 환경 설정</name>
  <files>package.json, .env.example, .env.local</files>
  <action>
    1. @anthropic-ai/sdk 패키지 설치:
       ```bash
       npm install @anthropic-ai/sdk
       ```

    2. .env.example에 ANTHROPIC_API_KEY 추가:
       ```
       # Anthropic Claude API for AI image analysis
       ANTHROPIC_API_KEY=your_anthropic_api_key_here
       ```

    3. .env.local에 실제 API 키 설정 (사용자가 입력 필요)

    참고: 이미 sharp는 패키지에 설치되어 있음 (package.json 확인)
  </action>
  <verify>
    ```bash
    grep -q "@anthropic-ai/sdk" package.json && echo "SDK installed" || echo "SDK not found"
    grep -q "ANTHROPIC_API_KEY" .env.example && echo "Env var documented" || echo "Missing env var"
    ```
  </verify>
  <done>
    @anthropic-ai/sdk가 package.json에 추가되고 ANTHROPIC_API_KEY가 .env.example에 문서화된다
  </done>
</task>

<task type="auto">
  <name>작업 2: Claude 클라이언트 생성</name>
  <files>src/lib/ai/claude.ts</files>
  <action>
    src/lib/ai/claude.ts 파일 생성:

    ```typescript
    import Anthropic from '@anthropic-ai/sdk'

    const anthropic = new Anthropic({
      apiKey: process.env.ANTHROPIC_API_KEY,
    })

    export { anthropic }

    // 타입 내보내기
    export type { MessageParam } from '@anthropic-ai/sdk'
    ```

    주의사항:
    - 환경변수가 없을 경우 개발 환경에서는 명확한 에러 메시지 표시
    - 프로덕션에서는 API 키 누락 시 서버 시작 차단
  </action>
  <verify>
    ```bash
    npx tsc --noEmit src/lib/ai/claude.ts
    ```
  </verify>
  <done>
    anthropic 인스턴스가 export되고 다른 모듈에서 import하여 사용 가능하다
  </done>
</task>

<task type="auto">
  <name>작업 3: AI 분석 프롬프트 템플릿 작성</name>
  <files>src/lib/ai/prompts.ts</files>
  <action>
    src/lib/ai/prompts.ts 파일 생성:

    ```typescript
    export const FACE_READING_PROMPT = `
    너는 한국 전통 관상학을 기반으로 얼굴을 분석하는 전문가야.

    다음 지침을 따라 얼굴 사진을 분석해주세요:

    1. **얼굴형 판정**: 계란형, 둥근형, 각진형, 긴형 중 분류
    2. **이목구비 분석**: 눈, 코, 입, 귀, 이마, 턱의 특징 상세히 묘사
    3. **성격 특성**: 전통 관상학 기반 성격 특성 3-5개 추출
    4. **운세 해석**: 학업, 진로, 인간관계 관련 운세 간단히 언급

    **중요:**
    - 과학적 근거가 없음을 명시
    - 긍정적이고 격려하는 톤 유지
    - 학생의 자존감 해칠만한 내용 제외
    - "전통 해석 참고용"임을 강조

    **출력 형식 (JSON):**
    {
      "faceShape": "string",
      "features": {
        "eyes": "string",
        "nose": "string",
        "mouth": "string",
        "ears": "string",
        "forehead": "string",
        "chin": "string"
      },
      "personalityTraits": ["string"],
      "fortune": {
        "academic": "string",
        "career": "string",
        "relationships": "string"
      },
      "overallInterpretation": "string",
      "disclaimer": "전통 관상학에 기반한 참고용 해석입니다."
    }
    `.trim()

    export const PALM_READING_PROMPT = `
    너는 한국 전통 손금학을 기반으로 손바닥 사진을 분석하는 전문가야.

    다음 지침을 따라 손바닥 사진을 분석해주세요:

    1. **주요 손금 확인**: 생명선, 두뇌선, 감정선 식별 및 특징 묘사
    2. **부속 손금 확인**: 운명선, 결혼선 등 있는지 확인
    3. **선의 특징**: 길이, 깊이, 분기점, 끊어짐 등 관찰
    4. **성격 및 운세**: 손금 기반 성격 특성 3-5개, 학업/진로 운세

    **중요:**
    - 손금이 불분명하면 "손금이 잘 보이지 않아 정확한 분석이 어렵습니다"라고 안내
    - 과학적 근거 없음을 명시
    - 긍정적이고 격려하는 톤 유지
    - 학생의 자존감 해칠만한 내용 제외

    **출력 형식 (JSON):**
    {
      "linesDetected": {
        "lifeLine": "string (description)",
        "headLine": "string (description)",
        "heartLine": "string (description)",
        "fateLine": "string or null",
        "marriageLine": "string or null"
      },
      "personalityTraits": ["string"],
      "fortune": {
        "academic": "string",
        "career": "string",
        "talents": "string"
      },
      "overallInterpretation": "string",
      "clarity": "clear" | "unclear" | "partial",
      "disclaimer": "전통 손금학에 기반한 참고용 해석입니다."
    }
    `.trim()

    export const DISCLAIMER_TEXT = {
      face: "⚠️ 전통 관상학에 기반한 참고용 해석입니다. 과학적 근거가 없으며 엔터테인먼트 목적으로만 제공됩니다.",
      palm: "⚠️ 전통 손금학에 기반한 참고용 해석입니다. 과학적 근거가 없으며 엔터테인먼트 목적으로만 제공됩니다."
    }
    ```

    참고: RESEARCH.md의 섹션 3, 4에서 상세 프롬프트 내용 참조
  </action>
  <verify>
    ```bash
    npx tsc --noEmit src/lib/ai/prompts.ts
    ```
  </verify>
  <done>
    FACE_READING_PROMPT와 PALM_READING_PROMPT가 export되고 JSON 출력 형식이 명시된다
  </done>
</task>

<task type="auto">
  <name>작업 4: 이미지 검증 유틸리티 작성</name>
  <files>src/lib/ai/validation.ts</files>
  <action>
    src/lib/ai/validation.ts 파일 생성:

    ```typescript
    import sharp from 'sharp'

    /**
     * 이미지 블러 감지 (Sharp 기반)
     * Laplacian variance 방식으로 흐림 정도 계산
     */
    export async function detectBlur(imageBuffer: Buffer): Promise<{
      isBlurry: boolean
      score: number
    }> {
      try {
        const { data, info } = await sharp(imageBuffer)
          .greyscale()
          .resize(300, 300, { fit: 'cover' })
          .raw()
          .toBuffer({ resolveWithObject: true })

        // 엣지 검출 - 인접 픽셀 간 차이 계산
        let totalVariance = 0
        for (let i = 1; i < data.length; i++) {
          const diff = Math.abs(data[i] - data[i - 1])
          totalVariance += diff
        }

        const avgVariance = totalVariance / data.length
        const threshold = 10 // 튜닝 필요

        return {
          isBlurry: avgVariance < threshold,
          score: avgVariance
        }
      } catch (error) {
        console.error('Blur detection error:', error)
        return { isBlurry: false, score: 100 } // 에러 시 통과
      }
    }

    /**
     * 이미지 기본 검증 (크기, 포맷)
     */
    export async function validateImageBasic(imageBuffer: Buffer): Promise<{
      valid: boolean
      reason?: string
      metadata?: { width: number; height: number; format: string }
    }> {
      try {
        const metadata = await sharp(imageBuffer).metadata()

        if (!metadata.width || !metadata.height) {
          return { valid: false, reason: '이미지 메타데이터를读取할 수 없습니다.' }
        }

        if (metadata.width < 200 || metadata.height < 200) {
          return { valid: false, reason: '이미지 크기가 너무 작습니다. 최소 200x200 픽셀이 필요합니다.' }
        }

        return {
          valid: true,
          metadata: {
            width: metadata.width,
            height: metadata.height,
            format: metadata.format || 'unknown'
          }
        }
      } catch (error) {
        return { valid: false, reason: '이미지 포맷을 확인할 수 없습니다.' }
      }
    }
    ```

    참고: RESEARCH.md 섹션 2의 Sharp 블러 감지 로직 활용
  </action>
  <verify>
    ```bash
    npx tsc --noEmit src/lib/ai/validation.ts
    ```
  </verify>
  <done>
    detectBlur와 validateImageBasic 함수가 export되고 Buffer를 입력으로 받는다
  </done>
</task>

## 통합 포인트

- **의존:** 없음 (인프라 계층)
- **출력:** Claude 클라이언트, 프롬프트 템플릿, 검증 유틸리티
- **사용처:** Plan 05-02 (Server Actions), Plan 05-03/05-04 (UI 컴포넌트)

## 검증

1. **패키지 설치 확인:**
   ```bash
   npm list @anthropic-ai/sdk
   ```

2. **타입체크:**
   ```bash
   npx tsc --noEmit
   ```

3. **프롬프트 내용 확인:**
   - 전통 관상/손금학 용어 포함
   - JSON 출력 형식 명시
   - 면책 조항 포함

## 주의사항

- ANTHROPIC_API_KEY는 .env.local에만 저장하고 절대 커밋하지 않는다
- 프롬프트는 학생의 자존감을 해치지 않도록 긍정적인 톤으로 작성한다
- 블러 감지 threshold는 실제 테스트 후 튜닝이 필요할 수 있다

## 출력

완료 후 `.planning/phases/05-ai-image-analysis/05-01-SUMMARY.md` 생성
