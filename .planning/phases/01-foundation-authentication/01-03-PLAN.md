---
phase: 01-foundation-authentication
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/lib/actions/auth.ts
  - src/lib/validations/auth.ts
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/layout.tsx
  - src/components/auth/login-form.tsx
autonomous: true

must_haves:
  truths:
    - "선생님이 이메일/비밀번호로 로그인할 수 있다"
    - "잘못된 자격 증명 시 친절한 한국어 에러 메시지가 표시된다"
    - "로그인 성공 시 /students 페이지로 이동한다"
    - "로그아웃 시 세션이 삭제되고 로그인 페이지로 이동한다"
  artifacts:
    - path: "src/lib/actions/auth.ts"
      provides: "로그인/로그아웃 Server Actions"
      exports: ["login", "logout"]
      min_lines: 50
    - path: "src/app/(auth)/login/page.tsx"
      provides: "로그인 페이지"
      min_lines: 15
    - path: "src/components/auth/login-form.tsx"
      provides: "로그인 폼 컴포넌트"
      min_lines: 60
  key_links:
    - from: "src/components/auth/login-form.tsx"
      to: "src/lib/actions/auth.ts"
      via: "useActionState"
      pattern: "useActionState.*login"
    - from: "src/lib/actions/auth.ts"
      to: "src/lib/session.ts"
      via: "createSession call"
      pattern: "createSession"
---

<objective>
선생님 로그인/로그아웃 기능을 구현합니다.

Purpose: AUTH-01(이메일/비밀번호 로그인), AUTH-03(브라우저 새로고침 후 로그인 유지), AUTH-04(여러 선생님 계정 지원)를 충족합니다.
Output: 로그인 페이지, 로그인/로그아웃 Server Actions, 친절한 에러 메시지
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md
@.planning/phases/01-foundation-authentication/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 로그인/로그아웃 Server Actions 구현</name>
  <files>
    src/lib/validations/auth.ts
    src/lib/actions/auth.ts
  </files>
  <action>
    1. src/lib/validations/auth.ts 생성 - Zod 스키마:

    ```typescript
    import { z } from 'zod'

    export const LoginSchema = z.object({
      email: z.string().email('올바른 이메일 주소를 입력해주세요'),
      password: z.string().min(8, '비밀번호는 8자 이상이어야 해요'),
    })

    export const SignupSchema = z.object({
      name: z.string().min(2, '이름은 2자 이상이어야 해요'),
      email: z.string().email('올바른 이메일 주소를 입력해주세요'),
      password: z.string()
        .min(8, '비밀번호는 8자 이상이어야 해요')
        .regex(/[a-zA-Z]/, '비밀번호에 영문자가 포함되어야 해요')
        .regex(/[0-9]/, '비밀번호에 숫자가 포함되어야 해요'),
      confirmPassword: z.string(),
    }).refine((data) => data.password === data.confirmPassword, {
      message: '비밀번호가 일치하지 않아요',
      path: ['confirmPassword'],
    })

    export type LoginInput = z.infer<typeof LoginSchema>
    export type SignupInput = z.infer<typeof SignupSchema>
    ```

    2. src/lib/actions/auth.ts 생성 - Server Actions:

    ```typescript
    'use server'

    import { redirect } from 'next/navigation'
    import argon2 from 'argon2'
    import { db } from '@/lib/db'
    import { createSession, deleteSession } from '@/lib/session'
    import { LoginSchema, SignupSchema } from '@/lib/validations/auth'

    export type AuthFormState = {
      errors?: {
        email?: string[]
        password?: string[]
        name?: string[]
        confirmPassword?: string[]
        _form?: string[]
      }
      message?: string
    }

    export async function login(
      prevState: AuthFormState,
      formData: FormData
    ): Promise<AuthFormState> {
      // 1. Validate input
      const validatedFields = LoginSchema.safeParse({
        email: formData.get('email'),
        password: formData.get('password'),
      })

      if (!validatedFields.success) {
        return {
          errors: validatedFields.error.flatten().fieldErrors,
        }
      }

      const { email, password } = validatedFields.data

      // 2. Find teacher
      const teacher = await db.teacher.findUnique({
        where: { email },
      })

      if (!teacher) {
        // Security: Don't reveal if email exists
        return {
          errors: {
            _form: ['이메일 또는 비밀번호가 일치하지 않아요. 다시 확인해주세요.'],
          },
        }
      }

      // 3. Verify password
      const passwordMatch = await argon2.verify(teacher.password, password)

      if (!passwordMatch) {
        return {
          errors: {
            _form: ['이메일 또는 비밀번호가 일치하지 않아요. 다시 확인해주세요.'],
          },
        }
      }

      // 4. Create session
      await createSession(teacher.id)

      // 5. Redirect to students page
      redirect('/students')
    }

    export async function signup(
      prevState: AuthFormState,
      formData: FormData
    ): Promise<AuthFormState> {
      // 1. Validate input
      const validatedFields = SignupSchema.safeParse({
        name: formData.get('name'),
        email: formData.get('email'),
        password: formData.get('password'),
        confirmPassword: formData.get('confirmPassword'),
      })

      if (!validatedFields.success) {
        return {
          errors: validatedFields.error.flatten().fieldErrors,
        }
      }

      const { name, email, password } = validatedFields.data

      // 2. Check if email already exists
      const existingTeacher = await db.teacher.findUnique({
        where: { email },
      })

      if (existingTeacher) {
        return {
          errors: {
            email: ['이미 사용 중인 이메일이에요'],
          },
        }
      }

      // 3. Hash password (IMPORTANT: await!)
      const hashedPassword = await argon2.hash(password)

      // 4. Create teacher
      const teacher = await db.teacher.create({
        data: {
          name,
          email,
          password: hashedPassword,
        },
      })

      // 5. Create session
      await createSession(teacher.id)

      // 6. Redirect
      redirect('/students')
    }

    export async function logout(): Promise<void> {
      await deleteSession()
      redirect('/login')
    }
    ```

    중요 보안 사항:
    - argon2.hash()와 argon2.verify()에 반드시 await 사용
    - 로그인 실패 시 이메일/비밀번호 중 무엇이 틀렸는지 노출하지 않음
    - 비밀번호 정책: 8자 이상, 영문+숫자 조합
  </action>
  <verify>
    npm run build 성공
    TypeScript 에러 없음
  </verify>
  <done>
    login, signup, logout Server Actions가 구현되고 Argon2로 비밀번호를 해싱한다
  </done>
</task>

<task type="auto">
  <name>Task 2: 로그인 페이지 및 폼 컴포넌트 구현</name>
  <files>
    src/app/(auth)/layout.tsx
    src/app/(auth)/login/page.tsx
    src/components/auth/login-form.tsx
  </files>
  <action>
    1. src/app/(auth)/layout.tsx 생성 - 인증 페이지 레이아웃 (미니멀 중앙 정렬):

    ```tsx
    export default function AuthLayout({
      children,
    }: {
      children: React.ReactNode
    }) {
      return (
        <div className="min-h-screen flex items-center justify-center bg-gray-50">
          <div className="w-full max-w-md">
            {children}
          </div>
        </div>
      )
    }
    ```

    2. src/components/auth/login-form.tsx 생성 - 로그인 폼:

    ```tsx
    'use client'

    import { useActionState } from 'react'
    import { useForm } from 'react-hook-form'
    import { zodResolver } from '@hookform/resolvers/zod'
    import Link from 'next/link'
    import { login, type AuthFormState } from '@/lib/actions/auth'
    import { LoginSchema, type LoginInput } from '@/lib/validations/auth'
    import { Button } from '@/components/ui/button'
    import { Input } from '@/components/ui/input'
    import { Label } from '@/components/ui/label'
    import {
      Card,
      CardContent,
      CardDescription,
      CardFooter,
      CardHeader,
      CardTitle,
    } from '@/components/ui/card'

    export function LoginForm() {
      const [state, formAction, pending] = useActionState<AuthFormState, FormData>(
        login,
        { errors: {} }
      )

      const form = useForm<LoginInput>({
        resolver: zodResolver(LoginSchema),
        defaultValues: {
          email: '',
          password: '',
        },
      })

      return (
        <Card>
          <CardHeader className="space-y-1">
            <CardTitle className="text-2xl font-bold text-center">
              로그인
            </CardTitle>
            <CardDescription className="text-center">
              AI AfterSchool에 오신 것을 환영합니다
            </CardDescription>
          </CardHeader>
          <CardContent>
            <form action={formAction} className="space-y-4">
              {/* Form-level error */}
              {state?.errors?._form && (
                <div className="p-3 rounded-md bg-red-50 text-red-600 text-sm">
                  {state.errors._form[0]}
                </div>
              )}

              {/* Email */}
              <div className="space-y-2">
                <Label htmlFor="email">이메일</Label>
                <Input
                  id="email"
                  name="email"
                  type="email"
                  placeholder="teacher@school.com"
                  autoComplete="email"
                  {...form.register('email')}
                />
                {(state?.errors?.email || form.formState.errors.email) && (
                  <p className="text-sm text-red-600">
                    {state?.errors?.email?.[0] || form.formState.errors.email?.message}
                  </p>
                )}
              </div>

              {/* Password */}
              <div className="space-y-2">
                <Label htmlFor="password">비밀번호</Label>
                <Input
                  id="password"
                  name="password"
                  type="password"
                  placeholder="••••••••"
                  autoComplete="current-password"
                  {...form.register('password')}
                />
                {(state?.errors?.password || form.formState.errors.password) && (
                  <p className="text-sm text-red-600">
                    {state?.errors?.password?.[0] || form.formState.errors.password?.message}
                  </p>
                )}
              </div>

              {/* Submit */}
              <Button type="submit" className="w-full" disabled={pending}>
                {pending ? '로그인 중...' : '로그인'}
              </Button>
            </form>
          </CardContent>
          <CardFooter className="flex flex-col space-y-2">
            <Link
              href="/reset-password"
              className="text-sm text-gray-600 hover:text-gray-900 hover:underline"
            >
              비밀번호를 잊으셨나요?
            </Link>
          </CardFooter>
        </Card>
      )
    }
    ```

    3. src/app/(auth)/login/page.tsx 생성:

    ```tsx
    import { LoginForm } from '@/components/auth/login-form'

    export default function LoginPage() {
      return <LoginForm />
    }
    ```

    4. (선택) 로그아웃 버튼 컴포넌트 - 추후 대시보드에서 사용:

    src/components/auth/logout-button.tsx 생성:
    ```tsx
    'use client'

    import { logout } from '@/lib/actions/auth'
    import { Button } from '@/components/ui/button'

    export function LogoutButton() {
      return (
        <form action={logout}>
          <Button variant="ghost" type="submit">
            로그아웃
          </Button>
        </form>
      )
    }
    ```

    UI 특징 (CONTEXT.md 반영):
    - 미니멀 중앙 정렬 레이아웃: 흰 배경에 로그인 카드만 중앙 배치
    - 친절한 에러 메시지: "이메일 또는 비밀번호가 일치하지 않아요" 존댓말 사용
    - 비밀번호 재설정 링크 포함
  </action>
  <verify>
    npm run dev 후 http://localhost:3000/login 접속
    - 로그인 폼이 중앙에 카드 형태로 표시됨
    - 빈 필드로 제출 시 유효성 검증 에러 표시됨
    - "비밀번호를 잊으셨나요?" 링크가 표시됨
  </verify>
  <done>
    로그인 페이지가 미니멀 중앙 카드 레이아웃으로 표시되고, 실시간 유효성 검증과 친절한 에러 메시지를 제공한다
  </done>
</task>

<task type="auto">
  <name>Task 3: 테스트용 선생님 계정 시드 스크립트</name>
  <files>
    prisma/seed.ts
    package.json
  </files>
  <action>
    1. prisma/seed.ts 생성 - 테스트 계정 생성:

    ```typescript
    import { PrismaClient } from '@prisma/client'
    import argon2 from 'argon2'

    const prisma = new PrismaClient()

    async function main() {
      // Check if test teacher exists
      const existingTeacher = await prisma.teacher.findUnique({
        where: { email: 'test@afterschool.com' },
      })

      if (!existingTeacher) {
        const hashedPassword = await argon2.hash('test1234')

        await prisma.teacher.create({
          data: {
            name: '테스트 선생님',
            email: 'test@afterschool.com',
            password: hashedPassword,
          },
        })

        console.log('Test teacher created: test@afterschool.com / test1234')
      } else {
        console.log('Test teacher already exists')
      }
    }

    main()
      .catch((e) => {
        console.error(e)
        process.exit(1)
      })
      .finally(async () => {
        await prisma.$disconnect()
      })
    ```

    2. package.json에 seed 스크립트 추가:
    ```json
    {
      "prisma": {
        "seed": "npx ts-node --compiler-options {\"module\":\"CommonJS\"} prisma/seed.ts"
      }
    }
    ```

    또는 tsx 사용:
    ```bash
    npm install -D tsx
    ```
    ```json
    {
      "prisma": {
        "seed": "tsx prisma/seed.ts"
      }
    }
    ```

    3. 시드 실행:
    ```bash
    npx prisma db seed
    ```
  </action>
  <verify>
    npx prisma db seed 실행 후 "Test teacher created" 메시지 확인
    http://localhost:3000/login에서 test@afterschool.com / test1234로 로그인 성공
    로그인 후 /students로 리다이렉트됨 (아직 페이지 없어서 404 예상)
  </verify>
  <done>
    테스트 계정(test@afterschool.com / test1234)으로 로그인이 성공하고 /students로 리다이렉트된다
  </done>
</task>

</tasks>

<verification>
1. `npm run build` - 빌드 성공
2. `npm run dev` 후 /login 페이지 접속
3. 잘못된 이메일/비밀번호 입력 시 "이메일 또는 비밀번호가 일치하지 않아요" 메시지 표시
4. test@afterschool.com / test1234로 로그인 성공
5. 로그인 후 /students로 리다이렉트
6. 브라우저 새로고침 후에도 로그인 상태 유지 (AUTH-03)
</verification>

<success_criteria>
- 선생님이 이메일/비밀번호로 로그인할 수 있다 (AUTH-01)
- 잘못된 자격 증명 시 "이메일 또는 비밀번호가 일치하지 않아요" 메시지가 표시된다
- 로그인 성공 시 /students 페이지로 리다이렉트된다
- 브라우저 새로고침 후에도 로그인이 유지된다 (AUTH-03)
- 여러 선생님 계정이 각각 로그인 가능하다 (AUTH-04)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-03-SUMMARY.md`
</output>
