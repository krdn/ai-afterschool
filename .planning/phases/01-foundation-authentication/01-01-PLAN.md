---
phase: 01-foundation-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tailwind.config.ts
  - prisma/schema.prisma
  - .env.local.example
  - app/layout.tsx
  - app/globals.css
  - components/ui/*
autonomous: true

must_haves:
  truths:
    - "Next.js 프로젝트가 생성되고 dev 서버가 실행된다"
    - "Prisma가 PostgreSQL에 연결되고 Teacher/Student 모델이 존재한다"
    - "shadcn/ui 컴포넌트가 설치되어 사용 가능하다"
  artifacts:
    - path: "package.json"
      provides: "프로젝트 의존성 정의"
      contains: "next"
    - path: "prisma/schema.prisma"
      provides: "DB 스키마 정의"
      contains: "model Teacher"
    - path: "app/layout.tsx"
      provides: "앱 루트 레이아웃"
      min_lines: 15
  key_links:
    - from: "prisma/schema.prisma"
      to: "PostgreSQL database"
      via: "DATABASE_URL env var"
      pattern: "datasource db"
---

<objective>
Next.js 프로젝트를 초기화하고 Phase 1에 필요한 기반 인프라를 구축합니다.

Purpose: 모든 후속 플랜이 의존하는 프로젝트 구조, DB 스키마, UI 컴포넌트 라이브러리를 설정합니다.
Output: 실행 가능한 Next.js 앱, PostgreSQL 연결된 Prisma, shadcn/ui 컴포넌트
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.js 프로젝트 생성 및 의존성 설치</name>
  <files>
    package.json
    tsconfig.json
    tailwind.config.ts
    postcss.config.mjs
    next.config.ts
    app/layout.tsx
    app/page.tsx
    app/globals.css
    .env.local.example
  </files>
  <action>
    1. Next.js 15.x 프로젝트 생성 (App Router, TypeScript, Tailwind CSS, ESLint)
       ```bash
       npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm
       ```
       만약 현재 디렉토리가 비어있지 않으면 --yes 플래그 추가

    2. 핵심 의존성 설치:
       ```bash
       npm install jose zod argon2 @prisma/client resend sonner
       npm install -D prisma
       ```

    3. shadcn/ui 초기화 및 필수 컴포넌트 설치:
       ```bash
       npx shadcn@latest init -d
       npx shadcn@latest add button input label form table card toast
       ```

    4. TanStack Table 및 React Hook Form 설치:
       ```bash
       npm install @tanstack/react-table react-hook-form @hookform/resolvers
       ```

    5. .env.local.example 파일 생성:
       ```
       # Database
       DATABASE_URL="postgresql://user:password@localhost:5432/ai_afterschool"

       # Session (32+ character random string)
       SESSION_SECRET="your-session-secret-here-minimum-32-characters"

       # Email (Resend)
       RESEND_API_KEY="re_xxxxxxxxxxxx"

       # App
       NEXT_PUBLIC_APP_URL="http://localhost:3000"
       ```

    6. app/layout.tsx에 Sonner Toaster 추가:
       ```tsx
       import { Toaster } from 'sonner'
       // ... 기존 코드
       <Toaster position="top-center" richColors />
       ```

    7. 한국어 폰트 설정 (Pretendard 또는 Noto Sans KR)
  </action>
  <verify>
    npm run dev 실행 후 http://localhost:3000 접속 시 페이지 로드 확인
    npm run lint 에러 없음 확인
  </verify>
  <done>
    Next.js dev 서버가 정상 실행되고, shadcn/ui 컴포넌트가 사용 가능하다
  </done>
</task>

<task type="auto">
  <name>Task 2: Prisma 스키마 정의 및 DB 마이그레이션</name>
  <files>
    prisma/schema.prisma
    src/lib/db.ts
  </files>
  <action>
    1. Prisma 초기화:
       ```bash
       npx prisma init
       ```

    2. prisma/schema.prisma 작성:
       ```prisma
       generator client {
         provider = "prisma-client-js"
       }

       datasource db {
         provider = "postgresql"
         url      = env("DATABASE_URL")
       }

       model Teacher {
         id        String   @id @default(cuid())
         email     String   @unique
         password  String
         name      String
         createdAt DateTime @default(now())
         updatedAt DateTime @updatedAt

         students           Student[]
         passwordResetTokens PasswordResetToken[]
       }

       model Student {
         id               String    @id @default(cuid())
         name             String
         birthDate        DateTime
         phone            String?
         school           String
         grade            Int
         targetUniversity String?
         targetMajor      String?
         bloodType        BloodType?

         teacherId        String
         teacher          Teacher   @relation(fields: [teacherId], references: [id])

         createdAt        DateTime  @default(now())
         updatedAt        DateTime  @updatedAt

         // PIPA compliance: retention period
         expiresAt        DateTime?

         @@index([teacherId])
         @@index([name])
         @@index([school])
       }

       model PasswordResetToken {
         id        String   @id @default(cuid())
         token     String   @unique
         expiresAt DateTime
         used      Boolean  @default(false)

         teacherId String
         teacher   Teacher  @relation(fields: [teacherId], references: [id])

         createdAt DateTime @default(now())
       }

       enum BloodType {
         A
         B
         AB
         O
       }
       ```

    3. src/lib/db.ts 생성 (Prisma Client 싱글톤):
       ```typescript
       import { PrismaClient } from '@prisma/client'

       const globalForPrisma = globalThis as unknown as {
         prisma: PrismaClient | undefined
       }

       export const db = globalForPrisma.prisma ?? new PrismaClient()

       if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = db
       ```

    4. .env.local 파일 생성 (실제 DB 연결 정보):
       - 운영 서버 PostgreSQL 사용: 192.168.0.5:5433 (news-postgres) 또는 새 DB 생성
       - 로컬 개발 시: localhost:5432 또는 Docker PostgreSQL

    5. DB 마이그레이션 실행:
       ```bash
       npx prisma migrate dev --name init
       ```

    6. Prisma Client 생성:
       ```bash
       npx prisma generate
       ```
  </action>
  <verify>
    npx prisma studio 실행하여 Teacher, Student, PasswordResetToken 테이블 확인
    npx prisma db push --dry-run 에러 없음 확인
  </verify>
  <done>
    PostgreSQL에 Teacher, Student, PasswordResetToken 테이블이 생성되고, Prisma Client로 접근 가능하다
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` - 개발 서버 정상 실행
2. `npm run lint` - 린트 에러 없음
3. `npm run build` - 빌드 성공
4. `npx prisma studio` - DB 테이블 확인
5. shadcn/ui Button 컴포넌트 import 가능 확인
</verification>

<success_criteria>
- Next.js 15.x 프로젝트가 생성되어 localhost:3000에서 실행된다
- Prisma가 PostgreSQL에 연결되어 Teacher, Student, PasswordResetToken 모델이 존재한다
- shadcn/ui 컴포넌트(Button, Input, Label, Form, Table, Card)가 설치되어 사용 가능하다
- TanStack Table, React Hook Form, Zod, jose, argon2, resend, sonner 패키지가 설치되어 있다
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-01-SUMMARY.md`
</output>
