---
phase: 01-foundation-authentication
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/session.ts
  - src/lib/dal.ts
  - src/middleware.ts
autonomous: true

must_haves:
  truths:
    - "세션이 암호화된 쿠키에 저장되고 7일 후 만료된다"
    - "verifySession()이 세션 없으면 로그인 페이지로 리다이렉트한다"
    - "Middleware가 보호된 경로 접근 시 세션 확인한다"
  artifacts:
    - path: "src/lib/session.ts"
      provides: "세션 암호화/복호화/생성/삭제"
      exports: ["encrypt", "decrypt", "createSession", "deleteSession"]
      min_lines: 40
    - path: "src/lib/dal.ts"
      provides: "Data Access Layer with auth verification"
      exports: ["verifySession"]
      min_lines: 20
    - path: "src/middleware.ts"
      provides: "Route protection middleware"
      min_lines: 25
  key_links:
    - from: "src/lib/dal.ts"
      to: "src/lib/session.ts"
      via: "import decrypt"
      pattern: "import.*decrypt.*from.*session"
    - from: "src/middleware.ts"
      to: "src/lib/session.ts"
      via: "import decrypt"
      pattern: "import.*decrypt.*from.*session"
---

<objective>
CVE-2025-29927 대응을 위한 2계층 인증 인프라를 구축합니다.

Purpose: Middleware(UX용 빠른 리다이렉트) + DAL(보안용 권한 검증)의 방어 심층 패턴을 구현합니다.
Output: 세션 관리 라이브러리, Data Access Layer, Route Protection Middleware
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md
@.planning/phases/01-foundation-authentication/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 세션 관리 라이브러리 구현</name>
  <files>
    src/lib/session.ts
  </files>
  <action>
    src/lib/session.ts 생성 - jose 라이브러리를 사용한 JWT 기반 세션:

    ```typescript
    import 'server-only'
    import { SignJWT, jwtVerify } from 'jose'
    import { cookies } from 'next/headers'

    const secretKey = process.env.SESSION_SECRET
    if (!secretKey) {
      throw new Error('SESSION_SECRET environment variable is not set')
    }
    const encodedKey = new TextEncoder().encode(secretKey)

    export type SessionPayload = {
      userId: string
      expiresAt: Date
    }

    export async function encrypt(payload: SessionPayload): Promise<string> {
      return new SignJWT({ ...payload, expiresAt: payload.expiresAt.toISOString() })
        .setProtectedHeader({ alg: 'HS256' })
        .setIssuedAt()
        .setExpirationTime('7d')
        .sign(encodedKey)
    }

    export async function decrypt(session: string | undefined): Promise<SessionPayload | null> {
      if (!session) return null

      try {
        const { payload } = await jwtVerify(session, encodedKey, {
          algorithms: ['HS256'],
        })
        return {
          userId: payload.userId as string,
          expiresAt: new Date(payload.expiresAt as string),
        }
      } catch (error) {
        console.error('Session decryption failed:', error)
        return null
      }
    }

    export async function createSession(userId: string): Promise<void> {
      const expiresAt = new Date(Date.now() + 7 * 24 * 60 * 60 * 1000) // 7일
      const session = await encrypt({ userId, expiresAt })
      const cookieStore = await cookies()

      cookieStore.set('session', session, {
        httpOnly: true,
        secure: process.env.NODE_ENV === 'production',
        expires: expiresAt,
        sameSite: 'lax',
        path: '/',
      })
    }

    export async function updateSession(): Promise<void> {
      const cookieStore = await cookies()
      const session = cookieStore.get('session')?.value
      const payload = await decrypt(session)

      if (!payload) return

      // 1일 이내 만료되면 갱신
      const oneDayFromNow = new Date(Date.now() + 24 * 60 * 60 * 1000)
      if (payload.expiresAt < oneDayFromNow) {
        await createSession(payload.userId)
      }
    }

    export async function deleteSession(): Promise<void> {
      const cookieStore = await cookies()
      cookieStore.delete('session')
    }

    export async function getSession(): Promise<SessionPayload | null> {
      const cookieStore = await cookies()
      const session = cookieStore.get('session')?.value
      return decrypt(session)
    }
    ```

    주요 특징:
    - 'server-only' import로 클라이언트 번들링 방지
    - HS256 알고리즘으로 JWT 서명
    - 7일 만료, 1일 이내 만료 시 자동 갱신
    - httpOnly + sameSite=lax로 XSS/CSRF 방어
    - production에서만 secure 플래그 활성화
  </action>
  <verify>
    TypeScript 컴파일 에러 없음 확인
    npm run build 성공
  </verify>
  <done>
    세션 암호화/복호화/생성/삭제/갱신 함수가 구현되어 있다
  </done>
</task>

<task type="auto">
  <name>Task 2: Data Access Layer 및 Middleware 구현</name>
  <files>
    src/lib/dal.ts
    src/middleware.ts
  </files>
  <action>
    1. src/lib/dal.ts 생성 - CVE-2025-29927 방어를 위한 DAL:

    ```typescript
    import 'server-only'
    import { cookies } from 'next/headers'
    import { redirect } from 'next/navigation'
    import { cache } from 'react'
    import { decrypt, updateSession } from '@/lib/session'
    import { db } from '@/lib/db'

    /**
     * Verifies the session and returns the authenticated user.
     * CRITICAL: Call this in every Server Action and Server Component that accesses data.
     * This is the security layer - middleware is for UX (fast redirects) only.
     */
    export const verifySession = cache(async () => {
      const cookieStore = await cookies()
      const session = cookieStore.get('session')?.value
      const payload = await decrypt(session)

      if (!payload?.userId) {
        redirect('/login')
      }

      // Renew session if expiring soon
      await updateSession()

      return { isAuth: true, userId: payload.userId }
    })

    /**
     * Get current teacher with session verification.
     * Returns teacher data after verifying auth.
     */
    export const getCurrentTeacher = cache(async () => {
      const session = await verifySession()

      const teacher = await db.teacher.findUnique({
        where: { id: session.userId },
        select: {
          id: true,
          email: true,
          name: true,
        },
      })

      if (!teacher) {
        redirect('/login')
      }

      return teacher
    })
    ```

    2. src/middleware.ts 생성 - Route Protection (UX용):

    ```typescript
    import { NextResponse } from 'next/server'
    import type { NextRequest } from 'next/server'
    import { decrypt } from '@/lib/session'

    // Routes that require authentication
    const protectedRoutes = ['/students', '/dashboard']

    // Routes that should redirect to dashboard if already authenticated
    const authRoutes = ['/login', '/reset-password']

    export async function middleware(req: NextRequest) {
      const currentPath = req.nextUrl.pathname

      // Get session from cookie
      const sessionCookie = req.cookies.get('session')?.value
      const session = await decrypt(sessionCookie)

      const isProtectedRoute = protectedRoutes.some(route =>
        currentPath.startsWith(route)
      )
      const isAuthRoute = authRoutes.some(route =>
        currentPath.startsWith(route)
      )

      // Redirect to login if accessing protected route without session
      if (isProtectedRoute && !session?.userId) {
        const loginUrl = new URL('/login', req.nextUrl)
        // Store the intended destination for redirect after login
        loginUrl.searchParams.set('callbackUrl', currentPath)
        return NextResponse.redirect(loginUrl)
      }

      // Redirect to dashboard if accessing auth routes with valid session
      if (isAuthRoute && session?.userId) {
        return NextResponse.redirect(new URL('/students', req.nextUrl))
      }

      return NextResponse.next()
    }

    export const config = {
      matcher: [
        /*
         * Match all request paths except:
         * - api (API routes)
         * - _next/static (static files)
         * - _next/image (image optimization files)
         * - favicon.ico (favicon file)
         * - public folder
         */
        '/((?!api|_next/static|_next/image|favicon.ico|.*\\..*).*)' ,
      ],
    }
    ```

    중요 보안 패턴:
    - Middleware는 UX용 (빠른 리다이렉트)
    - DAL의 verifySession()이 실제 보안 검증
    - 모든 Server Action/Component는 반드시 verifySession() 호출
    - cache()로 동일 요청 내 중복 검증 방지
  </action>
  <verify>
    npm run build 성공
    TypeScript 에러 없음
    middleware.ts가 matcher에 맞게 동작하는지 확인 (로그인 없이 /students 접근 시 /login으로 리다이렉트)
  </verify>
  <done>
    verifySession()이 세션 없으면 /login으로 리다이렉트하고, middleware가 보호된 경로를 필터링한다
  </done>
</task>

</tasks>

<verification>
1. `npm run build` - 빌드 성공
2. `npm run lint` - 린트 에러 없음
3. 로그인 없이 /students 접근 시 /login으로 리다이렉트 확인
4. session.ts의 encrypt/decrypt가 정상 동작 확인
</verification>

<success_criteria>
- src/lib/session.ts가 JWT 암호화/복호화/생성/삭제 기능을 제공한다
- src/lib/dal.ts가 verifySession()으로 세션을 검증하고 미인증 시 리다이렉트한다
- src/middleware.ts가 /students, /dashboard 경로를 보호한다
- 세션이 7일 후 만료되고, 1일 이내 만료 시 자동 갱신된다
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-02-SUMMARY.md`
</output>
