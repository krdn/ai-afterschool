---
phase: 13-compatibility-analysis-matching
plan: 04
type: execute
wave: 4
depends_on: ["13-01", "13-02"]
files_modified:
  - src/lib/actions/assignment.ts
  - src/app/(dashboard)/students/[id]/matching/page.tsx
autonomous: true

must_haves:
  truths:
    - "학생별로 적합한 선생님 순위와 추천 이유가 계산된다"
    - "/students/[id]/matching 페이지에서 궁합 분석 결과를 확인할 수 있다"
    - "RBAC가 적용되어 팀 내 데이터만 접근 가능하다"
  artifacts:
    - path: "src/lib/actions/assignment.ts"
      provides: "학생별 선생님 추천 Server Action"
      exports: ["getTeacherRecommendations"]
    - path: "src/app/(dashboard)/students/[id]/matching/page.tsx"
      provides: "학생 매칭 페이지"
      contains: "getTeacherRecommendations 호출"
  key_links:
    - from: "src/app/(dashboard)/students/[id]/matching/page.tsx"
      to: "src/lib/actions/assignment.ts"
      via: "import getTeacherRecommendations"
      pattern: "getTeacherRecommendations("
---

<objective>
학생별 선생님 추천 Backend 및 페이지 구현

Purpose: 학생별로 적합한 선생님 순위와 추천 이유를 계산하는 Server Action과 매칭 페이지를 구현합니다.

Output:
- 학생별 선생님 추천 Server Action (getTeacherRecommendations)
- /students/[id]/matching 페이지
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-compatibility-analysis-matching/13-RESEARCH.md

# Use compatibility scoring from 13-01
@.planning/phases/13-compatibility-analysis-matching/13-01-SUMMARY.md

# Follow Server Action pattern
@/mnt/data/projects/ai/ai-afterschool/src/lib/actions/teacher-analysis.ts

# Follow page route pattern
@/mnt/data/projects/ai/ai-afterschool/src/app/(dashboard)/students/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>학생별 선생님 추천 Server Action 구현</name>
  <files>src/lib/actions/assignment.ts</files>
  <action>
1. src/lib/actions/assignment.ts에 getTeacherRecommendations 추가:

2. getTeacherRecommendations(studentId) 함수:
   - verifySession()으로 인증 및 RBAC 확인
   - Student 조회 (studentId, 본인 팀 데이터만):
     * include: mbtiAnalysis, sajuAnalysis, nameAnalysis
   - 팀 내 Teacher 목록 조회:
     * role in [TEACHER, MANAGER, TEAM_LEADER]
     * include: teacherMbtiAnalysis, teacherSajuAnalysis, teacherNameAnalysis
     * include: _count { select { students: true } }
   - 전체 Student/Teacher 수로 averageLoad 계산
   - 각 Teacher에 대해 calculateCompatibilityScore 호출:
     * teacherMbti: teacher.teacherMbtiAnalysis ? { type, percentages } : null
     * studentMbti: student.mbtiAnalysis ? { type, percentages } : null
     * teacherSaju, teacherName, studentSaju, studentName: null 처리 포함
     * teacherCurrentLoad, averageLoad 전달
   - 결과를 score.overall 내림차순 정렬
   - { studentId, studentName, recommendations } 반환
     * recommendations: [{ teacherId, teacherName, teacherRole, score, breakdown, reasons }]

참고: 13-02의 analyzeCompatibility 패턴 따르기 (DB 조회 → calculateCompatibilityScore → 정렬 → 반환)
  </action>
  <verify>
```bash
npm run type-check
```
  </verify>
  <done>getTeacherRecommendations가 studentId를 받아 팀 내 모든 Teacher의 궁합 점수 계산 후 순위 반환</done>
</task>

<task type="auto">
  <name>학생 매칭 페이지 구현</name>
  <files>src/app/(dashboard)/students/[id]/matching/page.tsx</files>
  <action>
1. src/app/(dashboard)/students/[id]/matching/page.tsx 생성:

2. 페이지 구조:
   - params를 Promise로 await (Next.js 15 패턴)
   - verifySession()으로 인증
   - Student 조회 (studentId, 본인 팀 데이터만)
   - getTeacherRecommendations(studentId) 호출
   - 페이지 레이아웃:
     * 헤더: "{studentName}님의 적합 선생님"
     * TeacherRecommendationList 컴포넌트 (recommendations 전달)
     * 배정 버튼 (현재 배정된 선생님 표시)

3. 에러 처리:
   - Student not found: notFound()
   - 권한 없음: redirect 또는 error

참고: src/app/(dashboard)/students/[id]/page.tsx의 패턴 따르기 (params await, verifySession, Student 조회)
  </action>
  <verify>
```bash
npm run type-check
```
  </verify>
  <done>/students/[id]/matching 페이지가 getTeacherRecommendations를 호출하고 결과를 TeacherRecommendationList에 전달</done>
</task>

</tasks>

<verification>
1. npm run type-check로 타입 안전성 확인
2. getTeacherRecommendations Server Action 테스트:
   - studentId 전달
   - 반환된 recommendations가 score.overall 내림차순 정렬
   - 각 recommendation이 teacherId, teacherName, score, breakdown, reasons 포함
3. /students/[id]/matching 페이지 테스트:
   - 페이지가 정상 렌더링
   - getTeacherRecommendations 호출 확인
   - Student not found 시 notFound() 동작 확인
</verification>

<success_criteria>
1. getTeacherRecommendations가 팀 내 모든 Teacher의 궁합 점수를 계산하고 순위 반환
2. /students/[id]/matching 페이지가 getTeacherRecommendations를 호출하여 데이터 로드
3. 페이지가 RBAC를 적용하여 본인 팀 데이터만 접근
</success_criteria>

<output>
After completion, create `.planning/phases/13-compatibility-analysis-matching/13-04-SUMMARY.md`
</output>
