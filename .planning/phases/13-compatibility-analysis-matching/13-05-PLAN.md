---
phase: 13-compatibility-analysis-matching
plan: 05
type: execute
wave: 5
depends_on: ["13-02"]
files_modified:
  - src/lib/actions/assignment.ts
  - src/components/assignment/manual-assignment-form.tsx
  - src/components/assignment/batch-assignment.tsx
autonomous: true

must_haves:
  truths:
    - "선생님이 학생을 수동으로 배정할 수 있다"
    - "일괄 배정 기능으로 다수 학생을 한 번에 배정할 수 있다"
    - "수동 배정 폼과 일괄 배정 폼이 Dialog로 표시된다"
  artifacts:
    - path: "src/lib/actions/assignment.ts"
      provides: "수동 배정 Server Actions"
      exports: ["assignStudentBatch"]
    - path: "src/components/assignment/manual-assignment-form.tsx"
      provides: "수동 배정 폼 컴포넌트"
    - path: "src/components/assignment/batch-assignment.tsx"
      provides: "일괄 배정 UI 컴포넌트"
  key_links:
    - from: "src/components/assignment/manual-assignment-form.tsx"
      to: "src/lib/actions/assignment.ts"
      via: "import assignStudentToTeacher"
      pattern: "assignStudentToTeacher("
    - from: "src/components/assignment/batch-assignment.tsx"
      to: "src/lib/actions/assignment.ts"
      via: "import assignStudentBatch"
      pattern: "assignStudentBatch("
    - from: "src/lib/actions/assignment.ts"
      to: "src/lib/dal"
      via: "import verifySession for RBAC"
      pattern: "verifySession()"
---

<objective>
수동 배정 UI 컴포넌트 구현

Purpose: 선생님이 학생을 수동으로 배정할 수 있는 UI 컴포넌트를 구현합니다. 단일 학생 배정 폼과 다수 학생 일괄 배정 폼을 제공합니다.

Output:
- 일괄 배정 Server Action (assignStudentBatch)
- 수동 배정 폼 컴포넌트 (ManualAssignmentForm)
- 일괄 배정 폼 컴포넌트 (BatchAssignment)
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-compatibility-analysis-matching/13-RESEARCH.md

# Use assignment actions from 13-02
@.planning/phases/13-compatibility-analysis-matching/13-02-SUMMARY.md

# Follow UI component pattern
@/mnt/data/projects/ai/ai-afterschool/src/components/ui/dialog.tsx
@/mnt/data/projects/ai/ai-afterschool/src/components/ui/select.tsx
</context>

<tasks>

<task type="auto">
  <name>일괄 배정 Server Action 구현</name>
  <files>src/lib/actions/assignment.ts</files>
  <action>
1. src/lib/actions/assignment.ts에 assignStudentBatch 추가:

2. assignStudentBatch(studentIds, teacherId) 함수:
   - verifySession()으로 인증 및 권한 확인 (DIRECTOR, TEAM_LEADER만)
   - Teacher 조회 (본인 팀 데이터만)
   - Promise.all로 일괄 업데이트:
     * db.student.update({ where: { id: studentId }, data: { teacherId } })
   - revalidatePath("/matching")
   - revalidatePath("/students")
   - { success: true, count: studentIds.length } 반환

참고: 13-02의 assignStudentToTeacher 패턴 따르기 (RBAC 확인 → DB 업데이트 → revalidatePath)
  </action>
  <verify>
```bash
npm run type-check
```
  </verify>
  <done>assignStudentBatch가 studentIds 배열과 teacherId를 받아 일괄 배정</done>
</task>

<task type="auto">
  <name>수동 배정 폼 컴포넌트 구현</name>
  <files>src/components/assignment/manual-assignment-form.tsx</files>
  <action>
1. src/components/assignment/manual-assignment-form.tsx 생성:

2. 컴포넌트 구조 ("use client"):
   - props: { studentId?: string } (선택: 특정 학생 배정 시)
   - Dialog로 폼 표시
   - 폼 필드:
     * Student 선택 (Select) - studentId가 없는 경우
     * Teacher 선택 (Select) - 팀 내 Teacher 목록
   - "배정" 버튼 (assignStudentToTeacher 호출)
   - 성공 시 revalidatePath 후 Dialog 닫기

3. TanStack Query로 Student/Teacher 목록 조회:
   - useQuery({ queryKey: ['students'], queryFn: () => getStudents() })
   - useQuery({ queryKey: ['teachers'], queryFn: () => getTeachers() })

참고: shadcn/ui Dialog, Select, Button 컴포넌트 사용
  </action>
  <verify>
```bash
npm run type-check
```
  </verify>
  <done>ManualAssignmentForm이 Student와 Teacher를 선택하고 assignStudentToTeacher를 호출하여 배정</done>
</task>

<task type="auto">
  <name>일괄 배정 컴포넌트 구현</name>
  <files>src/components/assignment/batch-assignment.tsx</files>
  <action>
1. src/components/assignment/batch-assignment.tsx 생성:

2. 컴포넌트 구조 ("use client"):
   - props: {}
   - Dialog로 폼 표시
   - 폼 필드:
     * 학생 선택 (Multi-Select 또는 Checkbox 그룹)
     * Teacher 선택 (Select)
     * "일괄 배정" 버튼
   - 선택된 모든 Student에 대해 같은 Teacher 배정:
     * assignStudentBatch(studentIds, teacherId) 호출

3. TanStack Mutation으로 일괄 배정:
   - const mutation = useMutation({
       mutationFn: ({ studentIds, teacherId }) => assignStudentBatch(studentIds, teacherId)
     })

참고: ManualAssignmentForm과 유사한 구조, 다중 Student 선택 추가
  </action>
  <verify>
```bash
npm run type-check
```
  </verify>
  <done>BatchAssignment가 다수 Student를 선택하고 일괄 배정</done>
</task>

</tasks>

<verification>
1. npm run type-check로 타입 안전성 확인
2. assignStudentBatch Server Action 테스트:
   - studentIds 배열 전달
   - Promise.all로 일괄 업데이트 확인
   - revalidatePath로 캐시 무효화 확인
3. ManualAssignmentForm 테스트:
   - Student/Teacher 선택 가능
   - "배정" 버튼으로 assignStudentToTeacher 호출 성공
   - Dialog 닫기 및 경로 갱신 확인
4. BatchAssignment 테스트:
   - 다중 Student 선택 가능
   - "일괄 배정" 버튼으로 assignStudentBatch 호출 성공
   - 성공 메시지 표시 확인
</verification>

<success_criteria>
1. assignStudentBatch가 다수 Student 일괄 배정 지원
2. ManualAssignmentForm이 단일 Student-Teacher 배정 지원
3. BatchAssignment가 다수 Student 일괄 배정 지원
4. 모든 컴포넌트가 shadcn/ui Dialog, Select, Button을 사용하여 일관된 스타일 유지
5. RBAC가 적용되어 DIRECTOR, TEAM_LEADER만 배정 가능
</success_criteria>

<output>
After completion, create `.planning/phases/13-compatibility-analysis-matching/13-05-SUMMARY.md`
</output>
