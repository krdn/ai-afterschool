---
phase: 13-compatibility-analysis-matching
plan: 07
type: execute
wave: 6
depends_on: ["13-03", "13-05"]
files_modified:
  - src/lib/actions/assignment.ts
  - src/app/(dashboard)/matching/page.tsx
  - src/app/(dashboard)/matching/auto-assign/page.tsx
  - src/components/assignment/auto-assignment-suggestion.tsx
autonomous: true

must_haves:
  truths:
    - "AI가 생성한 자동 배정 제안을 확인하고 적용할 수 있다"
    - "배정 관리 페이지에서 현재 배정 현황을 확인할 수 있다"
    - "자동 배정 제안 페이지에서 제안 생성 및 적용이 가능하다"
  artifacts:
    - path: "src/lib/actions/assignment.ts"
      provides: "자동 배정 제안 및 적용 Server Actions"
      exports: ["generateAutoAssignmentSuggestions", "applyAutoAssignment"]
    - path: "src/app/(dashboard)/matching/page.tsx"
      provides: "배정 관리 대시보드 페이지"
    - path: "src/app/(dashboard)/matching/auto-assign/page.tsx"
      provides: "자동 배정 제안 페이지"
    - path: "src/components/assignment/auto-assignment-suggestion.tsx"
      provides: "자동 배정 제안 UI 컴포넌트"
  key_links:
    - from: "src/app/(dashboard)/matching/page.tsx"
      to: "src/lib/db"
      via: "import db for Student/Teacher queries"
      pattern: "db.student.findMany"
    - from: "src/app/(dashboard)/matching/auto-assign/page.tsx"
      to: "src/lib/actions/assignment.ts"
      via: "import generateAutoAssignmentSuggestions"
      pattern: "generateAutoAssignmentSuggestions("
    - from: "src/components/assignment/auto-assignment-suggestion.tsx"
      to: "src/lib/actions/assignment.ts"
      via: "import applyAutoAssignment"
      pattern: "applyAutoAssignment("
    - from: "src/lib/actions/assignment.ts"
      to: "src/lib/optimization/auto-assignment.ts"
      via: "import generateAutoAssignment"
      pattern: "from '@/lib/optimization/auto-assignment'"
---

<objective>
자동 배정 제안 페이지 구현

Purpose: 선생님이 AI가 생성한 자동 배정 제안을 확인하고 적용할 수 있는 페이지를 구현합니다. 배정 관리 대시보드에서 현재 배정 현황을 확인하고, 자동 배정 제안 페이지에서 제안을 생성하고 적용합니다.

Output:
- 자동 배정 제안 Server Actions (generateAutoAssignmentSuggestions, applyAutoAssignment)
- 배정 관리 대시보드 페이지 (/matching)
- 자동 배정 제안 페이지 (/matching/auto-assign)
- 자동 배정 제안 UI 컴포넌트 (AutoAssignmentSuggestion)
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-compatibility-analysis-matching/13-RESEARCH.md

# Use auto-assignment from 13-03
@.planning/phases/13-compatibility-analysis-matching/13-03-SUMMARY.md

# Use manual assignment components from 13-05
@.planning/phases/13-compatibility-analysis-matching/13-05-SUMMARY.md

# Follow page layout pattern
@/mnt/data/projects/ai/ai-afterschool/src/app/(dashboard)/students/page.tsx
</context>

<tasks>

<task type="auto">
  <name>자동 배정 제안 Server Actions 구현</name>
  <files>src/lib/actions/assignment.ts</files>
  <action>
1. src/lib/actions/assignment.ts에 generateAutoAssignmentSuggestions 추가:

2. generateAutoAssignmentSuggestions(studentIds, options) 함수:
   - verifySession()으로 인증 및 권한 확인 (DIRECTOR, TEAM_LEADER만)
   - generateAutoAssignment(studentIds, options) 호출 (13-03에서 구현)
   - calculateFairnessMetrics(assignments) 호출 (13-06에서 구현, 임시로 빈 객체 반환 가능)
   - summary 계산:
     * totalStudents: studentIds.length
     * assignedStudents: assignments.length
     * averageScore: assignments.reduce((sum, a) => sum + a.score.overall, 0) / assignments.length
   - { assignments, fairnessMetrics, summary } 반환

3. applyAutoAssignment(assignments) 함수:
   - verifySession()으로 인증 및 권한 확인 (DIRECTOR, TEAM_LEADER만)
   - Promise.all로 일괄 업데이트:
     * db.student.update({ where: { id: a.studentId }, data: { teacherId: a.teacherId } })
   - revalidatePath("/matching")
   - { success: true, count: assignments.length } 반환

참고: 13-02의 assignStudentToTeacher 패턴 따르기 (RBAC 확인 → DB 업데이트 → revalidatePath)
  </action>
  <verify>
```bash
npm run type-check
```
  </verify>
  <done>generateAutoAssignmentSuggestions가 studentIds를 받아 assignments와 fairnessMetrics 반환</done>
</task>

<task type="auto">
  <name>배정 관리 대시보드 페이지 구현</name>
  <files>src/app/(dashboard)/matching/page.tsx</files>
  <action>
1. src/app/(dashboard)/matching/page.tsx 생성:

2. 페이지 구조 (Server Component):
   - verifySession()으로 인증
   - DB 조회:
     * 전체 Student 수 (db.student.count)
     * 미배정 Student 수 (db.student.count({ where: { teacherId: null } }))
     * Teacher별 담당 Student 수 (db.teacher.findMany include _count)
   - 페이지 레이아웃:
     * 헤더: "배정 관리"
     * 통계 카드: 전체 학생, 미배정 학생, 평균 담당 학생 수
     * 액션 버튼:
       - "수동 배정" (Dialog로 ManualAssignmentForm 표시)
       - "자동 배정 제안" (/matching/auto-assign로 링크)
       - "일괄 배정" (Dialog로 BatchAssignment 표시)
     * Teacher별 배정 현황 테이블 (선생님 이름, 담당 학생 수, 학생 목록)

3. TanStack Table로 테이블 구현 (클라이언트 컴포넌트 분리)

참고: src/app/(dashboard)/students/page.tsx의 테이블 패턴 따르기
  </action>
  <verify>
```bash
npm run type-check
```
  </verify>
  <done>/matching 페이지가 배정 통계와 Teacher별 배정 현황 테이블 표시</done>
</task>

<task type="auto">
  <name>자동 배정 제안 페이지 구현</name>
  <files>src/app/(dashboard)/matching/auto-assign/page.tsx, src/components/assignment/auto-assignment-suggestion.tsx</files>
  <action>
1. src/app/(dashboard)/matching/auto-assign/page.tsx 생성:

2. 페이지 구조 (Server Component):
   - verifySession()으로 인증 (DIRECTOR, TEAM_LEADER만)
   - DB 조회: 미배정 Student 목록 (db.student.findMany({ where: { teacherId: null } }))
   - 페이지 레이아웃:
     * 헤더: "AI 자동 배정 제안"
     * AutoAssignmentSuggestion 컴포넌트 (unassignedStudents 전달)
     * 이전에 생성된 제안 목록 (db.assignmentProposal.findMany({ where: { status: 'pending' } }))

3. AutoAssignmentSuggestion 컴포넌트 (src/components/assignment/auto-assignment-suggestion.tsx):
   - "use client"
   - props: { unassignedStudents: Student[] }
   - "자동 배정 생성" 버튼 (generateAutoAssignmentSuggestions 호출)
   - 생성된 제안 표시:
     * summary 카드 (전체 학생, 배정 학생, 평균 점수)
     * FairnessMetricsPanel (13-06에서 구현, 임시로 placeholder)
     * AssignmentPreview (assignments 목록 테이블)
       * Student 이름, Teacher 이름, 점수 표시
     * "적용" 버튼 (applyAutoAssignment 호출)
     * "취소" 버튼

4. TanStack Mutation으로 자동 배정 생성:
   - const mutation = useMutation({
       mutationFn: (studentIds) => generateAutoAssignmentSuggestions(studentIds, options)
     })

참고: 13-04의 페이지 패턴 따르기 (params await, verifySession, DB 조회)
  </action>
  <verify>
```bash
npm run type-check
```
  </verify>
  <done>/matching/auto-assign 페이지가 미배정 학생 목록을 표시하고 자동 배정 생성 기능 제공</done>
</task>

</tasks>

<verification>
1. npm run type-check로 타입 안전성 확인
2. generateAutoAssignmentSuggestions Server Action 테스트:
   - studentIds 배열 전달
   - 반환된 assignments가 Assignment[] 형식
   - fairnessMetrics 포함
3. /matching 페이지 테스트:
   - 배정 통계가 정확하게 표시
   - Teacher별 배정 현황 테이블이 렌더링
   - 액션 버튼이 정상 작동 (Dialog 열기)
4. /matching/auto-assign 페이지 테스트:
   - 미배정 학생 목록 표시
   - "자동 배정 생성" 버튼이 정상 작동
   - 생성된 제안이 표시되고 "적용" 버튼으로 배정 완료
5. AutoAssignmentSuggestion 테스트:
   - 자동 배정 생성 로딩 상태 표시
   - 제안 표시 (summary, assignments)
   - "적용" 버튼으로 applyAutoAssignment 호출 성공
</verification>

<success_criteria>
1. generateAutoAssignmentSuggestions가 미배정 학생 목록을 받아 자동 배정 제안 생성
2. /matching 페이지가 배정 통계와 Teacher별 배정 현황 표시
3. /matching/auto-assign 페이지가 자동 배정 제안 생성 및 적용 기능 제공
4. AutoAssignmentSuggestion이 자동 배정 제안을 시각화하고 적용 기능 제공
5. 모든 Server Action이 RBAC를 적용하여 인증 및 권한 확인
6. revalidatePath로 적절한 경로 캐시 무효화
</success_criteria>

<output>
After completion, create `.planning/phases/13-compatibility-analysis-matching/13-07-SUMMARY.md`
</output>
