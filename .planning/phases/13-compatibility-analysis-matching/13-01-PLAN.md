---
phase: 13-compatibility-analysis-matching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/analysis/compatibility-scoring.ts
  - src/lib/analysis/mbti-compatibility.ts
  - src/lib/analysis/saju-compatibility.ts
  - src/lib/analysis/name-compatibility.ts
  - src/lib/analysis/learning-style-compatibility.ts
  - src/lib/db/compatibility-result.ts
autonomous: true

must_haves:
  truths:
    - "선생님-학생 궁합 점수가 가중 평균(MBTI 25%, 학습 스타일 25%, 사주 20%, 성명학 15%, 부하 분산 15%)으로 계산된다"
    - "궁합 분석 결과가 CompatibilityResult 테이블에 저장된다"
    - "기존 분석 모듈(Saju, Name, MBTI)이 순수 함수로 재사용된다"
    - "학습 스타일 점수가 MBTI percentages에서 유도된다"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "CompatibilityResult 모델"
      contains: "model CompatibilityResult"
    - path: "src/lib/analysis/compatibility-scoring.ts"
      provides: "가중 평균 궁합 점수 계산 함수"
      exports: ["calculateCompatibilityScore", "CompatibilityScore", "CompatibilityBreakdown"]
    - path: "src/lib/analysis/mbti-compatibility.ts"
      provides: "MBTI 유사도 계산"
      exports: ["calculateMbtiCompatibility"]
    - path: "src/lib/analysis/learning-style-compatibility.ts"
      provides: "학습 스타일 유도 및 호환도 계산"
      exports: ["deriveLearningStyle", "calculateLearningStyleCompatibility"]
    - path: "src/lib/db/compatibility-result.ts"
      provides: "CompatibilityResult CRUD 함수"
      exports: ["upsertCompatibilityResult", "getCompatibilityResult", "getAllCompatibilityResultsForStudent"]
  key_links:
    - from: "src/lib/analysis/compatibility-scoring.ts"
      to: "src/lib/analysis/mbti-scoring.ts"
      via: "import MbtiPercentages type"
      pattern: "from '@/lib/analysis/mbti-scoring'"
    - from: "src/lib/analysis/compatibility-scoring.ts"
      to: "src/lib/analysis/saju.ts"
      via: "import SajuResult type and calculateSaju"
      pattern: "from '@/lib/analysis/saju'"
    - from: "src/lib/analysis/compatibility-scoring.ts"
      to: "src/lib/analysis/name-numerology.ts"
      via: "import NameNumerologyResult type"
      pattern: "from '@/lib/analysis/name-numerology'"
---

<objective>
궁합 분석 알고리즘 구현 및 데이터베이스 스키마 생성

Purpose: 선생님-학생 궁합 점수를 계산하는 가중 평균 알고리즘과 결과 저장을 위한 데이터베이스 스키마를 구축합니다. 기존 분석 모듈(Saju, Name, MBTI)을 순수 함수로 재사용하여 코드 중복을 방지합니다.

Output:
- CompatibilityResult Prisma 모델
- 가중 평균 기반 궁합 점수 계산 순수 함수들
- MBTI percentages 기반 학습 스타일 유도 함수
- CompatibilityResult CRUD DB 함수
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-compatibility-analysis-matching/13-RESEARCH.md

# Reuse existing analysis modules
@/mnt/data/projects/ai/ai-afterschool/src/lib/analysis/mbti-scoring.ts
@/mnt/data/projects/ai/ai-afterschool/src/lib/analysis/saju.ts
@/mnt/data/projects/ai/ai-afterschool/src/lib/analysis/name-numerology.ts

# Follow Teacher Analysis pattern
@/mnt/data/projects/ai/ai-afterschool/src/lib/actions/teacher-analysis.ts

# Prisma schema reference
@/mnt/data/projects/ai/ai-afterschool/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>데이터베이스 스키마 추가 (CompatibilityResult 모델)</name>
  <files>prisma/schema.prisma</files>
  <action>
1. prisma/schema.prisma에 CompatibilityResult 모델 추가:
   - id: String @id @default(cuid())
   - teacherId: String (FK to Teacher)
   - studentId: String (FK to Student)
   - overallScore: Float (0-100 가중 평균 점수)
   - breakdown: Json (항목별 점수: { mbti: 25, learningStyle: 25, saju: 20, name: 15, loadBalance: 15 })
   - reasons: Json? (추천 이유 문자열 배열)
   - calculatedAt: DateTime @default(now())
   - createdAt: DateTime @default(now())
   - updatedAt: DateTime @updatedAt
   - teacher: Teacher @relation(..., onDelete: Cascade)
   - student: Student @relation(..., onDelete: Cascade)
   - @@unique([teacherId, studentId])
   - @@index([teacherId])
   - @@index([studentId])
   - @@index([overallScore])

2. Teacher 모델에 compatibilityResults CompatibilityResult[] relation 추가
3. Student 모델에 compatibilityResults CompatibilityResult[] relation 추가

4. 마이그레이션 실행:
   ```bash
   npx prisma migrate dev --name add_compatibility_result
   ```

참고: 기존 Teacher/Student 모델의 relation 패턴 따르기 (TeacherSajuAnalysis, MbtiAnalysis 등)
  </action>
  <verify>prisma migrate dev 실행 완료, Prisma Studio에서 CompatibilityResult 테이블 확인</verify>
  <done>CompatibilityResult 모델이 schema.prisma에 추가되고 마이그레이션 완료</done>
</task>

<task type="auto">
  <name>궁합 점수 계산 순수 함수 구현</name>
  <files>src/lib/analysis/compatibility-scoring.ts</files>
  <action>
1. src/lib/analysis/compatibility-scoring.ts 생성:

2. 타입 정의:
   - CompatibilityBreakdown: { mbti: number, learningStyle: number, saju: number, name: number, loadBalance: number }
   - CompatibilityScore: { overall: number, breakdown: CompatibilityBreakdown, reasons: string[] }
   - ScoringInput: { teacherMbti, teacherSaju, teacherName, studentMbti, studentSaju, studentName, teacherCurrentLoad, averageLoad }

3. calculateCompatibilityScore 함수 구현:
   - MBTI 유사도 × 0.25 (mbti-compatibility.ts)
   - 학습 스타일 유사도 × 0.25 (learning-style-compatibility.ts, MBTI에서 유도)
   - 사주 오행 유사도 × 0.20 (saju-compatibility.ts)
   - 성명학 유사도 × 0.15 (name-compatibility.ts)
   - 부하 분산 점수 × 0.15 (내장 로직)
   - 전체 점수 = 합계 (0-100)
   - 추천 이유 생성 (generateReasons 함수)

참고: 기존 mbti-scoring.ts의 scoreMbti 함수 패턴 따르기 (순수 함수)
  </action>
  <verify>
```bash
npm run type-check
```
  </verify>
  <done>calculateCompatibilityScore 순수 함수가 MbtiPercentages, SajuResult, NameNumerologyResult 타입을 사용하여 가중 평균 점수 계산</done>
</task>

<task type="auto">
  <name>호환도 계산 서브 모듈 구현</name>
  <files>
    src/lib/analysis/mbti-compatibility.ts
    src/lib/analysis/learning-style-compatibility.ts
    src/lib/analysis/saju-compatibility.ts
    src/lib/analysis/name-compatibility.ts
  </files>
  <action>
1. src/lib/analysis/mbti-compatibility.ts 생성:
   - calculateMbtiCompatibility(teacher, student): MBTI 4차원(E/I, S/N, T/J, J/P)별 percentages 차이 기반 유사도 계산
   - 차이가 적을수록 높은 점수 (0-1 범위)

2. src/lib/analysis/learning-style-compatibility.ts 생성:
   - deriveLearningStyle(mbti): MBTI percentages → VARK 학습 스타일 유도
     * High S + High J → Visual
     * High N + High P → Kinesthetic
     * High E → Auditory
     * High I → Read/Write
   - calculateLearningStyleCompatibility(teacher, student): 유클리드 거리 기반 유사도

3. src/lib/analysis/saju-compatibility.ts 생성:
   - calculateSajuCompatibility(teacher, student): 오행(목화토금수) 균형 차이 기반 유사도
   - SajuResult.elements 참조

4. src/lib/analysis/name-compatibility.ts 생성:
   - calculateNameCompatibility(teacher, student): 4격(원형, 형격, 이격, 정격) 유사도 평균
   - NameNumerologyResult.grids 참조

모든 함수는 null 처리: 분석 미완료 시 기본값 0.5 반환
  </action>
  <verify>
```bash
npm run type-check
```
  </verify>
  <done>4개의 호환도 계산 함수가 각각 MBTI, 학습 스타일, 사주, 성명학 유사도를 0-1 범위로 반환</done>
</task>

<task type="auto">
  <name>CompatibilityResult DB CRUD 모듈 구현</name>
  <files>src/lib/db/compatibility-result.ts</files>
  <action>
1. src/lib/db/compatibility-result.ts 생성:

2. upsertCompatibilityResult 함수:
   - teacherId, studentId, score(CalculateCompatibilityScore 반환값) 받음
   - db.compatibilityResult.upsert 호출
   - @@unique([teacherId, studentId]) 제약조건 활용

3. getCompatibilityResult 함수:
   - teacherId, studentId로 단일 결과 조회

4. getAllCompatibilityResultsForStudent 함수:
   - studentId로 모든 선생님과의 궁합 점수 조회
   - overallScore 내림차순 정렬

5. getAllCompatibilityResultsForTeacher 함수:
   - teacherId로 모든 학생과의 궁합 저수 조회
   - overallScore 내림차순 정렬

참고: 기존 upsertTeacherSajuAnalysis, getStudentSajuAnalysis 등의 패턴 따르기 (src/lib/db/teacher-saju-analysis.ts)
  </action>
  <verify>
```bash
npm run type-check
```
  </verify>
  <done>CompatibilityResult CRUD 함수 4개가 db.compatibilityResult를 통해 Prisma 쿼리 실행</done>
</task>

</tasks>

<verification>
1. Prisma migrate dev 실행으로 CompatibilityResult 테이블 생성 확인
2. npm run type-check로 타입 안전성 확인
3. calculateCompatibilityScore 함수 테스트:
   - 입력: teacherMbti, studentMbti (percentages 포함)
   - 출력: overall (0-100), breakdown (각 항목 점수), reasons (배열)
4. 기존 분석 모듈(Saju, Name, MBTI) import 확인:
   - from '@/lib/analysis/saju'
   - from '@/lib/analysis/name-numerology'
   - from '@/lib/analysis/mbti-scoring'
5. 학습 스타일 유도 로직 확인:
   - deriveLearningStyle가 MbtiPercentages를 입력으로 받음
   - { visual, auditory, readWrite, kinesthetic } 반환
</verification>

<success_criteria>
1. CompatibilityResult 모델이 Prisma schema에 추가되고 마이그레이션 완료
2. calculateCompatibilityScore 함수가 5개 항목 가중 평균(각각 가중치 적용)으로 0-100 점수 계산
3. 4개의 호환도 계산 함수가 각자 도메인(MBTI, 학습 스타일, 사주, 성명학)의 유사도를 0-1로 반환
4. 학습 스타일이 MBTI percentages에서 유도됨 (별도 설문 없음)
5. CompatibilityResult CRUD 함수가 정상 작동 (Prisma 쿼리 실행)
6. 모든 코드가 순수 함수로 작성되어 기존 분석 모듈 재사용
</success_criteria>

<output>
After completion, create `.planning/phases/13-compatibility-analysis-matching/13-01-SUMMARY.md`
</output>
