---
phase: 13-compatibility-analysis-matching
plan: 08
type: execute
wave: 5
depends_on: ["13-04"]
files_modified:
  - src/components/compatibility/teacher-recommendation-list.tsx
  - src/components/compatibility/compatibility-score-card.tsx
  - src/components/compatibility/compatibility-radar-chart.tsx
autonomous: true

must_haves:
  truths:
    - "선생님 추천 목록이 순위별로 표시된다"
    - "궁합 점수가 항목별(MBTI, 학습 스타일, 사주, 성명학, 부하 분산)로 시각화된다"
    - "레이더 차트로 궁합 항목별 점수를 직관적으로 이해할 수 있다"
  artifacts:
    - path: "src/components/compatibility/teacher-recommendation-list.tsx"
      provides: "선생님 추천 목록 UI 컴포넌트"
    - path: "src/components/compatibility/compatibility-score-card.tsx"
      provides: "궁합 점수 카드 UI 컴포넌트"
    - path: "src/components/compatibility/compatibility-radar-chart.tsx"
      provides: "궁합 레이더 차트 UI 컴포넌트"
  key_links:
    - from: "src/components/compatibility/teacher-recommendation-list.tsx"
      to: "src/lib/actions/assignment.ts"
      via: "useQuery with getTeacherRecommendations"
      pattern: "useQuery\(\{ queryKey: \['teacher-recommendations'"
    - from: "src/components/compatibility/compatibility-radar-chart.tsx"
      to: "recharts"
      via: "import RadarChart, PolarGrid, PolarAngleAxis, PolarRadiusAxis, Radar"
      pattern: "from 'recharts'"
---

<objective>
선생님 추천 UI 컴포넌트 구현

Purpose: 학생별 선생님 추천 목록과 궁합 점수를 시각화하는 UI 컴포넌트를 구현합니다. 레이더 차트로 직관적인 이해를 돕습니다.

Output:
- 선생님 추천 목록 컴포넌트 (TeacherRecommendationList)
- 궁합 점수 카드 컴포넌트 (CompatibilityScoreCard)
- 궁합 레이더 차트 컴포넌트 (CompatibilityRadarChart)
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-compatibility-analysis-matching/13-RESEARCH.md

# Use getTeacherRecommendations from 13-04
@.planning/phases/13-compatibility-analysis-matching/13-04-SUMMARY.md

# Follow UI component pattern
@/mnt/data/projects/ai/ai-afterschool/src/components/teachers/teacher-analysis-panels.tsx

# Recharts for visualization
@/mnt/data/projects/ai/ai-afterschool/src/components/ui/chart.tsx (if exists)
</context>

<tasks>

<task type="auto">
  <name>선생님 추천 목록 컴포넌트 구현</name>
  <files>src/components/compatibility/teacher-recommendation-list.tsx</files>
  <action>
1. src/components/compatibility/teacher-recommendation-list.tsx 생성:

2. 컴포넌트 구조 ("use client"):
   - props: { recommendations: TeacherRecommendation[] }
   - TanStack Table 또는 간단한 map으로 순위 표시
   - 각 항목:
     * 순위 (1, 2, 3...)
     * 선생님 이름 (teacherName)
     * 역할 (teacherRole)
     * 전체 점수 (score.overall) - Badge로 표시
     * CompatibilityScoreCard로 breakdown 표시
     * 배정 버튼 (assignStudentToTeacher 호출)

3. 점수별 색상:
   - >= 80: green (매우 좋음)
   - >= 60: blue (좋음)
   - >= 40: yellow (보통)
   - < 40: gray (낮음)

참고: src/components/teachers/teacher-analysis-panels.tsx의 패널 구조 따르기 (Card, Badge 사용)
  </action>
  <verify>
```bash
npm run type-check
```
  </verify>
  <done>TeacherRecommendationList가 recommendations 배열을 받아 순위별로 선생님 목록 표시</done>
</task>

<task type="auto">
  <name>궁합 점수 카드 컴포넌트 구현</name>
  <files>src/components/compatibility/compatibility-score-card.tsx</files>
  <action>
1. src/components/compatibility/compatibility-score-card.tsx 생성:

2. 컴포넌트 구조 ("use client"):
   - props: { score: number, breakdown: CompatibilityBreakdown, reasons: string[] }
   - Card, CardHeader, CardTitle, CardContent 사용
   - 전체 점수: 큰 폰트로 score.overall 표시 (3자리)
   - 항목별 점수: CompatibilityBar 서브컴포넌트로 Progress bar 표시
     * MBTI: breakdown.mbti / 25
     * 학습 스타일: breakdown.learningStyle / 25
     * 사주: breakdown.saju / 20
     * 성명학: breakdown.name / 15
     * 부하 분산: breakdown.loadBalance / 15
   - 추천 이유: reasons 배열을 불릿 포인트로 표시

3. CompatibilityBar 서브컴포넌트:
   - label, value, max props
   - shadcn/ui Progress 컴포넌트 사용
   - (value / max) * 100으로 percentage 계산

참고: RESEARCH.md의 CompatibilityScoreCard 예시 코드 참조
  </action>
  <verify>
```bash
npm run type-check
```
  </verify>
  <done>CompatibilityScoreCard가 score, breakdown, reasons를 받아 카드 형태로 궁합 정보 표시</done>
</task>

<task type="auto">
  <name>궁합 레이더 차트 컴포넌트 구현</name>
  <files>src/components/compatibility/compatibility-radar-chart.tsx</files>
  <action>
1. src/components/compatibility/compatibility-radar-chart.tsx 생성:

2. 컴포넌트 구조 ("use client"):
   - props: { breakdown: CompatibilityBreakdown }
   - Recharts RadarChart 사용:
     * data: [
         { subject: 'MBTI', value: breakdown.mbti, max: 25 },
         { subject: '학습 스타일', value: breakdown.learningStyle, max: 25 },
         { subject: '사주', value: breakdown.saju, max: 20 },
         { subject: '성명학', value: breakdown.name, max: 15 },
         { subject: '부하 분산', value: breakdown.loadBalance, max: 15 },
       ]
     * PolarGrid: 그리드 표시
     * PolarAngleAxis: 축 라벨 (subject)
     * PolarRadiusAxis: 값 범위 (0-100)
     * Radar: 데이터 표시 (name="궁합 점수", stroke="#8884d8", fill="#8884d8", fillOpacity=0.6)
   - 5각형 레이더 차트로 시각화

참고: Recharts 공식 문서의 RadarChart 예시 참조
  </action>
  <verify>
```bash
npm run type-check
```
  </verify>
  <done>CompatibilityRadarChart가 breakdown을 받아 5각형 레이더 차트로 궁합 항목별 점수 시각화</done>
</task>

</tasks>

<verification>
1. npm run type-check로 타입 안전성 확인
2. TeacherRecommendationList 테스트:
   - 순위가 1부터 순차적으로 표시
   - 점수별 색상이 정확히 적용
3. CompatibilityScoreCard 테스트:
   - 전체 점수가 크게 표시
   - 항목별 Progress bar가 정상 표시
   - 추천 이유가 불릿 포인트로 표시
4. CompatibilityRadarChart 테스트:
   - 5각형 레이더 차트가 렌더링
   - 각 항목이 올바른 위치에 표시
</verification>

<success_criteria>
1. TeacherRecommendationList가 순위, 선생님 이름, 점수, breakdown을 표시
2. CompatibilityScoreCard가 항목별 점수를 Progress bar로 시각화
3. CompatibilityRadarChart가 5각형 차트로 궁합을 직관적으로 표현
4. 모든 컴포넌트가 shadcn/ui Card, Badge, Progress를 사용하여 일관된 스타일 유지
</success_criteria>

<output>
After completion, create `.planning/phases/13-compatibility-analysis-matching/13-08-SUMMARY.md`
</output>
