---
phase: 13-compatibility-analysis-matching
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/lib/actions/compatibility.ts
  - src/app/api/compatibility/calculate/route.ts
  - src/lib/actions/assignment.ts
autonomous: true

must_haves:
  truths:
    - "선생님이 궁합 분석 Server Action을 호출할 수 있다"
    - "POST /api/compatibility/calculate 엔드포인트가 궁합 점수 계산 API를 제공한다"
    - "궁합 분석 결과가 CompatibilityResult 테이블에 저장된다"
    - "RBAC가 적용되어 팀 내 데이터만 접근 가능하다"
  artifacts:
    - path: "src/lib/actions/compatibility.ts"
      provides: "궁합 분석 Server Actions"
      exports: ["analyzeCompatibility", "batchAnalyzeCompatibility"]
    - path: "src/app/api/compatibility/calculate/route.ts"
      provides: "REST API 엔드포인트"
      exports: ["POST handler"]
    - path: "src/lib/actions/assignment.ts"
      provides: "수동 배정 Server Action"
      exports: ["assignStudentToTeacher"]
  key_links:
    - from: "src/lib/actions/compatibility.ts"
      to: "src/lib/analysis/compatibility-scoring.ts"
      via: "import calculateCompatibilityScore"
      pattern: "from '@/lib/analysis/compatibility-scoring'"
    - from: "src/lib/actions/compatibility.ts"
      to: "src/lib/db/compatibility-result.ts"
      via: "import upsertCompatibilityResult"
      pattern: "from '@/lib/db/compatibility-result'"
    - from: "src/lib/actions/compatibility.ts"
      to: "src/lib/dal"
      via: "import verifySession for RBAC"
      pattern: "verifySession()"
    - from: "src/app/api/compatibility/calculate/route.ts"
      to: "src/lib/actions/compatibility.ts"
      via: "call analyzeCompatibility"
      pattern: "analyzeCompatibility("
---

<objective>
궁합 분석 Server Actions 및 API 엔드포인트 구현

Purpose: UI에서 궁합 분석을 실행할 수 있는 Server Actions와 REST API를 구현합니다. RBAC를 적용하여 팀 내 데이터만 접근 가능하도록 하고, 계산된 궁합 점수를 DB에 저장합니다.

Output:
- 궁합 분석 Server Actions (analyzeCompatibility, batchAnalyzeCompatibility)
- POST /api/compatibility/calculate REST API 엔드포인트
- 수동 학생 배정 Server Action (assignStudentToTeacher)
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-compatibility-analysis-matching/13-RESEARCH.md

# Use compatibility scoring from 13-01
@.planning/phases/13-compatibility-analysis-matching/13-01-SUMMARY.md

# Follow Server Actions pattern
@/mnt/data/projects/ai/ai-afterschool/src/lib/actions/teacher-analysis.ts

# Follow API route pattern
@/mnt/data/projects/ai/ai-afterschool/src/app/api/students/route.ts

# RBAC pattern
@/mnt/data/projects/ai/ai-afterschool/src/lib/dal
</context>

<tasks>

<task type="auto">
  <name>궁합 분석 Server Actions 구현</name>
  <files>src/lib/actions/compatibility.ts</files>
  <action>
1. src/lib/actions/compatibility.ts 생성:

2. analyzeCompatibility(teacherId, studentId) 함수:
   - verifySession()으로 인증 및 RBAC 확인
   - Teacher, Student 조회 (관련 분석 포함: teacherMbtiAnalysis, teacherSajuAnalysis, teacherNameAnalysis, mbtiAnalysis, sajuAnalysis, nameAnalysis)
   - RBAC: 본인 팀 데이터만 접근 가능 (verifySession의 teamId 필터링 활용)
   - calculateCompatibilityScore 호출 (13-01에서 구현)
   - upsertCompatibilityResult로 DB 저장
   - revalidatePath("/students/{studentId}")
   - revalidatePath("/teachers/{teacherId}")
   - { success: true, score } 반환

3. batchAnalyzeCompatibility(studentIds) 함수:
   - 다수 학생에 대해 궁합 분석 일괄 실행
   - 팀 내 모든 Teacher와 각 Student 쌍에 대해 analyzeCompatibility 호출
   - 병렬 처리로 성능 최적화 (Promise.all)

참고: teacher-analysis.ts의 runTeacherSajuAnalysis 패턴 따르기 (verifySession → DB 조회 → 분석 함수 호출 → DB 저장 → revalidatePath)
  </action>
  <verify>
```bash
npm run type-check
```
  </verify>
  <done>analyzeCompatibility 함수가 teacherId, studentId를 받아 궁합 점수 계산 후 DB 저장</done>
</task>

<task type="auto">
  <name>REST API 엔드포인트 구현</name>
  <files>src/app/api/compatibility/calculate/route.ts</files>
  <action>
1. src/app/api/compatibility/calculate/route.ts 생성:

2. POST 핸들러 구현:
   - Request body: { teacherId: string, studentId: string }
   - verifySession()으로 인증
   - analyzeCompatibility(teacherId, studentId) 호출
   - 200 OK: { success: true, score }
   - 400 Bad Request: Missing teacherId or studentId
   - 401 Unauthorized: Invalid session
   - 404 Not Found: Teacher or Student not found

3. Error handling:
   - try-catch로 에러 감지
   - 적절한 HTTP 상태 코드 반환

참고: src/app/api/students/route.ts의 POST 핸들러 패턴 따르기
  </action>
  <verify>
```bash
npm run type-check
```
curl -X POST http://localhost:3000/api/compatibility/calculate \
  -H "Content-Type: application/json" \
  -d '{"teacherId":"...","studentId":"..."}'
  </verify>
  <done>POST /api/compatibility/calculate가 요청을 받아 analyzeCompatibility를 실행하고 결과 반환</done>
</task>

<task type="auto">
  <name>수동 배정 Server Action 구현</name>
  <files>src/lib/actions/assignment.ts</files>
  <action>
1. src/lib/actions/assignment.ts 생성:

2. assignStudentToTeacher(studentId, teacherId) 함수:
   - verifySession()으로 인증 및 권한 확인
   - RBAC: DIRECTOR, TEAM_LEADER만 배정 가능
   - Student 조회 (본인 팀 데이터만)
   - Teacher 조회 (본인 팀 데이터만)
   - db.student.update({ where: { id: studentId }, data: { teacherId } })
   - revalidatePath("/matching")
   - revalidatePath("/students/{studentId}")
   - revalidatePath("/teachers/{teacherId}")
   - { success: true } 반환

3. Error handling:
   - 권한 없음: "배정 권한이 없습니다."
   - Student/Teacher not found: "학생/선생님을 찾을 수 없습니다."

참고: teams.ts의 createTeam, updateTeam 패턴 따르기 (RBAC 확인 → DB 업데이트 → revalidatePath)
  </action>
  <verify>
```bash
npm run type-check
```
  </verify>
  <done>assignStudentToTeacher 함수가 studentId, teacherId를 받아 Student.teacherId 업데이트</done>
</task>

</tasks>

<verification>
1. npm run type-check로 타입 안전성 확인
2. analyzeCompatibility Server Action 테스트:
   - teacherId, studentId 전달
   - CompatibilityResult DB에 저장 확인
   - revalidatePath로 캐시 무효화 확인
3. POST /api/compatibility/calculate API 테스트:
   - curl 또는 Postman으로 200 OK 응답 확인
   - 잘못된 요청 시 400/401/404 응답 확인
4. assignStudentToTeacher Server Action 테스트:
   - RBAC 확인 (TEACHER role 실행 시 에러)
   - DIRECTOR/TEAM_LEADER 실행 시 성공
   - Student.teacherId 업데이트 확인
</verification>

<success_criteria>
1. analyzeCompatibility Server Action이 궁합 점수를 계산하고 DB에 저장
2. POST /api/compatibility/calculate API가 외부 호출을 지원
3. assignStudentToTeacher가 RBAC를 적용하여 수동 배정 기능 제공
4. 모든 Server Action이 verifySession을 통해 인증 및 RBAC 확인
5. revalidatePath로 적절한 경로 캐시 무효화
</success_criteria>

<output>
After completion, create `.planning/phases/13-compatibility-analysis-matching/13-02-SUMMARY.md`
</output>
