---
phase: 26-counseling-&-matching-ui-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/counseling/UpcomingCounselingWidget.tsx
  - src/lib/actions/upcoming-counseling.ts
  - src/app/(dashboard)/dashboard/page.tsx
autonomous: true

must_haves:
  truths:
    - "대시보드 페이지 상단에 다가오는 상담 알림이 표시된다"
    - "오늘부터 7일 이내의 예약된 상담이 예약 개수와 함께 요약된다"
    - "요약 카드를 클릭하면 예약 목록이 펼쳐진다"
    - "각 예약 항목에 학생 이름, 예약 일시가 표시된다"
  artifacts:
    - path: "src/components/counseling/UpcomingCounselingWidget.tsx"
      provides: "상담 알림/리마인더 위젯 (요약 카드 + 펼침 목록)"
      min_lines: 60
      exports: ["UpcomingCounselingWidget"]
    - path: "src/lib/actions/upcoming-counseling.ts"
      provides: "다가오는 상담 조회 Server Action (오늘 + 7일 이내 SCHEDULED 예약)"
      min_lines: 40
      exports: ["getUpcomingCounseling"]
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "대시보드 페이지에 알림 위젯 통합"
      contains: "UpcomingCounselingWidget"
  key_links:
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "src/lib/actions/upcoming-counseling.ts"
      via: "getUpcomingCounseling action call on Server Component mount"
      pattern: "getUpcomingCounseling.*await"
    - from: "src/lib/actions/upcoming-counseling.ts"
      to: "prisma.parentCounselingReservation"
      via: "findMany with status=SCHEDULED and date range filter"
      pattern: "findMany.*status.*SCHEDULED.*scheduledAt.*gte.*lte"
    - from: "src/components/counseling/UpcomingCounselingWidget.tsx"
      to: "shadcn/ui Alert, Collapsible"
      via: "Alert component wrapper + Collapsible for expandable list"
      pattern: "Alert.*Collapsible.*CollapsibleContent"
---

<objective>
상담 알림/리마인더 위젯을 구현하여 대시보드 페이지 상단에 오늘부터 7일 이내의 예약된 상담을 요약 카드 형태로 표시합니다. 요약 카드 클릭 시 예약 목록이 펼쳐지며 각 예약의 학생 이름과 예약 일시를 확인할 수 있습니다.

Purpose: 다가오는 상담 시각적 알림으로 선생님이 예약된 상담을 놓치지 않게 함
Output: UpcomingCounselingWidget 컴포넌트, getUpcomingCounseling Server Action, dashboard/page.tsx 수정
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-counseling-&-matching-ui-enhancement/26-CONTEXT.md
@.planning/phases/26-counseling-&-matching-ui-enhancement/26-RESEARCH.md
@src/app/(dashboard)/dashboard/page.tsx
@src/lib/actions/reservations.ts
@src/components/counseling/ReservationCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 다가오는 상담 조회 Server Action 구현</name>
  <files>src/lib/actions/upcoming-counseling.ts</files>
  <action>
Create src/lib/actions/upcoming-counseling.ts with getUpcomingCounseling function:

1. **함수 시그니처**:
   ```typescript
   export async function getUpcomingCounseling(): Promise<{
     success: boolean
     data?: Array<{
       id: string
       scheduledAt: Date
       student: { id: string; name: string }
       parent: { id: string; name: string; relation: string }
     }>
     error?: string
   }>
   ```

2. **날짜 범위 계산**:
   - import { addDays, startOfDay, endOfDay } from 'date-fns'
   - now = new Date()
   - sevenDaysLater = addDays(now, 7)
   - range: [startOfDay(now), endOfDay(sevenDaysLater)]

3. **Prisma 쿼리**:
   - status: 'SCHEDULED'
   - teacherId: session.userId (verifySession으로 인증)
   - scheduledAt: { gte: startOfDay(now), lte: endOfDay(sevenDaysLater) }
   - include: { student: { select: { id, name } }, parent: { select: { id, name, relation } } }
   - orderBy: { scheduledAt: 'asc' }

4. **에러 처리**: verifySession 실패 시 { success: false, error: '인증되지 않은 요청입니다.' }

Important: RBAC 필요 없음 (자신의 예약만 조회하므로 teacherId 필터링으로 충분)
  </action>
  <verify>
1. File exists at src/lib/actions/upcoming-counseling.ts
2. Exports getUpcomingCounseling function
3. Uses date-fns for date range calculation (7 days from now)
4. Prisma query filters by status=SCHEDULED and date range
5. Returns { success: true, data: reservations } format
6. Includes student and parent select fields
  </verify>
  <done>
getUpcomingCounseling 함수가 오늘부터 7일 이내의 SCHEDULED 상태 예약을 조회하여 학생과 학부모 정보와 함께 반환한다.
  </done>
</task>

<task type="auto">
  <name>Task 2: 상담 알림 위젯 컴포넌트 구현</name>
  <files>src/components/counseling/UpcomingCounselingWidget.tsx</files>
  <action>
Create src/components/counseling/UpcomingCounselingWidget.tsx Client Component:

1. **Props 인터페이스**:
   ```typescript
   interface UpcomingCounselingWidgetProps {
     reservations: Array<{
       id: string
       scheduledAt: Date
       student: { id: string; name: string }
       parent: { id: string; name: string; relation: string }
     }>
   }
   ```

2. **UI 구조**:
   - shadcn/ui Alert wrapper (default variant)
   - AlertTitle: "다가오는 상담" + "{count}개 예정" badge
   - AlertDescription with shadcn/ui Collapsible
   - CollapsibleTrigger: "목록 보기" / "접기" 버튼 + ChevronDown icon
   - CollapsibleContent: 예약 목록 (ul > li)

3. **예약 항목 스타일링**:
   - 각 li: border-b pb-2 (마지막 제외)
   - 학생 이름: font-medium
   - 예약 일시: text-muted-foreground with toLocaleString('ko-KR')
   - Calendar icon (lucide-react) for visual cue

4. **빈 상태 처리**:
   - reservations.length === 0 → "예정된 상담이 없습니다" 메시지
   - Alert variant를 "default"에서 "secondary"로 변경

5. **data-testid**: "upcoming-counseling-alert", "upcoming-count", "counseling-list"

Pattern: Research.md Pattern 4 (Alert Widget with Collapsible Content) 참고
  </action>
  <verify>
1. File exists at src/components/counseling/UpcomingCounselingWidget.tsx
2. "use client" directive present
3. shadcn/ui Alert, Collapsible components imported and used
4. Collapsible toggle button changes text ("목록 보기" ↔ "접기")
5. ChevronDown icon rotates when open (rotate-180)
6. data-testid attributes present
7. Empty state handled (0 reservations)
  </verify>
  <done>
UpcomingCounselingWidget 컴포넌트가 예약 개수를 요약해서 표시하고 클릭 시 예약 목록을 펼쳐서 각 상담의 학생 이름과 예약 일시를 보여준다.
  </done>
</task>

<task type="auto">
  <name>Task 3: 대시보드 페이지에 알림 위젯 통합</name>
  <files>src/app/(dashboard)/dashboard/page.tsx</files>
  <action>
Modify src/app/(dashboard)/dashboard/page.tsx:

1. **페이지 구조 확인**:
   - 기존 대시보드 페이지의 layout 파악
   - 최상단에 위젯 배치 (다른 카드들보다 우선)

2. **Server Component에서 데이터 페칭**:
   ```typescript
   import { getUpcomingCounseling } from '@/lib/actions/upcoming-counseling'

   const session = await verifySession()
   let upcomingReservations = []

   if (session) {
     const result = await getUpcomingCounseling()
     if (result.success) {
       upcomingReservations = result.data || []
     }
   }
   ```

3. **위젯 렌더링**:
   - import { UpcomingCounselingWidget } from '@/components/counseling/UpcomingCounselingWidget'
   - 다른 컨텐츠 위에 렌더링 (최상단)
   - 전체 너비: col-span-full 또는 container mx-auto

4. **스타일링 통합**:
   - 기존 대시보드 스타일과 일치시키기
   - 필요시 mb-4/mb-6으로 아래 간격 조정

Important: 위젯이 session이 있을 때만 렌더링되도록 조건부 렌더링 (if (session && upcomingReservations.length > 0))
  </action>
  <verify>
1. getUpcomingCounseling imported and called in page.tsx
2. UpcomingCounselingWidget imported and rendered
3. Widget positioned at top of dashboard (before other content)
4. Conditional rendering when session exists
5. Widget displays correctly with data (or empty state)
6. Styling consistent with existing dashboard
  </verify>
  <done>
대시보드 페이지가 상단에 UpcomingCounselingWidget을 렌더링하여 다가오는 상담을 시각적으로 알리고 사용자가 클릭으로 상세 목록을 확인할 수 있게 한다.
  </done>
</task>

</tasks>

<verification>
**Overall Phase Verification:**

1. **알림 표시 확인**:
   - 대시보드 접속 → 상단에 "다가오는 상담" Alert 표시
   - "N개 예정" 배지가 정확한 개수 표시
   - 7일 이내 예약이 없으면 "예정된 상담이 없습니다" 메시지

2. **목록 펼침 확인**:
   - "목록 보기" 버튼 클릭 → 예약 목록 펼짐
   - ChevronDown 아이콘 아래로 회전 (rotate-180)
   - 각 예약에 학생 이름, 예약 일시(KST) 표시

3. **접기 동작 확인**:
   - "접기" 버튼 클릭 → 목록 접힘
   - ChevronDown 아이콘 원위치

4. **날짜 범위 정확성 확인**:
   - 오늘 예약 포함 (startOfDay)
   - 7일 후 예약 포함 (endOfDay(addDays(7)))
   - 8일 후 예약 제외

5. **권한 확인**:
   - TEACHER 역할 → 자신의 예약만 표시
   - SCHEDULED 상태만 표시 (COMPLETED, CANCELLED, NO_SHOW 제외)
</verification>

<success_criteria>
1. 대시보드 페이지 상단에 다가오는 상담 알림이 표시된다
2. 오늘부터 7일 이내의 예약된 상담 개수가 정확히 표시된다
3. 요약 카드 클릭 시 예약 목록이 펼쳐진다
4. 각 예약 항목에 학생 이름과 예약 일시가 표시된다
5. 예약이 없을 경우 "예정된 상담이 없습니다" 메시지가 표시된다
</success_criteria>

<output>
After completion, create `.planning/phases/26-counseling-&-matching-ui-enhancement/26-02-SUMMARY.md` with:
- Implemented widget and action
- Verification results
- Any deviations from plan
- Next steps (26-03)
</output>
