---
phase: 26-counseling-&-matching-ui-enhancement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/matching/MatchingHistoryTab.tsx
  - src/components/matching/MatchingAuditTable.tsx
  - src/components/matching/AuditLogDetailDialog.tsx
  - src/lib/actions/matching-history.ts
  - src/app/(dashboard)/matching/page.tsx
autonomous: true

must_haves:
  truths:
    - "매칭/배정 페이지에서 변경 이력 테이블이 표시된다"
    - "변경 일시, 변경자, 변경 유형, 변경 내용이 테이블로 표시된다"
    - "날짜 범위, 변경자, 변경 유형 필터가 제공된다"
    - "테이블 행 클릭 시 변경 상세 모달이 표시된다"
    - "상세 모달에서 이전/후 값을 비교할 수 있다"
  artifacts:
    - path: "src/components/matching/MatchingHistoryTab.tsx"
      provides: "매칭 이력 탭 컴포넌트 (필터 + 테이블 + 모달)"
      min_lines: 80
      exports: ["MatchingHistoryTab"]
    - path: "src/components/matching/MatchingAuditTable.tsx"
      provides: "감사 로그 테이블 컴포넌트 (shadcn/ui Table)"
      min_lines: 100
      exports: ["MatchingAuditTable"]
    - path: "src/components/matching/AuditLogDetailDialog.tsx"
      provides: "변경 상세 모달 (이전/후 값 비교)"
      min_lines: 60
      exports: ["AuditLogDetailDialog"]
    - path: "src/lib/actions/matching-history.ts"
      provides: "매칭 이력 조회 Server Action (AuditLog 기반)"
      min_lines: 60
      exports: ["getMatchingHistory"]
    - path: "src/app/(dashboard)/matching/page.tsx"
      provides: "매칭 페이지에 이력 탭 추가"
      contains: "MatchingHistoryTab"
  key_links:
    - from: "src/app/(dashboard)/matching/page.tsx"
      to: "src/lib/actions/matching-history.ts"
      via: "getMatchingHistory action call with filter params"
      pattern: "getMatchingHistory.*startDate.*endDate"
    - from: "src/lib/actions/matching-history.ts"
      to: "prisma.auditLog"
      via: "findMany with entityType='Student' filter"
      pattern: "findMany.*entityType.*Student"
    - from: "src/components/matching/MatchingAuditTable.tsx"
      to: "src/components/matching/AuditLogDetailDialog.tsx"
      via: "onClick handler opens dialog with selected log"
      pattern: "onClick.*setSelectedLog.*Dialog"
    - from: "src/components/matching/AuditLogDetailDialog.tsx"
      to: "changes JSON"
      via: "formatChanges function for before/after display"
      pattern: "formatChanges.*before.*after"
---

<objective>
매칭 이력/감사 로그 UI를 구현하여 배정 페이지에서 학생 배정 변경 이력을 테이블 형태로 조회하고 필터링할 수 있게 합니다. 테이블 행 클릭 시 변경 상세 모달이 표시되며 이전/후 값을 비교할 수 있습니다. AuditLog 모델을 활용하여 학생 배정 관련 변경만 추출합니다.

Purpose: 매칭/배정 변경 추적 및 투명성 확보 (감사 로그 테이블 + 필터 + 상세 보기)
Output: MatchingHistoryTab, MatchingAuditTable, AuditLogDetailDialog 컴포넌트, getMatchingHistory Server Action, matching/page.tsx 수정
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-counseling-&-matching-ui-enhancement/26-CONTEXT.md
@.planning/phases/26-counseling-&-matching-ui-enhancement/26-RESEARCH.md
@src/app/(dashboard)/matching/page.tsx
@src/lib/actions/audit.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: 매칭 이력 조회 Server Action 구현</name>
  <files>src/lib/actions/matching-history.ts</files>
  <action>
Create src/lib/actions/matching-history.ts with getMatchingHistory function:

1. **함수 시그니처**:
   ```typescript
   export interface MatchingHistoryParams {
     startDate?: string
     endDate?: string
     teacherId?: string
     action?: string
     page?: number
     pageSize?: number
   }

   export async function getMatchingHistory(params: MatchingHistoryParams): Promise<{
     success: boolean
     data?: { logs: AuditLogEntry[]; total: number }
     error?: string
   }>
   ```

2. **Prisma 쿼리 조건**:
   - entityType: 'Student' (학생 배정 변경만 추적)
   - 날짜 범위: createdAt { gte: startDate?, lte: endDate? }
   - 변경자 필터: teacherId (optional)
   - 변경 유형 필터: action (optional: CREATE, UPDATE, DELETE)
   - 권한: DIRECTOR만 조회 가능 (verifySession role check)

3. **페이지네이션**:
   - skip: (page - 1) * pageSize
   - take: pageSize (default 20)
   - total: db.auditLog.count({ where })

4. **관계 포함**:
   - include: { teacher: { select: { id, name, email } } }

5. **응답 형식**: { success: true, data: { logs: [...], total: N } }

Important: AuditLogEntry 타입은 src/lib/actions/audit.ts에서 가져오기 (이미 정의됨)
  </action>
  <verify>
1. File exists at src/lib/actions/matching-history.ts
2. Exports getMatchingHistory with MatchingHistoryParams interface
3. Prisma query filters by entityType='Student'
4. Pagination parameters (page, pageSize) respected
5. DIRECTOR role check present (verifySession)
6. Returns { success, data, error } format
7. Imports AuditLogEntry from audit.ts
  </verify>
  <done>
getMatchingHistory 함수가 학생 배정 관련 AuditLog를 조회하여 날짜 범위, 변경자, 변경 유형 필터와 페이지네이션을 적용하여 반환한다.
  </done>
</task>

<task type="auto">
  <name>Task 2: 감사 로그 테이블 컴포넌트 구현</name>
  <files>src/components/matching/MatchingAuditTable.tsx</files>
  <action>
Create src/components/matching/MatchingAuditTable.tsx Client Component:

1. **Props 인터페이스**:
   ```typescript
   interface MatchingAuditTableProps {
     logs: AuditLogEntry[]
     onRowClick: (log: AuditLogEntry) => void
   }
   ```

2. **테이블 구조** (shadcn/ui Table):
   - TableHeader: 변경 일시, 변경자, 변경 유형, 변경 내용
   - TableBody: logs.map(log => TableRow)

3. **셀 스타일링**:
   - 변경 일시: toLocaleString('ko-KR')
   - 변경 유형: Badge with color coding
     - CREATE: bg-green-100 text-green-800
     - UPDATE: bg-blue-100 text-blue-800
     - DELETE: bg-red-100 text-red-800
   - 변경 내용: formatChanges(log.changes) (truncate with max-w-md)

4. **행 클릭 핸들러**:
   - onClick={() => onRowClick(log)}
   - cursor-pointer hover:bg-muted

5. **formatChanges 함수**:
   - changes가 null/undefined면 '-'
   - { before, after } 구조면 "key: before → after"
   - 단순 값이면 "key: value"

6. **data-testid**: "audit-log-table", "audit-log-row-{id}"

Pattern: Research.md Pattern 5 (Audit Log Table with Detail Modal) 참고
  </action>
  <verify>
1. File exists at src/components/matching/MatchingAuditTable.tsx
2. "use client" directive present
3. shadcn/ui Table components imported
4. Action badge color coding (CREATE/UPDATE/DELETE)
5. formatChanges function handles before/after structure
6. Row onClick triggers onRowClick callback
7. data-testid attributes present
  </verify>
  <done>
MatchingAuditTable 컴포넌트가 AuditLog 배열을 테이블로 렌더링하고 각 행 클릭 시 상세 보기 콜백을 호출한다.
  </done>
</task>

<task type="auto">
  <name>Task 3: 변경 상세 모달 컴포넌트 구현</name>
  <files>src/components/matching/AuditLogDetailDialog.tsx</files>
  <action>
Create src/components/matching/AuditLogDetailDialog.tsx Client Component:

1. **Props 인터페이스**:
   ```typescript
   interface AuditLogDetailDialogProps {
     log: AuditLogEntry | null
     open: boolean
     onOpenChange: (open: boolean) => void
   }
   ```

2. **모달 구조** (shadcn/ui Dialog):
   - DialogTitle: "변경 상세"
   -DialogContent with space-y-4

3. **상세 정보 섹션**:
   - 변경 일시: log.createdAt.toLocaleString('ko-KR')
   - 변경자: log.teacherName (log.teacher.email 추가 표시)
   - 변경 유형: Badge (테이블과 동일 색상)
   - 변경 내용: pre 태그로 JSONpretty print

4. **변경 내용 포맷팅**:
   - before/after 구조를 비교 테이블로 표시
   - 간단한 diff UI (이전 값 | 새 값)
   - deleted 속성은 빨간색, added는 초록색 하이라이트

5. **스타일링**:
   - pre: bg-muted rounded p-4 text-sm overflow-x-auto
   - 변경자 정보: flex items-center gap-2

6. **data-testid**: "audit-log-detail-dialog", "change-details"

Pattern: Research.md Pattern 5 (Audit Log Table with Detail Modal) 참고
  </action>
  <verify>
1. File exists at src/components/matching/AuditLogDetailDialog.tsx
2. "use client" directive present
3. shadcn/ui Dialog components imported
4. Dialog displays log details (date, teacher, action, changes)
5. Changes JSON formatted with pre tag or diff view
6. onOpenChange callback works correctly
7. data-testid attributes present
  </verify>
  <done>
AuditLogDetailDialog 컴포넌트가 선택된 AuditLog의 상세 정보를 모달로 표시하고 변경 내용을 가독성 있게 포맷팅한다.
  </done>
</task>

<task type="auto">
  <name>Task 4: 매칭 이력 탭 컴포넌트 구현</name>
  <files>src/components/matching/MatchingHistoryTab.tsx</files>
  <action>
Create src/components/matching/MatchingHistoryTab.tsx Client Component:

1. **Props 인터페이스**:
   ```typescript
   interface MatchingHistoryTabProps {
     initialFilters?: {
       startDate?: string
       endDate?: string
       teacherId?: string
       action?: string
     }
   }
   ```

2. **상태 관리**:
   - logs: AuditLogEntry[]
   - selectedLog: AuditLogEntry | null
   - filters: { startDate, endDate, teacherId, action }
   - pagination: { page, pageSize, total }

3. **필터 UI**:
   - 날짜 범위: Input type="date" (시작일, 종료일)
   - 변경자: Select (선생님 목록 - getTeachers action으로 가져오기)
   - 변경 유형: Select (CREATE, UPDATE, DELETE, ALL)
   - 적용 버튼: fetchLogs() 호출

4. **데이터 페칭**:
   - useEffect(() => { fetchLogs() }, [filters, page])
   - getMatchingHistory action 호출
   - loading/error 상태 관리

5. **컴포넌트 조합**:
   - 필터 폼 (Grid layout)
   - MatchingAuditTable (logs, onRowClick)
   - AuditLogDetailDialog (selectedLog, open, onOpenChange)
   - Pagination (이전/다음 버튼)

6. **스타일링**: Card > CardHeader > CardContent 구조

Important: DIRECTOR 권한 확인 - 없으면 "권한이 없습니다" 메시지 표시
  </action>
  <verify>
1. File exists at src/components/matching/MatchingHistoryTab.tsx
2. "use client" directive present
3. Filter inputs (date, teacher, action) present
4. getMatchingHistory action called on filter/page change
5. MatchingAuditTable and AuditLogDetailDialog composed
6. Pagination controls (previous/next) functional
7. Loading state shown during fetch
8. Director role check present
  </verify>
  <done>
MatchingHistoryTab 컴포넌트가 필터 UI와 테이블, 모달을 조합하여 매칭 이력 조회 기능을 완성하고 페이지네이션을 지원한다.
  </done>
</task>

<task type="auto">
  <name>Task 5: 매칭 페이지에 이력 탭 추가</name>
  <files>src/app/(dashboard)/matching/page.tsx</files>
  <action>
Modify src/app/(dashboard)/matching/page.tsx:

1. **탭 UI 추가**:
   - 기존 컨텐츠를 "배정 현황" 탭으로 이동
   - "이력 조회" 탭 추가
   - shadcn/ui Tabs 사용 (value="current" | "history")

2. **이력 탭 컨텐츠**:
   - MatchingHistoryTab 렌더링
   - Server Component에서 초기 필터 전달 가능

3. **탭 상태 관리**:
   - Client Component로 Tabs state 관리
   - 또는 URL query param으로 탭 상태 관리 (?tab=history)

4. **레이아웃 수정**:
   - 기존 배정 관리 카드들을 "배정 현황" 탭 안으로 이동
   - TabsList > TabsTrigger (배정 현황, 이력 조회)
   - TabsContent로 각 탭 구분

5. **초기 렌더링 최적화**:
   - history 탭은 클릭 시 데이터 페칭 (lazy loading)
   - current 탭은 기존 동작 유지

Pattern: 기존 CounselingPageTabs 패턴 참고
  </action>
  <verify>
1. Tabs component added to matching page
2. Two tabs: "배정 현황" (current) and "이력 조회" (history)
3. Existing content moved to "배정 현황" tab
4. MatchingHistoryTab rendered in "이력 조회" tab
5. Tab switching works without page reload
6. URL state or client state for tab selection
7. Existing functionality preserved
  </verify>
  <done>
매칭 페이지가 배정 현황과 이력 조회 두 개의 탭을 제공하고 이력 조회 탭에서 MatchingHistoryTab을 렌더링한다.
  </done>
</task>

</tasks>

<verification>
**Overall Phase Verification:**

1. **이력 테이블 표시 확인**:
   - 매칭 페이지 접속 → "이력 조회" 탭 클릭
   - 감사 로그 테이블 표시 (변경 일시, 변경자, 변경 유형, 변경 내용)

2. **필터 동작 확인**:
   - 날짜 범위 설정 → 해당 기간 로그만 표시
   - 변경자 선택 → 특정 선생님 변경만 표시
   - 변경 유형 선택 → CREATE/UPDATE/DELETE 필터링
   - "적용" 버튼 클릭 시 필터 적용

3. **페이지네이션 확인**:
   - 20개 이상 로그 시 페이지네이션 표시
   - 다음/이전 버튼으로 페이지 이동
   - 페이지 번호 표시

4. **상세 모달 확인**:
   - 테이블 행 클릭 → 상세 모달 오픈
   - 변경 일시, 변경자, 변경 유형, 변경 내용 표시
   - before/after 값 비교 표시
   - 닫기 버튼 또는 바깥 클릭 시 모달 닫기

5. **권한 확인**:
   - DIRECTOR 역할 → 이력 조회 접근 가능
   - TEACHER 역할 → "권한이 없습니다" 메시지 또는 탭 숨김
</verification>

<success_criteria>
1. 매칭 페이지에서 변경 이력 테이블이 표시된다
2. 변경 일시, 변경자, 변경 유형, 변경 내용이 각 컬럼으로 표시된다
3. 날짜 범위, 변경자, 변경 유형 필터가 제공되어 동작한다
4. 테이블 행 클릭 시 변경 상세 모달이 표시된다
5. 상세 모달에서 이전/후 값을 비교할 수 있다
</success_criteria>

<output>
After completion, create `.planning/phases/26-counseling-&-matching-ui-enhancement/26-03-SUMMARY.md` with:
- Implemented components and action
- Verification results
- Any deviations from plan
- Next steps (26-04)
</output>
