---
phase: 26-counseling-&-matching-ui-enhancement
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/matching/AssignmentResultCard.tsx
  - src/lib/actions/assignment-results.ts
  - src/app/(dashboard)/matching/auto-assign/page.tsx
  - src/components/statistics/PerformanceTrendChart.tsx
  - src/lib/utils/date-range.ts
  - src/components/statistics/DateRangeFilter.tsx
  - src/app/(dashboard)/analytics/page.tsx
autonomous: true

must_haves:
  truths:
    - "자동 배정 결과 카드가 배정된 학생 수, 제외된 학생, 성공/실패 카운트를 표시한다"
    - "성과 차트에 기간 선택 프리셋 버튼이 제공된다 (오늘, 최근 7일, 최근 30일, 최근 3개월, 전체)"
    - "기간 선택 시 향상률 차트가 해당 기간 데이터로 업데이트된다"
  artifacts:
    - path: "src/components/matching/AssignmentResultCard.tsx"
      provides: "자동 배정 결과 요약 카드 (배정/제외/성공/실패 카운트)"
      min_lines: 50
      exports: ["AssignmentResultCard"]
    - path: "src/lib/actions/assignment-results.ts"
      provides: "배정 결과 집계 Server Action (AssignmentProposal 기반)"
      min_lines: 40
      exports: ["getAssignmentResults"]
    - path: "src/app/(dashboard)/matching/auto-assign/page.tsx"
      provides: "자동 배정 페이지에 결과 카드 추가"
      contains: "AssignmentResultCard"
    - path: "src/components/statistics/PerformanceTrendChart.tsx"
      provides: "향상률 차트 컴포넌트 (기간 선택 + Recharts)"
      min_lines: 80
      exports: ["PerformanceTrendChart"]
    - path: "src/lib/utils/date-range.ts"
      provides: "기간 프리셋 유틸리티 (TODAY, 7D, 30D, 3M, ALL)"
      min_lines: 30
      exports: ["getDateRangeFromPreset", type ExtendedDatePreset]
    - path: "src/components/statistics/DateRangeFilter.tsx"
      provides: "확장된 기간 필터 (프리셋 커스텀 지원)"
      contains: "presets, labels props"
    - path: "src/app/(dashboard)/analytics/page.tsx"
      provides: "성과 분석 페이지에 차트 기간 선택 추가"
      contains: "PerformanceTrendChart, DateRangeFilter"
  key_links:
    - from: "src/app/(dashboard)/matching/auto-assign/page.tsx"
      to: "src/lib/actions/assignment-results.ts"
      via: "getAssignmentResults(proposalId) call"
      pattern: "getAssignmentResults.*proposalId"
    - from: "src/lib/actions/assignment-results.ts"
      to: "prisma.assignmentProposal"
      via: "findUnique with summary, assignments include"
      pattern: "findUnique.*id.*summary.*assignments"
    - from: "src/app/(dashboard)/analytics/page.tsx"
      to: "src/components/statistics/PerformanceTrendChart.tsx"
      via: "onPresetChange prop to update chart data"
      pattern: "PerformanceTrendChart.*onPresetChange"
    - from: "src/components/statistics/PerformanceTrendChart.tsx"
      to: "src/lib/utils/date-range.ts"
      via: "getDateRangeFromPreset function call"
      pattern: "getDateRangeFromPreset.*preset"
    - from: "src/components/statistics/DateRangeFilter.tsx"
      to: "src/lib/utils/date-range.ts"
      via: "ExtendedDatePreset type import and usage"
      pattern: "ExtendedDatePreset.*TODAY.*7D"
---

<objective>
자동 배정 결과 카드와 성과 향상률 차트의 기간 선택 기능을 구현합니다. 배정 결과 카드는 AssignmentProposal 데이터를 기반으로 배정된 학생 수, 제외된 학생, 성공/실패 카운트를 요약해서 표시합니다. 성과 차트는 DateRangeFilter 컴포넌트를 확장하여 오늘, 최근 7일, 최근 30일, 최근 3개월, 전체 프리셋을 제공하고 기간 선택 시 차트 데이터를 업데이트합니다.

Purpose: 배정 결과 시각화 + 성과 추이 분석 (기간 선택 프리셋)
Output: AssignmentResultCard, getAssignmentResults action, auto-assign/page.tsx 수정, PerformanceTrendChart, getDateRangeFromPreset 유틸리티, DateRangeFilter 확장, analytics/page.tsx 수정
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-counseling-&-matching-ui-enhancement/26-CONTEXT.md
@.planning/phases/26-counseling-&-matching-ui-enhancement/26-RESEARCH.md
@src/app/(dashboard)/matching/auto-assign/page.tsx
@src/app/(dashboard)/analytics/page.tsx
@src/components/statistics/DateRangeFilter.tsx
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: 배정 결과 집계 Server Action 구현</name>
  <files>src/lib/actions/assignment-results.ts</files>
  <action>
Create src/lib/actions/assignment-results.ts with getAssignmentResults function:

1. **함수 시그니처**:
   ```typescript
   export async function getAssignmentResults(proposalId: string): Promise<{
     success: boolean
     data?: {
       totalStudents: number
       assignedCount: number
       excludedCount: number
       successCount: number
       failureCount: number
       averageScore: number
       createdAt: Date
       status: string
     }
     error?: string
   }>
   ```

2. **Prisma 쿼리**:
   - assignmentProposal.findUnique({ where: { id: proposalId } })
   - include: { proposer: { select: { id, name } } }

3. **데이터 추출**:
   - summary: { totalStudents, assignedStudents, averageScore, minScore, maxScore }
   - assignments: Array<{ studentId, teacherId, score }>
   - 성공/실패 카운트: score 임계값 (예: 60점 이상 성공)

4. **제외된 학생 계산**:
   - allStudents = await db.student.count()
   - excludedCount = allStudents - assignedStudents

5. **인증**: verifySession()으로 세션 확인

Important: AssignmentProposal 모델의 summary와 assignments는 Json 타입이므로 타입 캐스팅 필요
  </action>
  <verify>
1. File exists at src/lib/actions/assignment-results.ts
2. Exports getAssignmentResults function
3. Returns totalStudents, assignedCount, excludedCount, successCount, failureCount
4. Calculates excludedCount from total - assigned
5. Determines success/failure based on score threshold
6. verifySession() call present
7. Returns { success, data, error } format
  </verify>
  <done>
getAssignmentResults 함수가 AssignmentProposal ID로 배정 결과를 조회하여 전체/배정/제외/성공/실패 카운트를 집계하여 반환한다.
  </done>
</task>

<task type="auto">
  <name>Task 2: 배정 결과 카드 컴포넌트 구현</name>
  <files>src/components/matching/AssignmentResultCard.tsx</files>
  <action>
Create src/components/matching/AssignmentResultCard.tsx:

1. **Props 인터페이스**:
   ```typescript
   interface AssignmentResultCardProps {
     totalStudents: number
     assignedCount: number
     excludedCount: number
     successCount: number
     failureCount: number
     averageScore: number
     createdAt: Date
     status: string
   }
   ```

2. **카드 구조** (shadcn/ui Card):
   - CardHeader: CardTitle "배정 결과 요약"
   - CardContent: Grid layout with metric cards

3. **메트릭 카드들** (4개):
   - 배정 완료: assignedCount / totalStudents (success color)
   - 제외됨: excludedCount (warning color)
   - 성공: successCount (green)
   - 실패: failureCount (red)

4. **스타일링**:
   - Grid: grid grid-cols-2 md:grid-cols-4 gap-4
   - 각 메트릭: border rounded-lg p-4
   - 라벨: text-sm text-muted-foreground
   - 값: text-2xl font-bold

5. **추가 정보**:
   - 평균 궁합 점수: averageScore.toFixed(1)
   - 생성일시: createdAt.toLocaleString('ko-KR')
   - 상태: Badge (PENDING, APPLIED, CANCELLED)

6. **data-testid**: "assignment-result-card", "assigned-count", "excluded-count", etc.

Pattern: Research.md "배정 결과: 요약 카드 형태" 참고
  </action>
  <verify>
1. File exists at src/components/matching/AssignmentResultCard.tsx
2. Props interface matches expected fields
3. Card component structure with CardHeader, CardContent
4. 4 metric cards: assigned, excluded, success, failure
5. Grid layout (2 cols mobile, 4 cols desktop)
6. Average score and creation date displayed
7. Status badge with appropriate color
8. data-testid attributes present
  </verify>
  <done>
AssignmentResultCard 컴포넌트가 배정 결과를 4개 메트릭 카드(배정/제외/성공/실패)로 시각화하고 평균 점수와 생성일시를 추가 정보로 표시한다.
  </done>
</task>

<task type="auto">
  <name>Task 3: 자동 배정 페이지에 결과 카드 추가</name>
  <files>src/app/(dashboard)/matching/auto-assign/page.tsx</files>
  <action>
Modify src/app/(dashboard)/matching/auto-assign/page.tsx:

1. **기존 페이지 구조 파악**:
   - 자동 배정 제안 생성/표시 UI 확인
   - AssignmentProposal 데이터 사용 부분 확인

2. **데이터 페칭 추가**:
   - proposalId가 있으면 getAssignmentResults(proposalId) 호출
   - Server Component에서 await으로 데이터 가져오기
   - results = result?.data (if success)

3. **카드 렌더링**:
   - import { AssignmentResultCard } from '@/components/matching/AssignmentResultCard'
   - 제안 적용 후 또는 제안 상세 보기에 카드 표시
   - 배정 작업 카드 다음에 배치

4. **조건부 렌더링**:
   - results가 있고 status가 'APPLIED'일 때만 표시
   - 또는 항상 표시 (제안 생성 후 미리보기)

5. **스타일링 통합**:
   - 기존 페이지 스타일과 일치
   - space-y-6으로 카드 간격 유지

Important: 기존 배정 기능 파괴하지 않도록 결과 카드를 추가만 함
  </action>
  <verify>
1. getAssignmentResults imported and called
2. AssignmentResultCard imported and rendered
3. Card positioned appropriately in page layout
4. Conditional rendering based on proposal status
5. Props passed correctly to AssignmentResultCard
6. Existing auto-assign functionality preserved
7. Styling consistent with page design
  </verify>
  <done>
자동 배정 페이지가 AssignmentResultCard를 렌더링하여 배정 결과 요약(배정/제외/성공/실패 카운트)을 시각화한다.
  </done>
</task>

<task type="auto">
  <name>Task 4: 기간 프리셋 유틸리티 확장</name>
  <files>src/lib/utils/date-range.ts</files>
  <action>
Create or modify src/lib/utils/date-range.ts with extended presets:

1. **타입 정의**:
   ```typescript
   import { DatePreset, DateRange } from '@/types/statistics'

   export type ExtendedDatePreset = DatePreset | 'TODAY' | '7D' | '30D' | 'ALL'

   export function getDateRangeFromPreset(preset: ExtendedDatePreset): DateRange {
     // ...
   }
   ```

2. **프리셋 로직**:
   - TODAY: { start: startOfDay(now), end: endOfDay(now) }
   - 7D: { start: startOfDay(subDays(now, 7)), end: endOfDay(now) }
   - 30D: { start: startOfDay(subDays(now, 30)), end: endOfDay(now) }
   - 3M: { start: startOfDay(subMonths(now, 3)), end: endOfDay(now) }
   - ALL: { start: new Date(2020, 0, 1), end: endOfDay(now) }
   - 기존 1M, 6M, 1Y도 유지 (호환성)

3. **date-fns import**:
   - import { startOfDay, endOfDay, subDays, subMonths } from 'date-fns'

4. **내보내기**:
   - export type { ExtendedDatePreset }
   - export { getDateRangeFromPreset }

Pattern: Research.md Pitfall 3 (DateRangeFilter 프리셋 확장) 참고
  </action>
  <verify>
1. File exists at src/lib/utils/date-range.ts
2. Exports ExtendedDatePreset type
3. getDateRangeFromPreset handles TODAY, 7D, 30D, 3M, ALL
4. Existing presets (1M, 6M, 1Y) still work
5. Uses date-fns for date calculations
6. Returns { start: Date, end: Date } format
  </verify>
  <done>
getDateRangeFromPreset 유틸리티가 TODAY, 7D, 30D, 3M, ALL 프리셋을 지원하여 기간 범위를 계산한다.
  </done>
</task>

<task type="auto">
  <name>Task 5: DateRangeFilter 컴포넌트 확장</name>
  <files>src/components/statistics/DateRangeFilter.tsx</files>
  <action>
Modify src/components/statistics/DateRangeFilter.tsx:

1. **Props 인터페이스 확장**:
   ```typescript
   interface DateRangeFilterProps {
     value: string  // ExtendedDatePreset
     onChange: (preset: string) => void
     variant?: 'buttons' | 'dropdown'
     presets?: string[]  // 커스텀 프리셋 지원
     labels?: Record<string, string>  // 커스텀 라벨 지원
   }
   ```

2. **기본 프리셋 확장**:
   - 기본값: ['1M', '3M', '6M', '1Y'] → ['TODAY', '7D', '30D', '3M', 'ALL']
   - 커스텀 프리셋이 있으면 해당 값 사용

3. **라벨 확장**:
   - const PRESET_LABELS = labels || {
       'TODAY': '오늘',
       '7D': '최근 7일',
       '30D': '최근 30일',
       '3M': '최근 3개월',
       'ALL': '전체',
       // 기존 라벨 유지
     }

4. **variant='buttons'일 때**:
   - presets prop으로 전달된 배열만큼 버튼 생성
   - 각 버튼에 PRESET_LABELS[preset] 라벨 적용

5. **내보내기**:
   - export type { ExtendedDatePreset } from '@/lib/utils/date-range' (재내보내기)

Important: 기존 DatePreset 타입 호환성 유지
  </action>
  <verify>
1. Props interface includes presets and labels
2. Default presets updated to ['TODAY', '7D', '30D', '3M', 'ALL']
3. Custom labels applied when provided
4. Buttons rendered based on presets prop
5. Existing functionality preserved (backward compatible)
6. Exports ExtendedDatePreset type
  </verify>
  <done>
DateRangeFilter 컴포넌트가 presets와 labels prop을 받아 커스텀 프리셋과 라벨을 지원하고 기본값으로 오늘/7일/30일/3개월/전체를 제공한다.
  </done>
</task>

<task type="auto">
  <name>Task 6: 성과 향상률 차트 컴포넌트 구현</name>
  <files>src/components/statistics/PerformanceTrendChart.tsx</files>
  <action>
Create src/components/statistics/PerformanceTrendChart.tsx Client Component:

1. **Props 인터페이스**:
   ```typescript
   interface PerformanceTrendChartProps {
     initialPreset?: string
     onDataRequest: (range: DateRange) => Promise<TrendDataPoint[]>
   }
   ```

2. **상태 관리**:
   - preset: '7D' (기본값)
   - chartData: TrendDataPoint[]
   - loading: boolean

3. **데이터 페칭**:
   - useEffect(() => {
       const range = getDateRangeFromPreset(preset)
       onDataRequest(range).then(setChartData)
     }, [preset])

4. **UI 구조**:
   - Card > CardHeader > CardContent
   - CardHeader: 제목 + DateRangeFilter
   - CardContent: Recharts LineChart

5. **Recharts 구성**:
   - ResponsiveContainer width="100%" height={300}
   - LineChart data={chartData}
   - XAxis dataKey="date"
   - YAxis
   - CartesianGrid strokeDasharray="3 3"
   - Tooltip
   - Line type="monotone" dataKey="improvement" stroke="#3b82f6" strokeWidth={2}

6. **DateRangeFilter 연동**:
   - value={preset}, onChange={setPreset}
   - presets={['TODAY', '7D', '30D', '3M', 'ALL']}
   - labels={PRESET_LABELS}

7. **data-testid**: "performance-trend-chart", "date-range-filter"

Pattern: Research.md Pattern 6 (Date Range Presets with Recharts) 참고
  </action>
  <verify>
1. File exists at src/components/statistics/PerformanceTrendChart.tsx
2. "use client" directive present
3. preset state managed with '7D' default
4. DateRangeFilter integrated with onChange handler
5. Recharts components imported and used
6. Line chart renders improvement trend
7. data-testid attributes present
8. Loading state shown during fetch
  </verify>
  <done>
PerformanceTrendChart 컴포넌트가 DateRangeFilter와 Recharts LineChart를 조합하여 기간 선택 시 향상률 추이를 시각화한다.
  </done>
</task>

<task type="auto">
  <name>Task 7: 성과 분석 페이지에 차트 추가</name>
  <files>src/app/(dashboard)/analytics/page.tsx</files>
  <action>
Modify src/app/(dashboard)/analytics/page.tsx:

1. **기존 페이지 구조 확인**:
   - PerformanceDashboard 컴포넌트 사용 확인
   - 기존 차트 데이터 페칭 로직 확인

2. **데이터 요청 핸들러 구현**:
   ```typescript
   const fetchTrendData = async (range: DateRange): Promise<TrendDataPoint[]> => {
     //_gradeHistory 또는 성적 관련 데이터를 range로 필터링
     // 월별 향상률 집계 로직
     return trendData
   }
   ```

3. **PerformanceTrendChart 렌더링**:
   - import { PerformanceTrendChart } from '@/components/statistics/PerformanceTrendChart'
   - 기존 차트 영역에 추가 또는 대체
   - onDataRequest={fetchTrendData} prop 전달

4. **레이아웃 조정**:
   - 다른 통계 카드들과 함께 grid 배치
   - 전체 너비: col-span-full 또는 md:col-span-2

5. **초기 프리셋 설정**:
   - initialPreset="3M" (최근 3개월)
   - 또는 URL query param에서 읽기

Important: 기존 성과 대시보드 기능 유지하며 차트만 추가
  </action>
  <verify>
1. PerformanceTrendChart imported and rendered
2. fetchTrendData handler implements date range filtering
3. Chart integrated with existing dashboard layout
4. Initial preset set to '3M' or from URL
5. Existing analytics functionality preserved
6. Chart updates when preset changes
7. Date range passed to data fetch correctly
  </verify>
  <done>
성과 분석 페이지가 PerformanceTrendChart를 렌더링하여 향상률 추이 차트와 기간 선택 프리셋을 제공한다.
  </done>
</task>

</tasks>

<verification>
**Overall Phase Verification:**

1. **배정 결과 카드 확인**:
   - 자동 배정 페이지 접속 → 배정 결과 요약 카드 표시
   - 배정 완료: N/M명 (배정된 학생 수)
   - 제외됨: N명 (미배정 학생 수)
   - 성공: N명 (60점 이상)
   - 실패: N명 (60점 미만)

2. **성과 차트 기간 선택 확인**:
   - 성과 분석 페이지 접속 → 향상률 차트 표시
   - 기본 프리셋 버튼들: 오늘, 최근 7일, 최근 30일, 최근 3개월, 전체
   - 버튼 클릭 시 차트 데이터 업데이트

3. **차트 데이터 정확성 확인**:
   - "오늘" 클릭 → 오늘 데이터만 표시
   - "최근 7일" 클릭 → 최근 7일 데이터 표시
   - "최근 3개월" 클릭 → 최근 3개월 데이터 표시
   - "전체" 클릭 → 전체 기간 데이터 표시

4. **UI 일관성 확인**:
   - 배정 결과 카드 스타일이 기존 카드와 일치
   - 성과 차트가 기존 대시보드와 자연스럽게 통합
   - DateRangeFilter가 다른 필터와 일관된 스타일

5. **반응형 확인**:
   - 모바일에서 배정 결과 카드 2열로 표시
   - 데스크톱에서 4열로 표시
   - 차트가 화면 너비에 맞춰 반응형으로 렌더링
</verification>

<success_criteria>
1. 자동 배정 결과 카드가 배정된 학생 수, 제외된 학생, 성공/실패 카운트를 표시한다
2. 성과 차트에 기간 선택 프리셋 버튼이 제공된다 (오늘, 최근 7일, 최근 30일, 최근 3개월, 전체)
3. 기간 선택 시 향상률 차트가 해당 기간 데이터로 업데이트된다
4. 배정 결과 카드와 성과 차트가 기존 UI와 자연스럽게 통합된다
5. 모든 기능이 반응형으로 동작한다
</success_criteria>

<output>
After completion, create `.planning/phases/26-counseling-&-matching-ui-enhancement/26-04-SUMMARY.md` with:
- Implemented components and actions
- Verification results
- Any deviations from plan
- Phase 26 complete summary
</output>
