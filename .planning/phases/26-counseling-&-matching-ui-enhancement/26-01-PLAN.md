---
phase: 26-counseling-&-matching-ui-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/counseling/page.tsx
  - src/components/counseling/CounselingSearchBar.tsx
  - src/components/counseling/CounselingFilters.tsx
  - src/lib/actions/counseling-search.ts
autonomous: true

must_haves:
  truths:
    - "사용자가 학생 이름, 학부모 이름, 상담 주제를 하나의 검색창에서 통합 검색할 수 있다"
    - "Enter 키 또는 검색 버튼 클릭 시에만 검색이 실행된다 (즉시 검색 아님)"
    - "상담 유형, 날짜 범위, 선생님, 상태 등 복합 필터가 제공된다"
    - "검색 결과가 URL 쿼리 파라미터로 유지되어 북마크/공유가 가능하다"
  artifacts:
    - path: "src/components/counseling/CounselingSearchBar.tsx"
      provides: "통합 검색 컴포넌트 (학생 이름, 학부모 이름, 상담 주제)"
      min_lines: 50
      exports: ["CounselingSearchBar"]
    - path: "src/components/counseling/CounselingFilters.tsx"
      provides: "다중 필터 컴포넌트 (상담 유형, 날짜 범위, 선생님, 상태)"
      min_lines: 80
      exports: ["CounselingFilters"]
    - path: "src/lib/actions/counseling-search.ts"
      provides: "통합 검색 Server Action (Prisma OR 쿼리로 다중 필드 검색)"
      min_lines: 60
      exports: ["searchCounselingSessions"]
    - path: "src/app/(dashboard)/counseling/page.tsx"
      provides: "검색/필터 UI 통합 및 URL 상태 관리"
      contains: "searchParams, CounselingSearchBar, CounselingFilters"
  key_links:
    - from: "src/components/counseling/CounselingSearchBar.tsx"
      to: "/counseling"
      via: "router.push with URLSearchParams"
      pattern: "router.push.*searchParams"
    - from: "src/app/(dashboard)/counseling/page.tsx"
      to: "src/lib/actions/counseling-search.ts"
      via: "searchCounselingSessions action call with searchParams"
      pattern: "searchCounselingSessions.*searchParams"
    - from: "src/lib/actions/counseling-search.ts"
      to: "prisma.counselingSession"
      via: "findMany with OR condition for unified search"
      pattern: "OR.*student.*name.*contains|OR.*summary.*contains"
---

<objective>
상담 기록 검색/필터 UI를 구현하여 사용자가 학생 이름, 학부모 이름, 상담 주제를 통합 검색하고 복합 필터링을 수행할 수 있게 합니다. Enter 키 또는 검색 버튼 클릭 시에만 검색을 실행하여 불필요한 요청을 줄이고, URL 상태로 관리하여 북마크/공유가 가능하게 합니다.

Purpose: 상담 기록 검색 효율성 향상 (통합 검색 + 복합 필터 + URL 상태 관리)
Output: CounselingSearchBar 컴포넌트, CounselingFilters 컴포넌트, searchCounselingSessions Server Action, counseling/page.tsx 수정
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-counseling-&-matching-ui-enhancement/26-CONTEXT.md
@.planning/phases/26-counseling-&-matching-ui-enhancement/26-RESEARCH.md
@src/app/(dashboard)/counseling/page.tsx
@src/components/counseling/CounselingSessionCard.tsx
@src/lib/actions/reservations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 통합 검색 Server Action 구현</name>
  <files>src/lib/actions/counseling-search.ts</files>
  <action>
Create src/lib/actions/counseling-search.ts with searchCounselingSessions function:

1. **통합 검색 파라미터 정의**:
   - query?: string (학생 이름, 학부모 이름, 상담 주제 통합 검색)
   - type?: CounselingType (ACADEMIC, CAREER, PSYCHOLOGICAL, BEHAVIORAL)
   - startDate?: string, endDate?: string
   - teacherId?: string (권한에 따라)
   - followUpRequired?: boolean

2. **Prisma OR 쿼리로 통합 검색 구현**:
   ```typescript
   if (params.query) {
     where.OR = [
       { student: { name: { contains: params.query, mode: 'insensitive' } } },
       { summary: { contains: params.query, mode: 'insensitive' } },
       // 학부모 이름은 student.parents 관계 쿼리로 검색
     ]
   }
   ```

3. **RBAC 적용**: getRBACPrisma(session) 사용하여 팀 기반 접근 제어

4. **데이터 포함**: student.parents 포함하여 학부모 정보 반환

Important: 학부모 이름 검색은 student.parents.some 사용. 권한에 따라 teacherId 필터 적용 (TEACHER는 자신만, DIRECTOR는 전체).
  </action>
  <verify>
1. File exists at src/lib/actions/counseling-search.ts
2. Exports searchCounselingSessions with CounselingSearchParams interface
3. Prisma query includes OR condition for student.name and summary
4. Returns { success: true, data: sessions } format
  </verify>
  <done>
searchCounselingSessions 함수가 query 파라미터로 학생 이름, 상담 요약을 검색하고 type, dateRange, followUpRequired 필터를 적용하여 상담 기록을 반환한다.
  </done>
</task>

<task type="auto">
  <name>Task 2: 통합 검색 컴포넌트 구현</name>
  <files>src/components/counseling/CounselingSearchBar.tsx</files>
  <action>
Create src/components/counseling/CounselingSearchBar.tsx Client Component:

1. **Props 인터페이스**:
   - initialQuery?: string (searchParams에서 초기값)
   - onSearch: (query: string) => void

2. **명시적 검색 패턴 구현**:
   - useState로 query 상태 관리 (즉시 검색 아님)
   - form onSubmit 핸들러로 검색 실행
   - Input 컴포넌트에 onKeyDownEnter 지원
   - 검색 버튼 클릭 시도 검색 실행

3. **UI 구성**:
   - shadcn/ui Input with Search icon (lucide-react)
   - shadcn/ui Button (검색 버튼)
   - placeholder: "학생 이름, 학부모 이름, 상담 주제 검색..."
   - data-testid="unified-search-input", "search-button"

4. **스타일링**: Tailwind CSS (relative, pl-10 for icon, gap-2)

Pattern: Research.md Pattern 2 (Controlled Search with Explicit Submission) 참고
  </action>
  <verify>
1. File exists at src/components/counseling/CounselingSearchBar.tsx
2. "use client" directive present
3. useState manages query state (not immediate search)
4. form onSubmit handler calls onSearch(query)
5. Search button and Enter key both trigger search
6. data-testid attributes present
  </verify>
  <done>
CounselingSearchBar 컴포넌트가 사용자 입력을 상태로 관리하고 Enter 키 또는 검색 버튼 클릭 시 onSearch 콜백을 호출하여 명시적 검색을 실행한다.
  </done>
</task>

<task type="auto">
  <name>Task 3: 다중 필터 컴포넌트 구현</name>
  <files>src/components/counseling/CounselingFilters.tsx</files>
  <action>
Create src/components/counseling/CounselingFilters.tsx Client Component:

1. **Props 인터페이스**:
   - initialFilters: { type?, startDate?, endDate?, followUpRequired? }
   - onFilterChange: (filters) => void
   - canViewTeam: boolean (선생님 필터 표시 여부)

2. **필터 구성**:
   - 상담 유형: Select (ACADEMIC, CAREER, PSYCHOLOGICAL, BEHAVIORAL)
   - 시작일/종료일: Input type="date"
   - 후속 조치: Select (all, true, false)
   - 선생님: Select (canViewTeam=true时만 표시)

3. **URL 상태 관리**:
   - 각 필터 변경 시 URLSearchParams로 URL 업데이트
   - params.delete(key) when value is empty
   - router.push(\`/counseling?\${params.toString()}\`)

4. **초기화 버튼**: router.push('/counseling')로 모든 필터 초기화

5. **스타일링**: grid grid-cols-1 md:grid-cols-4 gap-4

Pattern: Research.md Pattern 3 (Multi-Filter Form with URL State) 참고
  </action>
  <verify>
1. File exists at src/components/counseling/CounselingFilters.tsx
2. "use client" directive present
3. Each filter updates URL on change
4. Reset button clears all filters (navigates to /counseling)
5. Teacher filter only shows when canViewTeam=true
6. data-testid attributes present (optional, for consistency)
  </verify>
  <done>
CounselingFilters 컴포넌트가 상담 유형, 날짜 범위, 후속 조치, 선생님 필터를 제공하고 필터 변경 시 URL을 업데이트하여 북마크 가능한 상태를 유지한다.
  </done>
</task>

<task type="auto">
  <name>Task 4: 상담 페이지에 통합 검색/필터 연동</name>
  <files>src/app/(dashboard)/counseling/page.tsx</files>
  <action>
Modify src/app/(dashboard)/counseling/page.tsx:

1. **searchParams 확장**:
   - query?: string (통합 검색어)
   - 기존 파라미터 유지 (studentName, teacherName, type, startDate, endDate, followUpRequired)

2. **데이터 페칭 수정**:
   - query가 있으면 searchCounselingSessions 호출
   - 기존 Prisma 쿼리 로직은 searchCounselingSessions로 이동 (Task 1)
   - sessions = searchResult.data (if search) or fallback to existing query

3. **UI 통합**:
   - CounselingSearchBar 컴포넌트 추가 (CounselingHistoryContent 위)
   - CounselingFilters 컴포넌트 추가 (기존 필터 폼 대체)
   - 기존 필터 폼 (lines 278-366) 제거

4. **Client Component 래퍼**: 기존 페이지는 Server Component 유지, 검색/필터만 Client Component로 분리

Important: 기존 URL 파라미터 호환성 유지 (studentName → query 통합 가능하지만 기존 코드도 동작하게)
  </action>
  <verify>
1. CounselingSearchBar imported and rendered
2. CounselingFilters imported and rendered
3. Old filter form (lines 278-366) removed or replaced
4. query searchParam passed to searchCounselingSessions
5. sessions data displayed correctly with new components
6. URL params preserved on filter change
  </verify>
  <done>
상담 페이지가 CounselingSearchBar와 CounselingFilters를 통합하여 검색어 입력과 복합 필터링을 제공하고 검색 결과를 URL 상태로 유지한다.
  </done>
</task>

</tasks>

<verification>
**Overall Phase Verification:**

1. **통합 검색 동작 확인**:
   - 검색창에 "김" 입력 후 Enter 키 → 김씨 학생/학부모/상담 주제 결과 표시
   - 검색 버튼 클릭 → 동일하게 검색 실행
   - URL에 ?query=김 파라미터 유지

2. **복합 필터 동작 확인**:
   - 상담 유형 "학업" 선택 → ACADEMIC 타입만 필터링
   - 날짜 범위 설정 → 해당 기간 상담만 표시
   - 후속 조치 "필요함" → followUpRequired=true만 표시
   - URL 파라미터로 필터 상태 유지

3. **URL 상태 관리 확인**:
   - 검색/필터 후 URL 복사 → 새 탭에서 열어도 동일한 결과
   - 뒤로 가기 버튼 → 이전 필터 상태로 복원
   - 필터 초기화 버튼 → 모든 파라미터 제거

4. **권한별 필터 확인**:
   - TEACHER 역할 → 선생님 필터 숨김
   - DIRECTOR/TEAM_LEADER → 선생님 필터 표시
</verification>

<success_criteria>
1. 통합 검색창에서 학생 이름, 학부모 이름, 상담 주제를 검색할 수 있다
2. Enter 키 또는 검색 버튼 클릭 시에만 검색이 실행된다 (즉시 검색 아님)
3. 상담 유형, 날짜 범위, 선생님, 상태 필터가 복합 적용된다
4. 검색/필터 상태가 URL 쿼리 파라미터로 유지되어 북마크/공유가 가능하다
5. 기존 상담 페이지 UI가 파괴되지 않고 검색/필터 컴포넌트가 자연스럽게 통합된다
</success_criteria>

<output>
After completion, create `.planning/phases/26-counseling-&-matching-ui-enhancement/26-01-SUMMARY.md` with:
- Implemented components and actions
- Verification results
- Any deviations from plan
- Next steps (26-02)
</output>
