---
phase: 13-compatibility-matching
plan: 04
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - src/app/students/[id]/compatibility/page.tsx
  - src/components/compatibility/teacher-recommendation-list.tsx
  - src/lib/actions/compatibility.ts
autonomous: false

must_haves:
  truths:
    - "학생별로 적합한 선생님 순위와 추천 이유가 표시된다"
    - "궁합 점수 기준 내림차순으로 선생님 목록이 정렬된다"
    - "각 선생님별 구성 요소 점수(MBTI, 학습 스타일, 사주, 성명학, 부하)가 시각화된다"
  artifacts:
    - path: "src/app/students/[id]/compatibility/page.tsx"
      provides: "Student compatibility ranking page"
      min_lines: 80
    - path: "src/components/compatibility/teacher-recommendation-list.tsx"
      provides: "Teacher recommendation list with scores breakdown"
      min_lines: 100
  key_links:
    - from: "src/app/students/[id]/compatibility/page.tsx"
      to: "src/lib/actions/compatibility.ts"
      via: "batchCalculateCompatibilities to get all teacher scores"
      pattern: "batchCalculateCompatibilities"
    - from: "src/components/compatibility/teacher-recommendation-list.tsx"
      to: "src/lib/db/compatibility-result.ts"
      via: "retrieve compatibility results from database"
      pattern: "getAllCompatibilitiesForStudent"
---

<objective>
## Student-Specific Teacher Recommendations

**Purpose:** 학생별로 가장 적합한 선생님 순위와 추천 이유를 표시하는 페이지와 컴포넌트를 구현합니다.

**Output:**
- /students/[id]/compatibility 페이지
- TeacherRecommendationList 컴포넌트

**Why this matters:**
원장이 새로운 학생을 배정할 때 어떤 선생님이 가장 적합한지一目瞭然하게 확인할 수 있습니다.
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-compatibility-matching/13-02-PLAN.md
@src/app/students/[id]/page.tsx
@src/components/teachers/teacher-list.tsx

**기존 패턴:**
- teacher-list.tsx가 TanStack Table 사용
- 검색, 필터, 페이지네이션 패턴 재사용
</context>

<tasks>

<task type="auto">
  <name>Task 1: Student Compatibility 페이지 생성</name>
  <files>src/app/students/[id]/compatibility/page.tsx</files>
  <action>
  1. src/app/students/[id]/compatibility/page.tsx 생성

  2. 페이지 구조:
     a) 학생 정보 헤더 (이름, 학교, 학년)
     b) "선생님 궁합 분석" 제목
     c) "모든 선생님과의 궁합 계산" 버튼
        - 클릭 시 batchCalculateCompatibilities 호출
        - 계산 진행 상태 표시 (loading)
     d) TeacherRecommendationList 컴포넌트 렌더링
        - studentId prop 전달

  3. 데이터 로드:
     - getStudent(studentId)로 학생 정보 로드
     - getAllCompatibilitiesForStudent(studentId)로 궁합 목록 로드
     - 미계산 시 "궁합 분석을 먼저 실행해주세요" 메시지

  4. UI 레이아웃:
     - 좌측: 학생 기본 정보
     - 우측: 선생님 궁합 순위 목록
  </action>
  <verify>
  grep -c "batchCalculateCompatibilities\|TeacherRecommendationList" src/app/students/[id]/compatibility/page.tsx
  </verify>
  <done>
  페이지가 존재하고 학생 ID로 궁합 목록을 로드하며 TeacherRecommendationList를 렌더링한다
  </done>
</task>

<task type="auto">
  <name>Task 2: TeacherRecommendationList 컴포넌트 구현</name>
  <files>src/components/compatibility/teacher-recommendation-list.tsx</files>
  <action>
  1. src/components/compatibility/teacher-recommendation-list.tsx 생성

  2. 컴포넌트 구조:
     a) props: { studentId, compatibilities[], onRecalculate }
     b) 테이블 형태 (TanStack Table 또는 기본 테이블)
     c) 컬럼: 순위, 선생님 이름, 종합 점수, 구성 요소 점수, 추천 이유

  3. 구성 요소 점수 시각화:
     - MBTI (25%): Progress bar 색상 (점수 높음=녹색, 낮음=빨간색)
     - 학습 스타일 (25%): Progress bar
     - 사주 (20%): Progress bar
     - 성명학 (15%): Progress bar
     - 부하 분산 (15%): Progress bar (선생님 현재 학생 수 표시)

  4. 추천 이유 표시:
     - reasoning 필드 표시 (Markdown 또는 텍스트)
     - 간단한 요약: "MBTI 호환, 사주 보완, 부하 여유"

  5. 정렬: overallScore 기준 내림차순

  6. 빈 상태:
     - 궁합 계산 전: "선생님 궁합을 계산해주세요"
     - 계산 중: "계산 중..."

  7. 액션 버튼 (선택):
     - "이 선생님으로 배정" 버튼 (Plan 05에서 연결)
  </action>
  <verify>
  grep -c "Progress.*bar\|overallScore\|componentScores" src/components/compatibility/teacher-recommendation-list.tsx
  </verify>
  <done>
  컴포넌트가 궁합 목록을 테이블로 표시하고 구성 요소 점수를 Progress bar로 시각화한다
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Student Compatibility 페이지 (/students/[id]/compatibility)와 TeacherRecommendationList 컴포넌트

  모든 선생님과의 궁합을 계산하고 순위별로 표시하는 기능
  </what-built>
  <how-to-verify>
  1. 학생 상세 페이지 접속: /students/[id]
  2. "궁합 분석" 탭 클릭
  3. "모든 선생님과의 궁합 계산" 버튼 클릭
  4. 계산 완료 후 다음 확인:
     - 선생님 목록이 궁합 점수 순으로 정렬
     - 각 선생님별 구성 요소 점수가 Progress bar로 표시
     - 추천 이유(reasoning)가 표시
  5. 가장 높은 점수의 선생님이 상단에 위치
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
## Overall Verification

1. **페이지 검증:**
   - /students/[id]/compatibility 라우트 존재
   - batchCalculateCompatibilities 호출로 모든 선생님 궁합 계산

2. **컴포넌트 검증:**
   - TeacherRecommendationList가 compatibilities 배열을 순회
   - overallScore 기준 내림차순 정렬
   - Progress bar로 구성 요소 점수 시각화

3. **UI/UX 검증:**
   - 빈 상태 메시지 적절
   - 로딩 상태 표시
   - 추천 이유 가독성
</verification>

<success_criteria>
1. 페이지 접근 시 궁합 목록 표시
2. 계산 버튼 클릭 시 모든 선생님 궁합 계산
3. Progress bar로 점수 시각화
4. 사용자가 순위와 이유를 명확히 인지
5. 전체 파 실행 시점: ~40% context 사용
</success_criteria>

<output>
After completion, create `.planning/phases/13-compatibility-matching/13-04-SUMMARY.md`
</output>
