---
phase: 13-compatibility-matching
plan: 05
type: execute
wave: 4
depends_on: ["13-03", "13-04"]
files_modified:
  - src/components/compatibility/manual-assignment-dialog.tsx
  - src/lib/actions/student-assignment.ts
  - src/app/admin/assignments/page.tsx
autonomous: false

must_haves:
  truths:
    - "선생님이 학생을 수동으로 배정할 수 있다"
    - "자동 배정 제안을 승인하여 일괄 적용할 수 있다"
    - "배정 시 Student.teacherId가 업데이트된다"
    - "배정 후 궁합 분석 결과 페이지로 이동한다"
  artifacts:
    - path: "src/components/compatibility/manual-assignment-dialog.tsx"
      provides: "Manual student assignment dialog"
      min_lines: 80
    - path: "src/lib/actions/student-assignment.ts"
      provides: "Student assignment Server Actions"
      exports: ["assignStudentToTeacher", "applyAutoAssignment"]
      min_lines: 50
    - path: "src/app/admin/assignments/page.tsx"
      provides: "Admin assignments management page"
      min_lines: 100
  key_links:
    - from: "src/lib/actions/student-assignment.ts"
      to: "prisma.student"
      via: "update student.teacherId"
      pattern: "prisma\\.student\\.update"
    - from: "src/app/admin/assignments/page.tsx"
      to: "src/lib/actions/auto-assignment.ts"
      via: "generate and apply auto-assignment proposals"
      pattern: "generateAutoAssignmentAction"
---

<objective>
## Manual Student Assignment & Auto-Assignment Application

**Purpose:** 선생님이 학생을 수동으로 배정하는 기능과 원장이 자동 배정 제안을 승인하여 적용하는 기능을 구현합니다.

**Output:**
- ManualAssignmentDialog 컴포넌트
- assignStudentToTeacher Server Action
- /admin/assignments 페이지 (자동 배정 제안 관리)

**Why this matters:**
AI 제안만으로는 모든 맥락을 반영할 수 없으므로 수동 배정 기능이 필수입니다. 자동 배정 제안 승인은 대규모 배정의 효율성을 높입니다.
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-compatibility-matching/13-03-PLAN.md
@.planning/phases/13-compatibility-matching/13-04-PLAN.md
@src/app/students/[id]/page.tsx
@src/components/teachers/teacher-list.tsx

**기존 패턴:**
- shadcn/ui Dialog, Select 컴포넌트 사용
- Server Actions에서 revalidatePath로 캐시 무효화
</context>

<tasks>

<task type="auto">
  <name>Task 1: Student Assignment Server Actions 구현</name>
  <files>src/lib/actions/student-assignment.ts</files>
  <action>
  1. src/lib/actions/student-assignment.ts 생성

  2. assignStudentToTeacher 함수 구현 ("use server"):
     a) 인자: studentId, teacherId
     b) 세션 검증 및 권한 확인 (DIRECTOR, TEAM_LEADER, 또는 담당 선생님)
     c) Prisma.student.update로 teacherId 변경
     d) revalidatePath(`/students/${studentId}`, `/teachers/${teacherId}`)
     e) 성공 메시지 반환: { success: true, message: "학생이 배정되었습니다." }

  3. applyAutoAssignment 함수 구현 ("use server"):
     a) 인자: proposalId
     b) 세션 검증 및 권한 확인 (DIRECTOR or TEAM_LEADER)
     c) AssignmentProposal 조회
     d) 트랜잭션으로 일괄 배정:
        - Prisma.$transaction
        - 각 assignments 항목에 대해 Student.teacherId 업데이트
        - AssignmentProposal.status를 "approved"로 변경
        - appliedAt 현재 시간으로 설정
     e) revalidatePath(`/admin/assignments`, `/teams/${teamId}`)
     f) 성공 메시지 반환: { success: true, assignedCount }

  4. bulkAssignStudentsToTeachers 함수 (선택):
     - 인자: assignments: { studentId, teacherId }[]
     - 트랜잭션으로 다중 학생 일괄 배정
  </action>
  <verify>
  grep -c "assignStudentToTeacher\|applyAutoAssignment" src/lib/actions/student-assignment.ts
  </verify>
  <done>
  assignStudentToTeacher와 applyAutoAssignment가 존재하고 Prisma update/transaction을 사용한다
  </done>
</task>

<task type="auto">
  <name>Task 2: ManualAssignmentDialog 컴포넌트 구현</name>
  <files>src/components/compatibility/manual-assignment-dialog.tsx</files>
  <action>
  1. src/components/compatibility/manual-assignment-dialog.tsx 생성

  2. 컴포넌트 구조:
     a) props: { studentId, currentTeacherId, open, onOpenChange }
     b) shadcn/ui Dialog 사용
     c) Dialog 내용:
        - "학생 배정" 제목
        - 학생 이름 표시
        - 선생님 선택 드롭다운 (Select 컴포넌트)
        - 현재 배정된 선생님 표시
        - "배정" 버튼 (Primary)

  3. 선생님 목록 로드:
     - 팀 내 모든 선생님 조회 (Prisma query)
     - 현재 배정된 선생님 기본 선택
     - 드롭다운에 이름 표시

  4. 배정 액션:
     - "배정" 버튼 클릭 시 assignStudentToTeacher 호출
     - 로딩 상태 표시
     - 성공 시 onOpenChange(false)로 닫기
     - 에러 시 toast 메시지 (sonner)

  5. 학생 상세 페이지에서의 통합:
     - "선생님 변경" 버튼 클릭 시 Dialog 오픈
  </action>
  <verify>
  grep -c "assignStudentToTeacher\|Dialog\|Select.*teacher" src/components/compatibility/manual-assignment-dialog.tsx
  </verify>
  <done>
  Dialog가 존재하고 선생님 선택 드롭다운과 배정 버튼을 포함한다
  </done>
</task>

<task type="auto">
  <name>Task 3: Admin Assignments 페이지 구현</name>
  <files>src/app/admin/assignments/page.tsx</files>
  <action>
  1. src/app/admin/assignments/page.tsx 생성

  2. 페이지 구조:
     a) "배정 관리" 제목
     b) "자동 배정 제안 생성" 버튼
        - 클릭 시 generateAutoAssignmentAction 호출
        - 생성된 제안 목록 표시
     c) AssignmentProposal 목록 테이블:
        - 제안 ID, 생성일, 상태, 메트릭
        - "상세 보기" 버튼 (Dialog)
        - "승인" 버튼 (상태가 draft일 때만)

  3. 제안 상세 Dialog:
     - assignments 매핑 (학생 -> 선생님)
     - metrics 표시 (평균 궁합, 부하 분산)
     - teacherUtilization 차트 또는 테이블
     - "승인하여 적용" 버튼 (applyAutoAssignment 호출)

  4. 권한 확인:
     - DIRECTOR 또는 TEAM_LEADER만 접근 가능
     - 미인증 시 redirect to login

  5. 배정 현황 요약:
     - 전체 학생 수, 배정 완료 수, 미배정 수
     - 선생님별 학생 수 (간단 테이블)
  </action>
  <verify>
  grep -c "generateAutoAssignmentAction\|applyAutoAssignment\|AssignmentProposal" src/app/admin/assignments/page.tsx
  </verify>
  <done>
  페이지가 존재하고 자동 배정 제안 생성과 승인 기능을 포함한다
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Manual Assignment Dialog와 Admin Assignments 페이지

  수동 학생 배정과 자동 배정 제안 승인 기능
  </what-built>
  <how-to-verify>
  1. 수동 배정 테스트:
     - 학생 상세 페이지 접속
     - "선생님 변경" 버튼 클릭
     - 다른 선생님 선택 후 "배정" 클릭
     - Student.teacherId가 변경되었는지 확인

  2. 자동 배정 제안 테스트:
     - /admin/assignments 접속
     - "자동 배정 제안 생성" 클릭
     - 제안 상세 확인 (assignments, metrics)
     - "승인하여 적용" 클릭
     - 다수 학생의 teacherId가 변경되었는지 확인

  3. 궁합 분석 결과 확인:
     - 배정 후 /students/[id]/compatibility 페이지에서 해당 선생님과의 궁합 점수 확인
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
## Overall Verification

1. **Server Actions 검증:**
   - assignStudentToTeacher가 Student.teacherId 업데이트
   - applyAutoAssignment가 트랜잭션으로 일괄 배정

2. **Dialog 검증:**
   - 선생님 선택 드롭다운 동작
   - 배정 버튼 클릭 시 Server Action 호출

3. **Admin 페이지 검증:**
   - 자동 배정 제안 생성
   - 제안 승인 후 일괄 배정 적용
</verification>

<success_criteria>
1. 수동 배정이 Dialog에서 완료
2. 자동 배정 제안 생성 및 승인 가능
3. 배정 후 Student.teacherId 반영
4. 전체 파 실행 시점: ~45% context 사용
</success_criteria>

<output>
After completion, create `.planning/phases/13-compatibility-matching/13-05-SUMMARY.md`
</output>
