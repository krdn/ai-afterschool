---
phase: 13-compatibility-matching
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/lib/actions/compatibility.ts
  - src/app/api/compatibility/calculate/route.ts
autonomous: true

must_haves:
  truths:
    - "선생님이 학생에게 대한 궁합 계산을 API로 요청할 수 있다"
    - "궁합 계산 요청 시 선생님과 학생의 분석 결과가 자동으로 로드된다"
    - "계산된 궁합 결과가 DB에 저장되고 결과가 반환된다"
  artifacts:
    - path: "src/lib/actions/compatibility.ts"
      provides: "Server Action for compatibility calculation"
      exports: ["calculateCompatibilityAction"]
      min_lines: 60
    - path: "src/app/api/compatibility/calculate/route.ts"
      provides: "REST API endpoint for compatibility calculation"
      exports: ["POST"]
      min_lines: 40
  key_links:
    - from: "src/lib/actions/compatibility.ts"
      to: "src/lib/analysis/compatibility-scoring.ts"
      via: "call calculateCompatibilityScore with loaded analysis data"
      pattern: "calculateCompatibilityScore"
    - from: "src/lib/actions/compatibility.ts"
      to: "src/lib/db/compatibility-result.ts"
      via: "store result via upsertCompatibilityResult"
      pattern: "upsertCompatibilityResult"
    - from: "src/app/api/compatibility/calculate/route.ts"
      to: "src/lib/actions/compatibility.ts"
      via: "delegate calculation to server action"
      pattern: "calculateCompatibilityAction"
---

<objective>
## Compatibility Calculation Server Action & API

**Purpose:** 궁합 계산 순수 함수를 호출하는 Server Action과 REST API 엔드포인트를 구현합니다. UI에서 호출하여 선생님-학생 궁합을 계산할 수 있습니다.

**Output:**
- calculateCompatibilityAction Server Action
- POST /api/compatibility/calculate 엔드포인트

**Why this matters:**
UI 컴포넌트에서 궁합 계산을 트리거하는 진입점입니다. Server Action과 REST API를 모두 제공하여 유연한 통합을 지원합니다.
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-compatibility-matching/13-01-PLAN.md
@src/lib/actions/teacher-analysis.ts
@src/lib/db/teacher-saju-analysis.ts
@src/lib/db/teacher-mbti-analysis.ts
@src/lib/db/teacher-name-analysis.ts
@src/lib/db/saju-analysis.ts
@src/lib/db/mbti-analysis.ts
@src/lib/db/name-analysis.ts

**코드 재사용 패턴:**
- teacher-analysis.ts가 runTeacherSajuAnalysis, runTeacherMbtiAnalysis, runTeacherNameAnalysis 패턴 사용
- 동일한 패턴으로 호환성 계산 구현
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compatibility Calculation Server Action 구현</name>
  <files>src/lib/actions/compatibility.ts</files>
  <action>
  1. src/lib/actions/compatibility.ts 생성

  2. calculateCompatibilityAction 함수 구현 ("use server"):
     a) 인자: teacherId, studentId
     b) 세션 검증: verifySession()
     c) Teacher와 Student 분석 데이터 로드 (Prisma include):
        - Teacher: teacherSajuAnalysis, teacherMbtiAnalysis, teacherNameAnalysis, students (count)
        - Student: sajuAnalysis, mbtiAnalysis, nameAnalysis
     d) 필수 분석 존재 확인:
        - Saju, MBTI, NameAnalysis가 모두 존재해야 함
        - 없으면 에러: "선생님/학생의 성향 분석이 먼저 완료되어야 합니다."
     e) calculateCompatibilityScore 호출 (compatibility-scoring.ts):
        - teacherData: { mbti: teacherMbtiAnalysis, saju: teacherSajuAnalysis, name: teacherNameAnalysis }
        - studentData: { mbti: mbtiAnalysis, saju: sajuAnalysis, name: nameAnalysis }
        - teacherCurrentStudentCount: teacher.students.length
     f) upsertCompatibilityResult로 DB 저장
     g) revalidatePath(`/teachers/${teacherId}`, `/students/${studentId}`)
     h) 결과 반환: { success: true, overallScore, componentScores, reasoning }

  3. batchCalculateCompatibilities 함수 (선택, Plan 03에서 사용):
     - 인자: studentId (모든 선생님과의 궁합 계산)
     - 팀 내 모든 선생님에 대해 반복 계산
     - 병렬 처리 (Promise.all)
  </action>
  <verify>
  grep -c "calculateCompatibilityAction\|batchCalculateCompatibilities" src/lib/actions/compatibility.ts
  </verify>
  <done>
  calculateCompatibilityAction이 존재하고 선생님/학생 분석 데이터를 로드하여 calculateCompatibilityScore를 호출한다
  </done>
</task>

<task type="auto">
  <name>Task 2: Compatibility Calculation REST API 엔드포인트</name>
  <files>src/app/api/compatibility/calculate/route.ts</files>
  <action>
  1. src/app/api/compatibility/calculate/route.ts 생성

  2. POST 핸들러 구현:
     a) 요청 바디 검증: { teacherId, studentId }
     b) calculateCompatibilityAction 호출 (같은 로직 재사용)
     c) 에러 처리: 400 (bad request), 401 (unauthorized), 500 (server error)
     d) 성공 시 200 + 결과 반환
     e) Zod 스키마로 입력 검증 (선택, 다른 엔드포인트 패턴 따르기)

  3. 응답 형식:
     {
       success: true,
       data: {
         overallScore: number,
         componentScores: { mbti, learningStyle, saju, name, loadBalance },
         reasoning: string
       }
     }
  </action>
  <verify>
  curl -X POST http://localhost:3000/api/compatibility/calculate -d '{"teacherId":"test","studentId":"test"}' -H "Content-Type: application/json" (should return 200 or validation error)
  </verify>
  <done>
  POST /api/compatibility/calculate가 존재하고 teacherId, studentId를 받아 calculateCompatibilityAction을 호출한다
  </done>
</task>

</tasks>

<verification>
## Overall Verification

1. **Server Action 검증:**
   - calculateCompatibilityAction이 "use server" 지시어 포함
   - 선생님/학생 분석 데이터가 누락되면 명확한 에러 메시지

2. **API 엔드포인트 검증:**
   - POST 요청으로 궁합 계산 가능
   - 에러 코드 적절히 반환 (400, 401, 500)

3. **데이터 흐름 검증:**
   - Teacher/Student 분석 로드 → calculateCompatibilityScore → DB 저장 순서
</verification>

<success_criteria>
1. Server Action이 UI에서 호출 가능
2. REST API가 curl로 테스트 통과
3. 계산된 결과가 DB에 저장됨
4. 전체 파 실행 시점: ~35% context 사용
</success_criteria>

<output>
After completion, create `.planning/phases/13-compatibility-matching/13-02-SUMMARY.md`
</output>
