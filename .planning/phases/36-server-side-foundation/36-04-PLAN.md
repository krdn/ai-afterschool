---
phase: 36-server-side-foundation
plan: 04
type: execute
wave: 3
depends_on: [36-02, 36-03]
files_modified:
  - src/app/api/chat/route.ts
autonomous: true
requirements: [MENT-03, MENT-04, CTX-01, CTX-02, CTX-03, CTX-04, CTX-05]
must_haves:
  truths:
    - "@멘션이 포함된 채팅 메시지 전송 시 서버가 엔티티 ID/타입 목록을 파싱하여 DB에서 데이터를 조회한다"
    - "조회된 엔티티 데이터가 AI 시스템 프롬프트에 동적으로 주입된다"
    - "전송된 메시지의 ChatMessage 레코드에 mentionedEntities JSON 메타데이터가 저장된다"
    - "RBAC 실패 시 접근 불가 알림이 응답 헤더로 전달된다"
    - "멘션이 없는 기존 채팅은 이전과 동일하게 동작한다 (하위 호환)"
  artifacts:
    - path: "src/app/api/chat/route.ts"
      provides: "멘션 통합 채팅 API"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/chat/route.ts"
      to: "src/lib/chat/mention-resolver.ts"
      via: "resolveMentions 호출로 엔티티 데이터 조회"
      pattern: "resolveMentions"
    - from: "src/app/api/chat/route.ts"
      to: "src/lib/chat/context-builder.ts"
      via: "buildMentionContext 호출로 system prompt 구성"
      pattern: "buildMentionContext"
    - from: "src/app/api/chat/route.ts"
      to: "prisma ChatMessage.mentionedEntities"
      via: "메시지 저장 시 metadata 포함"
      pattern: "mentionedEntities"
---

<objective>
/api/chat/route.ts를 수정하여 멘션 배열을 수신하고, mention-resolver와 context-builder를 통합하여 동적 시스템 프롬프트를 구성하고, mentionedEntities 메타데이터를 저장한다.

Purpose: Phase 36의 모든 서버 측 컴포넌트를 하나의 API 엔드포인트로 통합하여 @멘션 기능을 완성한다.
Output: 수정된 /api/chat/route.ts
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-server-side-foundation/36-CONTEXT.md
@.planning/phases/36-server-side-foundation/36-RESEARCH.md
@.planning/phases/36-server-side-foundation/36-01-SUMMARY.md
@.planning/phases/36-server-side-foundation/36-02-SUMMARY.md
@.planning/phases/36-server-side-foundation/36-03-SUMMARY.md
@src/app/api/chat/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: /api/chat/route.ts에 멘션 시스템 통합</name>
  <files>src/app/api/chat/route.ts</files>
  <action>
  기존 `src/app/api/chat/route.ts`를 수정하여 멘션 시스템을 통합한다.

  **변경 사항:**

  1. **import 추가:**
     ```typescript
     import { resolveMentions } from '@/lib/chat/mention-resolver'
     import { buildMentionContext } from '@/lib/chat/context-builder'
     import type { MentionItem } from '@/lib/chat/mention-types'
     ```

  2. **body 타입 확장:**
     기존 body 타입에 `mentions?: MentionItem[]` 추가:
     ```typescript
     const body = (await request.json()) as {
       prompt: string;
       providerId?: string;
       modelId?: string;
       sessionId?: string;
       messages?: Array<{ role: 'user' | 'assistant'; content: string }>;
       mentions?: MentionItem[];  // 추가
     };
     ```

  3. **멘션 처리 로직 추가** (세션 처리 이후, streamWithProvider 이전):
     ```typescript
     // 멘션 처리
     let dynamicSystem = SYSTEM_PROMPT;
     let mentionedEntitiesData: import('@/lib/chat/mention-types').MentionedEntity[] | undefined;
     let accessDeniedMessages: string[] = [];

     if (body.mentions && body.mentions.length > 0) {
       const mentionResult = await resolveMentions(body.mentions, {
         userId: session.userId,
         role: session.role,
         teamId: session.teamId,
       });

       // 멘션 컨텍스트를 system prompt에 추가
       const mentionContext = buildMentionContext(mentionResult.resolved);
       if (mentionContext) {
         dynamicSystem = `${SYSTEM_PROMPT}\n\n${mentionContext}`;
       }

       // 메타데이터 저장용
       mentionedEntitiesData = mentionResult.metadata;
       accessDeniedMessages = mentionResult.accessDeniedMessages;
     }
     ```

  4. **streamWithProvider 호출 수정:**
     `system: SYSTEM_PROMPT` → `system: dynamicSystem`

  5. **user 메시지 DB 저장 수정:**
     `mentionedEntities` 추가:
     ```typescript
     await db.chatMessage.create({
       data: {
         sessionId: chatSessionId,
         role: 'user',
         content: trimmedPrompt,
         mentionedEntities: mentionedEntitiesData
           ? (mentionedEntitiesData as import('@prisma/client').Prisma.InputJsonValue)
           : undefined,
       },
     });
     ```

  6. **응답 헤더에 RBAC 실패 알림 추가:**
     accessDeniedMessages가 있으면 `X-Mention-Warnings` 헤더에 JSON 인코딩하여 추가:
     ```typescript
     if (accessDeniedMessages.length > 0) {
       headers.set('X-Mention-Warnings', JSON.stringify(accessDeniedMessages));
     }
     ```

  **하위 호환:** mentions가 없거나 빈 배열이면 기존 동작과 완전히 동일 (dynamicSystem = SYSTEM_PROMPT 그대로).
  </action>
  <verify>
  `npx tsc --noEmit src/app/api/chat/route.ts` 실행하여 타입 에러 없음 확인.
  기존 채팅 테스트 (mentions 없이 POST) 가 동일하게 동작하는지 확인.
  `pnpm build` 실행하여 빌드 에러 없음 확인.
  </verify>
  <done>
  /api/chat POST가 mentions 배열을 받아 resolveMentions → buildMentionContext를 거쳐 동적 system prompt를 구성하고, mentionedEntities를 ChatMessage에 저장한다.
  멘션이 없는 기존 요청은 이전과 동일하게 동작한다.
  RBAC 실패 알림은 X-Mention-Warnings 헤더로 전달된다.
  </done>
</task>

</tasks>

<verification>
1. `src/app/api/chat/route.ts`가 resolveMentions, buildMentionContext를 import함
2. body 타입에 mentions?: MentionItem[] 필드가 있음
3. mentions가 있을 때 dynamicSystem이 SYSTEM_PROMPT + mentionContext로 구성됨
4. chatMessage.create에 mentionedEntities가 포함됨
5. mentions가 없으면 기존 동작과 동일 (하위 호환)
6. pnpm build 성공
</verification>

<success_criteria>
- 멘션 포함 채팅 메시지 전송 시 서버가 엔티티 데이터를 조회하고 AI 시스템 프롬프트에 주입
- ChatMessage에 mentionedEntities JSON이 저장됨
- 멘션 없는 요청은 이전과 동일하게 동작 (하위 호환)
- 빌드 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/36-server-side-foundation/36-04-SUMMARY.md`
</output>
