---
phase: 04-mbti-analysis
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/(dashboard)/students/[id]/mbti/page.tsx
  - src/app/(dashboard)/students/[id]/mbti/actions.ts
  - src/components/mbti/survey-form.tsx
  - src/components/mbti/question-group.tsx
  - src/components/mbti/question-item.tsx
  - src/components/mbti/progress-indicator.tsx
  - src/lib/hooks/use-mbti-autosave.ts
  - package.json
autonomous: true

must_haves:
  truths:
    - "선생님이 한 페이지에서 60문항 전체를 스크롤하며 응답할 수 있다"
    - "응답이 2초 debounce 후 자동 저장된다"
    - "키보드 1-5키로 빠르게 응답할 수 있다"
    - "진행률이 퍼센트 바와 숫자로 표시된다"
    - "미응답 문항이 빨갛게 강조되고 제출이 차단된다"
  artifacts:
    - path: "src/app/(dashboard)/students/[id]/mbti/page.tsx"
      provides: "MBTI survey page entry point"
      min_lines: 20
    - path: "src/components/mbti/survey-form.tsx"
      provides: "Main survey form with react-hook-form"
      min_lines: 80
    - path: "src/components/mbti/progress-indicator.tsx"
      provides: "Progress bar UI component"
      exports: ["ProgressIndicator"]
    - path: "src/lib/hooks/use-mbti-autosave.ts"
      provides: "Debounced autosave hook"
      exports: ["useMbtiAutosave"]
  key_links:
    - from: "src/components/mbti/survey-form.tsx"
      to: "src/lib/actions/mbti-survey.ts"
      via: "saveMbtiDraft action call"
      pattern: "saveMbtiDraft"
    - from: "src/lib/hooks/use-mbti-autosave.ts"
      to: "use-debounce"
      via: "useDebounce hook"
      pattern: "useDebounce"
---

<objective>
MBTI 설문 입력 UI 구현

Purpose: 선생님이 학생을 대신하여 60문항 설문을 편리하게 진행할 수 있는 단일 페이지 폼을 구현합니다. 자동 저장, 키보드 단축키, 진행률 표시를 포함합니다.
Output: 설문 페이지, 폼 컴포넌트, autosave 훅
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-mbti-analysis/04-CONTEXT.md
@.planning/phases/04-mbti-analysis/04-RESEARCH.md
@.planning/phases/04-mbti-analysis/04-01-SUMMARY.md

# Reference patterns
@src/components/students/student-detail.tsx
@src/app/(dashboard)/students/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install use-debounce and create autosave hook</name>
  <files>package.json, src/lib/hooks/use-mbti-autosave.ts</files>
  <action>
**Install dependency:**
```bash
npm install use-debounce
```

**src/lib/hooks/use-mbti-autosave.ts:**

Create custom hook for debounced autosave:

```typescript
"use client"

import { useEffect, useRef, useCallback } from "react"
import { useDebounce } from "use-debounce"
import { useFormContext } from "react-hook-form"

export function useMbtiAutosave(
  studentId: string,
  onSave: (responses: Record<string, number>) => Promise<void>,
  debounceMs = 2000
) {
  const { watch, formState } = useFormContext()
  const responses = watch("responses")
  const [debouncedResponses] = useDebounce(responses, debounceMs)
  const isSaving = useRef(false)
  const lastSaved = useRef<string | null>(null)

  const save = useCallback(async (data: Record<string, number>) => {
    // Skip if same as last saved or currently saving
    const dataString = JSON.stringify(data)
    if (isSaving.current || dataString === lastSaved.current) return
    if (Object.keys(data || {}).length === 0) return

    isSaving.current = true
    try {
      await onSave(data)
      lastSaved.current = dataString
    } catch (error) {
      console.error("Autosave failed:", error)
    } finally {
      isSaving.current = false
    }
  }, [onSave])

  useEffect(() => {
    if (debouncedResponses && Object.keys(debouncedResponses).length > 0) {
      save(debouncedResponses)
    }
  }, [debouncedResponses, save])

  // Return cancel function for use before submit
  const cancelAutosave = useCallback(() => {
    lastSaved.current = JSON.stringify(responses)
  }, [responses])

  return { cancelAutosave, isSaving: isSaving.current }
}
```

Hook features:
- 2-second debounce (configurable)
- Skip duplicate saves (compare stringified data)
- Expose cancel function for submit
- Track saving state to prevent race conditions
  </action>
  <verify>
```bash
# Check package installed
grep "use-debounce" package.json

# Type check
npx tsc --noEmit
```
  </verify>
  <done>
use-debounce 설치됨. useMbtiAutosave 훅이 2초 debounce와 중복 저장 방지 로직을 포함.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create survey UI components</name>
  <files>
    src/components/mbti/progress-indicator.tsx,
    src/components/mbti/question-item.tsx,
    src/components/mbti/question-group.tsx,
    src/components/mbti/survey-form.tsx
  </files>
  <action>
**src/components/mbti/progress-indicator.tsx:**

Display progress bar with percentage:
- Props: `responses: Record<string, number>`, `total: number`
- Show: "45/60 (75%)" + progress bar
- Use Tailwind for styling

```typescript
export function ProgressIndicator({ responses, total }: Props) {
  const answered = Object.keys(responses || {}).length
  const percentage = Math.round((answered / total) * 100)

  return (
    <div className="sticky top-0 bg-white/95 backdrop-blur z-10 py-4 border-b">
      <div className="flex justify-between text-sm mb-2">
        <span className="font-medium">진행률</span>
        <span>{answered}/{total} ({percentage}%)</span>
      </div>
      <div className="h-2 bg-gray-200 rounded-full overflow-hidden">
        <div
          className="h-full bg-blue-600 transition-all duration-300"
          style={{ width: `${percentage}%` }}
        />
      </div>
    </div>
  )
}
```

**src/components/mbti/question-item.tsx:**

Single question with 5-point scale radio buttons:
- Props: `questionId: number`, `questionText: string`, `description?: string`, `isFocused: boolean`
- Show error state if submitted but unanswered
- Radio buttons styled as numbered circles (1-5)
- Highlight selected option with blue background

```typescript
export function QuestionItem({ questionId, questionText, description, isFocused }: Props) {
  const { watch, setValue, formState } = useFormContext()
  const responses = watch("responses")
  const answered = responses?.[questionId] !== undefined
  const showError = formState.isSubmitted && !answered

  return (
    <div
      id={`question-${questionId}`}
      className={cn(
        "p-4 rounded-lg border transition-colors",
        showError && "border-red-500 bg-red-50",
        isFocused && "ring-2 ring-blue-500",
        !showError && !isFocused && "border-gray-200"
      )}
    >
      <p className="mb-1 font-medium">{questionId}. {questionText}</p>
      {description && (
        <p className="text-sm text-gray-500 mb-3">{description}</p>
      )}
      {showError && (
        <p className="text-sm text-red-600 mb-2">이 문항에 답해주세요.</p>
      )}
      <div className="flex gap-3 items-center">
        <span className="text-sm text-gray-500">매우 아니다</span>
        {[1, 2, 3, 4, 5].map(value => (
          <label key={value} className="cursor-pointer">
            <input
              type="radio"
              name={`q-${questionId}`}
              value={value}
              checked={responses?.[questionId] === value}
              onChange={() => setValue(`responses.${questionId}`, value, { shouldDirty: true })}
              className="sr-only peer"
            />
            <div className={cn(
              "w-10 h-10 flex items-center justify-center rounded-full border-2",
              "peer-checked:border-blue-600 peer-checked:bg-blue-100 peer-checked:font-bold",
              "hover:border-blue-400 transition-colors"
            )}>
              {value}
            </div>
          </label>
        ))}
        <span className="text-sm text-gray-500">매우 그렇다</span>
      </div>
    </div>
  )
}
```

**src/components/mbti/question-group.tsx:**

Group questions by dimension:
- Props: `dimension: string`, `dimensionLabel: string`, `questions: Question[]`, `focusedQuestionId: number | null`
- Display section header (E/I 외향/내향 등)

```typescript
export function QuestionGroup({ dimension, dimensionLabel, questions, focusedQuestionId }: Props) {
  return (
    <section className="space-y-4">
      <h3 className="text-lg font-semibold sticky top-20 bg-white/95 backdrop-blur py-2 border-b">
        {dimensionLabel}
      </h3>
      {questions.map(q => (
        <QuestionItem
          key={q.id}
          questionId={q.id}
          questionText={q.text}
          description={q.description}
          isFocused={focusedQuestionId === q.id}
        />
      ))}
    </section>
  )
}
```

**src/components/mbti/survey-form.tsx:**

Main form component:
- Use FormProvider from react-hook-form
- Integrate autosave hook
- Handle keyboard shortcuts (1-5 keys)
- Group questions by dimension
- Submit validation (all 60 must be answered)
- Navigation warning on unsaved changes

```typescript
"use client"

import { useForm, FormProvider } from "react-hook-form"
import { useState, useEffect, useCallback } from "react"
import { useRouter } from "next/navigation"
import { useMbtiAutosave } from "@/lib/hooks/use-mbti-autosave"
import { saveMbtiDraft, submitMbtiSurvey } from "@/lib/actions/mbti-survey"
import { ProgressIndicator } from "./progress-indicator"
import { QuestionGroup } from "./question-group"
import questions from "@/data/mbti/questions.json"

type FormData = {
  responses: Record<string, number>
}

const dimensionLabels = {
  EI: "외향(E) / 내향(I) - 에너지 방향",
  SN: "감각(S) / 직관(N) - 정보 인식",
  TF: "사고(T) / 감정(F) - 판단 기준",
  JP: "판단(J) / 인식(P) - 생활 양식"
}

export function MbtiSurveyForm({ studentId, initialDraft }: {
  studentId: string
  initialDraft?: Record<string, number>
}) {
  const router = useRouter()
  const [focusedQuestionId, setFocusedQuestionId] = useState<number | null>(null)
  const [isSubmitting, setIsSubmitting] = useState(false)

  const methods = useForm<FormData>({
    defaultValues: {
      responses: initialDraft || {}
    }
  })

  const { cancelAutosave } = useMbtiAutosave(
    studentId,
    async (responses) => {
      await saveMbtiDraft(studentId, responses)
    }
  )

  // Keyboard shortcuts (1-5)
  useEffect(() => {
    function handleKeyPress(e: KeyboardEvent) {
      if (e.target instanceof HTMLInputElement || e.target instanceof HTMLTextAreaElement) return
      if (focusedQuestionId === null) return

      const key = parseInt(e.key)
      if (key >= 1 && key <= 5) {
        methods.setValue(`responses.${focusedQuestionId}`, key, { shouldDirty: true })
        // Move to next question
        if (focusedQuestionId < 60) {
          setFocusedQuestionId(focusedQuestionId + 1)
          document.getElementById(`question-${focusedQuestionId + 1}`)?.scrollIntoView({
            behavior: "smooth",
            block: "center"
          })
        }
        e.preventDefault()
      }
    }

    window.addEventListener("keydown", handleKeyPress)
    return () => window.removeEventListener("keydown", handleKeyPress)
  }, [focusedQuestionId, methods])

  // Track focused question on scroll
  useEffect(() => {
    const observer = new IntersectionObserver(
      (entries) => {
        const visible = entries.filter(e => e.isIntersecting)
        if (visible.length > 0) {
          // Get the most centered one
          const centered = visible.reduce((prev, curr) => {
            return curr.intersectionRatio > prev.intersectionRatio ? curr : prev
          })
          const id = parseInt(centered.target.id.replace("question-", ""))
          setFocusedQuestionId(id)
        }
      },
      { threshold: 0.5, rootMargin: "-20% 0px -20% 0px" }
    )

    questions.forEach(q => {
      const el = document.getElementById(`question-${q.id}`)
      if (el) observer.observe(el)
    })

    return () => observer.disconnect()
  }, [])

  const onSubmit = useCallback(async (data: FormData) => {
    const unanswered = questions.filter(q => data.responses[q.id] === undefined)
    if (unanswered.length > 0) {
      // Scroll to first unanswered
      document.getElementById(`question-${unanswered[0].id}`)?.scrollIntoView({
        behavior: "smooth",
        block: "center"
      })
      return
    }

    setIsSubmitting(true)
    cancelAutosave()

    try {
      const result = await submitMbtiSurvey(studentId, data.responses)
      if (result.success) {
        router.push(`/students/${studentId}`)
      }
    } catch (error) {
      console.error("Submit failed:", error)
    } finally {
      setIsSubmitting(false)
    }
  }, [studentId, cancelAutosave, router])

  // Group questions by dimension
  const grouped = questions.reduce((acc, q) => {
    if (!acc[q.dimension]) acc[q.dimension] = []
    acc[q.dimension].push(q)
    return acc
  }, {} as Record<string, typeof questions>)

  return (
    <FormProvider {...methods}>
      <form onSubmit={methods.handleSubmit(onSubmit)} className="space-y-8">
        <ProgressIndicator responses={methods.watch("responses")} total={60} />

        <div className="text-sm text-gray-600 bg-blue-50 p-4 rounded-lg">
          <p className="font-medium mb-1">키보드 단축키</p>
          <p>문항에 포커스된 상태에서 1~5 키를 눌러 빠르게 응답할 수 있습니다.</p>
        </div>

        {(["EI", "SN", "TF", "JP"] as const).map(dim => (
          <QuestionGroup
            key={dim}
            dimension={dim}
            dimensionLabel={dimensionLabels[dim]}
            questions={grouped[dim] || []}
            focusedQuestionId={focusedQuestionId}
          />
        ))}

        <div className="sticky bottom-0 bg-white border-t py-4">
          <button
            type="submit"
            disabled={isSubmitting}
            className="w-full py-3 bg-blue-600 text-white rounded-lg font-medium hover:bg-blue-700 disabled:opacity-50 disabled:cursor-not-allowed"
          >
            {isSubmitting ? "제출 중..." : "설문 제출"}
          </button>
          <p className="text-sm text-gray-500 text-center mt-2">
            응답은 자동으로 저장됩니다.
          </p>
        </div>
      </form>
    </FormProvider>
  )
}
```
  </action>
  <verify>
```bash
# Type check
npx tsc --noEmit
```
  </verify>
  <done>
설문 UI 컴포넌트가 구현됨: 진행률 표시, 문항 그룹화, 키보드 단축키, 자동 저장.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create MBTI survey page route</name>
  <files>
    src/app/(dashboard)/students/[id]/mbti/page.tsx,
    src/app/(dashboard)/students/[id]/mbti/actions.ts
  </files>
  <action>
**src/app/(dashboard)/students/[id]/mbti/page.tsx:**

Server component that loads student and draft:

```typescript
import { notFound, redirect } from "next/navigation"
import Link from "next/link"
import { ChevronLeft } from "lucide-react"
import { verifySession } from "@/lib/dal"
import { db } from "@/lib/db"
import { MbtiSurveyForm } from "@/components/mbti/survey-form"

export default async function MbtiSurveyPage({
  params,
}: {
  params: Promise<{ id: string }>
}) {
  const { id } = await params
  const session = await verifySession()

  const student = await db.student.findFirst({
    where: {
      id,
      teacherId: session.userId,
    },
    select: {
      id: true,
      name: true,
    },
  })

  if (!student) {
    notFound()
  }

  // Check if already has analysis - redirect to results if so (user can re-edit from there)
  const existingAnalysis = await db.mbtiAnalysis.findUnique({
    where: { studentId: id },
    select: { id: true }
  })

  // Load draft if exists
  const draft = await db.mbtiSurveyDraft.findUnique({
    where: { studentId: id },
    select: { responses: true }
  })

  const initialResponses = (draft?.responses as Record<string, number>) ||
    (existingAnalysis ? await loadAnalysisResponses(id) : {})

  return (
    <div className="max-w-3xl mx-auto">
      <div className="mb-6">
        <Link
          href={`/students/${student.id}`}
          className="inline-flex items-center text-sm text-gray-600 hover:text-gray-900"
        >
          <ChevronLeft className="w-4 h-4 mr-1" />
          {student.name} 프로필로 돌아가기
        </Link>
      </div>

      <div className="bg-white rounded-lg shadow-sm p-6 mb-6">
        <h1 className="text-2xl font-bold mb-2">MBTI 성향 검사</h1>
        <p className="text-gray-600">
          {student.name} 학생의 MBTI 성향을 검사합니다. 60개 문항에 답변해 주세요.
        </p>
        {existingAnalysis && (
          <p className="text-sm text-amber-600 mt-2">
            기존 검사 결과가 있습니다. 제출하면 새로운 결과로 업데이트됩니다.
          </p>
        )}
      </div>

      <div className="bg-white rounded-lg shadow-sm p-6">
        <MbtiSurveyForm studentId={student.id} initialDraft={initialResponses} />
      </div>
    </div>
  )
}

async function loadAnalysisResponses(studentId: string) {
  const analysis = await db.mbtiAnalysis.findUnique({
    where: { studentId },
    select: { responses: true }
  })
  return (analysis?.responses as Record<string, number>) || {}
}
```

**src/app/(dashboard)/students/[id]/mbti/actions.ts:**

Re-export server actions for client components (optional, but follows existing pattern):

```typescript
"use server"

export { saveMbtiDraft, submitMbtiSurvey, getMbtiDraft } from "@/lib/actions/mbti-survey"
```
  </action>
  <verify>
```bash
# Type check
npx tsc --noEmit

# Dev server and manual test
npm run dev
# Navigate to /students/{id}/mbti - page should load without errors
```
  </verify>
  <done>
MBTI 설문 페이지가 /students/[id]/mbti 경로에 생성됨. 기존 draft/analysis 로드 지원.
  </done>
</task>

</tasks>

<verification>
Phase 4 Plan 02 완료 검증:
1. `npm run dev` - 설문 페이지 접근 가능
2. 문항 응답 시 2초 후 자동 저장 확인 (console.log 또는 DB 확인)
3. 키보드 1-5 키로 응답 가능
4. 진행률 바가 응답에 따라 업데이트됨
5. 미응답 문항 있을 때 제출 시 해당 문항으로 스크롤
</verification>

<success_criteria>
- 60문항이 차원별로 그룹화되어 한 페이지에 표시됨
- 응답이 2초 debounce 후 자동 저장됨
- 키보드 1-5 키로 빠르게 응답 가능
- 진행률이 "45/60 (75%)" 형식과 프로그레스 바로 표시됨
- 미응답 문항이 빨간 테두리로 강조되고 제출 차단됨
</success_criteria>

<output>
After completion, create `.planning/phases/04-mbti-analysis/04-02-SUMMARY.md`
</output>
