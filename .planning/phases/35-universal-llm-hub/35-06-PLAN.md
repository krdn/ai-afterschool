---
phase: 35-universal-llm-hub
plan: 06
type: execute
wave: 4
depends_on: [35-03]
files_modified:
  - src/app/admin/llm-providers/page.tsx
  - src/app/admin/llm-providers/new/page.tsx
  - src/components/admin/llm-providers/provider-list.tsx
  - src/components/admin/llm-providers/provider-card.tsx
  - src/components/admin/llm-providers/provider-form.tsx
  - src/components/admin/llm-providers/template-selector.tsx
autonomous: true

must_haves:
  truths:
    - 관리자가 등록된 LLM 제공자 목록을 볼 수 있다
    - 템플릿을 선택하여 새 제공자를 쉽게 등록할 수 있다
    - 각 제공자의 연결 상태를 확인하고 테스트할 수 있다
  artifacts:
    - path: src/app/admin/llm-providers/page.tsx
      provides: 제공자 목록 페이지
    - path: src/components/admin/llm-providers/provider-form.tsx
      provides: 제공자 등록/수정 폼
    - path: src/components/admin/llm-providers/template-selector.tsx
      provides: 템플릿 선택 컴포넌트
  key_links:
    - from: UI Components
      to: provider-actions.ts
      via: Server Actions 호출
    - from: Template Selector
      to: templates.ts
      via: 템플릿 데이터 로드
---

<objective>
Phase 35 Plan 06: Admin Dashboard UI - Provider Management

Purpose: 직관적인 LLM 제공자 관리 UI 구현 - 템플릿 기반 등록 + 상태 관리
Output: 제공자 목록 페이지 + 등록 폼 + 템플릿 선택 UI
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-universal-llm-hub/35-03-SUMMARY.md
@src/lib/actions/provider-actions.ts
@src/lib/ai/templates.ts

## Current State
- Server Actions 구현 완료
- 템플릿 정의 완료
- 기존 /admin/llm-settings 페이지 존재

## New UI Requirements
- /admin/llm-providers 새 페이지
- 템플릿 카드 형태로 표시 (로고 + 이름 + 설명)
- 제공자 목록 (상태 배지 + 연결 테스트 버튼)
- 직접 설정 모드도 지원
</context>

<tasks>

<task type="auto">
  <name>Task 1: 템플릿 선택 컴포넌트</name>
  <files>src/components/admin/llm-providers/template-selector.tsx</files>
  <action>
    TemplateSelector 컴포넌트 구현:

    Props:
    - templates: ProviderTemplate[]
    - onSelect: (templateId: string) => void
    - selectedId?: string

    UI 구성:
    1. 인기 템플릿 섹션 (isPopular === true)
       - 큰 카드 형태 (grid 2-3열)
       - 로고 아이콘 (Lucide 아이콘 또는 외부 이미지)
       - 제공자명 (OpenAI, Claude 등)
       - 간단한 설명
       - "선택" 버튼

    2. 기타 템플릿 섹션 (grid 4-6열)
       - 작은 카드 형태
       - 제공자명만 표시

    3. 직접 설정 옵션
       - "직접 설정" 카드
       - 커스텀 제공자 등록

    스타일:
    - 선택된 템플릿: ring-2 ring-primary
    - 호버 효과: shadow-lg transform scale-105
    - 반응형: 모바일 1열 → 태블릿 2열 → 데스크톱 3열
  </action>
  <verify>grep "TemplateSelector\|isPopular\|onSelect" src/components/admin/llm-providers/template-selector.tsx | head -5</verify>
  <done>TemplateSelector 컴포넌트 구현 완료</done>
</task>

<task type="auto">
  <name>Task 2: 제공자 등록/수정 폼</name>
  <files>src/components/admin/llm-providers/provider-form.tsx</files>
  <action>
    ProviderForm 컴포넌트 구현:

    Props:
    - provider?: ProviderWithModels (수정 시)
    - template?: ProviderTemplate (템플릿 기반 생성 시)
    - onSuccess?: () => void

    Form 필드 (shadcn/ui 사용):
    1. 기본 정보:
       - name: 입력 필드 (템플릿 있으면 기본값 채워짐)
       - providerType: Select (템플릿 있으면 disabled)

    2. 연결 설정:
       - baseUrl: 입력 필드 (템플릿 기본값 표시)
       - authType: Select (api_key | bearer | custom_header)
       - customAuthHeader: 입력 필드 (authType === 'custom_header' 시 표시)
       - apiKey: Password 입력 필드 (마스킹)
       - apiKeyHelp: 템플릿의 apiKeyInstructions 표시

    3. 기능 태그:
       - capabilities: Multi-select (체크박스 그룹)
         * vision, function_calling, json_mode, streaming
       - costTier: Select (free, low, medium, high)
       - qualityTier: Select (fast, balanced, premium)

    4. 모델 설정:
       - 기본 모델 선택 (템플릿의 defaultModels 기반)
       - "모델 동기화" 버튼 (API로 실제 모델 목록 가져오기)

    5. 폼 액션:
       - "연결 테스트" 버튼 (validateProviderAction)
       - "저장" 버튼 (create/update action)
       - "취소" 버튼

    유효성 검사:
    - Zod 스키마 사용
    - name: 최소 1자
    - apiKey: 생성 시 필수, 수정 시 선택
    - baseUrl: URL 형식 검증
  </action>
  <verify>grep "ProviderForm\|zod\|useForm" src/components/admin/llm-providers/provider-form.tsx | head -5</verify>
  <done>ProviderForm 컴포넌트 구현 완료 (Zod 유효성 검사 포함)</done>
</task>

<task type="auto">
  <name>Task 3: 제공자 카드 컴포넌트</name>
  <files>src/components/admin/llm-providers/provider-card.tsx</files>
  <action>
    ProviderCard 컴포넌트 구현:

    Props:
    - provider: ProviderWithModels
    - onEdit: () => void
    - onDelete: () => void
    - onTest: () => void
    - onToggle: (enabled: boolean) => void

    UI 구성:
    1. 헤더:
       - 제공자명 (name)
       - 타입 배지 (providerType)
       - 상태 배지 (isValidated ? '연결됨' : '미검증', isEnabled ? '활성' : '비활성')

    2. 본문:
       - baseUrl (있는 경우)
       - 등록된 모델 수 (models.length)
       - 기능 태그 (capabilities.map → Badge))
       - 비용/품질 등급 (costTier, qualityTier)

    3. 액션:
       - 활성화/비활성화 토글 (Switch)
       - "연결 테스트" 버튼 (outline variant)
       - "편집" 버튼 (ghost variant)
       - "삭제" 버튼 (destructive variant, AlertDialog 확인)

    상태 표시:
    - 연결 성공: 초록색 배지 + 체크 아이콘
    - 연결 실패: 빨간색 배지 + X 아이콘 + 에러 메시지 툴팁
    - 미검증: 회색 배지 + 물음표 아이콘

    shadcn/ui 컴포넌트: Card, Badge, Button, Switch, AlertDialog, Tooltip
  </action>
  <verify>grep "ProviderCard\|isValidated\|isEnabled\|onToggle" src/components/admin/llm-providers/provider-card.tsx | head -5</verify>
  <done>ProviderCard 컴포넌트 구현 완료</done>
</task>

<task type="auto">
  <name>Task 4: 제공자 목록 페이지</name>
  <files>
    - src/app/admin/llm-providers/page.tsx
    - src/components/admin/llm-providers/provider-list.tsx
  </files>
  <action>
    1. src/components/admin/llm-providers/provider-list.tsx:
       - ProviderCard 목록을 grid로 표시
       - 로딩 상태 (Skeleton 카드)
       - 빈 상태 ("등록된 제공자가 없습니다" 메시지 + 등록 버튼)
       - providers.map(...)으로 렌더링

    2. src/app/admin/llm-providers/page.tsx:
       - Server Component
       - getProvidersAction() 호출
       - Page header: "LLM 제공자 관리" + 설명
       - "새 제공자 추가" 버튼 (/admin/llm-providers/new로 이동)
       - ProviderList 렌더링

    3. src/app/admin/llm-providers/new/page.tsx:
       - "새 제공자 등록" 페이지
       - 2단계 진행:
         * Step 1: TemplateSelector (템플릿 또는 직접 설정 선택)
         * Step 2: ProviderForm (선택된 템플릿 기반으로 폼 렌더링)
       - 뒤로 가기 버튼 (Step 2에서 Step 1으로)

    레이아웃:
    - max-w-7xl mx-auto
    - padding: py-6 px-4 sm:px-6 lg:px-8
    - gap: gap-6
  </action>
  <verify>grep "getProvidersAction\|TemplateSelector\|ProviderForm" src/app/admin/llm-providers/page.tsx src/app/admin/llm-providers/new/page.tsx | head -5</verify>
  <done>제공자 목록 페이지 및 신규 등록 페이지 구현 완료</done>
</task>

</tasks>

<verification>
- /admin/llm-providers 접속 시 목록 표시
- "새 제공자 추가" 클릭 시 템플릿 선택 화면
- 템플릿 선택 후 폼에 기본값 자동 채워짐
- 제공자 등록 후 목록에 표시
- 연결 테스트 버튼 동작
</verification>

<success_criteria>
- 템플릿 선택 UI가 직관적
- 제공자 등록 폼이 모든 필드 포함
- 연결 상태가 시각적으로 명확히 표시
- 반응형 디자인 작동
</success_criteria>

<output>
After completion, create `.planning/phases/35-universal-llm-hub/35-06-SUMMARY.md`
</output>
