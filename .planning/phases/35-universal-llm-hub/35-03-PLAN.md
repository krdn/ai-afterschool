---
phase: 35-universal-llm-hub
plan: 03
type: execute
wave: 2
depends_on: [35-02]
files_modified:
  - src/lib/ai/templates.ts
  - src/lib/db/seed-provider-templates.ts
  - src/app/api/providers/route.ts
  - src/app/api/providers/[id]/route.ts
  - src/lib/actions/provider-actions.ts
autonomous: true

must_haves:
  truths:
    - 인기 LLM 제공자 템플릿이 미리 정의되어 있다
    - 관리자가 새 제공자를 템플릿 기반으로 등록할 수 있다
    - 제공자 등록 API가 구현되어 있다
  artifacts:
    - path: src/lib/ai/templates.ts
      provides: 미리 정의된 제공자 템플릿
    - path: src/lib/db/seed-provider-templates.ts
      provides: 템플릿 DB 시딩 스크립트
    - path: src/app/api/providers/route.ts
      provides: 제공자 CRUD API 엔드포인트
  key_links:
    - from: Template
      to: Provider
      via: 템플릿 기반 생성 시 자동 채워짐
    - from: API
      to: ProviderRegistry
      via: provider-actions.ts Server Actions
---

<objective>
Phase 35 Plan 03: Provider Template System & Registration API

Purpose: 일반인도 쉽게 사용할 수 있는 템플릿 기반 제공자 등록 시스템 구현
Output: 템플릿 정의 + 시딩 + API 엔드포인트 + Server Actions
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-universal-llm-hub/35-02-SUMMARY.md
@src/lib/ai/types.ts
@src/lib/ai/provider-registry.ts

## Current State
- ProviderRegistry 구현 완료
- 타입 정의 완료

## New Requirements
- 인기 제공자 템플릿 미리 정의 (OpenAI, Anthropic, Google, Ollama 등)
- 템플릿 선택 시 필드 자동 채워짐
- 직접 설정 모드도 지원
</context>

<tasks>

<task type="auto">
  <name>Task 1: 제공자 템플릿 정의</name>
  <files>src/lib/ai/templates.ts</files>
  <action>
    src/lib/ai/templates.ts 생성:

    ProviderTemplate 인터페이스:
    - templateId: string (고유 ID)
    - name: string (표시명)
    - providerType: ProviderType
    - description: string (설명)
    - logoUrl?: string (로고 URL)
    - defaultBaseUrl?: string (기본 API URL)
    - defaultAuthType: AuthType
    - customAuthHeaderName?: string (커스텀 인증 시 헤더명)
    - defaultCapabilities: Capability[]
    - defaultCostTier: CostTier
    - defaultQualityTier: QualityTier
    - defaultModels: Array<{ modelId: string; displayName: string; contextWindow?: number; supportsVision?: boolean }>
    - apiKeyInstructions: string (API 키 발급 방법)
    - apiKeyUrl: string (API 키 발급 페이지 URL)
    - helpUrl?: string (도움말 URL)
    - isPopular: boolean (인기 제공자 여부)
    - sortOrder: number (정렬 순서)

    템플릿 목록:
    1. openai: OpenAI GPT (인기)
    2. anthropic: Anthropic Claude (인기)
    3. google: Google Gemini (인기)
    4. ollama: Ollama (로컬, 물뤂)
    5. deepseek: DeepSeek
    6. mistral: Mistral AI
    7. cohere: Cohere
    8. xai: xAI Grok
    9. zhipu: Zhipu AI (중국)
    10. moonshot: Moonshot AI (중국)
    11. custom: 직접 설정 (범용 OpenAI 호환)

    getProviderTemplates(): ProviderTemplate[] - 모든 템플릿 반환
    getProviderTemplate(id: string): ProviderTemplate | undefined - 특정 템플릿 조회
    getPopularTemplates(): ProviderTemplate[] - 인기 템플릿만 반환
  </action>
  <verify>grep -A 3 "templateId: 'openai'" src/lib/ai/templates.ts && grep "export function getProviderTemplates" src/lib/ai/templates.ts</verify>
  <done>모든 인기 제공자 템플릿이 정의됨 (11개)</done>
</task>

<task type="auto">
  <name>Task 2: 템플릿 DB 시딩</name>
  <files>src/lib/db/seed-provider-templates.ts</files>
  <action>
    src/lib/db/seed-provider-templates.ts 생성:

    - 템플릿 데이터를 ProviderTemplate DB 테이블에 삽입
    - 중복 방지 (upsert 사용)
    - 실행: npx tsx src/lib/db/seed-provider-templates.ts

    주요 로직:
    1. 기존 템플릿 확인
    2. templates.ts의 각 템플릿을 DB에 upsert
    3. 결과 로그 출력 (생성/업데이트/스킵)

    Prisma 코드 예시:
    ```typescript
    for (const template of templates) {
      await db.providerTemplate.upsert({
        where: { templateId: template.templateId },
        update: { ...template },
        create: { ...template },
      });
    }
    ```
  </action>
  <verify>ls src/lib/db/seed-provider-templates.ts && grep "upsert" src/lib/db/seed-provider-templates.ts</verify>
  <done>템플릿 시딩 스크립트 작성 완료</done>
</task>

<task type="auto">
  <name>Task 3: 제공자 API 엔드포인트</name>
  <files>
    - src/app/api/providers/route.ts
    - src/app/api/providers/[id]/route.ts
    - src/app/api/providers/[id]/validate/route.ts
    - src/app/api/providers/[id]/models/route.ts
  </files>
  <action>
    1. src/app/api/providers/route.ts:
       - GET: /api/providers - 제공자 목록 조회 (쿼리: enabledOnly)
       - POST: /api/providers - 새 제공자 등록
         * body: { templateId?: string; name: string; providerType: string; apiKey?: string; baseUrl?: string; ... }
         * templateId가 있으면 템플릿 기반으로 필드 자동 채움
         * 없으면 직접 설정 모드

    2. src/app/api/providers/[id]/route.ts:
       - GET: /api/providers/[id] - 특정 제공자 조회
       - PATCH: /api/providers/[id] - 제공자 수정
       - DELETE: /api/providers/[id] - 제공자 삭제

    3. src/app/api/providers/[id]/validate/route.ts:
       - POST: /api/providers/[id]/validate - 연결 테스트
         * ProviderRegistry.validate() 호출
         * 결과 반환: { valid: boolean; error?: string; models?: ModelInfo[] }

    4. src/app/api/providers/[id]/models/route.ts:
       - GET: /api/providers/[id]/models - 모델 목록 조회
       - POST: /api/providers/[id]/sync - 모델 동기화
         * ProviderRegistry.syncModels() 호출

    모든 엔드포인트는 DIRECTOR 권한 필요
  </action>
  <verify>ls src/app/api/providers/*.ts src/app/api/providers/&&>/dev/null || echo "Checking files..."</verify>
  <done>모든 API 엔드포인트 구현 완료 (GET, POST, PATCH, DELETE)</done>
</task>

<task type="auto">
  <name>Task 4: Server Actions</name>
  <files>src/lib/actions/provider-actions.ts</files>
  <action>
    src/lib/actions/provider-actions.ts 생성:

    'use server' 지시문 포함

    함수 목록:
    1. getTemplatesAction(): Promise<ProviderTemplate[]>
       - 모든 템플릿 조회
       - DIRECTOR 권한 확인

    2. getTemplateAction(templateId: string): Promise<ProviderTemplate | null>
       - 특정 템플릿 조회

    3. createProviderFromTemplateAction(templateId: string, config: Partial<ProviderInput>): Promise<ProviderConfig>
       - 템플릿 기반 제공자 생성
       - 템플릿의 기본값 + 사용자 입력 병합

    4. getProvidersAction(options?: { enabledOnly?: boolean }): Promise<ProviderWithModels[]>
       - 제공자 목록 조회

    5. updateProviderAction(id: string, input: Partial<ProviderInput>): Promise<ProviderConfig>
       - 제공자 수정
       - revalidatePath('/admin/llm-providers')

    6. deleteProviderAction(id: string): Promise<void>
       - 제공자 삭제
       - revalidatePath('/admin/llm-providers')

    7. validateProviderAction(id: string): Promise<ValidationResult>
       - 연결 테스트
       - 성공 시 isValidated = true 업데이트

    8. syncProviderModelsAction(id: string): Promise<ModelConfig[]>
       - 모델 목록 동기화

    모든 함수는 verifySession()으로 DIRECTOR 권한 확인
  </action>
  <verify>grep "'use server'" src/lib/actions/provider-actions.ts && grep "export async function" src/lib/actions/provider-actions.ts | wc -l</verify>
  <done>8개 Server Actions 구현 완료</done>
</task>

</tasks>

<verification>
- 템플릿이 DB에 시딩됨
- API 엔드포인트가 Postman/curl로 테스트 가능
- Server Actions가 컴포넌트에서 import 가능
- 템플릿 기반 생성이 정상 작동
</verification>

<success_criteria>
- POST /api/providers로 템플릿 기반 제공자 생성 가능
- GET /api/providers로 목록 조회 가능
- validate 엔드포인트로 연결 테스트 가능
</success_criteria>

<output>
After completion, create `.planning/phases/35-universal-llm-hub/35-03-SUMMARY.md`
</output>
