---
phase: 35-universal-llm-hub
plan: 09
type: execute
wave: 6
depends_on: [35-05, 35-08]
files_modified:
  - tests/lib/ai/provider-registry.test.ts
  - tests/lib/ai/feature-resolver.test.ts
  - tests/lib/ai/universal-router.test.ts
  - tests/e2e/admin-llm-providers.spec.ts
  - tests/e2e/admin-llm-features.spec.ts
  - src/lib/ai/test-integration.ts
autonomous: true

must_haves:
  truths:
    - 모든 핵심 기능에 대한 단위 테스트가 작성된다
    - E2E 테스트로 주요 사용자 시나리오를 검증한다
    - 통합 테스트로 전체 흐름이 정상 작동함을 확인한다
  artifacts:
    - path: tests/lib/ai/provider-registry.test.ts
      provides: ProviderRegistry 단위 테스트
    - path: tests/lib/ai/feature-resolver.test.ts
      provides: FeatureResolver 단위 테스트
    - path: tests/e2e/admin-llm-providers.spec.ts
      provides: 제공자 관리 E2E 테스트
  key_links:
    - from: 테스트 파일
      to: 구현 파일
      via: import 및 함수 호출
---

<objective>
Phase 35 Plan 09: Testing & Validation

Purpose: Universal LLM Hub의 모든 기능을 검증하는 테스트 작성
Output: 단위 테스트 + 통합 테스트 + E2E 테스트
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-universal-llm-hub/35-05-SUMMARY.md
@.planning/phases/35-universal-llm-hub/35-08-SUMMARY.md
@tests/setup.ts

## Current State
- 모든 핵심 기능 구현 완료
- 기존 tests/lib/ai/router.test.ts 존재

## Testing Strategy
- 단위 테스트: Registry, Resolver, Router
- 통합 테스트: end-to-end 흐름
- E2E 테스트: Playwright로 UI 시나리오
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProviderRegistry 단위 테스트</name>
  <files>tests/lib/ai/provider-registry.test.ts</files>
  <action>
    tests/lib/ai/provider-registry.test.ts 생성:

    테스트 케이스:

    1. register():
       - 새 제공자를 정상적으로 등록
       - 템플릿 기반으로 기본값이 채워지는지
       - 중복된 이름 처리
       - 필수 필드 누락 시 에러

    2. update():
       - 제공자 정보 업데이트
       - API 키 변경 시 isValidated 리셋
       - 존재하지 않는 ID 처리

    3. remove():
       - 제공자 삭제
       - 연결된 모델도 함께 삭제
       - 존재하지 않는 ID는 무시

    4. get() / list():
       - ID로 제공자 조회
       - 목록 조회 (enabled 필터)
       - 캐싱 동작 확인

    5. validate():
       - 연결 테스트 성공 시 isValidated = true
       - 실패 시 false
       - 어댑터 호출 확인

    6. syncModels():
       - 모델 동기화
       - 새 모델 추가
       - 기존 모델 업데이트

    Mock:
    - Prisma Client mock
    - Adapter mock
  </action>
  <verify>npx vitest run tests/lib/ai/provider-registry.test.ts --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|Tests)"</verify>
  <done>ProviderRegistry 테스트 작성 및 통과</done>
</task>

<task type="auto">
  <name>Task 2: FeatureResolver 단위 테스트</name>
  <files>tests/lib/ai/feature-resolver.test.ts</files>
  <action>
    tests/lib/ai/feature-resolver.test.ts 생성:

    테스트 케이스:

    1. resolve() - 기본:
       - 기능 타입으로 적절한 모델 선택
       - 우선순위가 높은 규칙 먼저 적용

    2. resolve() - 태그 기반:
       - requiredTags로 필터링
       - excludedTags로 제외
       - costTier/qualityTier 필터링
       - 여러 태그 조합

    3. resolve() - 직접 지정:
       - specific_model 모드 동작
       - 지정된 모델 반환

    4. resolve() - Vision 요구사항:
       - needsVision: true 시 Vision 지원 모델만 선택
       - Vision 미지원 모델 제외

    5. resolveWithFallback():
       - 폴 백 체인 반환
       - 우선순위 순서 확인

    6. resolve() - 결과 없음:
       - 조건을 만족하는 모델이 없을 때 에러
       - 적절한 에러 메시지

    7. CRUD:
       - createOrUpdateMapping
       - deleteMapping
       - getMappings

    테스트 데이터:
    - 여러 Provider + Model fixture
    - 다양한 FeatureMapping fixture
  </action>
  <verify>npx vitest run tests/lib/ai/feature-resolver.test.ts --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|Tests)"</verify>
  <done>FeatureResolver 테스트 작성 및 통과</done>
</task>

<task type="auto">
  <name>Task 3: Universal Router 단위 테스트</name>
  <files>tests/lib/ai/universal-router.test.ts</files>
  <action>
    tests/lib/ai/universal-router.test.ts 생성:

    테스트 케이스:

    1. generateWithProvider():
       - FeatureResolver로 모델 선택
       - Adapter로 생성 요청
       - 결과 반환
       - 사용량 추적 호출

    2. generateWithProvider() - 폴 백:
       - 첫 번째 제공자 실패 시 다음 제공자로
       - 폴 백 정보가 결과에 포함
       - 모든 제공자 실패 시 에러

    3. generateWithVision():
       - Vision 지원 모델만 선택
       - 이미지 포함 요청
       - Vision 미지원 모델 제외

    4. generateWithSpecificProvider():
       - 지정된 제공자 강제 사용
       - FeatureResolver 무시

    5. streamWithProvider():
       - 스트리밍 요청
       - onFinish 콜백에서 사용량 추적

    6. 하위호환성:
       - 기존 인터페이스와 호환
       - 기존 테스트와 동일한 동작

    Mock:
    - FeatureResolver mock
    - ProviderRegistry mock
    - Adapter mock
    - usage-tracker mock
  </action>
  <verify>npx vitest run tests/lib/ai/universal-router.test.ts --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|Tests)"</verify>
  <done>Universal Router 테스트 작성 및 통과</done>
</task>

<task type="auto">
  <name>Task 4: 통합 테스트</name>
  <files>src/lib/ai/test-integration.ts</files>
  <action>
    src/lib/ai/test-integration.ts 생성 (수동 실행용):

    통합 테스트 시나리오:

    1. 전체 흐름 테스트:
       - ProviderTemplate 시딩
       - 템플릿 기반 Provider 등록
       - Model 동기화
       - FeatureMapping 설정
       - generateWithProvider 호출
       - 결과 검증

    2. 폴 백 체인 테스트:
       - 첫 번째 제공자 실패 설정
       - 두 번째 제공자로 폴 백 확인

    3. 태그 기반 매칭 테스트:
       - 다양한 태그 조합으로 매칭
       - 우선순위 확인

    4. Vision 기능 테스트:
       - Vision 필요 기능 설정
       - Vision 지원 모델 선택 확인

    실행 방법:
    ```bash
    npx tsx src/lib/ai/test-integration.ts
    ```

    출력:
    - 각 시나리오별 성공/실패 표시
    - 상세 로그
    - 최종 결과 요약
  </action>
  <verify>ls src/lib/ai/test-integration.ts && head -30 src/lib/ai/test-integration.ts</verify>
  <done>통합 테스트 스크립트 작성 완료</done>
</task>

<task type="auto">
  <name>Task 5: E2E 테스트</name>
  <files>
    - tests/e2e/admin-llm-providers.spec.ts
    - tests/e2e/admin-llm-features.spec.ts
  </files>
  <action>
    1. tests/e2e/admin-llm-providers.spec.ts:
       테스트 시나리오:
       - 관리자 로그인 후 /admin/llm-providers 접근
       - "새 제공자 추가" 클릭
       - 템플릿 선택 (OpenAI)
       - API 키 입력
       - 연결 테스트 클릭
       - 저장
       - 목록에 새 제공자 표시 확인
       - 제공자 수정
       - 제공자 삭제

    2. tests/e2e/admin-llm-features.spec.ts:
       테스트 시나리오:
       - /admin/llm-features 접근
       - 기능별 탭 확인
       - "+ 규칙 추가" 클릭
       - 태그 기반 매핑 생성
       - 미리보기 확인
       - 저장
       - 목록에 새 규칙 표시

    Page Object Model:
    - LlmProvidersPage 클래스
    - LlmFeaturesPage 클래스
    - data-testid 속성 추가 (이미 구현된 것 활용)

    Fixture:
    - 테스트용 제공자 데이터
    - 테스트용 매핑 데이터
  </action>
  <verify>npx playwright test tests/e2e/admin-llm-providers.spec.ts --reporter=list 2>&1 | tail -10</verify>
  <done>E2E 테스트 작성 완료</done>
</task>

<task type="auto">
  <name>Task 6: 테스트 실행 및 검증</name>
  <files>
    - package.json (scripts 확인)
  </files>
  <action>
    전체 테스트 실행 및 검증:

    1. 단위 테스트 실행:
       ```bash
       npm run test
       ```
       - 모든 테스트 통과 확인

    2. 특정 테스트만 실행:
       ```bash
       npx vitest run tests/lib/ai/ --reporter=verbose
       ```

    3. E2E 테스트 실행:
       ```bash
       npx playwright test tests/e2e/admin-llm-
       ```

    4. 커버리지 확인:
       ```bash
       npx vitest run --coverage tests/lib/ai/
       ```
       - 목표: 80% 이상

    5. 문제 해결:
       - 실패한 테스트 디버깅
       - 필요 시 구현 수정
       - 테스트 업데이트

    검증 항목:
    - [ ] ProviderRegistry 테스트 통과
    - [ ] FeatureResolver 테스트 통과
    - [ ] Universal Router 테스트 통과
    - [ ] 기존 router.test.ts 통과 (하위호환성)
    - [ ] E2E 테스트 통과
  </action>
  <verify>npm run test 2>&1 | tail -20</verify>
  <done>모든 테스트 통과 검증 완료</done>
</task>

</tasks>

<verification>
- npm run test 실행 시 모든 테스트 통과
- npx playwright test 실행 시 E2E 테스트 통과
- 커버리지 80% 이상
- 기존 코드와의 하위호환성 유지
</verification>

<success_criteria>
- 모든 단위 테스트 통과 (30+ 테스트)
- E2E 테스트 통과 (10+ 시나리오)
- 코드 커버리지 80% 이상
- 기존 테스트와의 충돌 없음
</success_criteria>

<output>
After completion, create `.planning/phases/35-universal-llm-hub/35-09-SUMMARY.md`

Phase 35 완료 후:
1. VERIFICATION.md 생성하여 검증 결과 기록
2. MIGRATION_GUIDE.md 생성하여 기존 사용자 마이그레이션 가이드 작성
3. ROADMAP.md 업데이트
</output>
