---
phase: 21-statistics-dashboard
plan: 04
type: execute
wave: 2
depends_on: ["21-02"]
files_modified:
  - src/components/statistics/FollowUpList.tsx
  - src/components/statistics/FollowUpCard.tsx
autonomous: true

must_haves:
  truths:
    - "오늘/이번 주 후속 조치 목록이 표시된다"
    - "지연된 후속 조치가 빨간색 배경과 아이콘으로 강조된다"
    - "후속 조치 완료 체크가 가능하다"
  artifacts:
    - path: "src/components/statistics/FollowUpList.tsx"
      provides: "후속 조치 목록 컴포넌트"
      min_lines: 60
    - path: "src/components/statistics/FollowUpCard.tsx"
      provides: "후속 조치 개별 카드"
      min_lines: 50
  key_links:
    - from: "src/components/statistics/FollowUpList.tsx"
      to: "src/lib/actions/follow-up.ts"
      via: "getFollowUpsAction import"
      pattern: "import.*getFollowUpsAction"
    - from: "src/components/statistics/FollowUpCard.tsx"
      to: "src/components/ui/badge"
      via: "Badge 상태 표시"
      pattern: "import.*Badge"
---

<objective>
후속 조치 UI 컴포넌트 구현 (목록 및 카드)

Purpose: CounselingSession의 후속 조치를 시각적으로 관리할 수 있는 UI 컴포넌트를 구현합니다. 지연된 항목은 빨간색으로 강조하고, 완료 체크 기능을 제공합니다.
Output: FollowUpList.tsx, FollowUpCard.tsx
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-statistics-dashboard/21-CONTEXT.md
@.planning/phases/21-statistics-dashboard/21-RESEARCH.md

# 기존 Badge 및 Card 패턴 참조
@src/components/counseling/ReservationCard.tsx
@src/components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 후속 조치 카드 컴포넌트 구현</name>
  <files>src/components/statistics/FollowUpCard.tsx</files>
  <action>
"use client" 컴포넌트로 개별 후속 조치 카드를 구현합니다.

CONTEXT.md 결정사항:
- 지연 표시: 빨간색 배경 + 경고 아이콘으로 강조
- 완료 기록: 체크박스 + 선택적 메모 입력 필드

1. Props 정의:
   - item: FollowUpItem (src/types/follow-up.ts)
   - onComplete?: (id: string, note?: string) => Promise<void>
   - loading?: boolean

2. 상태별 스타일링:
   - overdue: bg-red-50 border-red-200 (빨간색 배경)
   - pending: bg-white border-gray-200
   - completed: bg-gray-50 border-gray-200 (흐린 스타일)

3. Badge 표시:
   - overdue: <Badge variant="destructive"><AlertCircle /> 지연</Badge>
   - pending: <Badge variant="default">예정</Badge>
   - completed: <Badge variant="outline">완료</Badge>

4. 카드 내용:
   - 학생 이름 + 선생님 이름
   - 상담 요약 (summary)
   - 후속 조치 마감일 (formatDistanceToNow로 상대 시간)
   - 원본 상담 날짜

5. 완료 처리 UI:
   - 미완료 시: Checkbox + "완료" 라벨
   - 클릭 시: 간단한 메모 입력 Dialog 또는 직접 완료 처리
   - onComplete 호출하여 상위 컴포넌트에 알림

lucide-react 아이콘:
- AlertCircle: 지연 경고
- Calendar: 날짜
- CheckCircle: 완료
- User: 학생/선생님

date-fns:
- formatDistanceToNow(followUpDate, { addSuffix: true, locale: ko })
  </action>
  <verify>npx tsc --noEmit src/components/statistics/FollowUpCard.tsx</verify>
  <done>
- 상태별 스타일링이 적용됨 (overdue: 빨간색 배경)
- Badge로 상태가 표시됨
- 완료 처리 UI가 구현됨
- 상대 시간이 한국어로 표시됨
  </done>
</task>

<task type="auto">
  <name>Task 2: 후속 조치 목록 컴포넌트 구현</name>
  <files>src/components/statistics/FollowUpList.tsx</files>
  <action>
"use client" 컴포넌트로 후속 조치 목록을 구현합니다.

CONTEXT.md 결정사항:
- 후속 조치 범위: 오늘 + 이번 주
- 정렬 방식: 마감일 임박순 (긴급한 것 먼저)

1. Props 정의:
   - items: FollowUpItem[]
   - onComplete?: (id: string, note?: string) => Promise<void>
   - loading?: boolean
   - title?: string (기본값: "후속 조치 목록")

2. 탭/필터 UI:
   - 오늘 / 이번 주 / 전체 탭 (Tabs 컴포넌트 사용)
   - 완료된 항목 표시/숨김 토글

3. 정렬:
   - followUpDate ASC (마감일 임박순)
   - 지연된 항목이 최상단

4. 렌더링:
   - Card로 감싸서 제목 표시
   - items.map으로 FollowUpCard 렌더링
   - 빈 상태: "후속 조치가 없습니다"
   - 로딩 상태: 3개 스켈레톤 카드

5. 완료 처리:
   - FollowUpCard의 onComplete를 연결
   - 완료 후 optimistic update 또는 revalidation

내부 정렬 로직:
const sortedItems = [...items].sort((a, b) => {
  // 지연된 항목 우선
  if (a.status === 'overdue' && b.status !== 'overdue') return -1;
  if (b.status === 'overdue' && a.status !== 'overdue') return 1;
  // 마감일 임박순
  return a.followUpDate.getTime() - b.followUpDate.getTime();
});
  </action>
  <verify>npx tsc --noEmit src/components/statistics/FollowUpList.tsx</verify>
  <done>
- 탭으로 범위 필터링 가능
- 마감일 임박순 정렬됨
- 지연된 항목이 최상단에 표시됨
- 빈 상태/로딩 상태 처리됨
- 완료 처리 콜백이 연결됨
  </done>
</task>

</tasks>

<verification>
1. 타입 체크: `npx tsc --noEmit src/components/statistics/FollowUpList.tsx src/components/statistics/FollowUpCard.tsx`
2. Lint: `npm run lint -- src/components/statistics/`
3. 빌드: `npm run build`
</verification>

<success_criteria>
- FollowUpCard: 상태별 스타일링, Badge, 완료 체크박스
- FollowUpList: 탭 필터, 정렬, 빈/로딩 상태
- 지연된 항목이 빨간색 배경 + AlertCircle 아이콘으로 강조
- 타입 체크 및 lint 오류 없음
</success_criteria>

<output>
After completion, create `.planning/phases/21-statistics-dashboard/21-04-SUMMARY.md`
</output>
