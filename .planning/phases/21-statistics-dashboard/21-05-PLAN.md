---
phase: 21-statistics-dashboard
plan: 05
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - src/components/statistics/TeacherStatsTable.tsx
  - src/components/statistics/DateRangeFilter.tsx
  - src/components/statistics/CsvExportButton.tsx
autonomous: true

must_haves:
  truths:
    - "선생님별 상담 통계가 테이블 + 미니 바 차트로 표시된다"
    - "기간 필터 프리셋 버튼이 작동한다"
    - "CSV 내보내기가 가능하다"
  artifacts:
    - path: "src/components/statistics/TeacherStatsTable.tsx"
      provides: "선생님별 통계 테이블"
      min_lines: 60
    - path: "src/components/statistics/DateRangeFilter.tsx"
      provides: "기간 필터 컴포넌트"
      min_lines: 40
    - path: "src/components/statistics/CsvExportButton.tsx"
      provides: "CSV 내보내기 버튼"
      min_lines: 30
  key_links:
    - from: "src/components/statistics/CsvExportButton.tsx"
      to: "Blob API"
      via: "CSV 생성 및 다운로드"
      pattern: "new Blob\\["
---

<objective>
선생님별 통계 테이블, 기간 필터, CSV 내보내기 구현

Purpose: 대시보드의 필터링 및 데이터 내보내기 기능을 제공하는 보조 컴포넌트들을 구현합니다.
Output: TeacherStatsTable.tsx, DateRangeFilter.tsx, CsvExportButton.tsx
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-statistics-dashboard/21-CONTEXT.md
@.planning/phases/21-statistics-dashboard/21-RESEARCH.md

# 기존 Select 및 Table 패턴 참조
@src/components/analytics/PerformanceDashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 기간 필터 컴포넌트 구현</name>
  <files>src/components/statistics/DateRangeFilter.tsx</files>
  <action>
"use client" 컴포넌트로 기간 필터 프리셋 버튼을 구현합니다.

CONTEXT.md 결정사항:
- 기간 필터: 프리셋 버튼 (1개월/3개월/6개월/1년) + 사용자 지정

1. Props 정의:
   - value: DatePreset
   - onChange: (preset: DatePreset) => void
   - showCustom?: boolean (사용자 지정 옵션 표시 여부)

2. 두 가지 UI 옵션:
   A. 버튼 그룹 스타일:
      - 4개 버튼을 나란히 배치
      - 선택된 버튼: bg-primary text-primary-foreground
      - 미선택 버튼: bg-secondary

   B. Select 드롭다운 스타일:
      - shadcn/ui Select 사용
      - SelectItem: 1M, 3M, 6M, 1Y

두 스타일 모두 구현하되, 기본값은 버튼 그룹으로 설정.
variant prop으로 선택 가능: 'buttons' | 'dropdown'

3. 프리셋 레이블:
   const PRESET_LABELS: Record<DatePreset, string> = {
     '1M': '최근 1개월',
     '3M': '최근 3개월',
     '6M': '최근 6개월',
     '1Y': '최근 1년'
   }
  </action>
  <verify>npx tsc --noEmit src/components/statistics/DateRangeFilter.tsx</verify>
  <done>
- 버튼 그룹과 드롭다운 두 가지 variant 지원
- 선택된 프리셋이 시각적으로 강조됨
- onChange 콜백이 올바르게 호출됨
  </done>
</task>

<task type="auto">
  <name>Task 2: 선생님별 통계 테이블 구현</name>
  <files>src/components/statistics/TeacherStatsTable.tsx</files>
  <action>
"use client" 컴포넌트로 선생님별 상담 통계 테이블을 구현합니다.

CONTEXT.md 결정사항:
- 선생님별 비교: 테이블 + 미니 바 차트 조합 (순위와 시각적 비교 모두 제공)

1. Props 정의:
   - data: TeacherMonthlyStats[]
   - loading?: boolean
   - maxVisible?: number (기본값: 10, 더보기 버튼으로 확장)

2. 테이블 컬럼:
   - 순위 (#)
   - 선생님 이름
   - 상담 횟수 (숫자 + 미니 바)
   - 유형별 분포 (작은 도넛 또는 세그먼트 바)

3. 미니 바 차트:
   - 최대값 대비 비율로 너비 계산
   - 예: width: `${(count / maxCount) * 100}%`
   - bg-blue-500 rounded-full h-2

4. 정렬:
   - 기본: 상담 횟수 내림차순 (상위 순위)
   - 클릭 시 정렬 변경 가능 (optional)

5. 스타일링:
   - shadcn/ui Table 컴포넌트 사용
   - Card로 감싸기
   - 로딩 시 스켈레톤 행 표시

6. 상호작용:
   - 행 클릭 시 해당 선생님 상세 (optional, 나중에 확장 가능)
  </action>
  <verify>npx tsc --noEmit src/components/statistics/TeacherStatsTable.tsx</verify>
  <done>
- 테이블에 순위, 이름, 횟수, 미니 바가 표시됨
- 상담 횟수 내림차순 정렬됨
- 로딩 상태에서 스켈레톤 표시됨
- maxVisible로 표시 개수 제한 가능
  </done>
</task>

<task type="auto">
  <name>Task 3: CSV 내보내기 버튼 구현</name>
  <files>src/components/statistics/CsvExportButton.tsx</files>
  <action>
"use client" 컴포넌트로 CSV 내보내기 기능을 구현합니다.

CONTEXT.md 결정사항:
- 내보내기: CSV 다운로드 지원

RESEARCH.md Pattern 3 참조 (Blob API):

1. Props 정의:
   - data: any[] (제네릭하게 받음)
   - filename?: string (기본값: 'counseling-stats.csv')
   - headers?: string[] (CSV 헤더, 선택적)
   - getRow?: (item: any) => (string | number)[] (행 변환 함수)
   - children?: React.ReactNode (버튼 라벨)

2. exportToCSV 함수:
   - CSV 헤더 생성
   - 데이터를 행으로 변환 (getRow 또는 Object.values)
   - BOM 추가 (\uFEFF) - 한글 깨짐 방지
   - Blob 생성: new Blob([BOM + csvString], { type: 'text/csv;charset=utf-8;' })
   - 다운로드 트리거: URL.createObjectURL, link.click()
   - 정리: URL.revokeObjectURL

3. UI:
   - Button variant="outline"
   - FileDown 아이콘
   - 기본 라벨: "CSV 다운로드"

4. 에러 처리:
   - 빈 데이터 시 toast 경고
   - 변환 실패 시 catch하여 에러 표시
  </action>
  <verify>npx tsc --noEmit src/components/statistics/CsvExportButton.tsx</verify>
  <done>
- Blob API로 CSV 생성됨
- BOM이 포함되어 한글 깨짐 방지됨
- 다운로드가 트리거됨
- 빈 데이터 처리됨
  </done>
</task>

</tasks>

<verification>
1. 타입 체크: `npx tsc --noEmit src/components/statistics/*.tsx`
2. Lint: `npm run lint -- src/components/statistics/`
3. 빌드: `npm run build`
</verification>

<success_criteria>
- DateRangeFilter: 버튼 그룹 및 드롭다운 variant
- TeacherStatsTable: 순위, 이름, 횟수, 미니 바 표시
- CsvExportButton: Blob API, BOM, 다운로드 트리거
- 타입 체크 및 lint 오류 없음
</success_criteria>

<output>
After completion, create `.planning/phases/21-statistics-dashboard/21-05-SUMMARY.md`
</output>
