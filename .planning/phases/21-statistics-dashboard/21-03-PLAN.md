---
phase: 21-statistics-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - src/components/statistics/StatisticsCards.tsx
  - src/components/statistics/CounselingTrendChart.tsx
  - src/components/statistics/CounselingTypeChart.tsx
autonomous: true

must_haves:
  truths:
    - "4개 요약 카드가 표시된다 (이번 달 상담, 대기 예약, 지연 후속조치, 완료율)"
    - "월별 상담 추이가 라인/영역 차트로 시각화된다"
    - "상담 유형별 분포가 도넛 차트로 시각화된다"
  artifacts:
    - path: "src/components/statistics/StatisticsCards.tsx"
      provides: "대시보드 요약 카드 컴포넌트"
      min_lines: 50
    - path: "src/components/statistics/CounselingTrendChart.tsx"
      provides: "월별 추이 라인/영역 차트"
      min_lines: 80
    - path: "src/components/statistics/CounselingTypeChart.tsx"
      provides: "유형별 도넛 차트"
      min_lines: 60
  key_links:
    - from: "src/components/statistics/CounselingTrendChart.tsx"
      to: "recharts"
      via: "LineChart, AreaChart import"
      pattern: "import.*from 'recharts'"
    - from: "src/components/statistics/CounselingTypeChart.tsx"
      to: "recharts"
      via: "PieChart import"
      pattern: "import.*PieChart.*from 'recharts'"
---

<objective>
통계 시각화 UI 컴포넌트 구현 (요약 카드, 추이 차트, 유형 차트)

Purpose: Phase 14의 검증된 Recharts 패턴을 재사용하여 상담 통계 대시보드의 핵심 시각화 컴포넌트를 구현합니다.
Output: StatisticsCards.tsx, CounselingTrendChart.tsx, CounselingTypeChart.tsx
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-statistics-dashboard/21-CONTEXT.md
@.planning/phases/21-statistics-dashboard/21-RESEARCH.md

# 기존 Recharts 패턴 참조
@src/components/analytics/GradeTrendChart.tsx
@src/components/analytics/PerformanceDashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 요약 카드 컴포넌트 구현</name>
  <files>src/components/statistics/StatisticsCards.tsx</files>
  <action>
"use client" 컴포넌트로 4개 요약 카드를 구현합니다.

CONTEXT.md 결정사항:
- 4개 카드: 이번 달 상담, 대기 예약, 지연 후속조치, 완료율

구현:
1. StatCard 내부 컴포넌트:
   - Props: title, value, icon, variant ('default' | 'success' | 'warning' | 'danger')
   - shadcn/ui Card, CardHeader, CardContent 사용
   - lucide-react 아이콘

2. StatisticsCards 컴포넌트:
   - Props: monthlySessionCount, pendingReservationCount, overdueFollowUpCount, completionRate
   - loading?: boolean (스켈레톤 지원)
   - 4개 StatCard를 grid로 배치

아이콘 선택 (Claude's Discretion):
- 이번 달 상담: Calendar
- 대기 예약: Users
- 지연 후속조치: AlertCircle (빨간색)
- 완료율: TrendingUp (초록색)

variant 색상:
- danger: text-red-600
- success: text-green-600
- warning: text-orange-600
- default: text-gray-900

반응형:
- 모바일: grid-cols-1
- 태블릿: grid-cols-2
- 데스크탑: grid-cols-4
  </action>
  <verify>npx tsc --noEmit src/components/statistics/StatisticsCards.tsx</verify>
  <done>
- 4개 요약 카드가 grid로 배치됨
- 각 카드에 아이콘과 값이 표시됨
- 반응형 레이아웃 적용됨
- loading 상태에서 스켈레톤 표시됨
  </done>
</task>

<task type="auto">
  <name>Task 2: 월별 추이 차트 컴포넌트 구현</name>
  <files>src/components/statistics/CounselingTrendChart.tsx</files>
  <action>
"use client" 컴포넌트로 월별 상담 추이 차트를 구현합니다.

CONTEXT.md 결정사항:
- 라인 차트 + 영역 차트 토글 전환 지원
- 기본 표시 기간: 최근 6개월

GradeTrendChart.tsx 패턴 참조:

1. Props 정의:
   - data: MonthlyTrend[] (src/types/statistics.ts에서 import)
   - loading?: boolean
   - title?: string (기본값: "월별 상담 추이")

2. 차트 타입 토글:
   - useState<'line' | 'area'>('line')
   - 토글 버튼: "라인" | "영역"

3. Recharts 구현:
   - ResponsiveContainer width="100%" height={300}
   - LineChart 또는 AreaChart (chartType에 따라)
   - XAxis: dataKey="label" (예: "2026-01")
   - YAxis: 상담 건수
   - CartesianGrid strokeDasharray="3 3"
   - Tooltip: 한국어 포맷팅
   - Line/Area: stroke="#3b82f6", fill="#3b82f6" (영역 시 fillOpacity={0.6})

4. 빈 상태 / 로딩 상태:
   - loading: animate-pulse 스켈레톤
   - 데이터 없음: "데이터가 충분하지 않습니다" 메시지

Card로 감싸서 제목 표시.
  </action>
  <verify>npx tsc --noEmit src/components/statistics/CounselingTrendChart.tsx</verify>
  <done>
- 라인/영역 차트 토글이 작동함
- ResponsiveContainer height={300}으로 높이 지정됨
- 로딩/빈 상태 처리됨
- Card로 감싸져 제목이 표시됨
  </done>
</task>

<task type="auto">
  <name>Task 3: 상담 유형별 도넛 차트 구현</name>
  <files>src/components/statistics/CounselingTypeChart.tsx</files>
  <action>
"use client" 컴포넌트로 상담 유형별 분포 도넛 차트를 구현합니다.

CONTEXT.md 결정사항:
- 도넛 차트로 전체 대비 비율 표시

RESEARCH.md Pattern 4 참조:

1. Props 정의:
   - data: TypeDistribution[] (src/types/statistics.ts에서 import)
   - loading?: boolean
   - title?: string (기본값: "상담 유형별 분포")

2. 색상 상수:
   const COUNSELING_TYPE_COLORS: Record<string, string> = {
     ACADEMIC: '#3b82f6',    // 학습 - 파랑
     CAREER: '#10b981',      // 진로 - 초록
     PSYCHOLOGICAL: '#f59e0b', // 생활 - 주황
     BEHAVIORAL: '#8b5cf6'   // 기타 - 보라
   }

   const COUNSELING_TYPE_LABELS: Record<string, string> = {
     ACADEMIC: '학습',
     CAREER: '진로',
     PSYCHOLOGICAL: '생활',
     BEHAVIORAL: '기타'
   }

3. Recharts 구현:
   - ResponsiveContainer width="100%" height={300}
   - PieChart
   - Pie: dataKey="count", nameKey="type"
   - cx="50%", cy="50%"
   - outerRadius={100}, innerRadius={60} (도넛 효과)
   - label: 백분율 표시 (예: "학습: 45%")
   - Cell: 각 타입별 색상 적용
   - Tooltip: "N회" 형식
   - Legend

4. 빈 상태 / 로딩 상태 처리

Card로 감싸서 제목 표시.
  </action>
  <verify>npx tsc --noEmit src/components/statistics/CounselingTypeChart.tsx</verify>
  <done>
- 도넛 차트가 innerRadius로 구현됨
- 각 유형별 색상이 적용됨
- 라벨에 백분율이 표시됨
- Legend가 표시됨
- 로딩/빈 상태 처리됨
  </done>
</task>

</tasks>

<verification>
1. 타입 체크: `npx tsc --noEmit src/components/statistics/*.tsx`
2. Lint: `npm run lint -- src/components/statistics/`
3. 빌드: `npm run build`
</verification>

<success_criteria>
- 3개 컴포넌트 파일이 생성됨
- StatisticsCards: 4개 카드가 반응형 grid로 배치
- CounselingTrendChart: 라인/영역 토글, ResponsiveContainer height={300}
- CounselingTypeChart: 도넛 차트 (innerRadius), 색상 매핑, Legend
- 모든 컴포넌트에 loading/empty 상태 처리
- 타입 체크 및 lint 오류 없음
</success_criteria>

<output>
After completion, create `.planning/phases/21-statistics-dashboard/21-03-SUMMARY.md`
</output>
