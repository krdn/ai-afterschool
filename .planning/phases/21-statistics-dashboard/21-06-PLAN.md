---
phase: 21-statistics-dashboard
plan: 06
type: execute
wave: 3
depends_on: ["21-03", "21-04", "21-05"]
files_modified:
  - src/app/(dashboard)/dashboard/statistics/page.tsx
  - src/app/(dashboard)/dashboard/statistics/layout.tsx
  - src/components/statistics/StatisticsDashboard.tsx
autonomous: true

must_haves:
  truths:
    - "통계 대시보드 페이지가 /dashboard/statistics에서 접근 가능하다"
    - "요약 카드, 차트, 후속 조치 목록이 한 페이지에 표시된다"
    - "기간 필터링이 모든 차트에 적용된다"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/statistics/page.tsx"
      provides: "통계 대시보드 페이지"
      min_lines: 40
    - path: "src/components/statistics/StatisticsDashboard.tsx"
      provides: "대시보드 메인 컴포넌트"
      min_lines: 100
  key_links:
    - from: "src/components/statistics/StatisticsDashboard.tsx"
      to: "src/lib/actions/counseling-stats.ts"
      via: "Server Actions import"
      pattern: "import.*from '@/lib/actions/counseling-stats'"
    - from: "src/components/statistics/StatisticsDashboard.tsx"
      to: "src/lib/actions/follow-up.ts"
      via: "Server Actions import"
      pattern: "import.*from '@/lib/actions/follow-up'"
---

<objective>
통계 대시보드 페이지 통합 및 레이아웃 구성

Purpose: Plan 21-01 ~ 21-05에서 구현한 모든 컴포넌트를 하나의 대시보드 페이지로 통합합니다. Server Component에서 데이터를 fetch하고 Client Component로 전달합니다.
Output: page.tsx, layout.tsx, StatisticsDashboard.tsx
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-statistics-dashboard/21-CONTEXT.md
@.planning/phases/21-statistics-dashboard/21-RESEARCH.md

# 기존 대시보드 페이지 패턴 참조
@src/app/(dashboard)/analytics/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 대시보드 레이아웃 생성</name>
  <files>src/app/(dashboard)/dashboard/statistics/layout.tsx</files>
  <action>
Next.js 레이아웃 파일을 생성합니다.

기존 (dashboard) 레이아웃을 상속하므로 간단한 wrapper만 필요:

```typescript
export default function StatisticsLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}

export const metadata = {
  title: '상담 통계 | AI 방과후학교',
  description: '상담 통계 및 후속 조치 대시보드',
}
```

또는 추가 레이아웃 요소가 필요하면:
- 네비게이션 탭 (통계 / 후속조치)
- 브레드크럼 (대시보드 > 상담 통계)
  </action>
  <verify>npx tsc --noEmit src/app/\\(dashboard\\)/dashboard/statistics/layout.tsx</verify>
  <done>
- 레이아웃 파일 생성됨
- metadata가 설정됨
  </done>
</task>

<task type="auto">
  <name>Task 2: 대시보드 메인 컴포넌트 구현</name>
  <files>src/components/statistics/StatisticsDashboard.tsx</files>
  <action>
"use client" 컴포넌트로 대시보드 메인 레이아웃을 구현합니다.

CONTEXT.md 결정사항:
- 섹션 배치: 2열 그리드 (왼쪽: 통계/차트, 오른쪽: 후속조치 목록)
- 모바일: 카드 세로 스택으로 반응형 전환

1. Props 정의:
   - initialStats: 초기 통계 데이터 (Server Component에서 전달)
   - initialFollowUps: 초기 후속 조치 데이터

2. 상태 관리:
   - datePreset: DatePreset (기본값: '6M')
   - teacherFilter: string | undefined
   - typeFilter: CounselingType[] (다중 선택)
   - loading: boolean

3. 필터 변경 시 데이터 재fetch:
   - useEffect로 datePreset/teacherFilter 변경 감지
   - Server Action 호출하여 데이터 업데이트
   - loading 상태 토글

4. 레이아웃:
   상단:
   - 페이지 제목 + DateRangeFilter + CsvExportButton

   요약 카드:
   - StatisticsCards (4개 카드, grid-cols-4)

   메인 그리드 (2열):
   왼쪽 (lg:col-span-2):
   - CounselingTrendChart (월별 추이)
   - CounselingTypeChart (유형별 분포)
   - TeacherStatsTable (선생님별 통계)

   오른쪽 (lg:col-span-1):
   - FollowUpList (후속 조치 목록)

5. 반응형:
   - 모바일: flex-col (세로 스택)
   - 태블릿: grid-cols-1
   - 데스크탑: grid-cols-3 (왼쪽 2, 오른쪽 1)

6. 후속 조치 완료 처리:
   - completeFollowUpAction 호출
   - 성공 시 toast 표시
   - 데이터 revalidate
  </action>
  <verify>npx tsc --noEmit src/components/statistics/StatisticsDashboard.tsx</verify>
  <done>
- 2열 그리드 레이아웃 구현됨
- 필터 변경 시 데이터가 재fetch됨
- 모든 하위 컴포넌트가 통합됨
- 반응형 레이아웃 적용됨
  </done>
</task>

<task type="auto">
  <name>Task 3: 통계 대시보드 페이지 구현</name>
  <files>src/app/(dashboard)/dashboard/statistics/page.tsx</files>
  <action>
Server Component로 통계 대시보드 페이지를 구현합니다.

1. 데이터 초기 로드 (Server-side):
   - getTeacherMonthlyStatsAction({ dateFrom, dateTo })
   - getCounselingTypeDistributionAction({ dateFrom, dateTo })
   - getMonthlyTrendAction({ months: 6 })
   - getFollowUpsAction({ scope: 'week' })
   - getReservationStatsAction() (대기 예약 수)
   - getOverdueCountAction() (지연 후속조치 수)

2. 완료율 계산:
   - 이번 달 완료 / 이번 달 전체 * 100

3. StatisticsDashboard에 초기 데이터 전달:
   ```tsx
   <StatisticsDashboard
     initialStats={{
       monthlySessionCount,
       pendingReservationCount,
       overdueFollowUpCount,
       completionRate,
       teacherStats,
       typeDistribution,
       monthlyTrend,
     }}
     initialFollowUps={followUps}
   />
   ```

4. 에러 처리:
   - Server Action 실패 시 기본값 사용
   - 에러 메시지 콘솔 로그
  </action>
  <verify>npx tsc --noEmit src/app/\\(dashboard\\)/dashboard/statistics/page.tsx</verify>
  <done>
- Server Component에서 초기 데이터 fetch
- StatisticsDashboard로 데이터 전달
- 페이지가 /dashboard/statistics에서 접근 가능
  </done>
</task>

</tasks>

<verification>
1. 타입 체크: `npx tsc --noEmit`
2. Lint: `npm run lint`
3. 빌드: `npm run build`
4. 페이지 접근: npm run dev 후 http://localhost:3000/dashboard/statistics 확인
</verification>

<success_criteria>
- /dashboard/statistics 페이지가 접근 가능함
- 요약 카드 4개가 상단에 표시됨
- 월별 추이 차트, 유형별 도넛 차트가 표시됨
- 선생님별 통계 테이블이 표시됨
- 후속 조치 목록이 오른쪽에 표시됨
- 기간 필터 변경 시 모든 차트가 업데이트됨
- 타입 체크 및 lint 오류 없음
</success_criteria>

<output>
After completion, create `.planning/phases/21-statistics-dashboard/21-06-SUMMARY.md`
</output>
