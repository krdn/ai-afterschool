---
phase: 09-performance-&-database-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/app/api/health/route.ts
autonomous: true

must_haves:
  truths:
    - "Prisma 연결 풀이 최대 10개 연결로 제한되어 데이터베이스 연결 고갈 방지"
    - "개발 환경에서 쿼리 로그가 활성화되어 N+1 쿼리 패턴 감지 가능"
    - "헬스체크 엔드포인트가 연결 풀 상태(전체/유휴 연결 수)를 반환"
  artifacts:
    - path: "src/lib/db.ts"
      provides: "Prisma 싱글톤 인스턴스와 연결 풀 구성"
      contains: "new Pool({ max: 10, idleTimeoutMillis: 30000, connectionTimeoutMillis: 2000 })"
      exports: ["db"]
    - path: "src/app/api/health/route.ts"
      provides: "연결 풀 메트릭을 포함한 헬스체크"
      exports: ["GET", "HEAD"]
  key_links:
    - from: "src/lib/db.ts"
      to: "pg.Pool"
      via: "PrismaPg 어댑터"
      pattern: "new PrismaPg\\(pool\\)"
    - from: "src/app/api/health/route.ts"
      to: "src/lib/db.ts"
      via: "pool.totalCount 및 pool.idleCount 조회"
      pattern: "pool\\.(totalCount|idleCount)"
---

<objective>
Prisma 연결 풀링을 명시적으로 구성하여 동시 요청 처리 시 데이터베이스 연결 고갈 방지 및 개발 환경에서 쿼리 로그 활성화

Purpose: CONTEXT.md 결정에 따라 최대 10개 연결로 고정 설정하여 작은 규모(50~200명 학생)에서 빠른 응답 보장, 연결 풀 사용률 모니터링

Output: 구성된 연결 풀을 가진 PrismaClient 인스턴스와 연결 풀 메트릭을 반환하는 헬스체크
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-performance-&-database-optimization/09-CONTEXT.md
@.planning/phases/09-performance-&-database-optimization/09-RESEARCH.md

@src/lib/db.ts
@src/app/api/health/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma 연결 풀 명시적 구성</name>
  <files>src/lib/db.ts</files>
  <action>
    현재 구현된 싱글톤 패턴을 유지하면서 연결 풀을 명시적으로 구성:

    1. `Pool` 생성자에 연결 풀 설정 추가:
       ```typescript
       const pool = globalForPrisma.pool ?? new Pool({
         connectionString: databaseUrl,
         max: 10,                      // CONTEXT.md 결정: 최대 10개 연결
         idleTimeoutMillis: 30000,    // 30초 후 유휴 연결 종료
         connectionTimeoutMillis: 2000, // 2초 대기 후 타임아웃
       })
       ```

    2. PrismaClient 생성자에 쿼리 로깅 추가:
       ```typescript
       export const db = globalForPrisma.prisma ?? new PrismaClient({
         adapter,
         log: process.env.NODE_ENV === "development"
           ? ['query', 'error', 'warn']  // 개발 환경: 모든 로그
           : ['error'],                   // 프로덕션: 에러만
       })
       ```

    3. `globalForPrisma`에 `pool` 저장 (이미 구현됨, 확인만)

    **주의사항:**
    - 기존 싱글톤 패턴(전역 변수, 조건부 할당)을 그대로 유지
    - `DATABASE_URL`이 없을 때의 에러 처리를 유지
    - 프로덕션 환경에서는 쿼리 로그를 비활성화하여 성능 저하 방지

    **RESEARCH.md 패턴 참고:**
    - Pattern 1: Prisma Connection Pooling with Singleton
    - Pitfall 1: Connection Pool Exhaustion
  </action>
  <verify>grep -A 5 "new Pool(" src/lib/db.ts | grep -E "(max:|idleTimeoutMillis:|connectionTimeoutMillis:)"</verify>
  <done>db.ts에 Pool 구성이 추가되고 개발 환경에서 쿼리 로그 활성화</done>
</task>

<task type="auto">
  <name>Task 2: 헬스체크에 연결 풀 메트릭 추가</name>
  <files>src/app/api/health/route.ts</files>
  <action>
    기존 헬스체크 엔드포인트에 연결 풀 상태를 추가하여 모니터링 가능:

    1. `pool` 객체를 db.ts에서 export하여 헬스체크에서 접근:
       - db.ts 수정: `export { pool }` 추가
       - health/route.ts에서 import: `import { db, pool } from "@/lib/db"`

    2. 헬스체크 응답에 연결 풀 메트릭 추가:
       ```typescript
       const healthData = {
         status: "healthy",
         timestamp: new Date().toISOString(),
         database: {
           status: dbStatus,
           responseTimeMs: dbResponseTime,
           connectionPool: {
             total: pool.totalCount,    // 전체 연결 수
             idle: pool.idleCount,      // 유휴 연결 수
             waiting: pool.waitingCount // 대기 중인 요청 수
           }
         },
         storage: {
           // ... 기존 스토리지 상태 ...
         }
       }
       ```

    3. 연결 풀 사용률 계산 및 경고 로직 추가:
       ```typescript
       const poolUsage = pool.totalCount / 10  // max: 10
       if (poolUsage > 0.8) {
         // 연결 풀 사용률 80% 초과 시 경고
         console.warn(`Connection pool usage high: ${(poolUsage * 100).toFixed(0)}%`)
       }
       ```

    **주의사항:**
    - 기존 헬스체크 구조(status, timestamp, database, storage)를 유지
    - `pool` 객체는 `pg` 패키지의 Pool 인스턴스 ( totalCount, idleCount, waitingCount 속성)
    - 연결 풀 메트릭은 프로덕션 모니터링을 위함 (CONTEXT.md: "연결 풀 사용률을 주기적으로 기록하고 경보 설정")

    **RESEARCH.md 참고:**
    - Open Question 2: "Connection Pool Monitoring - health check endpoint that reports pool stats"
  </action>
  <verify>grep -n "connectionPool" src/app/api/health/route.ts</verify>
  <done>health/route.ts가 연결 풀 메트릭(total, idle, waiting)을 반환</done>
</task>

</tasks>

<verification>
1. db.ts 문법 확인: TypeScript 컴파일 에러 없음
2. 연결 풀 설정 확인: `max: 10` 설정이 포함되어 있는지 확인
3. 헬스체크 응답 확인: GET /api/health 호출 시 connectionPool 필드 포함
4. 쿼리 로그 확인: 개발 환경에서 Prisma 쿼리가 콘솔에 출력되는지 확인
</verification>

<success_criteria>
1. `src/lib/db.ts`에 Pool 설정(max: 10, idleTimeoutMillis: 30000, connectionTimeoutMillis: 2000)이 포함
2. 개발 환경(NODE_ENV=development)에서 Prisma 쿼리가 콘솔에 로그됨
3. `/api/health` 엔드포인트가 `connectionPool` 객체(total, idle, waiting)를 반환
4. 연결 풀 사용률이 80%를 초과할 경우 콘솔에 경고 메시지 출력
</success_criteria>

<output>
After completion, create `.planning/phases/09-performance-&-database-optimization/09-02-SUMMARY.md`
</output>
