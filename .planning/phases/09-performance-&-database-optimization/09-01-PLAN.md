---
phase: 09-performance-&-database-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/deploy.sh
  - docker-compose.prod.yml
autonomous: true

must_haves:
  truths:
    - "배포 시 데이터베이스 마이그레이션이 자동으로 실행되어 적용되지 않은 마이그레이션을 적용"
    - "마이그레이션 실패 시 배포가 중단되고 이전 버전으로 자동 롤백"
    - "마이그레이션 실행 결과가 배포 로그에 기록됨"
  artifacts:
    - path: "scripts/deploy.sh"
      provides: "마이그레이션 자동화가 포함된 배포 스크립트"
      contains: "prisma migrate deploy"
    - path: "docker-compose.prod.yml"
      provides: "Docker Compose 프로덕션 구성"
      contains: "app service with migration support"
  key_links:
    - from: "scripts/deploy.sh"
      to: "prisma migrate deploy"
      via: "pre_deploy_checks 단계에서 실행"
      pattern: "npx prisma migrate deploy"
---

<objective>
데이터베이스 마이그레이션을 배포 과정에 자동화하여 수동 개입 없이 안전한 스키마 변경 적용

Purpose: 배포 시마다 수동으로 마이그레이션을 실행하는 실수를 방지하고, 마이그레이션 실패 시 즉시 롤백하여 데이터베이스 무결성 보장

Output: `prisma migrate deploy`가 통합된 배포 스크립트와 Docker Compose 구성
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-performance-&-database-optimization/09-CONTEXT.md
@.planning/phases/09-performance-&-database-optimization/09-RESEARCH.md

@scripts/deploy.sh
@docker-compose.prod.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker Compose에 Prisma 마이그레이션 서비스 추가</name>
  <files>docker-compose.prod.yml</files>
  <action>
    Docker Compose 프로덕션 파일에 `migrate` 서비스를 추가하여 애플리케이션 시작 전에 마이그레이션 실행:

    1. `migrate` 서비스 정의 추가:
       - `app` 서비스와 동일한 이미지 사용
       - `depends_on: postgres` 설정 (데이터베이스 대기)
       - `command: npx prisma migrate deploy`
       - `restart: "no"` (한 번 실행 후 종료)
       - 환경변수: DATABASE_URL, DATABASE_URL_PROXY 등

    2. `app` 서비스에 `depends_on: migrate` 추가:
       - 마이그레이션 완료 후 애플리케이션 시작

    3. 네트워크 구성:
       - `migrate` 서비스는 `app`과 동일한 네트워크 사용

    **주의사항:**
    - 기존 `app` 서비스 구성(이미지, 환경변수, 볼륨 등)을 유지
    - `migrate` 서비스는 컨테이너 내부에서 Prisma 명령 실행
    - `command: sh -c "npx prisma migrate deploy && echo 'Migration complete'"`
  </action>
  <verify>docker compose -f docker-compose.prod.yml config | grep -A 10 "migrate:"</verify>
  <done>docker-compose.prod.yml에 migrate 서비스가 정의되고 app 서비스가 migrate에 의존</done>
</task>

<task type="auto">
  <name>Task 2: 배포 스크립트에 마이그레이션 전후 백업 추가</name>
  <files>scripts/deploy.sh</files>
  <action>
    배포 스크립트의 `pre_deploy_checks` 함수 다음에 `backup_database` 함수 추가:

    1. `backup_database()` 함수 정의:
       ```bash
       backup_database() {
           log_info "Creating database backup..."
           local backup_file="backups/db-backup-$(date +%Y%m%d-%H%M%S).sql"

           mkdir -p backups

           # Docker exec를 사용하여 pg_dump 실행
           if docker compose -f "$COMPOSE_FILE" exec -T postgres pg_dump -U postgres \
               --clean --if-exists --no-owner --no-acl > "$backup_file" 2>/dev/null; then
               log_success "Database backup created: $backup_file"
           else
               log_warning "Database backup failed (continuing anyway)"
           fi
       }
       </when>
       ```

    2. `main()` 함수에서 `pre_deploy_checks` 다음에 호출:
       ```bash
       pre_deploy_checks
       backup_database  # 이곳에 추가
       backup_current_deployment
       ```

    3. 실패 시 롤백 경로에 백업 파일 복구 추가 (선택사항)

    **주의사항:**
    - `backups/` 디렉토리가 .gitignore에 이미 포함되어 있는지 확인
    - 백업 실패가 배포를 중단하지 않도록 처리 (경고만 로그)
    - CONTEXT.md 결정: "마이그레이션 전후에 데이터베이스 전체 백업을 자동으로 생성"
  </action>
  <verify>grep -n "backup_database" scripts/deploy.sh</verify>
  <done>deploy.sh에 backup_database 함수가 정의되고 main 함수에서 호출됨</done>
</task>

<task type="auto">
  <name>Task 3: 마이그레이션 실행 로깅 강화</name>
  <files>scripts/deploy.sh</files>
  <action>
    Docker Compose의 `migrate` 서비스 실행 로그를 배포 로그에 명확히 표시:

    1. `deploy_new_version()` 함수에서 컨테이너 시작 후 마이그레이션 대기:
       ```bash
       # Start new containers
       log_info "Starting new containers..."
       if docker compose -f "$COMPOSE_FILE" up -d; then
           log_success "Containers started"
       else
           log_error "Failed to start containers"
           return 1
       fi

       # Wait for migration to complete
       log_info "Waiting for database migrations to complete..."
       if docker compose -f "$COMPOSE_FILE" up migrate --exit-code-from migrate; then
           log_success "Database migrations applied successfully"
       else
           log_error "Migration failed - check logs with: docker compose -f $COMPOSE_FILE logs migrate"
           return 1
       fi
       ```

    2. 마이그레이션 서비스 로그 확인 명령어를 에러 메시지에 포함

    **주의사항:**
    - `migrate` 서비스는 `restart: "no"` 설정으로 한 번 실행 후 종료되므로 `--exit-code-from`로 완료 대기
    - 마이그레이션 실패 시 `return 1`로 배포 중단 (자동 롤백 트리거)
    - CONTEXT.md 결정: "마이그레이션 실행 결과를 모두 알림 (Slack, 이메일 등 + 배포 로그 기록)"
  </action>
  <verify>grep -n "Waiting for database migrations" scripts/deploy.sh</verify>
  <done>deploy.sh에 마이그레이션 완료 대기 로직이 추가되고 실패 시 배포 중단</done>
</task>

</tasks>

<verification>
1. Docker Compose 구성 확인: `docker compose -f docker-compose.prod.yml config` 출력에 migrate 서비스 포함
2. 배포 스크립트 문법 확인: `bash -n scripts/deploy.sh`
3. 마이그레이션 실패 시 롤백 확인: deploy.sh의 마이그레이션 실패 처리 경로 확인
</verification>

<success_criteria>
1. `docker compose -f docker-compose.prod.yml up -d` 실행 시 migrate 서비스가 먼저 실행되고 완료 후 app 서비스 시작
2. 마이그레이션 실패 시 컨테이너가 시작되지 않고 배포 스크립트가 종료 코드 1로 반환
3. 배포 시 backups/ 디렉토리에 타임스탬프가 포함된 백업 파일 생성
4. 배포 로그에 "Database migrations applied successfully" 또는 실패 메시지가 명확히 표시
</success_criteria>

<output>
After completion, create `.planning/phases/09-performance-&-database-optimization/09-01-SUMMARY.md`
</output>
