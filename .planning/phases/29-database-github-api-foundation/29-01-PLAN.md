---
phase: 29-database-github-api-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
autonomous: true

must_haves:
  truths:
    - "Issue 모델과 IssueEvent 모델이 Prisma 스키마에 존재한다"
    - "IssueCategory, IssueStatus, IssuePriority enum이 정의되어 있다"
    - "Teacher 모델에 Issue 관련 relation이 추가되어 있다"
    - "마이그레이션이 성공적으로 적용되어 DB에 테이블이 생성된다"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Issue, IssueEvent 모델 및 enum 정의"
      contains: "model Issue"
    - path: "prisma/schema.prisma"
      provides: "IssueEvent 모델"
      contains: "model IssueEvent"
  key_links:
    - from: "prisma/schema.prisma (Issue)"
      to: "prisma/schema.prisma (Teacher)"
      via: "creator/assignee relations"
      pattern: '@relation\("Issue(Creator|Assignee)"'
    - from: "prisma/schema.prisma (IssueEvent)"
      to: "prisma/schema.prisma (Issue)"
      via: "Cascade delete"
      pattern: "onDelete: Cascade"
---

<objective>
Issue 및 IssueEvent Prisma 모델과 관련 enum을 schema.prisma에 추가하고 마이그레이션을 생성/적용한다.

Purpose: Phase 29의 모든 서버 기능(Server Action, GitHub 서비스)이 Issue/IssueEvent 테이블에 의존하므로, DB 스키마가 최우선으로 완성되어야 한다.
Output: Issue, IssueEvent 테이블이 PostgreSQL에 생성되고 Prisma Client가 업데이트된다.
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@prisma/schema.prisma
@.planning/phases/29-database-github-api-foundation/29-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Issue, IssueEvent 모델 및 enum 추가</name>
  <files>prisma/schema.prisma</files>
  <action>
prisma/schema.prisma 파일 끝(기존 enum 블록 바로 위)에 아래 모델과 enum을 추가한다.

**Enum 추가 (기존 enum 블록 근처):**

```prisma
enum IssueCategory {
  BUG
  FEATURE
  IMPROVEMENT
  UI_UX
  DOCUMENTATION
  PERFORMANCE
  SECURITY
}

enum IssueStatus {
  OPEN
  IN_PROGRESS
  IN_REVIEW
  CLOSED
  ARCHIVED
}

enum IssuePriority {
  LOW
  MEDIUM
  HIGH
  URGENT
}
```

**Issue 모델 추가 (SystemLog 모델 아래):**

```prisma
model Issue {
  id                String        @id @default(cuid())
  title             String
  description       String?       @db.Text
  category          IssueCategory
  priority          IssuePriority @default(MEDIUM)
  status            IssueStatus   @default(OPEN)
  githubIssueNumber Int?          @unique
  githubIssueUrl    String?
  githubBranchName  String?
  screenshotUrl     String?
  userContext       Json?
  createdBy         String
  assignedTo        String?
  closedAt          DateTime?
  createdAt         DateTime      @default(now())
  updatedAt         DateTime      @updatedAt

  creator           Teacher       @relation("IssueCreator", fields: [createdBy], references: [id])
  assignee          Teacher?      @relation("IssueAssignee", fields: [assignedTo], references: [id])
  events            IssueEvent[]

  @@index([status])
  @@index([category])
  @@index([createdBy])
  @@index([githubIssueNumber])
  @@index([createdAt(sort: Desc)])
  @@map("issues")
}

model IssueEvent {
  id          String   @id @default(cuid())
  issueId     String
  eventType   String   // "created", "labeled", "branch_created", "status_changed", "closed", "synced"
  performedBy String
  metadata    Json?
  createdAt   DateTime @default(now())

  issue       Issue    @relation(fields: [issueId], references: [id], onDelete: Cascade)
  performer   Teacher  @relation("IssueEventPerformer", fields: [performedBy], references: [id])

  @@index([issueId])
  @@index([eventType])
  @@index([createdAt(sort: Desc)])
  @@map("issue_events")
}
```

**Teacher 모델에 relation 추가:**
Teacher 모델의 relation 목록(auditLogs 아래)에 다음 3개 relation을 추가:

```prisma
  issuesCreated      Issue[]      @relation("IssueCreator")
  issuesAssigned     Issue[]      @relation("IssueAssignee")
  issueEvents        IssueEvent[] @relation("IssueEventPerformer")
```

**주의사항:**
- `@db.Text` annotation을 description에 사용하여 긴 텍스트 허용
- `screenshotUrl`과 `userContext`는 Phase 30(UI)에서 사용할 필드이지만, 마이그레이션 횟수를 줄이기 위해 지금 추가
- IssueEvent.eventType은 String으로 유연하게 유지 (webhook 이벤트 타입 추가 가능)
- Teacher 모델의 relation 이름은 기존 패턴과 일치하도록 named relation 사용
  </action>
  <verify>
`npx prisma validate` 실행하여 스키마 유효성 검증.
에러 없이 통과해야 한다.
  </verify>
  <done>
Issue 모델(15+ 필드), IssueEvent 모델(7 필드), 3개 enum, Teacher relation 3개가 schema.prisma에 추가되고 validate 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: Prisma 마이그레이션 생성 및 적용</name>
  <files>prisma/migrations/</files>
  <action>
Prisma 마이그레이션을 생성하고 적용한다.

```bash
cd /home/gon/projects/ai/ai-afterschool
npx prisma migrate dev --name add_issue_and_issue_event_models
```

마이그레이션 성공 후 Prisma Client를 재생성한다:

```bash
npx prisma generate
```

**검증:**
마이그레이션 SQL 파일이 생성되었는지 확인:
- `prisma/migrations/YYYYMMDDHHMMSS_add_issue_and_issue_event_models/migration.sql`

SQL 파일에 다음이 포함되어야 함:
- `CREATE TABLE "issues"` 문
- `CREATE TABLE "issue_events"` 문
- `CREATE TYPE "IssueCategory"` enum 문
- `CREATE TYPE "IssueStatus"` enum 문
- `CREATE TYPE "IssuePriority"` enum 문
- 인덱스 생성문 (6개 이상)
- Foreign key 제약조건

**주의:** DATABASE_URL이 로컬 개발 DB를 가리키는지 확인. 운영 DB(192.168.0.5:5436)에 직접 실행하지 않는다.
  </action>
  <verify>
1. `npx prisma migrate status` 실행하여 마이그레이션 상태가 "Database schema is up to date" 인지 확인
2. `npx prisma db pull --print | grep -c "issues\|issue_events"` 로 테이블 존재 확인
  </verify>
  <done>
마이그레이션 파일이 생성되고, 로컬 DB에 issues/issue_events 테이블이 생성되며, Prisma Client에 Issue/IssueEvent 타입이 포함됨
  </done>
</task>

</tasks>

<verification>
1. `npx prisma validate` — 스키마 검증 통과
2. `npx prisma migrate status` — 모든 마이그레이션 적용 완료
3. `npx prisma studio` (선택) — Issue, IssueEvent 테이블 확인 가능
4. TypeScript에서 `import { Issue, IssueEvent, IssueCategory } from '@prisma/client'` 가능
</verification>

<success_criteria>
- Issue 모델이 title, description, category, priority, status, githubIssueNumber, githubIssueUrl, githubBranchName 필드를 포함
- IssueEvent 모델이 issueId, eventType, performedBy, metadata 필드를 포함
- IssueCategory enum: BUG, FEATURE, IMPROVEMENT, UI_UX, DOCUMENTATION, PERFORMANCE, SECURITY
- IssueStatus enum: OPEN, IN_PROGRESS, IN_REVIEW, CLOSED, ARCHIVED
- IssuePriority enum: LOW, MEDIUM, HIGH, URGENT
- Teacher → Issue 양방향 relation (creator, assignee)
- Issue → IssueEvent 1:N relation (Cascade delete)
- 마이그레이션 파일이 존재하고 DB에 적용됨
</success_criteria>

<output>
After completion, create `.planning/phases/29-database-github-api-foundation/29-01-SUMMARY.md`
</output>
