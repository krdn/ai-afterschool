---
phase: 29-database-github-api-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/github/client.ts
  - src/lib/github/utils.ts
  - src/lib/github/constants.ts
  - .env.example
  - package.json
autonomous: true
user_setup:
  - service: github
    why: "GitHub API 연동을 위한 Personal Access Token 필요"
    env_vars:
      - name: GITHUB_TOKEN
        source: "https://github.com/settings/tokens → Generate new token (classic) → repo 스코프 선택"
      - name: GITHUB_OWNER
        source: "GitHub 저장소 소유자 (예: krdn)"
      - name: GITHUB_REPO
        source: "GitHub 저장소 이름 (예: ai-afterschool)"
    dashboard_config: []

must_haves:
  truths:
    - "Octokit 클라이언트가 GITHUB_TOKEN 환경 변수로 인증된다"
    - "GitHub 토큰이 서버 전용이며 클라이언트에 노출되지 않는다"
    - "slug 생성 함수가 한글 제목을 URL-safe 문자열로 변환한다"
    - "브랜치명 생성 함수가 카테고리에 따라 fix/feat/chore 접두사를 사용한다"
    - "rate limit 체크 함수가 남은 요청 수를 확인하고 임계값 이하이면 경고를 기록한다"
    - "카테고리별 GitHub 라벨 매핑이 정의되어 있다"
  artifacts:
    - path: "src/lib/github/client.ts"
      provides: "Octokit 싱글톤 인스턴스"
      exports: ["getOctokit", "isGitHubConfigured"]
    - path: "src/lib/github/utils.ts"
      provides: "slug, 브랜치명, rate limit 유틸리티"
      exports: ["generateSlug", "generateBranchName", "checkRateLimit"]
    - path: "src/lib/github/constants.ts"
      provides: "카테고리-라벨 매핑, 라벨 색상"
      exports: ["CATEGORY_LABEL_MAP", "LABEL_COLORS", "RATE_LIMIT_THRESHOLD"]
    - path: ".env.example"
      provides: "GitHub 환경 변수 템플릿"
      contains: "GITHUB_TOKEN"
  key_links:
    - from: "src/lib/github/client.ts"
      to: "process.env.GITHUB_TOKEN"
      via: "환경 변수 읽기"
      pattern: "process\\.env\\.GITHUB_TOKEN"
    - from: "src/lib/github/utils.ts"
      to: "src/lib/github/constants.ts"
      via: "import"
      pattern: 'from.*constants'
---

<objective>
GitHub API 클라이언트(Octokit) 설정, 유틸리티 함수(slug/브랜치명/rate limit), 라벨 매핑 상수를 구현한다.

Purpose: Plan 03의 GitHub 서비스와 Issue Server Action이 이 기반 모듈에 의존한다. GitHub API 호출의 단일 진입점과 공통 유틸리티를 제공한다.
Output: src/lib/github/ 디렉토리에 client, utils, constants 모듈 생성
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-database-github-api-foundation/29-RESEARCH.md
@src/lib/dal.ts
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Octokit 설치 및 GitHub 클라이언트 모듈 생성</name>
  <files>package.json, src/lib/github/client.ts, .env.example</files>
  <action>
**1. octokit 패키지 설치:**
```bash
cd /home/gon/projects/ai/ai-afterschool
npm install octokit
```

**2. `src/lib/github/client.ts` 생성:**

서버 전용 GitHub API 클라이언트 싱글톤을 생성한다.

```typescript
import 'server-only'
import { Octokit } from 'octokit'

// 싱글톤 패턴: 모듈 레벨 캐싱
let octokitInstance: Octokit | null = null

/**
 * GitHub API가 설정되어 있는지 확인
 * GITHUB_TOKEN, GITHUB_OWNER, GITHUB_REPO 모두 필요
 */
export function isGitHubConfigured(): boolean {
  return !!(
    process.env.GITHUB_TOKEN &&
    process.env.GITHUB_OWNER &&
    process.env.GITHUB_REPO
  )
}

/**
 * Octokit 인스턴스를 반환 (싱글톤)
 * GITHUB_TOKEN이 설정되지 않으면 에러를 throw
 */
export function getOctokit(): Octokit {
  if (!process.env.GITHUB_TOKEN) {
    throw new Error('GITHUB_TOKEN 환경 변수가 설정되지 않았습니다')
  }

  if (!octokitInstance) {
    octokitInstance = new Octokit({
      auth: process.env.GITHUB_TOKEN,
    })
  }

  return octokitInstance
}

/**
 * GitHub 저장소 owner/repo 정보 반환
 */
export function getRepoConfig() {
  const owner = process.env.GITHUB_OWNER
  const repo = process.env.GITHUB_REPO

  if (!owner || !repo) {
    throw new Error('GITHUB_OWNER, GITHUB_REPO 환경 변수가 설정되지 않았습니다')
  }

  return { owner, repo }
}
```

**핵심:**
- `import 'server-only'`로 클라이언트 번들에 포함 방지 (INFRA-04)
- GITHUB_TOKEN은 `NEXT_PUBLIC_` 접두사 없이 서버 전용으로 유지
- 싱글톤 패턴으로 불필요한 인스턴스 생성 방지

**3. `.env.example`에 GitHub 환경 변수 추가:**

파일 끝에 다음 섹션 추가:

```env
# =============================================================================
# GitHub Integration (Issue Management)
# =============================================================================
# GitHub Personal Access Token (PAT) with 'repo' scope
# Generate at: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_xxxxxxxxxxxxx

# Repository information
GITHUB_OWNER=krdn
GITHUB_REPO=ai-afterschool

# Rate limit warning threshold (remaining requests before warning)
GITHUB_RATE_LIMIT_THRESHOLD=100
```

**주의:** `.env.local`에는 실제 토큰을 넣어야 하지만, 이 태스크에서는 `.env.example` 템플릿만 수정. `.env.local`은 user_setup에서 사용자가 직접 설정.
  </action>
  <verify>
1. `npm ls octokit` — octokit 패키지가 설치되어 있는지 확인
2. `cat src/lib/github/client.ts` — 파일이 존재하고 `import 'server-only'`가 첫 줄에 있는지 확인
3. `grep "GITHUB_TOKEN" .env.example` — 환경 변수 템플릿에 포함되어 있는지 확인
4. `grep "NEXT_PUBLIC_GITHUB_TOKEN" src/lib/github/client.ts` — 이 패턴이 없어야 함 (클라이언트 노출 금지)
  </verify>
  <done>
octokit이 package.json에 추가되고, src/lib/github/client.ts가 server-only 싱글톤으로 생성되며, .env.example에 GitHub 환경 변수 섹션이 추가됨
  </done>
</task>

<task type="auto">
  <name>Task 2: 유틸리티 함수 및 상수 모듈 생성</name>
  <files>src/lib/github/utils.ts, src/lib/github/constants.ts, src/lib/github/index.ts</files>
  <action>
**1. `src/lib/github/constants.ts` 생성:**

카테고리-라벨 매핑, 라벨 색상, rate limit 임계값 등 공통 상수를 정의한다.

```typescript
import type { IssueCategory } from '@prisma/client'

/** IssueCategory → GitHub label 이름 매핑 */
export const CATEGORY_LABEL_MAP: Record<IssueCategory, string> = {
  BUG: 'bug',
  FEATURE: 'feature',
  IMPROVEMENT: 'improvement',
  UI_UX: 'ui-ux',
  DOCUMENTATION: 'documentation',
  PERFORMANCE: 'performance',
  SECURITY: 'security',
}

/** GitHub label 색상 (# 없이 6자리 hex) */
export const LABEL_COLORS: Record<string, string> = {
  bug: 'd73a4a',
  feature: '0075ca',
  improvement: '2ea44f',
  'ui-ux': 'a371f7',
  documentation: '0969da',
  performance: 'e4e669',
  security: 'b60205',
  // Phase 31에서 추가될 자동 생성 라벨
  sentry: 'f9826c',
  'auto-created': 'bfdadc',
}

/** GitHub label 설명 */
export const LABEL_DESCRIPTIONS: Record<string, string> = {
  bug: '버그 리포트',
  feature: '새 기능 요청',
  improvement: '기존 기능 개선',
  'ui-ux': 'UI/UX 개선',
  documentation: '문서 수정',
  performance: '성능 개선',
  security: '보안 이슈',
  sentry: 'Sentry에서 자동 수집된 에러',
  'auto-created': '자동 생성된 이슈',
}

/** IssueCategory → 브랜치 접두사 매핑 */
export const CATEGORY_BRANCH_PREFIX: Record<IssueCategory, string> = {
  BUG: 'fix',
  FEATURE: 'feat',
  IMPROVEMENT: 'chore',
  UI_UX: 'chore',
  DOCUMENTATION: 'docs',
  PERFORMANCE: 'perf',
  SECURITY: 'fix',
}

/** Rate limit 경고 임계값 (환경변수에서 읽거나 기본값 100) */
export const RATE_LIMIT_THRESHOLD = parseInt(
  process.env.GITHUB_RATE_LIMIT_THRESHOLD || '100',
  10
)
```

**2. `src/lib/github/utils.ts` 생성:**

slug 생성, 브랜치명 생성, rate limit 체크 유틸리티를 구현한다.

```typescript
import 'server-only'
import { CATEGORY_BRANCH_PREFIX, RATE_LIMIT_THRESHOLD } from './constants'
import { getOctokit, getRepoConfig } from './client'
import { logSystemAction } from '@/lib/dal'
import type { IssueCategory } from '@prisma/client'

/**
 * 제목을 URL-safe slug로 변환
 * 한글을 유지하고 특수문자를 제거한다.
 *
 * @example generateSlug("학생 등록 시 이름 오류") → "학생-등록-시-이름-오류"
 * @example generateSlug("Fix: login bug!!!") → "fix-login-bug"
 */
export function generateSlug(text: string): string {
  return text
    .toLowerCase()
    .replace(/[^a-z0-9가-힣\s-]/g, '')  // 영문, 숫자, 한글, 공백, 하이픈만 유지
    .trim()
    .replace(/\s+/g, '-')                // 공백 → 하이픈
    .replace(/-+/g, '-')                 // 중복 하이픈 제거
    .replace(/^-+|-+$/g, '')             // 앞뒤 하이픈 제거
    .substring(0, 50)                    // 최대 50자
}

/**
 * 이슈 번호와 제목으로 Git 브랜치명 생성
 *
 * @example generateBranchName(42, "학생 등록 오류", "BUG") → "fix/issue-42-학생-등록-오류"
 * @example generateBranchName(15, "새 대시보드", "FEATURE") → "feat/issue-15-새-대시보드"
 */
export function generateBranchName(
  issueNumber: number,
  title: string,
  category: IssueCategory
): string {
  const prefix = CATEGORY_BRANCH_PREFIX[category]
  const slug = generateSlug(title)
  return `${prefix}/issue-${issueNumber}-${slug}`
}

/**
 * GitHub API rate limit을 확인하고 임계값 이하이면 SystemLog에 경고 기록
 *
 * @returns 현재 rate limit 정보 { limit, remaining, reset }
 */
export async function checkRateLimit(): Promise<{
  limit: number
  remaining: number
  reset: Date
  isLow: boolean
}> {
  const octokit = getOctokit()
  const { data } = await octokit.rest.rateLimit.get()

  const { limit, remaining, reset } = data.resources.core
  const resetDate = new Date(reset * 1000)
  const isLow = remaining < RATE_LIMIT_THRESHOLD

  if (isLow) {
    await logSystemAction({
      level: 'WARN',
      message: `GitHub API rate limit 경고: ${remaining}/${limit} (임계값: ${RATE_LIMIT_THRESHOLD})`,
      context: {
        remaining,
        limit,
        threshold: RATE_LIMIT_THRESHOLD,
        resetAt: resetDate.toISOString(),
      },
    })
  }

  return { limit, remaining, reset: resetDate, isLow }
}

/**
 * API 응답 헤더에서 rate limit 정보를 추출하고 경고 기록
 * GitHub API 호출 후 응답 객체에서 사용
 */
export async function checkRateLimitFromHeaders(headers: Record<string, string | undefined>): Promise<void> {
  const remaining = parseInt(headers['x-ratelimit-remaining'] || '5000', 10)
  const limit = parseInt(headers['x-ratelimit-limit'] || '5000', 10)

  if (remaining < RATE_LIMIT_THRESHOLD) {
    const reset = parseInt(headers['x-ratelimit-reset'] || '0', 10)
    await logSystemAction({
      level: 'WARN',
      message: `GitHub API rate limit 경고: ${remaining}/${limit}`,
      context: {
        remaining,
        limit,
        threshold: RATE_LIMIT_THRESHOLD,
        resetAt: new Date(reset * 1000).toISOString(),
      },
    })
  }
}
```

**3. `src/lib/github/index.ts` 생성 (barrel export):**

```typescript
// GitHub Integration Module
// Phase 29: Database & GitHub API Foundation

export { getOctokit, isGitHubConfigured, getRepoConfig } from './client'
export { generateSlug, generateBranchName, checkRateLimit, checkRateLimitFromHeaders } from './utils'
export { CATEGORY_LABEL_MAP, LABEL_COLORS, LABEL_DESCRIPTIONS, CATEGORY_BRANCH_PREFIX, RATE_LIMIT_THRESHOLD } from './constants'
```

**주의사항:**
- `utils.ts`에도 `import 'server-only'` 추가 (Octokit import 때문)
- `generateSlug`은 순수 함수이지만, 같은 파일의 다른 함수가 서버 전용이므로 통일
- `checkRateLimitFromHeaders`는 GitHub API 호출마다 응답에서 rate limit을 확인하는 경량 함수
  </action>
  <verify>
1. TypeScript 컴파일 검증: `npx tsc --noEmit src/lib/github/index.ts` (또는 `npx tsc --noEmit` 전체)
2. `ls src/lib/github/` — client.ts, utils.ts, constants.ts, index.ts 4개 파일 존재
3. constants.ts에 7개 카테고리 매핑이 모두 있는지 확인
  </verify>
  <done>
src/lib/github/ 디렉토리에 client.ts(Octokit 싱글톤), utils.ts(slug/브랜치명/rate limit), constants.ts(매핑 상수), index.ts(barrel export) 4개 파일이 생성되고, TypeScript 타입 검증 통과
  </done>
</task>

</tasks>

<verification>
1. `npm ls octokit` — 패키지 설치 확인
2. `ls src/lib/github/` — 4개 파일 존재 (client.ts, utils.ts, constants.ts, index.ts)
3. `grep -r "server-only" src/lib/github/` — client.ts, utils.ts에 server-only import 존재
4. `grep "GITHUB_TOKEN" .env.example` — 환경 변수 템플릿 존재
5. `npx tsc --noEmit` — TypeScript 컴파일 에러 없음
</verification>

<success_criteria>
- octokit 패키지가 package.json dependencies에 추가됨
- src/lib/github/client.ts: getOctokit(), isGitHubConfigured(), getRepoConfig() 함수 export
- src/lib/github/constants.ts: 7개 IssueCategory에 대한 라벨/브랜치 접두사 매핑
- src/lib/github/utils.ts: generateSlug(), generateBranchName(), checkRateLimit(), checkRateLimitFromHeaders() 함수
- .env.example에 GITHUB_TOKEN, GITHUB_OWNER, GITHUB_REPO, GITHUB_RATE_LIMIT_THRESHOLD 추가
- 모든 GitHub 모듈에 'server-only' import → 클라이언트 번들에 포함되지 않음
</success_criteria>

<output>
After completion, create `.planning/phases/29-database-github-api-foundation/29-02-SUMMARY.md`
</output>
