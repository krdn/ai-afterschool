---
phase: 12-teacher-analysis
plan: 03
type: execute
wave: 1
depends_on: ["12-02"]
files_modified:
  - src/lib/actions/teacher-analysis.ts
autonomous: true

must_haves:
  truths:
    - "선생님 분석 실행을 위한 Server Action 함수가 존재한다"
    - "runTeacherSajuAnalysis는 Teacher.birthDate와 birthTime으로 사주 계산"
    - "runTeacherNameAnalysis는 Teacher.name과 nameHanja로 성명학 계산"
    - "runTeacherMbtiAnalysis는 MBTI 설문 응답으로 점수 계산"
    - "기존 분석 라이브러리(src/lib/analysis/*)를 그대로 재사용"
  artifacts:
    - path: "src/lib/actions/teacher-analysis.ts"
      provides: "Server actions for running teacher analysis"
      exports: ["runTeacherSajuAnalysis", "runTeacherNameAnalysis", "runTeacherMbtiAnalysis"]
  key_links:
    - from: "src/lib/actions/teacher-analysis.ts"
      to: "src/lib/analysis/saju.ts"
      via: "Pure function call"
      pattern: "calculateSaju\\("
    - from: "src/lib/actions/teacher-analysis.ts"
      to: "src/lib/analysis/name-numerology.ts"
      via: "Pure function call"
      pattern: "calculateNameNumerology\\("
    - from: "src/lib/actions/teacher-analysis.ts"
      to: "src/lib/analysis/mbti-scoring.ts"
      via: "Pure function call"
      pattern: "scoreMbti\\("
    - from: "src/lib/actions/teacher-analysis.ts"
      to: "src/lib/db/teacher-*-analysis.ts"
      via: "DB persistence"
      pattern: "upsertTeacher.*Analysis\\("
---

<objective>
선생님 성향 분석 실행을 위한 Server Action을 생성합니다.

Purpose: 기존 Student 분석 Server Actions(src/lib/actions/student-analysis.ts, mbti-survey.ts) 패턴 재사용 + 순수 함수 라이브러리 호출로 Teacher 분석 실행
Output: 사주/이름/MBTI 분석 실행 및 DB 저장 Server Actions
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-teacher-analysis-&-team-data-access/12-RESEARCH.md

# Reference patterns from Student actions
@src/lib/actions/student-analysis.ts (runSajuAnalysis, runNameAnalysis 패턴)
@src/lib/actions/mbti-survey.ts (saveMbtiDirectInput, calculateMbtiAnalysis 패턴)

# Reuse pure analysis libraries
@src/lib/analysis/saju.ts (calculateSaju function)
@src/lib/analysis/name-numerology.ts (calculateNameNumerology function)
@src/lib/analysis/mbti-scoring.ts (scoreMbti function)
</context>

<tasks>

<task type="auto">
  <name>Teacher 분석 Server Action 모듈 생성</name>
  <files>src/lib/actions/teacher-analysis.ts</files>
  <action>
src/lib/actions/teacher-analysis.ts 파일을 새로 생성합니다:

**파일 구조**:

1. **임포트**:
   ```typescript
   "use server"

   import { revalidatePath } from "next/cache"
   import { db } from "@/lib/db"
   import { verifySession } from "@/lib/dal"
   import { calculateSaju } from "@/lib/analysis/saju"
   import { calculateNameNumerology } from "@/lib/analysis/name-numerology"
   import { scoreMbti } from "@/lib/analysis/mbti-scoring"
   import { generateSajuInterpretation } from "@/lib/analysis/saju"
   import { generateNameInterpretation } from "@/lib/analysis/name-numerology"
   import { upsertTeacherSajuAnalysis } from "@/lib/db/teacher-saju-analysis"
   import { upsertTeacherNameAnalysis } from "@/lib/db/teacher-name-analysis"
   import { upsertTeacherMbtiAnalysis } from "@/lib/db/teacher-mbti-analysis"
   ```

2. **runTeacherSajuAnalysis** 함수:
   ```typescript
   export async function runTeacherSajuAnalysis(teacherId: string) {
     const session = await verifySession()
     if (!session) throw new Error("Unauthorized")

     // Teacher 조회
     const teacher = await db.teacher.findUnique({
       where: { id: teacherId },
     })

     if (!teacher || !teacher.birthDate) {
       throw new Error("Teacher not found or birth date missing")
     }

     // 사주 계산 (기존 라이브러리 재사용)
     const sajuInput = {
       birthDate: teacher.birthDate,
       time: teacher.birthTimeHour ? {
         hour: teacher.birthTimeHour,
         minute: teacher.birthTimeMinute ?? 0
       } : null
     }

     const sajuResult = calculateSaju(sajuInput)
     const interpretation = generateSajuInterpretation(sajuResult)

     // DB 저장
     await upsertTeacherSajuAnalysis(teacherId, {
       inputSnapshot: sajuInput as any,
       result: sajuResult as any,
       interpretation,
       status: "complete",
       version: 1,
       calculatedAt: new Date()
     })

     revalidatePath(`/teachers/${teacherId}`)

     return { success: true }
   }
   ```

3. **runTeacherNameAnalysis** 함수:
   ```typescript
   export async function runTeacherNameAnalysis(teacherId: string) {
     const session = await verifySession()
     if (!session) throw new Error("Unauthorized")

     const teacher = await db.teacher.findUnique({
       where: { id: teacherId },
     })

     if (!teacher) {
       throw new Error("Teacher not found")
     }

     // 성명학 계산 (기존 라이브러리 재사용)
     const nameInput = {
       name: teacher.name,
       hanjaName: (teacher as any).nameHanja // TODO: Teacher 모델에 nameHanja 필드 필요 (Phase 12-05)
     }

     const nameResult = calculateNameNumerology(nameInput)

     if (nameResult.status !== "ok") {
       throw new Error(nameResult.message)
     }

     const interpretation = generateNameInterpretation(nameResult.result)

     // DB 저장
     await upsertTeacherNameAnalysis(teacherId, {
       inputSnapshot: nameInput as any,
       result: nameResult.result as any,
       interpretation,
       status: "complete",
       version: 1,
       calculatedAt: new Date()
     })

     revalidatePath(`/teachers/${teacherId}`)

     return { success: true }
   }
   ```

4. **runTeacherMbtiAnalysis** 함수:
   ```typescript
   export async function runTeacherMbtiAnalysis(
     teacherId: string,
     responses: Record<string, number>
   ) {
     const session = await verifySession()
     if (!session) throw new Error("Unauthorized")

     // MBTI 설문 문항 로드 (기존 파일 재사용)
     const questions = [...] // TODO: mbti-questions.ts 또는 하드코딩된 60문항

     // MBTI 점수 계산 (기존 라이브러리 재사용)
     const mbtiResult = scoreMbti(responses, questions)

     // DB 저장
     await upsertTeacherMbtiAnalysis(teacherId, {
       responses: responses as any,
       scores: mbtiResult.scores as any,
       mbtiType: mbtiResult.mbtiType,
       percentages: mbtiResult.percentages as any,
       interpretation: null, // TODO: MBTI 해석 생성 로직 필요 시
       version: 1,
       calculatedAt: new Date()
     })

     revalidatePath(`/teachers/${teacherId}`)

     return { success: true }
   }
   ```

중요:
- calculateSaju, calculateNameNumerology, scoreMbti는 순수 함수로 Student/Teacher 구분 없음
- Teacher 모델에 birthDate, birthTimeHour, birthTimeMinute 필드가 있어야 함 (Phase 11에서 생성됨)
- nameHanja 필드는 Teacher 모델에 없으므로 이번 plan에서는 에러 처리 또는 TODO 주석
  </action>
  <verify>grep -E "export.*function.*runTeacher.*Analysis" src/lib/actions/teacher-analysis.ts</verify>
  <done>runTeacherSajuAnalysis, runTeacherNameAnalysis, runTeacherMbtiAnalysis 3개 함수 구현 완료</done>
</task>

</tasks>

<verification>
1. **"use server" 지시자**: 파일 최상단에 "use server" 존재
2. **함수 내보내기**: 3개의 runTeacher*Analysis 함수 export 확인
3. **분석 라이브러리 호출**: calculateSaju, calculateNameNumerology, scoreMbti 호출 확인
4. **DB 저장**: upsertTeacher*Analysis 함수 호출 확인
5. **세션 검증**: verifySession() 호출 확인
6. **revalidatePath**: 분석 후 페이지 재검증 호출 확인
</verification>

<success_criteria>
1. teacher-analysis.ts 파일 생성 완료
2. 3개 Server Action 함수 구현 완료 (saju, name, mbti)
3. 기존 분석 라이브러리 순수 함수 재사용 확인
4. DB 저장 및 revalidatePath 로직 포함
5. TypeScript 컴파일 에러 없음 (TODO 주석으로 누락된 nameHanja 처리)
</success_criteria>

<output>
After completion, create `.planning/phases/12-teacher-analysis-&-team-data-access/12-03-SUMMARY.md`
</output>
