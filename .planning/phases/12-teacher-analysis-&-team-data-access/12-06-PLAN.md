---
phase: 12-teacher-analysis
plan: 06
type: execute
wave: 3
depends_on: ["12-01", "12-05"]
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/{timestamp}_add_teacher_analysis_fields/migration.sql
autonomous: true

must_haves:
  truths:
    - "Teacher 모델에 성향 분석에 필요한 추가 필드가 존재한다"
    - "birthDate, birthTimeHour, birthTimeMinute 필드로 사주 분석 가능"
    - "nameHanja 필드로 성명학 분석 가능"
    - "마이그레이션으로 기존 Teacher 데이터에 영향 없이 필드 추가"
    - "Prisma include로 Team과 관계된 Teacher 조회 시 N+1 쿼리 문제 없음"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Teacher model with analysis input fields"
      contains: "birthDate|birthTimeHour|nameHanja"
    - path: "prisma/migrations/*/migration.sql"
      provides: "ALTER TABLE \"Teacher\" ADD COLUMN statements"
      contains: "ALTER TABLE.*Teacher.*ADD.*birthDate"
  key_links:
    - from: "prisma/schema.prisma"
      to: "prisma/schema.prisma (Student model)"
      via: "Field mirroring"
      pattern: "birthDate.*birthTimeHour.*birthTimeMinute"
    - from: "src/app/(dashboard)/teachers/[id]/page.tsx"
      to: "prisma.schema.Teacher"
      via: "Prisma include optimization"
      pattern: "include.*team.*teacher.*Analysis"
---

<objective>
Teacher 모델에 성향 분석 입력 필드를 추가하고 N+1 쿼리를 최적화합니다.

Purpose: 1) 생년월일시/한자이름 필드 추가로 분석 입력 데이터 확보, 2) Prisma include 최적화로 팀 기반 쿼리 성능 보장
Output: Teacher 모델 확장 마이그레이션 및 N+1 방지를 위한 인덱스/관계 최적화
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-teacher-analysis-&-team-data-access/12-RESEARCH.md

# Reference from Student model
@prisma/schema.prisma (lines 40-83 for Student.birthDate, birthTimeHour, birthTimeMinute, nameHanja)

# N+1 query optimization patterns
@.planning/phases/11-teacher-infrastructure/11-02-SUMMARY.md (Prisma Client Extensions for team filtering)
</context>

<tasks>

<task type="auto">
  <name>Teacher 모델에 분석 입력 필드 추가</name>
  <files>prisma/schema.prisma</files>
  <action>
Prisma schema.prisma의 Teacher 모델에 분석 입력 필드를 추가합니다:

**추가할 필드** (Student 모델에서 복제):

1. **생년월일시 필드** (사주 분석용):
   ```prisma
   birthDate          DateTime?    // 생년월일 (사주 분석용)
   birthTimeHour      Int?          // 출생 시 (0-23)
   birthTimeMinute    Int?          // 출생 분 (0-59)
   ```

2. **한자 이름 필드** (성명학 분석용):
   ```prisma
   nameHanja          Json?         // 한자 이름 (성명학 분석용)
   ```

**기존 Teacher 모델에 필드 추가**:
```prisma
model Teacher {
  id                  String               @id @default(cuid())
  email               String               @unique
  password            String
  name                String
  role                Role                 @default(TEACHER)
  teamId              String?
  team                Team?                @relation(fields: [teamId], references: [id])
  phone               String?

  // NEW: 성향 분석 입력 필드
  birthDate           DateTime?
  birthTimeHour       Int?
  birthTimeMinute     Int?
  nameHanja           Json?

  createdAt           DateTime             @default(now())
  updatedAt           DateTime             @updatedAt
  passwordResetTokens PasswordResetToken[]
  students            Student[]
  teacherSajuAnalysis    TeacherSajuAnalysis?
  teacherMbtiAnalysis    TeacherMbtiAnalysis?
  teacherNameAnalysis    TeacherNameAnalysis?

  @@index([teamId])
}
```

**중요**:
- 모든 필드 nullable (기존 Teacher 데이터에 영향 없음)
- nameHanja는 Json 타입 (Student.nameHanja와 동일)
- birthTimeHour/Minute는 Int 타입 (Student와 동일)
  </action>
  <verify>prisma validate</verify>
  <done>Teacher 모델에 4개 분석 입력 필드 추가되고 validate 통과</done>
</task>

<task type="auto">
  <name>마이그레이션 생성 및 적용</name>
  <files>prisma/migrations/*/migration.sql</files>
  <action>
마이그레이션을 생성하고 적용합니다:

1. **마이그레이션 생성**:
   ```bash
   npx prisma migrate dev --name add_teacher_analysis_fields
   ```

2. **마이그레이션 상태 확인**:
   ```bash
   npx prisma migrate status
   ```
   예상 출력: "Database schema is up to date!"

3. **Prisma Client 재생성**:
   ```bash
   npx prisma generate
   ```

**Shadow database 이슈 대처** (Phase 11-01과 동일):
- `prisma migrate dev` 실패 시 수동 마이그레이션 생성
- ALTER TABLE 문으로 필드 추가:
  ```sql
  ALTER TABLE "Teacher" ADD COLUMN "birthDate" TIMESTAMP(3);
  ALTER TABLE "Teacher" ADD COLUMN "birthTimeHour" INTEGER;
  ALTER TABLE "Teacher" ADD COLUMN "birthTimeMinute" INTEGER;
  ALTER TABLE "Teacher" ADD COLUMN "nameHanja" JSON;
  ```
- `npx prisma db execute --stdin`으로 직접 적용
- `prisma migrate resolve --applied`로 상태 업데이트
  </action>
  <verify>npx prisma migrate status (shows "Database schema is up to date!")</verify>
  <done>마이그레이션 적용 완료 및 DB 스키마 최신 상태</done>
</task>

<task type="auto">
  <name>Teacher 상세 페이지에서 필드 활용 및 N+1 최적화 확인</name>
  <files>src/app/(dashboard)/teachers/[id]/page.tsx</files>
  <action>
Teacher 상세 페이지를 업데이트하여 추가된 필드를 활용하고 N+1 쿼리 문제를 방지합니다:

1. **TODO 제거 및 실제 필드 사용**:
   기존의 null 하드코딩을 실제 teacher 필드로 교체:
   ```typescript
   // 기존:
   teacherBirthDate={null}
   teacherBirthTimeHour={null}

   // 변경:
   teacherBirthDate={teacher.birthDate}
   teacherBirthTimeHour={teacher.birthTimeHour}
   teacherBirthTimeMinute={teacher.birthTimeMinute}

   // 기존:
   teacherNameHanja={null}

   // 변경:
   teacherNameHanja={
     teacher.nameHanja
       ? JSON.stringify(teacher.nameHanja)
       : null
   }
   ```

2. **Prisma include로 N+1 방지 확인**:
   ```typescript
   const teacher = await db.teacher.findUnique({
     where: { id },
     include: {
       team: true, // Team 조인으로 추가 쿼리 방지
       teacherMbtiAnalysis: true,
       teacherSajuAnalysis: true,
       teacherNameAnalysis: true,
     },
   })
   ```
   - team: true로 Team 조인 (별도 쿼리 방지)
   - 모든 Analysis 관계 포함 (한 번의 쿼리로 모든 데이터 로드)

3. **RBAC Prisma Client 확인** (Phase 11-02 패턴):
   만약 getRBACPrisma를 사용하는 경우:
   ```typescript
   const db = getRBACPrisma(session) // 이미 팀 필터링 적용됨

   const teacher = await db.teacher.findUnique({
     where: { id },
     include: {
       team: true,
       teacherMbtiAnalysis: true,
       teacherSajuAnalysis: true,
       teacherNameAnalysis: true,
     },
   })
   ```
   - Phase 11-02의 Prisma Client Extensions가 자동으로 teamId 필터링
   - N+1 문제는 include로 해결

4. **쿼리 성능 확인** (선택 - 개발 중):
   - Prisma 로그에서 SELECT 쿼리 개수 확인
   - 예상: 1개의 SELECT문 (JOIN 포함)
   - N+1 발생 시: 여러 개의 SELECT문 (각 관계마다 1개씩)

**N+1 방지 원리**:
- Prisma의 include는 SQL JOIN으로 변환되어 한 번의 쿼리로 모든 관계 데이터 로드
- Phase 11-02의 Prisma Client Extensions는 teamId 자동 필터링 추가
- 결과: 1개의 최적화된 쿼리로 Teacher + Team + 모든 Analysis 로드
  </action>
  <verify>grep -E "teacher\.birthDate|teacher\.birthTimeHour|teacher\.nameHanja" src/app/\(dashboard\)/teachers/\[id\]/page.tsx</verify>
  <done>Teacher 상세 페이지가 추가된 필드를 사용하고 N+1 쿼리 최적화가 적용됨</done>
</task>

</tasks>

<verification>
1. **스키마 업데이트**: Teacher 모델에 4개 필드 추가 확인 (birthDate, birthTimeHour, birthTimeMinute, nameHanja)
2. **마이그레이션 성공**: `npx prisma migrate status`가 최신 상태 보고
3. **Prisma validate 통과**: 스키마 검증 에러 없음
4. **필드 활용**: Teacher 상세 페이지에서 실제 teacher 필드 사용 (null 제거)
5. **N+1 방지**: include 사용으로 관계 데이터 한 번의 쿼리로 로드
6. **RBAC 호환**: getRBACPrisma 사용 시 teamId 자동 필터링 확인
</verification>

<success_criteria>
1. Teacher 모델에 4개 분석 입력 필드 추가 완료
2. 마이그레이션 성공적 적용
3. Teacher 상세 페이지에서 필드 활용
4. Prisma include로 N+1 쿼리 방지
5. Phase 11-02 RBAC와 호환
</success_criteria>

<output>
After completion, create `.planning/phases/12-teacher-analysis-&-team-data-access/12-06-SUMMARY.md`
</output>
