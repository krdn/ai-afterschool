---
phase: 12-teacher-analysis
plan: 05
type: execute
wave: 5
depends_on: ["12-04", "12-07", "12-08"]
files_modified:
  - src/app/(dashboard)/teachers/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "선생님 상세 페이지(/teachers/[id])에 모든 분석 패널이 통합 표시된다"
    - "선생님 기본 정보(이름, 이메일, 역할, 팀)가 상단에 표시된다"
    - "MBTI, 사주, 이름, 관상, 손금 분석 패널이 각각 독립적인 섹션으로 렌더링된다"
    - "각 분석 패널이 해당 분석 데이터를 props로 받아 표시한다"
  artifacts:
    - path: "src/app/(dashboard)/teachers/[id]/page.tsx"
      provides: "Teacher profile page with integrated analysis"
      min_lines: 80
      contains: "TeacherMbtiPanel|TeacherSajuPanel|TeacherNamePanel|TeacherFacePanel|TeacherPalmPanel"
  key_links:
    - from: "src/app/(dashboard)/teachers/[id]/page.tsx"
      to: "src/components/teachers/teacher-mbti-panel.tsx"
      via: "Component import and render"
      pattern: "<TeacherMbtiPanel"
    - from: "src/app/(dashboard)/teachers/[id]/page.tsx"
      to: "src/components/teachers/teacher-face-panel.tsx"
      via: "Component import and render"
      pattern: "<TeacherFacePanel"
    - from: "src/app/(dashboard)/teachers/[id]/page.tsx"
      to: "src/components/teachers/teacher-palm-panel.tsx"
      via: "Component import and render"
      pattern: "<TeacherPalmPanel"
    - from: "src/app/(dashboard)/teachers/[id]/page.tsx"
      to: "prisma.schema.Teacher"
      via: "Prisma query with include"
      pattern: "include.*teacher.*Analysis"
    - from: "src/app/(dashboard)/teachers/[id]/page.tsx"
      to: "src/lib/dal.ts"
      via: "Session verification"
      pattern: "verifySession\\("
---

<objective>
선생님 상세 페이지에 모든 성향 분석 패널을 통합하여 표시합니다.

Purpose: 기존 Student 상세 페이지(src/app/(dashboard)/students/[id]/page.tsx) 구조를 Teacher 전용으로 복제하여 분석 결과 통합 표시
Output: Teacher 기본 정보 + 3개 분석 패널이 포함된 상세 페이지
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-teacher-analysis-&-team-data-access/12-RESEARCH.md

# Reference patterns from Student detail page
@src/app/(dashboard)/students/[id]/page.tsx

# Reuse RBAC from Phase 11
@.planning/phases/11-teacher-infrastructure/11-03-SUMMARY.md (Session RBAC)
@.planning/phases/11-teacher-infrastructure/11-04-SUMMARY.md (Teacher CRUD)
</context>

<tasks>

<task type="auto">
  <name>Teacher 상세 페이지 생성 (분석 통합)</name>
  <files>src/app/(dashboard)/teachers/[id]/page.tsx</files>
  <action>
src/app/(dashboard)/teachers/[id]/page.tsx 파일을 새로 생성합니다:

**파일 내용** (Student detail page 패턴 복제):

1. **임포트**:
   ```typescript
   import { notFound } from "next/navigation"
   import { verifySession } from "@/lib/dal"
   import { db } from "@/lib/db"
   import { TeacherMbtiPanel } from "@/components/teachers/teacher-mbti-panel"
   import { TeacherSajuPanel } from "@/components/teachers/teacher-saju-panel"
   import { TeacherNamePanel } from "@/components/teachers/teacher-name-panel"
   import { TeacherFacePanel } from "@/components/teachers/teacher-face-panel"
   import { TeacherPalmPanel } from "@/components/teachers/teacher-palm-panel"
   ```

2. **Page Props 타입**:
   ```typescript
   type PageProps = {
     params: Promise<{ id: string }>
   }
   ```

3. **데이터 조회 함수** (비동기 컴포넌트로 구현):
   ```typescript
   export default async function TeacherDetailPage({ params }: PageProps) {
     const session = await verifySession()
     if (!session) {
       return <div>Unauthorized</div>
     }

     const { id } = await params

     // Teacher 조회 (모든 분석 데이터 포함)
     const teacher = await db.teacher.findUnique({
       where: { id },
       include: {
         team: true,
         teacherMbtiAnalysis: true,
         teacherSajuAnalysis: true,
         teacherNameAnalysis: true,
         teacherFaceAnalysis: true,
         teacherPalmAnalysis: true,
       },
     })

     if (!teacher) {
       notFound()
     }

     // RBAC: 팀 데이터 접근 제어 (Phase 11-02 패턴 재사용)
     const canAccess =
       session.role === "DIRECTOR" ||
       (session.role === "TEAM_LEADER" && session.teamId === teacher.teamId) ||
       session.teacherId === teacher.id

     if (!canAccess) {
       return <div>Access Denied</div>
     }
   ```

4. **UI 렌더링**:
   ```typescript
     return (
       <div className="container mx-auto px-4 py-8">
         {/* 헤더: 기본 정보 */}
         <div className="mb-8">
           <h1 className="text-3xl font-bold">{teacher.name}</h1>
           <p className="text-gray-600">{teacher.email}</p>
           <div className="flex gap-2 mt-2">
             <span className="badge">{teacher.role}</span>
             {teacher.team && <span className="badge">{teacher.team.name}</span>}
           </div>
         </div>

         {/* 분석 패널 그리드 */}
         <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
           {/* MBTI 분석 */}
           <TeacherMbtiPanel
             teacherId={teacher.id}
             teacherName={teacher.name}
             analysis={teacher.teacherMbtiAnalysis}
           />

           {/* 사주 분석 */}
           <TeacherSajuPanel
             teacherId={teacher.id}
             teacherName={teacher.name}
             analysis={teacher.teacherSajuAnalysis}
             teacherBirthDate={null} // TODO: Teacher.birthDate 필드 필요
             teacherBirthTimeHour={null} // TODO: Teacher.birthTimeHour 필드 필요
             teacherBirthTimeMinute={null}
           />

           {/* 이름 분석 */}
           <TeacherNamePanel
             teacherId={teacher.id}
             teacherName={teacher.name}
             analysis={teacher.teacherNameAnalysis}
             teacherNameHanja={null} // TODO: Teacher.nameHanja 필드 필요
           />

           {/* 관상 분석 */}
           {teacher.teacherFaceAnalysis && (
             <TeacherFacePanel
               teacherId={teacher.id}
               teacherName={teacher.name}
               analysis={teacher.teacherFaceAnalysis}
             />
           )}

           {/* 손금 분석 */}
           {teacher.teacherPalmAnalysis && (
             <TeacherPalmPanel
               teacherId={teacher.id}
               teacherName={teacher.name}
               analysis={teacher.teacherPalmAnalysis}
             />
           )}
         </div>
       </div>
     )
   }
   ```

중요 사항:
- **params는 Promise**: Next.js 15에서 동적 route params는 Promise 형태, await 필요
- **RBAC 적용**: Phase 11-02의 팀 기반 접근 제어 패턴 재사용
- **TODO 처리**: Teacher 모델에 birthDate, nameHanja 필드가 없으므로 null 전달 (Phase 12-06에서 마이그레이션 예정)

5. **레이아웃**:
   - 상단: 선생님 기본 정보 (이름, 이메일, 역할, 팀)
   - 하단: 3개 분석 패널 그리드 (lg 화면에서 2열, 모바일에서 1열)
   - 각 패널: 독립적인 카드 형태
  </action>
  <verify>grep -E "export.*default.*async.*function.*TeacherDetailPage|TeacherMbtiPanel|TeacherSajuPanel|TeacherNamePanel" src/app/\(dashboard\)/teachers/\[id\]/page.tsx</verify>
  <done>Teacher 상세 페이지가 3개 분석 패널을 통합하여 렌더링하고 RBAC가 적용됨</done>
</task>

</tasks>

<verification>
1. **async 컴포넌트**: export default async function으로 선언되어 params await 가능
2. **Session 검증**: verifySession() 호출 및 unauthorized 처리 확인
3. **데이터 조회**: db.teacher.findUnique with include 확인
4. **RBAC 로직**: canAccess 조건 및 role/teamId 확인
5. **컴포넌트 렌더링**: 3개 Teacher*Panel 컴포넌트 import 및 사용 확인
6. **조회 실패 처리**: notFound() 호출 확인
</verification>

<success_criteria>
1. Teacher 상세 페이지 생성 완료
2. 5개 분석 패널 통합 표시 (MBTI, 사주, 이름, 관상, 손금)
3. RBAC 접근 제어 적용
4. 기본 정보 헤더 표시
5. TypeScript 컴파일 에러 없음 (TODO 주석으로 누락된 필드 처리)
</success_criteria>

<output>
After completion, create `.planning/phases/12-teacher-analysis-&-team-data-access/12-05-SUMMARY.md`
</output>
