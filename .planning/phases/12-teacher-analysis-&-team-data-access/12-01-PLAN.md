---
phase: 12-teacher-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/{timestamp}_add_teacher_analysis_tables/migration.sql
autonomous: true

must_haves:
  truths:
    - "선생님을 위한 MBTI, 사주, 성명학 분석 테이블이 데이터베이스에 존재한다"
    - "Student*Analysis 테이블과 동일한 구조로 Teacher*Analysis 테이블이 생성된다"
    - "Teacher 모델이 Teacher*Analysis 모델과 1:1 관계로 연결된다"
    - "마이그레이션이 성공적으로 적용되고 Prisma Client가 재생성된다"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "TeacherSajuAnalysis, TeacherMbtiAnalysis, TeacherNameAnalysis models"
      contains: "model TeacherSajuAnalysis|model TeacherMbtiAnalysis|model TeacherNameAnalysis"
    - path: "prisma/migrations/*/migration.sql"
      provides: "Teacher analysis tables creation SQL"
      contains: "CREATE TABLE.*TeacherSajuAnalysis"
  key_links:
    - from: "prisma/schema.prisma"
      to: "prisma/schema.prisma (Student models)"
      via: "Structural mirroring"
      pattern: "Teacher.*Analysis.*Analysis.*studentId|teacherId"
---

<objective>
선생님 성향 분석(MBTI, 사주, 성명학)을 위한 데이터베이스 스키마를 생성합니다.

Purpose: 기존 Student*Analysis 모델을 Teacher 전용으로 복제하여 선생님 성향 분석 데이터 저장소 확보
Output: TeacherSajuAnalysis, TeacherMbtiAnalysis, TeacherNameAnalysis Prisma 모델 및 마이그레이션
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-teacher-infrastructure/11-01-SUMMARY.md
@.planning/phases/12-teacher-analysis-&-team-data-access/12-RESEARCH.md

# Reuse patterns from Student models
@prisma/schema.prisma (lines 84-137 for SajuAnalysis, NameAnalysis, MbtiAnalysis)
</context>

<tasks>

<task type="auto">
  <name>Teacher 분석 테이블 모델 추가</name>
  <files>prisma/schema.prisma</files>
  <action>
Prisma schema.prisma에 Teacher 성향 분석 모델 3개를 추가합니다:

1. **TeacherSajuAnalysis** 모델 생성 (SajuAnalysis 복제):
   - id: String @id @default(cuid())
   - teacherId: String @unique
   - inputSnapshot: Json
   - result: Json
   - interpretation: String?
   - status: String @default("complete")
   - version: Int @default(1)
   - calculatedAt: DateTime
   - createdAt: DateTime @default(now())
   - updatedAt: DateTime @updatedAt
   - teacher: Teacher @relation(fields: [teacherId], references: [id], onDelete: Cascade)
   - @@index([teacherId])

2. **TeacherMbtiAnalysis** 모델 생성 (MbtiAnalysis 복제):
   - id: String @id @default(cuid())
   - teacherId: String @unique
   - responses: Json
   - scores: Json
   - mbtiType: String
   - percentages: Json
   - interpretation: String?
   - version: Int @default(1)
   - calculatedAt: DateTime
   - createdAt: DateTime @default(now())
   - updatedAt: DateTime @updatedAt
   - teacher: Teacher @relation(fields: [teacherId], references: [id], onDelete: Cascade)
   - @@index([teacherId])

3. **TeacherNameAnalysis** 모델 생성 (NameAnalysis 복제):
   - id: String @id @default(cuid())
   - teacherId: String @unique
   - inputSnapshot: Json
   - result: Json
   - interpretation: String?
   - status: String @default("complete")
   - version: Int @default(1)
   - calculatedAt: DateTime
   - createdAt: DateTime @default(now())
   - updatedAt: DateTime @updatedAt
   - teacher: Teacher @relation(fields: [teacherId], references: [id], onDelete: Cascade)
   - @@index([teacherId])

4. **Teacher 모델 업데이트** (기존 Teacher 모델에 관계 추가):
   - teacherSajuAnalysis: TeacherSajuAnalysis?
   - teacherMbtiAnalysis: TeacherMbtiAnalysis?
   - teacherNameAnalysis: TeacherNameAnalysis?

중요: Student*Analysis 모델과 정확히 동일한 필드 구조와 제약조건 사용. onDelete: Cascade로 Teacher 삭제 시 분석 데이터 자동 삭제.
  </action>
  <verify>prisma validate</verify>
  <done>Prisma schema에 3개 Teacher 분석 모델이 추가되고 validate 통과</done>
</task>

<task type="auto">
  <name>마이그레이션 생성 및 적용</name>
  <files>prisma/migrations/*/migration.sql</files>
  <action>
다음 순서로 마이그레이션을 생성하고 적용합니다:

1. 마이그레이션 생성:
   ```bash
   npx prisma migrate dev --name add_teacher_analysis_tables
   ```

2. 마이그레이션 적용 확인:
   ```bash
   npx prisma migrate status
   ```
   "Database schema is up to date!" 메시지 확인 필요

3. Prisma Client 재생성:
   ```bash
   npx prisma generate
   ```

만약 Prisma migrate dev가 실패하면(Phase 11-01과 동일한 shadow database 이슈 가능성):
- 수동으로 마이그레이션 디렉토리 생성
- schema 변경사항을 SQL로 작성
- `npx prisma db execute --stdin`으로 직접 적용
- `prisma migrate resolve --applied`로 마이그레이션 상태 업데이트
  </action>
  <verify>npx prisma migrate status (shows "Database schema is up to date!")</verify>
  <done>마이그레이션이 성공적으로 적용되고 데이터베이스 스키마 최신 상태</done>
</task>

<task type="auto">
  <name>마이그레이션 검증</name>
  <files>prisma/schema.prisma</files>
  <action>
마이그레이션 결과를 검증합니다:

1. Prisma schema 재검증:
   ```bash
   npx prisma validate
   ```

2. 생성된 마이그레이션 SQL 확인:
   - prisma/migrations/*/migration.sql 파일을 읽어
   - CREATE TABLE "TeacherSajuAnalysis" 문 존재 확인
   - CREATE TABLE "TeacherMbtiAnalysis" 문 존재 확인
   - CREATE TABLE "TeacherNameAnalysis" 문 존재 확인
   - 외래 키 제약조건 확인 (teacherId -> Teacher.id)

3. 데이터베이스 테이블 존재 확인 (선택 - psql 사용 가능 시):
   ```sql
   SELECT tablename FROM pg_tables WHERE tablename LIKE 'Teacher%Analysis';
   ```
   결과: 3개 테이블 존재 (TeacherSajuAnalysis, TeacherMbtiAnalysis, TeacherNameAnalysis)
  </action>
  <verify>grep -E "CREATE TABLE.*Teacher(.*Analysis)" prisma/migrations/*/migration.sql</verify>
  <done>3개 Teacher 분석 테이블이 마이그레이션 SQL에 존재하고 Prisma validate 통과</done>
</task>

</tasks>

<verification>
1. **스키마 검증**: `npx prisma validate` 명령이 에러 없이 완료
2. **마이그레이션 상태**: `npx prisma migrate status`가 "Database schema is up to date!" 반환
3. **모델 존재**: schema.prisma에 TeacherSajuAnalysis, TeacherMbtiAnalysis, TeacherNameAnalysis 모델 존재
4. **관계 설정**: Teacher 모델에 3개 분석 관계 필드 존재
5. **마이그레이션 SQL**: 생성된 migration.sql에 CREATE TABLE 문 3개 존재
</verification>

<success_criteria>
1. Prisma schema에 3개 Teacher*Analysis 모델 정의 완료
2. Teacher 모델에 분석 관계 필드 3개 추가 완료
3. 마이그레이션 성공적 적용 및 Prisma Client 재생성
4. npx prisma validate 통과
5. 데이터베이스에 3개 Teacher 분석 테이블 생성 완료
</success_criteria>

<output>
After completion, create `.planning/phases/12-teacher-analysis-&-team-data-access/12-01-SUMMARY.md`
</output>
