---
phase: 12-teacher-analysis
plan: 04
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - src/components/teachers/teacher-mbti-panel.tsx
  - src/components/teachers/teacher-saju-panel.tsx
  - src/components/teachers/teacher-name-panel.tsx
autonomous: true

must_haves:
  truths:
    - "선생님 MBTI/사주/이름 분석 결과를 표시하는 UI 컴포넌트가 존재한다"
    - "Student 분석 패널과 동일한 시각적 구조로 Teacher 분석이 표시된다"
    - "분석 미완료 시 '분석 시작' 버튼이 표시된다"
    - "분석 완료 시 결과 데이터와 해석이 표시된다"
  artifacts:
    - path: "src/components/teachers/teacher-mbti-panel.tsx"
      provides: "Teacher MBTI analysis display component"
      min_lines: 50
    - path: "src/components/teachers/teacher-saju-panel.tsx"
      provides: "Teacher saju analysis display component"
      min_lines: 50
    - path: "src/components/teachers/teacher-name-panel.tsx"
      provides: "Teacher name analysis display component"
      min_lines: 50
  key_links:
    - from: "src/components/teachers/teacher-mbti-panel.tsx"
      to: "src/components/students/mbti-analysis-panel.tsx"
      via: "Pattern mirroring"
      pattern: "MbtiResultsDisplay|runTeacherMbtiAnalysis"
    - from: "src/components/teachers/teacher-saju-panel.tsx"
      to: "src/components/students/saju-analysis-panel.tsx"
      via: "Pattern mirroring"
      pattern: "runTeacherSajuAnalysis"
    - from: "src/components/teachers/teacher-*-panel.tsx"
      to: "src/lib/actions/teacher-analysis.ts"
      via: "Server action call"
      pattern: "runTeacher.*Analysis\\("
---

<objective>
선생님 성향 분석 결과를 표시하는 UI 컴포넌트를 생성합니다.

Purpose: 기존 Student 분석 패널(MbtiAnalysisPanel, SajuAnalysisPanel, NameAnalysisPanel)을 Teacher 전용으로 복제하여 분석 결과 표시
Output: 3개 Teacher 분석 패널 컴포넌트 (MBTI, 사주, 이름)
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-teacher-analysis-&-team-data-access/12-RESEARCH.md

# Reference patterns from Student components
@src/components/students/mbti-analysis-panel.tsx (lines 1-139)
@src/components/students/saju-analysis-panel.tsx
@src/components/students/name-analysis-panel.tsx
@src/components/mbti/results-display.tsx (reuse - no student coupling)
</context>

<tasks>

<task type="auto">
  <name>Teacher MBTI 분석 패널 생성</name>
  <files>src/components/teachers/teacher-mbti-panel.tsx</files>
  <action>
src/components/teachers/teacher-mbti-panel.tsx 파일을 새로 생성합니다:

**파일 내용** (MbtiAnalysisPanel 패턴 복제):

1. **임포트**:
   ```typescript
   "use client"

   import { useState } from "react"
   import { Brain, Pencil } from "lucide-react"
   import { MbtiResultsDisplay } from "@/components/mbti/results-display"
   import { runTeacherMbtiAnalysis } from "@/lib/actions/teacher-analysis"
   import { useRouter } from "next/navigation"
   ```

2. **타입 정의**:
   ```typescript
   type TeacherMbtiAnalysis = {
     mbtiType: string
     percentages: Record<string, number>
     calculatedAt: Date
   } | null

   type Props = {
     teacherId: string
     teacherName: string
     analysis: TeacherMbtiAnalysis
   }
   ```

3. **컴포넌트 구조** (MbtiAnalysisPanel와 동일):
   - 분석 완료 시: MbtiResultsDisplay로 결과 표시
   - 분석 미완료 시: "MBTI 분석 시작" 버튼 표시
   - 버튼 클릭 시: runTeacherMbtiAnalysis(teacherId, mockResponses) 호출
     - NOTE: 실제 설문 폼은 별도 페이지 필요 (이번 plan에서는 모의 응답으로 테스트)

4. **주요 차이점**:
   - studentId → teacherId
   - studentName → teacherName
   - /students/${id}/mbti → /teachers/${id}/mbti (설문 페이지 링크, 나중에 구현)
   - saveMbtiDirectInput → runTeacherMbtiAnalysis (서로 다른 함수)

중요: MbtiResultsDisplay 컴포넌트는 이미 존재하며 Student/Teacher 구분 없이 재사용 가능
  </action>
  <verify>grep -E "export.*function.*TeacherMbtiPanel" src/components/teachers/teacher-mbti-panel.tsx</verify>
  <done>TeacherMbtiPanel 컴포넌트가 Student MbtiAnalysisPanel과 동일한 구조로 구현됨</done>
</task>

<task type="auto">
  <name>Teacher 사주 분석 패널 생성</name>
  <files>src/components/teachers/teacher-saju-panel.tsx</files>
  <action>
src/components/teachers/teacher-saju-panel.tsx 파일을 새로 생성합니다:

**파일 내용** (SajuAnalysisPanel 패턴 복제):

1. **임포트**:
   ```typescript
   "use client"

   import { useState } from "react"
   import { Calendar, RefreshCw } from "lucide-icon"
   import { runTeacherSajuAnalysis } from "@/lib/actions/teacher-analysis"
   import { useRouter } from "next/navigation"
   ```

2. **타입 정의**:
   ```typescript
   type TeacherSajuAnalysis = {
     pillars: any
     elements: any
     tenGods: any
     interpretation?: string | null
     calculatedAt: Date
   } | null

   type Props = {
     teacherId: string
     teacherName: string
     analysis: TeacherSajuAnalysis
     teacherBirthDate?: Date | null
     teacherBirthTimeHour?: number | null
     teacherBirthTimeMinute?: number | null
   }
   ```

3. **컴포넌트 구조**:
   - 분석 완료 시:
     - 사주 결과 (년월일시 주, 오행 균형, 십성) 표시
     - 해석 텍스트 표시 (interpretation)
     - "재분석" 버튼
   - 분석 미완료 시:
     - 생년월일시 정보 미존재 시: "생년월일 정보를 먼저 입력해주세요" 메시지
     - 정보 존재 시: "사주 분석 시작" 버튼
   - 버튼 클릭 시: runTeacherSajuAnalysis(teacherId) 호출

4. **시각적 요소**:
   - 카드 헤더: 아이콘 + "사주 성향 분석" 제목
   - 결과 표시: pillars, elements, tenGods를 읽기 쉬운 형식으로 변환
   - 오행 바 차트: elements 데이터를 시각화 (간단한 텍스트 또는 CSS 바)

중요: Student SajuAnalysisPanel의 복잡한 시각화 로직을 그대로 복사
  </action>
  <verify>grep -E "export.*function.*TeacherSajuPanel" src/components/teachers/teacher-saju-panel.tsx</verify>
  <done>TeacherSajuPanel 컴포넌트가 사주 분석 결과를 표시하고 재분석 기능 포함</done>
</task>

<task type="auto">
  <name>Teacher 이름 분석 패널 생성</name>
  <files>src/components/teachers/teacher-name-panel.tsx</files>
  <action>
src/components/teachers/teacher-name-panel.tsx 파일을 새로 생성합니다:

**파일 내용** (NameAnalysisPanel 패턴 복제):

1. **임포트**:
   ```typescript
   "use client"

   import { useState } from "react"
   import { User, RefreshCw } from "lucide-react"
   import { runTeacherNameAnalysis } from "@/lib/actions/teacher-analysis"
   import { useRouter } from "next/navigation"
   ```

2. **타입 정의**:
   ```typescript
   type TeacherNameAnalysis = {
     split: any
     strokes: any
     grids: any
     interpretations: any
     calculatedAt: Date
   } | null

   type Props = {
     teacherId: string
     teacherName: string
     analysis: TeacherNameAnalysis
     teacherNameHanja?: string | null
   }
   ```

3. **컴포넌트 구조**:
   - 분석 완료 시:
     - 이름 분할 결과 표시 (성/이름 분리)
     - 획수 정보 표시 (원격, 형격, 이격, 정격)
     - 각 격의 해석 텍스트 표시
     - "재분석" 버튼
   - 분석 미완료 시:
     - 한자 정보 미존재 시: "한자 정보를 먼저 입력해주세요" 메시지
     - 정보 존재 시: "성명학 분석 시작" 버튼
   - 버튼 클릭 시: runTeacherNameAnalysis(teacherId) 호출

4. **주의사항**:
   - Teacher 모델에 nameHanja 필드가 없을 수 있음 (Phase 11에서 생성되지 않음)
   - 이 경우 "선생님 정보 수정" 기능이 필요하다는 메시지 표시
   - 또는 nameHanja 필드를 Teacher 모델에 추가하는 마이그레이션이 필요 (이 plan 범위 밖)

중요: NameAnalysisPanel의 grids 표시 로직을 그대로 복사 (원격, 형격, 이격, 정격)
  </action>
  <verify>grep -E "export.*function.*TeacherNamePanel" src/components/teachers/teacher-name-panel.tsx</verify>
  <done>TeacherNamePanel 컴포넌트가 성명학 분석 결과를 표시하고 한자 정보 부족 시 안내</done>
</task>

</tasks>

<verification>
1. **"use client" 지시자**: 3개 파일 모두 최상단에 "use client" 존재
2. **컴포넌트 내보내기**: 각 파일에서 해당 Panel 컴포넌트 export 확인
3. **Server Action 호출**: runTeacher*Analysis 함수 호출 확인
4. **조건부 렌더링**: analysis null 여부에 따른 분석 완료/미완료 UI 분기 확인
5. **아이콘 사용**: lucide-react 아이콘 (Brain, Calendar, User 등) 임포트 확인
</verification>

<success_criteria>
1. 3개 Teacher 분석 패널 컴포넌트 생성 완료
2. 각 패널이 분석 완료/미완료 상태에 따른 UI 표시
3. Server Action 연동으로 분석 실행 기능
4. Student 패널과 유사한 시각적 구조
5. TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/12-teacher-analysis-&-team-data-access/12-04-SUMMARY.md`
</output>
