---
phase: 12-teacher-analysis
plan: 02
type: execute
wave: 1
depends_on: ["12-01"]
files_modified:
  - src/lib/db/teacher-saju-analysis.ts
  - src/lib/db/teacher-name-analysis.ts
  - src/lib/db/teacher-mbti-analysis.ts
autonomous: true

must_haves:
  truths:
    - "선생님 사주/이름/MBTI 분석 CRUD 함수가 존재한다"
    - "Student 분석 DB 함수와 동일한 패턴으로 Teacher 분석 함수가 구현된다"
    - "upsert 패턴으로 분석 결과 생성/업데이트가 가능하다"
    - "teacherId를 기반으로 분석 데이터를 조회할 수 있다"
  artifacts:
    - path: "src/lib/db/teacher-saju-analysis.ts"
      provides: "upsertTeacherSajuAnalysis, getTeacherSajuAnalysis functions"
      exports: ["upsertTeacherSajuAnalysis", "getTeacherSajuAnalysis"]
    - path: "src/lib/db/teacher-name-analysis.ts"
      provides: "upsertTeacherNameAnalysis, getTeacherNameAnalysis functions"
      exports: ["upsertTeacherNameAnalysis", "getTeacherNameAnalysis"]
    - path: "src/lib/db/teacher-mbti-analysis.ts"
      provides: "upsertTeacherMbtiAnalysis, getTeacherMbtiAnalysis functions"
      exports: ["upsertTeacherMbtiAnalysis", "getTeacherMbtiAnalysis"]
  key_links:
    - from: "src/lib/db/teacher-saju-analysis.ts"
      to: "src/lib/db/student-analysis.ts"
      via: "Pattern mirroring"
      pattern: "upsert.*Analysis|get.*Analysis"
    - from: "src/lib/db/teacher-*.ts"
      to: "prisma.schema.TeacherSajuAnalysis"
      via: "Prisma client usage"
      pattern: "db\\.teacherSajuAnalysis\\.upsert"
---

<objective>
선생님 성향 분석을 위한 데이터베이스 CRUD 함수를 생성합니다.

Purpose: 기존 Student 분석 DB 함수(src/lib/db/student-analysis.ts, mbti-analysis.ts) 패턴을 재사용하여 Teacher 전용 CRUD 구현
Output: Teacher 분석 데이터 생성/조회를 위한 3개 DB 모듈 (saju, name, mbti)
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-teacher-analysis-&-team-data-access/12-RESEARCH.md

# Reference patterns from Student modules
@src/lib/db/student-analysis.ts (lines 70-116 for upsertSajuAnalysis, upsertNameAnalysis)
@src/lib/db/mbti-analysis.ts (lines 47-84 for upsertMbtiAnalysis)
</context>

<tasks>

<task type="auto">
  <name>Teacher 사주 분석 DB 모듈 생성</name>
  <files>src/lib/db/teacher-saju-analysis.ts</files>
  <action>
src/lib/db/teacher-saju-analysis.ts 파일을 새로 생성합니다:

**파일 내용**:
1. Prisma 및 db 임포트:
   ```typescript
   import { Prisma } from "@prisma/client"
   import { db } from "@/lib/db"
   ```

2. AnalysisPayload 타입 정의 (student-analysis.ts 재사용):
   ```typescript
   type AnalysisPayload = {
     inputSnapshot: Prisma.JsonValue
     result: Prisma.JsonValue
     interpretation?: string | null
     status?: string
     version?: number
     calculatedAt?: Date
   }
   ```

3. **upsertTeacherSajuAnalysis** 함수 구현:
   - 입력: teacherId (string), payload (AnalysisPayload)
   - calculatedAt 기본값: new Date()
   - db.teacherSajuAnalysis.upsert 호출
   - where: { teacherId }
   - create: { teacherId, ...data }
   - update: data
   - 반환: upsert 결과

4. **getTeacherSajuAnalysis** 함수 구현:
   - 입력: teacherId (string)
   - db.teacherSajuAnalysis.findUnique({ where: { teacherId } })
   - 반환: 조회 결과 또는 null

중요: student-analysis.ts의 upsertSajuAnalysis 함수를 그대로 복사하고 studentId → teacherId, SajuAnalysis → TeacherSajuAnalysis로 변경만 하면 됩니다.
  </action>
  <verify>grep -E "export.*function.*(upsert|get)Teacher.*Analysis" src/lib/db/teacher-saju-analysis.ts</verify>
  <done>upsertTeacherSajuAnalysis, getTeacherSajuAnalysis 함수가 정의되고 Student 패턴과 동일한 구조</done>
</task>

<task type="auto">
  <name>Teacher 이름 분석 DB 모듈 생성</name>
  <files>src/lib/db/teacher-name-analysis.ts</files>
  <action>
src/lib/db/teacher-name-analysis.ts 파일을 새로 생성합니다:

**파일 내용** (student-analysis.ts의 upsertNameAnalysis 패턴 복제):

1. Prisma 및 db 임포트 (위와 동일)

2. **upsertTeacherNameAnalysis** 함수 구현:
   - 입력: teacherId (string), payload (AnalysisPayload)
   - calculatedAt 기본값: new Date()
   - db.teacherNameAnalysis.upsert 호출
   - where: { teacherId }
   - create: { teacherId, ...data }
   - update: data
   - 반환: upsert 결과

3. **getTeacherNameAnalysis** 함수 구현:
   - 입력: teacherId (string)
   - db.teacherNameAnalysis.findUnique({ where: { teacherId } })
   - 반환: 조회 결과 또는 null

중요: upsertSajuAnalysis와 동일한 패턴 사용. teacherNameAnalysis 테이블만 다름.
  </action>
  <verify>grep -E "export.*function.*(upsert|get)Teacher.*Analysis" src/lib/db/teacher-name-analysis.ts</verify>
  <done>upsertTeacherNameAnalysis, getTeacherNameAnalysis 함수가 정의되고 upsert 패턴 준수</done>
</task>

<task type="auto">
  <name>Teacher MBTI 분석 DB 모듈 생성</name>
  <files>src/lib/db/teacher-mbti-analysis.ts</files>
  <action>
src/lib/db/teacher-mbti-analysis.ts 파일을 새로 생성합니다:

**파일 내용** (src/lib/db/mbti-analysis.ts의 upsertMbtiAnalysis 패턴 복제):

1. Prisma 및 db 임포트:
   ```typescript
   import { Prisma } from "@prisma/client"
   import { db } from "@/lib/db"
   ```

2. **upsertTeacherMbtiAnalysis** 함수 구현:
   - 입력: teacherId (string), data 객체
     - responses: Prisma.InputJsonValue
     - scores: Prisma.InputJsonValue
     - mbtiType: string
     - percentages: Prisma.InputJsonValue
     - interpretation?: string | null
     - version?: number
     - calculatedAt?: Date
   - calculatedAt 기본값: new Date()
   - version 기본값: 1
   - db.teacherMbtiAnalysis.upsert 호출
   - where: { teacherId }
   - create: { teacherId, ...data }
   - update: { responses, scores, mbtiType, percentages, interpretation, version, calculatedAt }
   - 반환: upsert 결과

3. **getTeacherMbtiAnalysis** 함수 구현:
   - 입력: teacherId (string)
   - db.teacherMbtiAnalysis.findUnique({ where: { teacherId } })
   - 반환: 조회 결과 또는 null

4. (선택) **upsertTeacherMbtiDraft** 함수 구현:
   - mbti-analysis.ts의 upsertMbtiDraft 패턴 복제
   - TeacherMbtiSurveyDraft 테이블이 필요 (Phase 12-04에서 설계 예정)
   - 이번 plan에서는 생략 가능

중요: MBTI는 다른 분석과 달리 responses, scores, mbtiType, percentages 필드가 모두 필요함.
  </action>
  <verify>grep -E "export.*function.*(upsert|get)Teacher.*Analysis" src/lib/db/teacher-mbti-analysis.ts</verify>
  <done>upsertTeacherMbtiAnalysis, getTeacherMbtiAnalysis 함수가 정의되고 MBTI 특정 필드 포함</done>
</task>

</tasks>

<verification>
1. **모듈 존재**: 3개 파일 생성 (teacher-saju-analysis.ts, teacher-name-analysis.ts, teacher-mbti-analysis.ts)
2. **함수 내보내기**: 각 파일에서 upsert/get 함수 2개씩 export 확인
3. **타입 안전성**: Prisma.InputJsonValue, Prisma.JsonValue 타입 사용 확인
4. **패턴 일치**: Student 분석 모듈과 동일한 함수 시그니처 및 구조
</verification>

<success_criteria>
1. 3개 DB 모듈 파일 생성 완료
2. 각 모듈에서 upsert/get 함수 2개씩 구현 완료
3. Student 분석 모듈과 동일한 패턴으로 함수 구현
4. TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/12-teacher-analysis-&-team-data-access/12-02-SUMMARY.md`
</output>
