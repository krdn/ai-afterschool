---
phase: 10-technical-debt-monitoring
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - next.config.ts
  - .env.local
  - .env.example
autonomous: true
user_setup: []
must_haves:
  truths:
    - "@next/bundle-analyzer is installed and configured for Next.js"
    - "Bundle analysis can be triggered with ANALYZE=true environment variable"
    - "Build process generates visual bundle size reports for client and server bundles"
    - "Bundle analyzer is disabled for production builds (only enabled with ANALYZE=true)"
    - "Large dependencies and optimization opportunities are identifiable from reports"
  artifacts:
    - path: "next.config.ts"
      provides: "Bundle analyzer configuration with conditional enabling"
      contains: "@next/bundle-analyzer"
    - path: "package.json"
      provides: "Bundle analyzer dependency and analyze script"
      contains: "@next/bundle-analyzer|analyze"
    - path: ".env.example"
      provides: "ANALYZE environment variable documentation"
      contains: "ANALYZE="
  key_links:
    - from: "package.json scripts"
      to: "next.config.ts"
      via: "ANALYZE environment variable"
      pattern: "ANALYZE=true"
    - from: "next.config.ts"
      to: "bundle analyzer"
      via: "withBundleAnalyzer wrapper"
      pattern: "withBundleAnalyzer"

---

<objective>
Integrate Next.js bundle analyzer to identify large dependencies and code splitting opportunities for production bundle size optimization.

Purpose: Analyze bundle sizes to identify optimization opportunities, large dependencies, and code splitting opportunities before production deployment.
Output: Configured bundle analyzer with conditional enabling via ANALYZE environment variable and visual reports.
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-technical-debt-monitoring/10-CONTEXT.md
@.planning/phases/10-technical-debt-monitoring/10-RESEARCH.md
@package.json
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @next/bundle-analyzer</name>
  <files>package.json package-lock.json</files>
  <action>
    Install bundle analyzer:
    - Run `npm install -D @next/bundle-analyzer`
    - Verify installation in package.json devDependencies
    - Check that version is compatible with Next.js version
  </action>
  <verify>`grep "@next/bundle-analyzer" package.json`</verify>
  <done>@next/bundle-analyzer installed as dev dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Configure bundle analyzer in next.config.ts</name>
  <files>next.config.ts</files>
  <action>
    Update next.config.ts to wrap with bundle analyzer:
    - Import withBundleAnalyzer from '@next/bundle-analyzer'
    - If Sentry wrapper exists, wrap in correct order: bundle analyzer → Sentry
    - Configure withBundleAnalyzer options:
      - enabled: process.env.ANALYZE === 'true'
      - Set to false by default for production builds
    - Export the wrapped configuration
    - Ensure the wrapper doesn't break existing configuration
  </action>
  <verify>`grep -E "withBundleAnalyzer|ANALYZE" next.config.ts`</verify>
  <done>Next.js configured to use bundle analyzer when ANALYZE=true is set.</done>
</task>

<task type="auto">
  <name>Task 3: Add analyze script to package.json</name>
  <files>package.json</files>
  <action>
    Add analyze script to package.json:
    - In scripts section, add: `"analyze": "ANALYZE=true npm run build"`
    - Alternatively, add: `"analyze": "ANALYZE=true next build"`
    - This allows running `npm run analyze` to trigger bundle analysis
    - Place script alongside existing build, dev, start scripts
  </action>
  <verify>`grep "analyze" package.json`</verify>
  <done>Analyze script added to package.json for convenient bundle analysis.</done>
</task>

<task type="auto">
  <name>Task 4: Add ANALYZE environment variable documentation</name>
  <files>.env.example</files>
  <action>
    Add ANALYZE to .env.example:
    - Add: `ANALYZE=false`
    - Add comment explaining that setting to true enables bundle analyzer
    - Document that bundle analyzer should NOT be enabled in production
    - Explain that analyzer increases build time and bundle size
    - Add note about using `npm run analyze` script instead
  </action>
  <verify>`grep "ANALYZE" .env.example`</verify>
  <done>ANALYZE environment variable documented with usage guidelines.</done>
</task>

<task type="auto">
  <name>Task 5: Run initial bundle analysis</name>
  <files></files>
  <action>
    Execute bundle analysis:
    - Run `npm run analyze`
    - Wait for build to complete with bundle analysis
    - Browser should automatically open with bundle analyzer reports
    - If browser doesn't open, check for output file paths in build logs
    - Reports typically open at http://localhost:8888
    - Verify that both client and server bundles are analyzed
    - Check that reports are generated for all pages
  </action>
  <verify>`ls -la .next/analyze/ 2>/dev/null || echo "Build completed with ANALYZE=true"`</verify>
  <done>Bundle analysis completed and visual reports generated successfully.</done>
</task>

<task type="auto">
  <name>Task 6: Analyze bundle size reports</name>
  <files></files>
  <action>
    Review bundle analyzer reports:
    - Identify largest modules in client bundle
    - Identify largest modules in server bundle
    - Check for duplicate code across different chunks
    - Look for opportunities to code split large dependencies
    - Identify dependencies that could be replaced with smaller alternatives
    - Note which pages have the largest bundles
    - Check if any dependencies are unexpectedly large
    - Document findings for optimization in next phase
  </action>
  <verify>`echo "Bundle analysis reviewed and findings documented"`</verify>
  <done>Bundle reports analyzed and optimization opportunities identified.</done>
</task>

<task type="auto">
  <name>Task 7: Document bundle analysis findings</name>
  <files>.planning/phases/10-technical-debt-monitoring/10-05-SUMMARY.md</files>
  <action>
    Document analysis findings:
    - Note total bundle size for client and server
    - List top 5 largest dependencies
    - Identify pages with largest bundles
    - Note any unexpected large dependencies
    - Suggest specific optimizations:
      - Dynamic imports for large libraries
      - Code splitting opportunities
      - Tree shaking improvements
      - Alternative smaller libraries
    - Create recommendations for Plan 10-06 (parallel data fetching)
  </action>
  <verify>`test -f .planning/phases/10-technical-debt-monitoring/10-05-SUMMARY.md && grep -E "bundle|size|optimization" .planning/phases/10-technical-debt-monitoring/10-05-SUMMARY.md`</verify>
  <done>Bundle analysis findings documented with specific optimization recommendations.</done>
</task>

</tasks>

<verification>
`npm run analyze` completes successfully, bundle analyzer reports open in browser showing client and server bundle sizes, and findings are documented for optimization.
</verification>

<success_criteria>
Bundle analyzer is integrated and can be triggered with ANALYZE=true, visual reports show bundle composition and identify large modules, and optimization opportunities are documented.
</success_criteria>

<output>
After completion, create `.planning/phases/10-technical-debt-monitoring/10-05-SUMMARY.md`
</output>
