---
phase: 10-technical-debt-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/reports.ts
  - src/app/(dashboard)/students/[id]/report/actions.ts
  - src/app/api/students/[id]/report/route.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - "fetchReportData function is extracted to shared module in src/lib/db/reports.ts"
    - "Both Server Actions and API Routes import fetchReportData from shared module"
    - "No code duplication exists between actions.ts and route.ts for report data fetching"
  artifacts:
    - path: "src/lib/db/reports.ts"
      provides: "Shared fetchReportData function for report data aggregation"
      exports: ["fetchReportData"]
    - path: "src/app/(dashboard)/students/[id]/report/actions.ts"
      provides: "Server action using shared fetchReportData"
      imports: ["fetchReportData from @/lib/db/reports"]
    - path: "src/app/api/students/[id]/report/route.ts"
      provides: "API route using shared fetchReportData"
      imports: ["fetchReportData from @/lib/db/reports"]
  key_links:
    - from: "src/app/(dashboard)/students/[id]/report/actions.ts"
      to: "src/lib/db/reports.ts"
      via: "import { fetchReportData }"
      pattern: "fetchReportData\\(studentId, teacherId\\)"
    - from: "src/app/api/students/[id]/report/route.ts"
      to: "src/lib/db/reports.ts"
      via: "import { fetchReportData }"
      pattern: "fetchReportData\\(studentId, teacherId\\)"
---

<objective>
Extract duplicated fetchReportData function to a shared module in src/lib/db/reports.ts to eliminate code duplication between Server Actions and API Routes.

Purpose: Remove code duplication identified in Phase 9 by consolidating fetchReportData logic into a single shared module that both Server Actions and API Routes can import from.
Output: Shared fetchReportData function in src/lib/db/reports.ts, updated imports in actions.ts and route.ts.
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-technical-debt-monitoring/10-CONTEXT.md
@.planning/phases/10-technical-debt-monitoring/10-RESEARCH.md
@src/lib/db/reports.ts
@src/app/(dashboard)/students/[id]/report/actions.ts
@src/app/api/students/[id]/report/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract fetchReportData to shared module</name>
  <files>src/lib/db/reports.ts</files>
  <action>
    Add the fetchReportData function to src/lib/db/reports.ts:
    - Copy the existing fetchReportData implementation from either actions.ts or route.ts
    - Add proper JSDoc documentation explaining the function purpose and return type
    - Export the function as a named export
    - Ensure the function includes all necessary relations: images, sajuAnalysis, nameAnalysis, mbtiAnalysis, faceAnalysis, palmAnalysis, personalitySummary
    - Return structured data with student info, analyses, personalitySummary, and generatedAt timestamp
  </action>
  <verify>`grep -n "export.*fetchReportData" src/lib/db/reports.ts`</verify>
  <done>src/lib/db/reports.ts exports fetchReportData function with complete implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Update Server Actions to use shared fetchReportData</name>
  <files>src/app/(dashboard)/students/[id]/report/actions.ts</files>
  <action>
    Update the Server Actions file:
    - Remove the local fetchReportData function (lines 142-218)
    - Add import statement: `import { fetchReportData } from '@/lib/db/reports'`
    - Verify that generateConsultationReport function still calls fetchReportData correctly
    - Ensure no other code changes are needed (function signature is identical)
  </action>
  <verify>`grep -n "import.*fetchReportData.*from.*@/lib/db/reports" src/app/\(dashboard\)/students/\[id\]/report/actions.ts`</verify>
  <done>Server Actions imports and uses shared fetchReportData from lib/db/reports.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Update API Route to use shared fetchReportData</name>
  <files>src/app/api/students/[id]/report/route.ts</files>
  <action>
    Update the API Route file:
    - Remove the local fetchReportData function (lines 135-211)
    - Add import statement: `import { fetchReportData } from '@/lib/db/reports'`
    - Verify that GET handler still calls fetchReportData correctly
    - Remove the TODO comment about extracting to shared function
    - Ensure no other code changes are needed (function signature is identical)
  </action>
  <verify>`grep -n "import.*fetchReportData.*from.*@/lib/db/reports" src/app/api/students/\[id\]/report/route.ts`</verify>
  <done>API Route imports and uses shared fetchReportData from lib/db/reports.ts.</done>
</task>

<task type="auto">
  <name>Task 4: Verify no code duplication remains</name>
  <files>src/app/(dashboard)/students/[id]/report/actions.ts src/app/api/students/[id]/report/route.ts</files>
  <action>
    Verify code deduplication:
    - Search for any remaining fetchReportData function definitions in actions.ts and route.ts
    - Confirm both files only import fetchReportData, not define it locally
    - Run `npm run lint` to ensure no import/export errors
    - Run TypeScript compiler to verify type checking passes
  </action>
  <verify>`! grep -n "async function fetchReportData" src/app/\(dashboard\)/students/\[id\]/report/actions.ts src/app/api/students/\[id\]/report/route.ts`</verify>
  <done>No local fetchReportData definitions remain; both files import from shared module.</done>
</task>

</tasks>

<verification>
`npm run lint` passes and no duplicate fetchReportData function definitions exist in the codebase.
</verification>

<success_criteria>
fetchReportData is centralized in src/lib/db/reports.ts and both Server Actions and API Routes import from this shared module, eliminating code duplication.
</success_criteria>

<output>
After completion, create `.planning/phases/10-technical-debt-monitoring/10-01-SUMMARY.md`
</output>
