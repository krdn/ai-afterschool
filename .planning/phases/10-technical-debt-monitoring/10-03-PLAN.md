---
phase: 10-technical-debt-monitoring
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/logger/index.ts
  - src/lib/logger/request.ts
  - src/middleware.ts
  - .env.local
  - .env.example
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Pino logger is configured with JSON output for production and pretty printing for development"
    - "Request ID is generated for each request and attached to all logs via child logger"
    - "Sensitive data (passwords, tokens, API keys) is automatically redacted from log output"
    - "Logs are structured with consistent fields (timestamp, level, message, context)"
    - "Middleware attaches request ID to response headers for distributed tracing"
  artifacts:
    - path: "src/lib/logger/index.ts"
      provides: "Logger factory with environment-aware configuration"
      exports: ["logger"]
    - path: "src/lib/logger/request.ts"
      provides: "Request-scoped logger with request ID and metadata"
      exports: ["createRequestLogger", "RequestLogger"]
    - path: "src/middleware.ts"
      provides: "Next.js middleware for request ID generation and logging"
      exports: ["middleware"]
  key_links:
    - from: "src/middleware.ts"
      to: "src/lib/logger/request.ts"
      via: "createRequestLogger"
      pattern: "createRequestLogger\\(req\\)"
    - from: "src/middleware.ts"
      to: "response headers"
      via: "x-request-id"
      pattern: "response\\.headers\\.set\\('x-request-id'"
---

<objective>
Implement structured JSON logging with Pino for production-ready log management, including request ID tracing and automatic sensitive data redaction.

Purpose: Replace console.log with structured logging using Pino for better searchability, filtering, and performance in production environments, while maintaining human-readable logs in development.
Output: Configured Pino logger with request ID tracing, sensitive data redaction, and environment-aware formatting.
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-technical-debt-monitoring/10-CONTEXT.md
@.planning/phases/10-technical-debt-monitoring/10-RESEARCH.md
@package.json
@src/middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Pino and dependencies</name>
  <files>package.json package-lock.json</files>
  <action>
    Install Pino logging libraries:
    - Run `npm install pino pino-pretty uuid`
    - Run `npm install -D @types/uuid` (if TypeScript types are not included)
    - Verify installations in package.json dependencies
    - No other configuration needed yet
  </action>
  <verify>`grep -E "pino|uuid" package.json`</verify>
  <done>Pino, pino-pretty, and uuid packages installed for structured logging.</done>
</task>

<task type="auto">
  <name>Task 2: Create logger factory with environment-aware configuration</name>
  <files>src/lib/logger/index.ts</files>
  <action>
    Create src/lib/logger/index.ts:
    - Import pino from 'pino'
    - Detect environment: NODE_ENV === 'development', 'test', or 'production'
    - Configure logger options:
      - level: process.env.LOG_LEVEL or 'debug' (dev), 'info' (prod)
      - transport: for development only, use pino-pretty with colorize, ignore, translateTime options
      - enabled: false for test environment
      - redact: array of sensitive paths ['password', 'token', 'apiKey', '*.password', '*.token', 'req.headers.authorization', 'req.headers.cookie']
      - remove: true (redacted values are completely removed)
      - base: object with env and version fields
    - Export named logger instance: `export const logger = createLogger()`
  </action>
  <verify>`test -f src/lib/logger/index.ts && grep -E "pino|redact|logger" src/lib/logger/index.ts`</verify>
  <done>Logger factory created with environment-aware formatting and sensitive data redaction.</done>
</task>

<task type="auto">
  <name>Task 3: Create request-scoped logger factory</name>
  <files>src/lib/logger/request.ts</files>
  <action>
    Create src/lib/logger/request.ts:
    - Import { v4 as uuidv4 } from 'uuid'
    - Import { logger } from './index'
    - Import type { NextRequest } from 'next/server'
    - Define createRequestLogger function that accepts NextRequest:
      - Generate or extract request ID from headers: `req.headers.get('x-request-id') || uuidv4()`
      - Extract metadata: method, pathname, IP (from x-forwarded-for or x-real-ip headers)
      - Create child logger with logger.child() passing request context
      - Return child logger instance
    - Export RequestLogger type as ReturnType<typeof createRequestLogger>
    - Export createRequestLogger function
  </action>
  <verify>`test -f src/lib/logger/request.ts && grep -E "createRequestLogger|child|requestId" src/lib/logger/request.ts`</verify>
  <done>Request-scoped logger factory created with request ID generation and child logger pattern.</done>
</task>

<task type="auto">
  <name>Task 4: Update or create middleware for request logging</name>
  <files>src/middleware.ts</files>
  <action>
    Update or create src/middleware.ts:
    - Import createRequestLogger from '@/lib/logger/request'
    - Import { NextResponse } from 'next/server'
    - Define middleware function that accepts NextRequest:
      - Create request logger: `const log = createRequestLogger(req)`
      - Log incoming request: `log.info('Incoming request')` or similar
      - Create NextResponse.next() response
      - Attach request ID to response headers: `response.headers.set('x-request-id', log.bindings().requestId)`
      - Return response
    - Export matcher config to exclude static files and API routes that don't need logging:
      - matcher: ['/((?!_next/static|_next/image|favicon.ico).*)']
  </action>
  <verify>`test -f src/middleware.ts && grep -E "createRequestLogger|x-request-id|middleware" src/middleware.ts`</verify>
  <done>Middleware configured to generate request ID, log requests, and attach ID to response headers.</done>
</task>

<task type="auto">
  <name>Task 5: Add environment variables for logging configuration</name>
  <files>.env.local .env.example</files>
  <action>
    Add logging environment variables:
    - To .env.local (for local development):
      - LOG_LEVEL=debug (or info, warn, error)
    - To .env.example (as template):
      - LOG_LEVEL=info
    - Add comments explaining:
      - LOG_LEVEL: Controls logging verbosity (debug, info, warn, error)
      - Lower levels include higher levels (debug includes everything)
    - Document that in production, 'info' is recommended for performance
  </action>
  <verify>`grep "LOG_LEVEL" .env.example`</verify>
  <done>Environment variable for log level configured with documentation.</done>
</task>

<task type="auto">
  <name>Task 6: Replace console.log with structured logging in key files</name>
  <files>src/app/api/students/[id]/report/route.ts src/app/(dashboard)/students/[id]/report/actions.ts</files>
  <action>
    Replace console.log with structured logging:
    - In src/app/api/students/[id]/report/route.ts:
      - Import logger from '@/lib/logger'
      - Replace console.error('Cached PDF access error:', fileError) with logger.error({ error: fileError }, 'Cached PDF access error')
      - Replace console.error('PDF generation error:', error) with logger.error({ error }, 'PDF generation error')
    - In src/app/(dashboard)/students/[id]/report/actions.ts:
      - Import logger from '@/lib/logger'
      - Add logging for PDF generation start, success, and failure
    - Update any other console.log/console.error statements found in the codebase
    - Use structured format: logger.level({ context }, 'message')
  </action>
  <verify>`! grep -r "console\\.log|console\\.error" src/app/api/students/ src/app/\(dashboard\)/students/ | grep -v node_modules`</verify>
  <done>Console statements replaced with structured logging in report generation code.</done>
</task>

<task type="auto">
  <name>Task 7: Test logging in development and production modes</name>
  <files></files>
  <action>
    Test logging configuration:
    - Development mode:
      - Run `npm run dev`
      - Trigger API requests and verify logs are pretty-printed with colors
      - Check that request ID appears in logs
      - Verify sensitive data is redacted (check logs with passwords/tokens)
    - Production mode:
      - Run `npm run build && npm start`
      - Trigger API requests and verify logs are JSON formatted
      - Check that logs are structured with timestamp, level, message, requestId
      - Verify request ID is consistent across log entries for same request
      - Test that response headers contain x-request-id
  </action>
  <verify>`npm run lint && npm run build`</verify>
  <done>Logging tested in both development (pretty) and production (JSON) modes with request ID tracing.</done>
</task>

</tasks>

<verification>
Logs are JSON formatted in production with request ID tracing, pretty-printed in development, and sensitive data is redacted from all log output.
</verification>

<success_criteria>
Pino logger is integrated with request ID middleware, logs are structured consistently, and sensitive data is automatically redacted across all environments.
</success_criteria>

<output>
After completion, create `.planning/phases/10-technical-debt-monitoring/10-03-SUMMARY.md`
</output>
