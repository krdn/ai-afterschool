---
phase: 10-technical-debt-monitoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - sentry.server.config.ts
  - sentry.edge.config.ts
  - sentry.client.config.ts
  - instrumentation.ts
  - next.config.ts
  - app/global-error.tsx
  - .env.local
  - .env.example
autonomous: true
user_setup:
  - service: sentry
    why: "Application error tracking with source maps"
    env_vars:
      - name: NEXT_PUBLIC_SENTRY_DSN
        source: "Sentry Dashboard -> Client Keys (DSN)"
      - name: SENTRY_AUTH_TOKEN
        source: "Sentry Dashboard -> Auth Tokens"
      - name: SENTRY_ORG
        source: "Sentry Organization slug"
      - name: SENTRY_PROJECT
        source: "Sentry Project slug"
    dashboard_config:
      - "Create Sentry project for Next.js application"
      - "Configure source maps upload in project settings"
      - "Enable release tracking"
must_haves:
  truths:
    - "Sentry SDK is installed and configured for all Next.js runtimes (client, server, edge)"
    - "Source maps are automatically uploaded to Sentry on production builds"
    - "Application errors are captured and reported to Sentry with readable stack traces"
    - "Global error boundary renders fallback UI and captures client-side errors"
    - "Sensitive data (passwords, tokens) is filtered from error reports"
  artifacts:
    - path: "sentry.server.config.ts"
      provides: "Sentry configuration for server-side runtime"
      contains: "Sentry.init"
    - path: "sentry.edge.config.ts"
      provides: "Sentry configuration for edge runtime"
      contains: "Sentry.init"
    - path: "sentry.client.config.ts"
      provides: "Sentry configuration for client-side runtime"
      contains: "Sentry.init"
    - path: "instrumentation.ts"
      provides: "Next.js instrumentation hook for Sentry initialization"
      exports: ["register", "onRequestError"]
    - path: "app/global-error.tsx"
      provides: "Global error boundary for client-side error capture"
      exports: ["default"]
    - path: "next.config.ts"
      provides: "Sentry wrapper configuration with source maps upload"
      contains: "withSentryConfig"
  key_links:
    - from: "instrumentation.ts"
      to: "sentry.server.config.ts"
      via: "dynamic import"
      pattern: "import.*sentry\\.server\\.config"
    - from: "instrumentation.ts"
      to: "sentry.edge.config.ts"
      via: "dynamic import"
      pattern: "import.*sentry\\.edge\\.config"
    - from: "app/global-error.tsx"
      to: "@sentry/nextjs"
      via: "Sentry.captureException"
      pattern: "Sentry\\.captureException"
---

<objective>
Integrate Sentry error tracking with automatic source maps upload for production-ready error monitoring across all Next.js runtime environments.

Purpose: Implement comprehensive error tracking with readable stack traces by integrating Sentry SDK with automatic source maps upload for client, server, and edge runtimes.
Output: Configured Sentry integration with source maps, global error boundary, and sensitive data filtering.
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-technical-debt-monitoring/10-CONTEXT.md
@.planning/phases/10-technical-debt-monitoring/10-RESEARCH.md
@package.json
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Sentry SDK and run setup wizard</name>
  <files>package.json package-lock.json</files>
  <action>
    Install @sentry/nextjs SDK:
    - Run `npm install @sentry/nextjs`
    - Run `npx @sentry/wizard@latest -i nextjs` to launch the setup wizard
    - Follow wizard prompts to configure Sentry project
    - The wizard will create sentry.*.config.ts files and update next.config.ts
    - Keep track of the DSN and auth token generated by the wizard
  </action>
  <verify>`grep "@sentry/nextjs" package.json`</verify>
  <done>@sentry/nextjs is installed and wizard has created base configuration files.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Sentry server-side settings</name>
  <files>sentry.server.config.ts</files>
  <action>
    Configure sentry.server.config.ts:
    - Set DSN from environment variable: `process.env.NEXT_PUBLIC_SENTRY_DSN`
    - Set environment: `process.env.NODE_ENV`
    - Configure tracesSampleRate: 1.0 for development, 0.1 for production
    - Add beforeSend filter to redact sensitive data:
      - Replace password patterns: `password=\w+` → `password=***`
      - Replace token patterns: `token=[\w-]+` → `token=***`
      - Replace API key patterns: `apiKey=[\w-]+` → `apiKey=***`
    - Set integrations to capture context (breadcrumbs, http, etc.)
  </action>
  <verify>`grep -E "beforeSend|password|token" sentry.server.config.ts`</verify>
  <done>Server-side Sentry configuration initialized with DSN and sensitive data filtering.</done>
</task>

<task type="auto">
  <name>Task 3: Configure Sentry edge runtime settings</name>
  <files>sentry.edge.config.ts</files>
  <action>
    Configure sentry.edge.config.ts:
    - Set DSN from environment variable: `process.env.NEXT_PUBLIC_SENTRY_DSN`
    - Set environment: `process.env.NODE_ENV`
    - Configure tracesSampleRate for edge runtime
    - Add beforeSend filter for sensitive data (same as server config)
    - Keep edge-specific integrations minimal for performance
  </action>
  <verify>`test -f sentry.edge.config.ts && grep -E "Sentry\\.init" sentry.edge.config.ts`</verify>
  <done>Edge runtime Sentry configuration initialized with sensitive data filtering.</done>
</task>

<task type="auto">
  <name>Task 4: Configure Sentry client-side settings</name>
  <files>sentry.client.config.ts</files>
  <action>
    Configure sentry.client.config.ts:
    - Set DSN from environment variable: `process.env.NEXT_PUBLIC_SENTRY_DSN`
    - Set environment: `process.env.NODE_ENV`
    - Configure tracesSampleRate for client-side errors
    - Add beforeSend filter to redact sensitive data from client errors
    - Enable user feedback integration (optional, for error reports)
    - Configure replay session sampling (if using Session Replay)
  </action>
  <verify>`test -f sentry.client.config.ts && grep -E "Sentry\\.init" sentry.client.config.ts`</verify>
  <done>Client-side Sentry configuration initialized with sensitive data filtering.</done>
</task>

<task type="auto">
  <name>Task 5: Create instrumentation hook for Sentry</name>
  <files>instrumentation.ts</files>
  <action>
    Create instrumentation.ts in project root:
    - Import Sentry from @sentry/nextjs
    - Implement register() function that conditionally loads server/edge configs based on NEXT_RUNTIME
    - Export onRequestError handler: `export const onRequestError = Sentry.captureRequestError`
    - Ensure the file is at the root level (not in src/) for Next.js to detect it
    - Add type definitions for the instrumentation hook
  </action>
  <verify>`test -f instrumentation.ts && grep -E "register|onRequestError" instrumentation.ts`</verify>
  <done>Instrumentation hook created for automatic Sentry initialization across runtimes.</done>
</task>

<task type="auto">
  <name>Task 6: Create global error boundary</name>
  <files>app/global-error.tsx</files>
  <action>
    Create app/global-error.tsx error boundary:
    - Add 'use client' directive (required for error boundaries)
    - Import useEffect and Sentry from @sentry/nextjs
    - Accept error and reset properties as props
    - Use useEffect to capture exception with Sentry.captureException
    - Return fallback HTML with error message and reset button
    - Style the error page to match app design
  </action>
  <verify>`test -f app/global-error.tsx && grep -E "captureException|global-error" app/global-error.tsx`</verify>
  <done>Global error boundary created to catch and report client-side errors.</done>
</task>

<task type="auto">
  <name>Task 7: Configure Next.js with Sentry wrapper</name>
  <files>next.config.ts</files>
  <action>
    Update next.config.ts to wrap with Sentry:
    - Import withSentryConfig from @sentry/nextjs
    - Wrap the existing nextConfig with withSentryConfig(nextConfig, options)
    - Configure options:
      - org: process.env.SENTRY_ORG
      - project: process.env.SENTRY_PROJECT
      - authToken: process.env.SENTRY_AUTH_TOKEN
      - sourcemaps:
        - disable: false
        - assets: ['**/*.js', '**/*.js.map']
        - ignore: ['**/node_modules/**']
        - deleteSourcemapsAfterUpload: true
      - tunnelRoute: '/monitoring' (optional, for bypassing ad-blockers)
      - silent: !process.env.CI (reduce noise in CI logs)
  </action>
  <verify>`grep -E "withSentryConfig|sourcemaps" next.config.ts`</verify>
  <done>Next.js configured to upload source maps to Sentry on production builds.</done>
</task>

<task type="auto">
  <name>Task 8: Add environment variables</name>
  <files>.env.local .env.example</files>
  <action>
    Add Sentry environment variables:
    - To .env.local (for local development):
      - NEXT_PUBLIC_SENTRY_DSN=your-dsn-here
      - SENTRY_AUTH_TOKEN=your-auth-token-here
      - SENTRY_ORG=your-org-slug
      - SENTRY_PROJECT=your-project-slug
    - To .env.example (as templates):
      - NEXT_PUBLIC_SENTRY_DSN=
      - SENTRY_AUTH_TOKEN=
      - SENTRY_ORG=
      - SENTRY_PROJECT=
    - Add comments explaining each variable's purpose
    - Ensure SENTRY_AUTH_TOKEN is never committed to git
  </action>
  <verify>`grep -E "NEXT_PUBLIC_SENTRY_DSN|SENTRY_AUTH_TOKEN" .env.example`</verify>
  <done>Environment variables configured for Sentry integration with documentation.</done>
</task>

<task type="auto">
  <name>Task 9: Test Sentry integration</name>
  <files></files>
  <action>
    Test Sentry error tracking:
    - Run development server: `npm run dev`
    - Create a test route or modify existing route to throw an error
    - Verify error appears in Sentry dashboard with source maps
    - Test client-side errors by triggering error in browser
    - Test server-side errors by triggering error in API route
    - Verify sensitive data is redacted from error reports
    - Check that stack traces are readable (not minified)
    - Remove test error after verification
  </action>
  <verify>`curl -s https://sentry.io/api/0/ | grep -q "project"`</verify>
  <done>Sentry integration tested and verified to capture errors with readable stack traces.</done>
</task>

</tasks>

<verification>
`npm run build` completes successfully and source maps are uploaded to Sentry. Errors triggered in development and production appear in Sentry dashboard with readable stack traces and redacted sensitive data.
</verification>

<success_criteria>
Sentry is fully integrated with automatic source maps upload, global error boundary handles client errors, and all runtime environments (client, server, edge) report errors to Sentry with sensitive data filtered.
</success_criteria>

<output>
After completion, create `.planning/phases/10-technical-debt-monitoring/10-02-SUMMARY.md`
</output>
