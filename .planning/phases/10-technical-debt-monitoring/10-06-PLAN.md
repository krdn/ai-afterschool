---
phase: 10-technical-debt-monitoring
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - src/app/(dashboard)/students/[id]/page.tsx
  - src/app/(dashboard)/students/page.tsx
  - src/lib/actions/students.ts
  - src/lib/db/student-analysis.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Independent data sources are fetched in parallel using Promise.all()"
    - "Sequential await chains are replaced with parallel fetching where possible"
    - "Student detail page fetches all analyses concurrently"
    - "Student list page fetches data efficiently without waterfall requests"
    - "Page load times are improved by eliminating sequential data fetching"
  artifacts:
    - path: "src/lib/actions/students.ts"
      provides: "Student data fetching with parallel queries"
      contains: "Promise.all"
    - path: "src/lib/db/student-analysis.ts"
      provides: "Analysis data fetching utilities"
      contains: "Promise.all"
    - path: "src/app/(dashboard)/students/[id]/page.tsx"
      provides: "Student detail page with parallel data fetching"
      imports: ["Promise.all patterns"]
  key_links:
    - from: "src/app/(dashboard)/students/[id]/page.tsx"
      to: "data sources"
      via: "Promise.all"
      pattern: "Promise\\.all\\["
    - from: "src/lib/actions/students.ts"
      to: "database queries"
      via: "parallel execution"
      pattern: "await Promise\\.all"

---

<objective>
Implement parallel data fetching using Promise.all() to eliminate waterfall requests and improve page load performance across student detail and list pages.

Purpose: Replace sequential data fetching with parallel fetching using Promise.all() for independent data sources, reducing page load times and eliminating waterfall request patterns.
Output: Updated data fetching logic with parallel execution for independent queries.
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-technical-debt-monitoring/10-CONTEXT.md
@.planning/phases/10-technical-debt-monitoring/10-RESEARCH.md
@src/app/(dashboard)/students/[id]/page.tsx
@src/lib/actions/students.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze current data fetching patterns</name>
  <files>src/app/(dashboard)/students/[id]/page.tsx src/app/(dashboard)/students/page.tsx</files>
  <action>
    Analyze sequential data fetching:
    - Search for patterns of sequential await in page components
    - Identify which data sources are independent (can be fetched in parallel)
    - Check for waterfall patterns where one await blocks another
    - Look for opportunities to batch database queries
    - Review src/lib/actions/students.ts for sequential patterns
    - Document which fetches can be parallelized
  </action>
  <verify>`grep -n "await.*await" src/app/\(dashboard\)/students/\[id\]/page.tsx | head -5`</verify>
  <done>Current data fetching patterns analyzed and parallelization opportunities identified.</done>
</task>

<task type="auto">
  <name>Task 2: Update student detail page for parallel fetching</name>
  <files>src/app/(dashboard)/students/[id]/page.tsx</files>
  <action>
    Update student detail page to fetch data in parallel:
    - Identify independent data sources: student, sajuAnalysis, nameAnalysis, mbtiAnalysis, faceAnalysis, palmAnalysis, personalitySummary
    - Replace sequential awaits with Promise.all:
      - Create separate promises for each data source
      - Use `await Promise.all([studentPromise, sajuPromise, mbtiPromise, ...])`
      - Destructure results: `const [student, saju, mbti] = await Promise.all(...)`
    - Ensure dependencies are respected (e.g., analyses depend on student ID)
    - Keep related sequential data together if there are dependencies
    - Update type definitions if needed
  </action>
  <verify>`grep -E "Promise\\.all|await Promise" src/app/\(dashboard\)/students/\[id\]/page.tsx`</verify>
  <done>Student detail page updated to fetch independent data sources in parallel.</done>
</task>

<task type="auto">
  <name>Task 3: Update student list page for parallel fetching</name>
  <files>src/app/(dashboard)/students/page.tsx</files>
  <action>
    Update student list page for parallel data fetching:
    - Identify independent data sources: students list, pagination info, search results
    - Replace sequential awaits with Promise.all pattern
    - If fetching multiple independent lists, batch the queries
    - Ensure search and filter queries run in parallel with count queries
    - Update any component state management for parallel results
  </action>
  <verify>`grep -E "Promise\\.all|await Promise" src/app/\(dashboard\)/students/page.tsx`</verify>
  <done>Student list page updated to fetch data in parallel where possible.</done>
</task>

<task type="auto">
  <name>Task 4: Update library functions for parallel fetching</name>
  <files>src/lib/actions/students.ts src/lib/db/student-analysis.ts</files>
  <action>
    Update library functions to support parallel fetching:
    - In src/lib/actions/students.ts:
      - Look for functions that fetch multiple student-related data
      - Create helper functions that return promises
      - Use Promise.all for independent queries within the same function
    - In src/lib/db/student-analysis.ts:
      - Check for sequential fetching of analyses
      - Create parallel fetching utility for all analyses
      - Export a function like `fetchAllAnalyses(studentId)` that uses Promise.all
    - Ensure error handling works correctly with Promise.all (use Promise.allSettled if needed)
  </action>
  <verify>`grep -E "Promise\\.all|fetchAllAnalyses" src/lib/actions/students.ts src/lib/db/student-analysis.ts`</verify>
  <done>Library functions updated to support parallel data fetching patterns.</done>
</task>

<task type="auto">
  <name>Task 5: Update fetchReportData for parallel fetching (if applicable)</name>
  <files>src/lib/db/reports.ts</files>
  <action>
    Review and update fetchReportData function:
    - Check if fetchReportData can benefit from parallel fetching
    - The Prisma query with `include` is already optimized, but verify:
      - All relations are fetched in a single query (already optimal)
      - No additional sequential queries are made
      - Consider if any post-processing can be parallelized
    - If the function is already optimal (single Prisma query with include), document this
    - If there are opportunities for parallel post-processing, implement them
  </action>
  <verify>`grep -A 20 "export.*fetchReportData" src/lib/db/reports.ts | grep -E "include|findFirst"`</verify>
  <done>fetchReportData reviewed and verified for optimal parallel fetching (single query with include).</done>
</task>

<task type="auto">
  <name>Task 6: Test parallel fetching with load time measurement</name>
  <files></files>
  <action>
    Test and measure performance improvements:
    - Before: Measure page load time with sequential fetching (use browser DevTools Network tab)
    - After: Measure page load time with parallel fetching
    - Compare waterfall charts in DevTools
    - Verify that independent requests are fired simultaneously
    - Check for any errors or race conditions
    - Test with slow network conditions to verify improvement
    - Document performance gains (e.g., "page load time reduced from 1.2s to 0.6s")
  </action>
  <verify>`npm run build && npm run dev`</verify>
  <done>Parallel fetching tested and performance improvements measured and documented.</done>
</task>

<task type="auto">
  <name>Task 7: Add error handling for parallel requests</name>
  <files>src/app/(dashboard)/students/[id]/page.tsx src/app/(dashboard)/students/page.tsx</files>
  <action>
    Ensure robust error handling for parallel requests:
    - Wrap Promise.all in try-catch blocks
    - Consider using Promise.allSettled for non-critical parallel data
    - Add proper error messages when parallel requests fail
    - Handle partial failures gracefully
    - Add loading states that work with parallel fetching
    - Ensure UI doesn't break when one parallel request fails
    - Test error scenarios to verify graceful degradation
  </action>
  <verify>`grep -E "try.*catch|Promise\\.allSettled" src/app/\(dashboard\)/students/\[id\]/page.tsx`</verify>
  <done>Error handling added for parallel data fetching with graceful degradation.</done>
</task>

</tasks>

<verification>
Page load times are improved as shown in DevTools waterfall charts, independent requests are fired simultaneously, and error handling works correctly for parallel requests.
</verification>

<success_criteria>
Independent data sources are fetched in parallel using Promise.all(), sequential waterfall patterns are eliminated, and page load times are measurably improved.
</success_criteria>

<output>
After completion, create `.planning/phases/10-technical-debt-monitoring/10-06-SUMMARY.md`
</output>
