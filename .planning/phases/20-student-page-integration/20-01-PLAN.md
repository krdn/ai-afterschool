---
phase: 20-student-page-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/alert.tsx
  - src/components/ui/dialog.tsx
  - package.json
autonomous: true

must_haves:
  truths:
    - "shadcn/ui Alert component is installed and importable"
    - "shadcn/ui Dialog component is installed and importable"
    - "Alert component renders without errors"
    - "Dialog component renders without errors"
  artifacts:
    - path: "src/components/ui/alert.tsx"
      provides: "shadcn/ui Alert component for upcoming reservation display"
      exports: ["Alert", "AlertDescription", "AlertTitle"]
    - path: "src/components/ui/dialog.tsx"
      provides: "shadcn/ui Dialog component for session detail modal"
      exports: ["Dialog", "DialogContent", "DialogDescription", "DialogHeader", "DialogTitle"]
  key_links:
    - from: "src/components/counseling/CounselingSection.tsx"
      to: "@/components/ui/alert"
      via: "import statement"
      pattern: "import.*from.*@/components/ui/alert"
    - from: "src/components/counseling/CounselingSessionModal.tsx"
      to: "@/components/ui/dialog"
      via: "import statement"
      pattern: "import.*from.*@/components/ui/dialog"
---

<objective>
Install shadcn/ui Alert and Dialog components for displaying upcoming reservations and session details in the student counseling section.

Purpose: Phase 20 requires displaying upcoming reservations using Alert component and session details in Dialog modal. These shadcn/ui components don't exist in the codebase yet.

Output: Installed and functional Alert and Dialog components ready for use in counseling section.
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-student-page-integration/20-RESEARCH.md
@.planning/phases/20-student-page-integration/20-CONTEXT.md

# Key decision from CONTEXT.md
> **표시 스타일**: shadcn/ui Alert 패턴 사용
> **클릭 동작**: 상세 모달로 상담 내용 표시

# Research finding
> shadcn/ui Alert component does NOT exist in codebase
> Dialog component needs to be added via shadcn CLI
</context>

<tasks>

<task type="auto">
  <name>Install shadcn/ui Alert component</name>
  <files>src/components/ui/alert.tsx, package.json</files>
  <action>
    Run shadcn CLI to add Alert component:
    ```bash
    npx shadcn@latest add alert
    ```

    This will:
    1. Create src/components/ui/alert.tsx with Alert, AlertTitle, AlertDescription components
    2. Update package.json if any dependencies are needed
    3. Follow existing shadcn/ui patterns in the codebase (tabs, badge, button, etc.)

    Expected exports:
    - Alert: Main container component
    - AlertTitle: Title/heading component
    - AlertDescription: Description/content component

    Do NOT modify any existing files. The CLI handles component creation.
  </action>
  <verify>File exists: src/components/ui/alert.tsx</verify>
  <done>alert.tsx contains Alert, AlertTitle, AlertDescription exports</done>
</task>

<task type="auto">
  <name>Install shadcn/ui Dialog component</name>
  <files>src/components/ui/dialog.tsx, package.json</files>
  <action>
    Run shadcn CLI to add Dialog component:
    ```bash
    npx shadcn@latest add dialog
    ```

    This will:
    1. Create src/components/ui/dialog.tsx with Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle components
    2. Update package.json if any dependencies are needed
    3. Follow existing shadcn/ui patterns (similar to AlertDialog which already exists)

    Expected exports:
    - Dialog: Main dialog container
    - DialogContent: Content wrapper
    - DialogDescription: Description text
    - DialogHeader: Header section
    - DialogTitle: Title text

    Do NOT modify any existing files. The CLI handles component creation.
  </action>
  <verify>File exists: src/components/ui/dialog.tsx</verify>
  <done>dialog.tsx contains Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle exports</done>
</task>

<task type="auto">
  <name>Verify component imports work</name>
  <files>src/components/ui/alert.tsx, src/components/ui/dialog.tsx</files>
  <action>
    Create a simple test file to verify both components can be imported and used:

    Create src/components/counseling/__test-components.tsx (temporary test file):
    ```typescript
    import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"
    import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog"

    // Test that components are exported
    console.log("Alert:", Alert)
    console.log("Dialog:", Dialog)

    export { Alert, Dialog }
    ```

    Run TypeScript compiler to verify no import errors:
    ```bash
    npx tsc --noEmit --skipLibCheck src/components/counseling/__test-components.tsx
    ```

    Delete the test file after verification:
    ```bash
    rm src/components/counseling/__test-components.tsx
    ```

    This ensures both components are properly exportable and TypeScript types are correct.
  </action>
  <verify>TypeScript compilation succeeds without import errors</verify>
  <done>Both Alert and Dialog components can be imported without errors</done>
</task>

</tasks>

<verification>
- [ ] src/components/ui/alert.tsx exists and exports Alert, AlertTitle, AlertDescription
- [ ] src/components/ui/dialog.tsx exists and exports Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle
- [ ] Components follow existing shadcn/ui patterns (similar to tabs, badge, button)
- [ ] No TypeScript errors when importing components
</verification>

<success_criteria>
Both shadcn/ui Alert and Dialog components are installed and ready for use in the counseling section implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/20-student-page-integration/20-01-SUMMARY.md`
</output>
