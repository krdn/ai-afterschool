---
phase: 02-file-infrastructure
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - package.json
  - package-lock.json
  - src/components/students/student-form.tsx
  - src/components/students/student-detail.tsx
  - src/components/students/student-image-uploader.tsx
  - src/components/students/student-image-tabs.tsx
  - src/app/(dashboard)/students/[id]/page.tsx
  - src/app/(dashboard)/students/[id]/edit/page.tsx
  - src/app/(dashboard)/students/new/page.tsx
autonomous: true
must_haves:
  truths:
    - "Teacher can upload profile/face/palm images from student create/edit and detail screens."
    - "Uploaded images render in the student detail header with tabbed switching and placeholders when empty."
    - "Images can be replaced and removed per slot without affecting other student data."
  artifacts:
    - path: "src/components/students/student-image-uploader.tsx"
      provides: "Cloudinary widget upload UI and metadata capture"
    - path: "src/components/students/student-image-tabs.tsx"
      provides: "Tabbed image display with preview/placeholder"
    - path: "src/components/students/student-detail.tsx"
      provides: "Student detail header wiring for image tabs and actions"
  key_links:
    - from: "src/components/students/student-image-uploader.tsx"
      to: "/api/cloudinary/sign"
      via: "CldUploadWidget signatureEndpoint"
      pattern: "signatureEndpoint=\"/api/cloudinary/sign\""
    - from: "src/components/students/student-form.tsx"
      to: "src/components/students/student-image-uploader.tsx"
      via: "passes studentId or draftId for folder selection"
      pattern: "studentId|draftId"
    - from: "src/components/students/student-image-uploader.tsx"
      to: "src/lib/actions/student-images.ts"
      via: "setStudentImage/deleteStudentImage calls"
      pattern: "setStudentImage|deleteStudentImage"
---

<objective>
Add the student image upload UI and display components, wired to Cloudinary signatures and image persistence actions.

Purpose: Enable teachers to manage profile/face/palm images from student forms and detail pages.
Output: Upload UI, tabbed image display, and page wiring for image data.
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-file-infrastructure/02-CONTEXT.md
@.planning/phases/02-file-infrastructure/02-RESEARCH.md
@src/components/students/student-form.tsx
@src/components/students/student-detail.tsx
@src/lib/actions/students.ts
@src/lib/actions/student-images.ts
@src/app/(dashboard)/students/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Cloudinary upload components for image slots</name>
  <files>
    package.json
    package-lock.json
    src/components/students/student-image-uploader.tsx
    src/components/students/student-image-tabs.tsx
  </files>
  <action>
    Install `next-cloudinary` and build two client components:
    - `StudentImageUploader` renders a drag/drop style panel + button that opens CldUploadWidget.
    - Configure widget with `signatureEndpoint: "/api/cloudinary/sign"`, `multiple: false`, `maxFiles: 1`,
      `clientAllowedFormats: ["jpg", "jpeg", "png", "heic"]`, and a folder naming convention driven by `studentId` or `draftId`.
      - If `studentId` exists (edit/detail), upload to `students/{studentId}/{type}`.
      - If `studentId` is missing (new form), upload to `students/drafts/{draftId}/{type}` where `draftId` is stable for the form session.
    - On success, normalize payload to `{ type, originalUrl, publicId, format, bytes, width, height }` and expose it via `onChange`.
    - `StudentImageTabs` renders three tabs (profile/face/palm) and shows a preview using `resizedUrl` when present; otherwise a placeholder with CTA.
    Keep styles consistent with existing Card/Input patterns (Tailwind utility classes only).
  </action>
  <verify>`npm run lint`</verify>
  <done>Reusable upload + tabbed display components exist and compile without type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire image uploads in student create/edit form</name>
  <files>
    src/components/students/student-form.tsx
    src/app/(dashboard)/students/new/page.tsx
    src/app/(dashboard)/students/[id]/edit/page.tsx
  </files>
  <action>
    Extend StudentForm to render image upload section using `StudentImageUploader` for each type.
    - Maintain local state for `profileImage`, `faceImage`, `palmImage` payloads.
    - When state exists, append hidden inputs with JSON strings to FormData keys `profileImage`, `faceImage`, `palmImage`.
    - For edit mode, pass `student.id` to uploaders and prefill state from existing `student.images` so previews show current images.
    - For new mode, generate a stable `draftId` (once per form render) and pass it to all uploaders so the sign endpoint can pick a draft folder.
    Ensure create/edit pages pass `student.images` into StudentForm when available.
  </action>
  <verify>`npm run lint`</verify>
  <done>Form submissions include image metadata payloads when selected, and previews render for existing images.</done>
</task>

<task type="auto">
  <name>Task 3: Wire image tabs and actions in student detail page</name>
  <files>
    src/components/students/student-detail.tsx
    src/app/(dashboard)/students/[id]/page.tsx
  </files>
  <action>
    Update the student detail page query to include `images` so the UI can render slots.
    In `StudentDetail`, add a top summary section containing `StudentImageTabs` and action buttons:
    - Upload/replace uses `StudentImageUploader` in "detail" mode to call `setStudentImage` directly.
    - Delete uses `deleteStudentImage` with confirmation.
    - Use the tab selection to switch between profile/face/palm slots.
    Keep the existing layout below intact (basic/academic cards).
  </action>
  <verify>`npm run lint`</verify>
  <done>Detail page renders tabbed image previews and supports upload/replace/delete per slot.</done>
</task>

</tasks>

<verification>
`npm run lint` passes.
</verification>

<success_criteria>
Student form and detail pages allow uploading, replacing, and deleting images for all three types with visible previews.
</success_criteria>

<output>
After completion, create `.planning/phases/02-file-infrastructure/02-03-SUMMARY.md`
</output>
