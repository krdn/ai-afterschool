---
phase: 24-missing-routes-creation
plan: 07
type: execute
wave: 4
depends_on: ["24-01"]
files_modified:
  - src/app/(dashboard)/admin/audit-logs/page.tsx
  - src/lib/actions/audit.ts
autonomous: true

must_haves:
  truths:
    - "/admin/audit-logs 접근 시 감사 로그가 테이블 형식으로 표시된다"
    - "각 감사 로그의 시간, 사용자, 작업, 변경내용이 표시된다"
    - "작업 유형별 필터링이 가능하다"
    - "로그가 시간순(최신 순)으로 정렬되어 표시된다"
  artifacts:
    - path: src/app/(dashboard)/admin/audit-logs/page.tsx
      provides: "감사 로그 조회 페이지"
      min_lines: 100
    - path: src/lib/actions/audit.ts
      provides: "감사 로그 조회 Server Action"
      exports: ["getAuditLogs"]
  key_links:
    - from: src/app/(dashboard)/admin/audit-logs/page.tsx
      to: src/lib/actions/audit.ts
      via: getAuditLogs()
      pattern: "getAuditLogs\\(\\{.*action.*page.*\\}\\)"
    - from: src/lib/actions/audit.ts
      to: prisma.schema.prisma
      via: Prisma Client
      pattern: "prisma\\.auditLog\\.findMany"
---

<objective>
감사 로그 조회 페이지를 생성하여 설정 변경 이력을 추적할 수 있습니다.

Purpose: 운영자가 누가 언제 무엇을 변경했는지 확인하여 시스템 변경 이력을 추적할 수 있습니다.
Output: /admin/audit-logs 페이지와 getAuditLogs Server Action
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-missing-routes-creation/24-CONTEXT.md
@.planning/phases/24-missing-routes-creation/24-RESEARCH.md
@prisma/schema.prisma
@src/app/(dashboard)/admin/llm-settings/page.tsx
@src/components/ui/table.tsx
</context>

<tasks>

<task type="auto">
  <name>감사 로그 조회 Server Action 생성</name>
  <files>src/lib/actions/audit.ts</files>
  <action>
    src/lib/actions/audit.ts 파일을 생성합니다:

    ```typescript
    'use server'

    import { verifySession } from '@/lib/dal'
    import { db } from '@/lib/db'
    import type { TeacherRole } from '@/lib/db/rbac'

    export interface AuditLogEntry {
      id: string
      teacherId: string
      teacherName: string
      action: string
      entityType: string
      entityId: string | null
      changes: Record<string, unknown> | null
      ipAddress: string | null
      createdAt: Date
    }

    export interface AuditLogsResult {
      logs: AuditLogEntry[]
      total: number
      page: number
      pageSize: number
    }

    export async function getAuditLogs(params: {
      action?: string
      page?: number
      pageSize?: number
    }): Promise<AuditLogsResult> {
      const session = await verifySession()

      // 권한 검증: 원장만 접근 가능
      if (session.role !== 'DIRECTOR') {
        return { logs: [], total: 0, page: 1, pageSize: 20 }
      }

      const page = params.page || 1
      const pageSize = params.pageSize || 20
      const skip = (page - 1) * pageSize

      const where = params.action && params.action !== 'ALL'
        ? { action: params.action }
        : {}

      const [logs, total] = await Promise.all([
        db.auditLog.findMany({
          where,
          include: {
            teacher: {
              select: {
                id: true,
                name: true,
              },
            },
          },
          orderBy: { createdAt: 'desc' },
          skip,
          take: pageSize,
        }),
        db.auditLog.count({ where }),
      ])

      return {
        logs: logs.map((log) => ({
          id: log.id,
          teacherId: log.teacherId,
          teacherName: log.teacher.name,
          action: log.action,
          entityType: log.entityType,
          entityId: log.entityId,
          changes: log.changes as Record<string, unknown> | null,
          ipAddress: log.ipAddress,
          createdAt: log.createdAt,
        })),
        total,
        page,
        pageSize,
      }
    }
    ```

    제약사항:
    - DIRECTOR 역할만 조회 가능합니다
    - 최신 순(내림차순) 정렬합니다
    - 페이지네이션을 지원합니다
    - teacher 정보를 include하여 함께 반환합니다
  </action>
  <verify>TypeScript 컴파일 에러가 없는지 확인</verify>
  <done>getAuditLogs Server Action이 생성되고 감사 로그 조회가 가능함</done>
</task>

<task type="auto">
  <name>감사 로그 페이지 생성</name>
  <files>src/app/(dashboard)/admin/audit-logs/page.tsx</files>
  <action>
    src/app/(dashboard)/admin/audit-logs/page.tsx 파일을 생성합니다:

    ```typescript
    import { redirect } from 'next/navigation'
    import { verifySession } from '@/lib/dal'
    import { getAuditLogs } from '@/lib/actions/audit'
    import Link from 'next/link'
    {
      /* Table, TableBody, TableCell, TableHead, TableHeader, TableRow */
    } from '@/components/ui/table'

    export const metadata = {
      title: '감사 로그 | AI AfterSchool',
      description: '설정 변경 이력 추적',
    }

    function getActionColor(action: string): string {
      switch (action) {
        case 'CREATE':
          return 'bg-green-100 text-green-800'
        case 'UPDATE':
          return 'bg-blue-100 text-blue-800'
        case 'DELETE':
          return 'bg-red-100 text-red-800'
        default:
          return 'bg-gray-100 text-gray-800'
      }
    }

    function formatChanges(changes: Record<string, unknown> | null): string {
      if (!changes) return '-'
      const entries = Object.entries(changes)
      if (entries.length === 0) return '-'
      return entries.map(([key, value]) => `${key}: ${JSON.stringify(value)}`).join(', ')
    }

    export default async function AuditLogsPage({
      searchParams,
    }: {
      searchParams: Promise<{ action?: string; page?: string }>
    }) {
      const session = await verifySession()
      if (!session || session.role !== 'DIRECTOR') {
        redirect('/dashboard')
      }

      const params = await searchParams
      const action = params.action || 'ALL'
      const page = parseInt(params.page || '1')

      const { logs, total } = await getAuditLogs({ action, page })

      const totalPages = Math.ceil(total / 20)

      return (
        <div className="container py-6 space-y-8">
          <div>
            <h1 className="text-2xl font-bold">감사 로그</h1>
            <p className="text-gray-500">
              시스템 설정 변경 이력을 추적합니다.
            </p>
          </div>

          {/* 작업 유형 필터 */}
          <div className="flex gap-2" data-testid="action-filter">
            <Link
              href="/admin/audit-logs"
              className={`px-3 py-1 rounded ${action === 'ALL' ? 'bg-blue-600 text-white' : 'bg-gray-200'}`}
              data-testid="filter-all"
            >
              전체
            </Link>
            <Link
              href="/admin/audit-logs?action=CREATE"
              className={`px-3 py-1 rounded ${action === 'CREATE' ? 'bg-green-600 text-white' : 'bg-gray-200'}`}
              data-testid="filter-create"
            >
              생성
            </Link>
            <Link
              href="/admin/audit-logs?action=UPDATE"
              className={`px-3 py-1 rounded ${action === 'UPDATE' ? 'bg-blue-600 text-white' : 'bg-gray-200'}`}
              data-testid="filter-update"
            >
              수정
            </Link>
            <Link
              href="/admin/audit-logs?action=DELETE"
              className={`px-3 py-1 rounded ${action === 'DELETE' ? 'bg-red-600 text-white' : 'bg-gray-200'}`}
              data-testid="filter-delete"
            >
              삭제
            </Link>
          </div>

          {/* 감사 로그 테이블 */}
          {logs.length === 0 ? (
            <div className="text-center py-12 text-gray-500" data-testid="no-logs">
              감사 로그가 없습니다.
            </div>
          ) : (
            <div className="border rounded-lg" data-testid="audit-logs-table">
              <Table>
                <TableHeader>
                  <TableRow>
                    <TableHead>시간</TableHead>
                    <TableHead>사용자</TableHead>
                    <TableHead>작업</TableHead>
                    <TableHead>대상</TableHead>
                    <TableHead>변경내용</TableHead>
                  </TableRow>
                </TableHeader>
                <TableBody>
                  {logs.map((log) => (
                    <TableRow key={log.id} data-testid="audit-log-row">
                      <TableCell data-testid="log-timestamp">
                        {new Date(log.createdAt).toLocaleString('ko-KR')}
                      </TableCell>
                      <TableCell data-testid="log-user">
                        {log.teacherName}
                      </TableCell>
                      <TableCell>
                        <span
                          className={`px-2 py-1 rounded text-xs font-medium ${getActionColor(log.action)}`}
                          data-testid="log-action"
                        >
                          {log.action}
                        </span>
                      </TableCell>
                      <TableCell data-testid="log-entity">
                        {log.entityType}
                        {log.entityId && <span className="text-gray-400"> ({log.entityId.slice(0, 8)}...)</span>}
                      </TableCell>
                      <TableCell>
                        <div
                          className="max-w-md truncate text-sm text-gray-600"
                          data-testid="log-changes"
                          title={formatChanges(log.changes)}
                        >
                          {formatChanges(log.changes)}
                        </div>
                      </TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            </div>
          )}

          {/* 페이지네이션 */}
          {totalPages > 1 && (
            <div className="flex gap-2" data-testid="pagination">
              {page > 1 && (
                <Link
                  href={`/admin/audit-logs?action=${action}&page=${page - 1}`}
                  className="px-3 py-1 border rounded"
                  data-testid="prev-page"
                >
                  이전
                </Link>
              )}
              <span className="px-3 py-1">
                {page} / {totalPages}
              </span>
              {page < totalPages && (
                <Link
                  href={`/admin/audit-logs?action=${action}&page=${page + 1}`}
                  className="px-3 py-1 border rounded"
                  data-testid="next-page"
                >
                  다음
                </Link>
              )}
            </div>
          )}
        </div>
      )
    }
    ```

    제약사항:
    - DIRECTOR 역할만 접근 가능합니다
    - data-testid: action-filter, filter-all, filter-create, filter-update, filter-delete, audit-logs-table, no-logs, audit-log-row, log-timestamp, log-user, log-action, log-entity, log-changes, pagination, prev-page, next-page 추가
  </action>
  <verify>/admin/audit-logs 접근 시 감사 로그가 올바르게 표시되는지 확인</verify>
  <done>감사 로그 페이지가 생성되고 필터링과 페이지네이션이 동작함</done>
</task>

</tasks>

<verification>
1. /admin/audit-logs 접근 시 감사 로그가 테이블 형식으로 표시됨
2. 작업 유형별 필터가 동작함
3. 로그가 최신 순으로 정렬됨
4. 페이지네이션이 동작함
5. DIRECTOR가 아닌 사용자 접근 시 리다이렉트됨
</verification>

<success_criteria>
1. 감사 로그 페이지가 생성되고 로그가 표시됨
2. 필터링과 페이지네이션이 동작함
3. data-testid 속성이 추가되어 E2E 테스트 가능함
</success_criteria>

<output>
After completion, create `.planning/phases/24-missing-routes-creation/24-07-SUMMARY.md`
</output>
