---
phase: 24-missing-routes-creation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/teams/page.tsx
  - src/app/(dashboard)/teams/[id]/page.tsx
  - src/lib/actions/teams.ts
autonomous: true

must_haves:
  truths:
    - "/teams 접근 시 팀 목록이 카드 형식으로 표시된다"
    - "팀 카드 클릭 시 해당 팀의 상세 페이지로 이동한다"
    - "팀 상세 페이지에서 팀 정보, 소속 선생님, 소속 학생이 표시된다"
    - "원장은 모든 팀을 조회할 수 있고, 일반 선생님은 자신의 팀만 조회할 수 있다"
  artifacts:
    - path: src/app/(dashboard)/teams/page.tsx
      provides: "팀 목록 페이지"
      min_lines: 80
    - path: src/app/(dashboard)/teams/[id]/page.tsx
      provides: "팀 상세 페이지"
      min_lines: 60
    - path: src/lib/actions/teams.ts
      provides: "팀 관련 Server Actions"
      exports: ["getTeams", "getTeamById"]
  key_links:
    - from: src/app/(dashboard)/teams/page.tsx
      to: src/lib/actions/teams.ts
      via: getTeams()
      pattern: "getTeams\\(\\)"
    - from: src/app/(dashboard)/teams/[id]/page.tsx
      to: src/lib/actions/teams.ts
      via: getTeamById(id)
      pattern: "getTeamById\\(id\\)"
---

<objective>
팀 목록 페이지와 팀 상세 페이지를 생성하여 팀 관리 기능의 UI를 완성합니다.

Purpose: 사용자가 팀을 조회하고 팀별 구성(선생님, 학생)을 확인할 수 있는 페이지를 제공합니다.
Output: /teams 목록 페이지와 /teams/[id] 상세 페이지
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-missing-routes-creation/24-CONTEXT.md
@.planning/phases/24-missing-routes-creation/24-RESEARCH.md
@src/app/(dashboard)/students/page.tsx
@src/app/(dashboard)/teams/[id]/layout.tsx
@src/lib/actions/teams.ts
@src/components/students/empty-state.tsx
</context>

<tasks>

<task type="auto">
  <name>팀 목록 페이지 생성</name>
  <files>src/app/(dashboard)/teams/page.tsx</files>
  <action>
    src/app/(dashboard)/teams/page.tsx 파일을 생성합니다. /students/page.tsx 패턴을 따르되 팀에 맞게 수정합니다:

    ```typescript
    import Link from 'next/link'
    import { getTeams } from '@/lib/actions/teams'
    import { EmptyState } from '@/components/students/empty-state'
    import { Users } from 'lucide-react'
    import { Button } from '@/components/ui/button'

    export const metadata = {
      title: '팀 관리 | AI AfterSchool',
      description: '팀 목록 조회 및 관리',
    }

    export default async function TeamsPage() {
      const teams = await getTeams()

      return (
        <div className="container mx-auto py-8 space-y-6">
          <div className="flex items-center justify-between">
            <div>
              <h1 className="text-2xl font-bold">팀 관리</h1>
              <p className="text-gray-500">
                {teams.length > 0
                  ? `총 ${teams.length}개의 팀이 있어요`
                  : '팀을 생성해보세요'}
              </p>
            </div>
          </div>

          {teams.length === 0 ? (
            <EmptyState
              icon={Users}
              title="아직 팀이 없어요"
              description="팀을 생성하고 선생님과 학생을 배정해보세요."
              actionLabel="관리자에게 문의"
              actionHref="/dashboard"
              tips={[
                '팀은 선생님과 학생을 그룹으로 관리하는 단위예요',
                '원장이 팀을 생성할 수 있어요',
                '팀 단위로 데이터 접근 권한이 제어돼요',
              ]}
            />
          ) : (
            <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
              {teams.map((team) => (
                <Link
                  key={team.id}
                  href={`/teams/${team.id}`}
                  data-testid="team-card"
                >
                  <div className="bg-white border rounded-lg p-6 hover:shadow-lg transition cursor-pointer">
                    <h3 className="text-lg font-semibold mb-2" data-testid="team-name">
                      {team.name}
                    </h3>
                    <div className="text-sm text-gray-500 space-y-1">
                      <p data-testid="team-teachers-count">
                        선생님 {team._count.teachers}명
                      </p>
                      <p data-testid="team-students-count">
                        학생 {team._count.students}명
                      </p>
                    </div>
                  </div>
                </Link>
              ))}
            </div>
          )}
        </div>
      )
    }
    ```

    제약사항:
    - getTeams()는 이미 RBAC 필터링이 포함되어 있음
    - 카드 클릭 시 팀 상세 페이지로 이동
    - data-testid: team-card, team-name, team-teachers-count, team-students-count 추가
  </action>
  <verify>/teams 접근 시 팀 목록이 카드 형식으로 표시되는지 확인</verify>
  <done>팀 목록 페이지가 생성되고 팀 카드가 올바르게 표시됨</done>
</task>

<task type="auto">
  <name>팀 상세 페이지 생성</name>
  <files>src/app/(dashboard)/teams/[id]/page.tsx</files>
  <action>
    src/app/(dashboard)/teams/[id]/page.tsx 파일을 생성합니다. layout.tsx는 이미 존재하므로 page.tsx만 추가합니다:

    ```typescript
    import Link from 'next/link'
    import { getTeamById } from '@/lib/actions/teams'
    import { notFound } from 'next/navigation'
    import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
    import { Users, UserGraduate } from 'lucide-react'

    export const metadata = {
      title: '팀 상세 | AI AfterSchool',
      description: '팀 정보 및 구성원 조회',
    }

    export default async function TeamDetailPage({
      params,
    }: {
      params: Promise<{ id: string }>
    }) {
      const { id } = await params
      const team = await getTeamById(id)

      if (!team) {
        notFound()
      }

      return (
        <div className="space-y-6">
          {/* 팀 기본 정보 */}
          <Card data-testid="team-info-card">
            <CardHeader>
              <CardTitle>팀 정보</CardTitle>
            </CardHeader>
            <CardContent>
              <div className="grid gap-4">
                <div>
                  <span className="text-gray-500">팀 ID:</span>{' '}
                  <span className="font-mono text-sm" data-testid="team-id">{team.id}</span>
                </div>
                <div>
                  <span className="text-gray-500">팀 이름:</span>{' '}
                  <span data-testid="team-name">{team.name}</span>
                </div>
              </div>
            </CardContent>
          </Card>

          {/* 소속 선생님 */}
          <Card data-testid="team-teachers-card">
            <CardHeader>
              <CardTitle className="flex items-center gap-2">
                <Users className="w-5 h-5" />
                소속 선생님 ({team.teachers.length}명)
              </CardTitle>
            </CardHeader>
            <CardContent>
              {team.teachers.length === 0 ? (
                <p className="text-gray-500" data-testid="no-teachers">소속 선생님이 없어요</p>
              ) : (
                <ul className="space-y-2">
                  {team.teachers.map((teacher) => (
                    <li key={teacher.id}>
                      <Link
                        href={`/teachers/${teacher.id}`}
                        className="text-blue-600 hover:underline"
                        data-testid="teacher-link"
                      >
                        {teacher.name} ({teacher.role})
                      </Link>
                    </li>
                  ))}
                </ul>
              )}
            </CardContent>
          </Card>

          {/* 소속 학생 */}
          <Card data-testid="team-students-card">
            <CardHeader>
              <CardTitle className="flex items-center gap-2">
                <UserGraduate className="w-5 h-5" />
                소속 학생 ({team.students.length}명)
              </CardTitle>
            </CardHeader>
            <CardContent>
              {team.students.length === 0 ? (
                <p className="text-gray-500" data-testid="no-students">소속 학생이 없어요</p>
              ) : (
                <ul className="space-y-2">
                  {team.students.map((student) => (
                    <li key={student.id}>
                      <Link
                        href={`/students/${student.id}`}
                        className="text-blue-600 hover:underline"
                        data-testid="student-link"
                      >
                        {student.name} ({student.grade}학년)
                      </Link>
                    </li>
                  ))}
                </ul>
              )}
            </CardContent>
          </Card>
        </div>
      )
    }
    ```

    제약사항:
    - getTeamById()는 이미 RBAC 필터링이 포함되어 있음
    - layout.tsx의 탭 내비게이션과 함께 표시됨
    - data-testid: team-info-card, team-id, team-name, team-teachers-card, team-students-card, teacher-link, student-link, no-teachers, no-students 추가
  </action>
  <verify>/teams/[id] 접근 시 팀 정보, 소속 선생님, 소속 학생이 올바르게 표시되는지 확인</verify>
  <done>팀 상세 페이지가 생성되고 모든 정보가 올바르게 표시됨</done>
</task>

</tasks>

<verification>
1. /teams 페이지 접근 시 팀 목록이 카드 형식으로 표시됨
2. 팀 카드 클릭 시 팀 상세 페이지로 이동함
3. /teams/[id] 페이지에서 팀 정보, 소속 선생님, 소속 학생이 표시됨
4. RBAC가 올바르게 동작하여 권한 없는 팀 접근 시 차단됨
</verification>

<success_criteria>
1. 팀 목록 페이지가 생성되고 팀 카드가 표시됨
2. 팀 상세 페이지가 생성되고 모든 정보가 표시됨
3. data-testid 속성이 추가되어 E2E 테스트 가능함
</success_criteria>

<output>
After completion, create `.planning/phases/24-missing-routes-creation/24-02-SUMMARY.md`
</output>
