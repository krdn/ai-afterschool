---
phase: 24-missing-routes-creation
plan: 04
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/app/(dashboard)/students/[id]/page.tsx
  - src/components/students/tabs/report-tab.tsx
autonomous: true

must_haves:
  truths:
    - "/students/[id]?tab=report 접근 시 리포트 탭이 표시된다"
    - "리포트 탭에서 PDF 다운로드 버튼이 제공된다"
    - "PDF 다운로드 버튼 클릭 시 학생 리포트가 다운로드된다"
    - "리포트에 포함될 내용이 안내되어 있다"
  artifacts:
    - path: src/app/(dashboard)/students/[id]/page.tsx
      provides: "수정된 학생 상세 페이지 (리포트 탭 추가)"
      contains: "{ id: 'report', label: '리포트' }"
    - path: src/components/students/tabs/report-tab.tsx
      provides: "리포트 탭 컴포넌트"
      min_lines: 50
  key_links:
    - from: src/app/(dashboard)/students/[id]/page.tsx
      to: src/components/students/tabs/report-tab.tsx
      via: import
      pattern: "import.*ReportTab"
    - from: src/components/students/tabs/report-tab.tsx
      to: /api/students/[id]/report
      via: fetch()
      pattern: "fetch.*api/students/\\$\\{studentId\\}/report"
---

<objective>
학생 상세 페이지에 리포트 탭을 추가하여 PDF 다운로드 기능을 제공합니다.

Purpose: 학생의 종합 분석 리포트를 PDF로 다운로드할 수 있는 UI를 제공합니다.
Output: 수정된 학생 상세 페이지와 새로운 ReportTab 컴포넌트
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-missing-routes-creation/24-CONTEXT.md
@.planning/phases/24-missing-routes-creation/24-RESEARCH.md
@src/app/(dashboard)/students/[id]/page.tsx
@src/app/api/students/[id]/report/route.ts
@src/components/students/tabs/analysis-tab.tsx
@src/components/students/tabs/learning-tab.tsx
</context>

<tasks>

<task type="auto">
  <name>리포트 탭 컴포넌트 생성</name>
  <files>src/components/students/tabs/report-tab.tsx</files>
  <action>
    src/components/students/tabs/report-tab.tsx 파일을 생성합니다:

    ```typescript
    'use client'

    import { useState } from 'react'
    import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
    import { Button } from '@/components/ui/button'
    import { Download, FileText, Loader2 } from 'lucide-react'
    import { toast } from 'sonner'

    interface ReportTabProps {
      studentId: string
      studentName: string
    }

    export default function ReportTab({ studentId, studentName }: ReportTabProps) {
      const [isDownloading, setIsDownloading] = useState(false)

      const handleDownload = async () => {
        setIsDownloading(true)
        try {
          const response = await fetch(`/api/students/${studentId}/report`)
          if (!response.ok) {
            throw new Error('PDF 생성 실패')
          }

          const blob = await response.blob()
          const url = window.URL.createObjectURL(blob)
          const a = document.createElement('a')
          a.href = url
          a.download = `report-${studentName}.pdf`
          document.body.appendChild(a)
          a.click()
          window.URL.revokeObjectURL(url)
          document.body.removeChild(a)

          toast.success('PDF 다운로드 완료')
        } catch (error) {
          console.error('PDF download error:', error)
          toast.error('PDF 다운로드 실패')
        } finally {
          setIsDownloading(false)
        }
      }

      return (
        <div className="space-y-6" data-testid="report-tab">
          <Card>
            <CardHeader>
              <CardTitle className="flex items-center gap-2">
                <FileText className="w-5 h-5" />
                종합 리포트
              </CardTitle>
            </CardHeader>
            <CardContent className="space-y-4">
              <p className="text-gray-600" data-testid="report-description">
                학생의 모든 분석 결과가 포함된 종합 리포트를 다운로드할 수 있습니다.
              </p>

              <div className="flex gap-4">
                <Button
                  onClick={handleDownload}
                  disabled={isDownloading}
                  className="gap-2"
                  data-testid="download-report-button"
                >
                  {isDownloading ? (
                    <>
                      <Loader2 className="w-4 h-4 animate-spin" />
                      생성 중...
                    </>
                  ) : (
                    <>
                      <Download className="w-4 h-4" />
                      PDF 다운로드
                    </>
                  )}
                </Button>
              </div>

              {/* 포함될 내용 안내 */}
              <div className="mt-6 border-t pt-6">
                <h3 className="font-semibold mb-4" data-testid="report-contents-title">
                  포함될 내용
                </h3>
                <ul className="list-disc list-inside text-gray-600 space-y-1" data-testid="report-contents-list">
                  <li>기본 정보 (이름, 학교, 학년)</li>
                  <li>사주 분석</li>
                  <li>이름 분석</li>
                  <li>MBTI 성격 유형</li>
                  <li>관상 분석</li>
                  <li>손금 분석</li>
                  <li>학습 전략</li>
                  <li>진로 가이드</li>
                </ul>
              </div>
            </CardContent>
          </Card>
        </div>
      )
    }
    ```

    제약사항:
    - Client Component로 작성합니다 ('use client')
    - data-testid: report-tab, report-description, download-report-button, report-contents-title, report-contents-list 추가
    - PDF 다운로드는 이미 구현된 API를 사용합니다
  </action>
  <verify>TypeScript 컴파일 에러가 없는지 확인</verify>
  <done>ReportTab 컴포넌트가 생성되고 PDF 다운로드 기능이 구현됨</done>
</task>

<task type="auto">
  <name>학생 상세 페이지에 리포트 탭 추가</name>
  <files>src/app/(dashboard)/students/[id]/page.tsx</files>
  <action>
    src/app/(dashboard)/students/[id]/page.tsx 파일을 수정하여 리포트 탭을 추가합니다:

    1. 파일 상단 import 구문에 추가:
    ```typescript
    import ReportTab from "@/components/students/tabs/report-tab";
    ```

    2. tabs 배열에 'report' 탭 추가 (기존 4개 탭 다음에):
    ```typescript
    const tabs = [
        { id: 'learning', label: '학습' },
        { id: 'analysis', label: '분석' },
        { id: 'matching', label: '매칭' },
        { id: 'counseling', label: '상담' },
        { id: 'report', label: '리포트' },  // 추가
    ];
    ```

    3. 탭 콘텐츠 렌더링 부분에 report 탭 조건 추가:
    ```typescript
    {currentTab === 'counseling' && (
        <div>
            <h3 className="text-lg font-bold mb-4">상담 기록</h3>
            <p className="text-gray-500">학부모 및 학생 상담 내역을 표시합니다.</p>
        </div>
    )}
    {currentTab === 'report' && (  // 추가
        <ReportTab studentId={student.id} studentName={student.name} />
    )}
    ```

    제약사항:
    - 기존 탭 순서와 UI는 변경하지 않습니다
    - report 탭은 마지막에 추가합니다
    - 기존 data-testid 속성을 유지합니다
  </action>
  <verify>/students/[id]?tab=report 접근 시 리포트 탭이 올바르게 표시되는지 확인</verify>
  <done>학생 상세 페이지에 리포트 탭이 추가되고 PDF 다운로드가 동작함</done>
</task>

</tasks>

<verification>
1. /students/[id]?tab=report 접근 시 리포트 탭이 표시됨
2. PDF 다운로드 버튼 클릭 시 PDF가 다운로드됨
3. 다운로드 중 로딩 상태가 표시됨
4. 포함될 내용 안내가 표시됨
5. data-testid 속성이 추가되어 E2E 테스트 가능함
</verification>

<success_criteria>
1. 리포트 탭이 학생 상세 페이지에 추가됨
2. PDF 다운로드 기능이 정상 동작함
3. data-testid 속성이 추가되어 E2E 테스트 가능함
</success_criteria>

<output>
After completion, create `.planning/phases/24-missing-routes-creation/24-04-SUMMARY.md`
</output>
