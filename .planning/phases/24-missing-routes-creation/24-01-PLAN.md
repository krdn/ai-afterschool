---
phase: 24-missing-routes-creation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/dal.ts
  - src/app/(dashboard)/teachers/me/page.tsx
autonomous: true

must_haves:
  truths:
    - "/teachers/me 접근 시 현재 로그인한 선생님의 프로필 페이지로 자동 리다이렉트된다"
    - "Prisma 스키마에 AuditLog와 SystemLog 모델이 추가되어 데이터베이스 마이그레이션이 완료된다"
    - "logAuditAction 함수가 생성되어 주요 설정 변경 시 감사 로그를 기록한다"
    - "logSystemAction 함수가 생성되어 시스템 이벤트를 기록한다"
  artifacts:
    - path: prisma/schema.prisma
      provides: "AuditLog, SystemLog 모델 정의"
      contains: "model AuditLog", "model SystemLog"
    - path: src/lib/dal.ts
      provides: "로그 기록 유틸리티 함수"
      exports: ["logAuditAction", "logSystemAction"]
    - path: src/app/(dashboard)/teachers/me/page.tsx
      provides: "/teachers/me 리다이렉트 페이지"
      min_lines: 10
  key_links:
    - from: src/app/(dashboard)/teachers/me/page.tsx
      to: src/app/(dashboard)/teachers/[id]/page.tsx
      via: redirect()
      pattern: "redirect\\(\\`/teachers/\\$\\{session\\.userId\\}\\`\\)"
    - from: src/lib/dal.ts
      to: prisma.schema.prisma
      via: Prisma Client
      pattern: "prisma\\.auditLog\\.create|prisma\\.systemLog\\.create"
---

<objective>
Prisma 스키마에 로그 모델을 추가하고 /teachers/me 리다이렉트 페이지를 생성하여 로깅 인프라를 구축합니다.

Purpose: 시스템 로그와 감사 로그를 저장할 데이터베이스 구조를 확보하고, 본인 프로필 빠른 접근 라우트를 구현합니다.
Output: AuditLog/SystemLog 모델이 추가된 스키마와 로깅 함수, /teachers/me 리다이렉트 페이지
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-missing-routes-creation/24-CONTEXT.md
@.planning/phases/24-missing-routes-creation/24-RESEARCH.md
@prisma/schema.prisma
@src/lib/dal.ts
@src/app/(dashboard)/teachers/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Prisma 스키마에 로그 모델 추가</name>
  <files>prisma/schema.prisma</files>
  <action>
    prisma/schema.prisma 파일에 다음 두 모델을 추가합니다:

    ```prisma
    // 감사 로그: 설정 변경 이력 추적
    model AuditLog {
      id         String   @id @default(cuid())
      teacherId  String
      action     String   // CREATE, UPDATE, DELETE 등
      entityType String   // Team, Teacher, Student, LLMConfig 등
      entityId   String?
      changes    Json?    // 변경 전후 데이터
      ipAddress  String?
      userAgent  String?
      createdAt  DateTime @default(now())

      teacher    Teacher  @relation(fields: [teacherId], references: [id], onDelete: Cascade)

      @@index([teacherId])
      @@index([entityType, entityId])
      @@index([createdAt(sort: Desc)])
      @@map("audit_logs")
    }

    // 시스템 로그: 애플리케이션 로그 기록
    model SystemLog {
      id        String   @id @default(cuid())
      level     String   // ERROR, WARN, INFO, DEBUG
      message   String
      context   Json?    // 추가 컨텍스트 데이터
      timestamp DateTime @default(now())

      @@index([level])
      @@index([timestamp(sort: Desc)])
      @@map("system_logs")
    }
    ```

    그리고 Teacher 모델에 auditLogs 관계를 추가합니다:
    ```prisma
    model Teacher {
      // ... 기존 필드들
      auditLogs  AuditLog[]
    }
    ```

    제약사항:
    - 기존 모델과 필드는 수정하지 않습니다
    - @@map으로 테이블 이름을 snake_case로 지정합니다
    - 적절한 인덱스를 추가하여 쿼리 성능을 최적화합니다
  </action>
  <verify>npx prisma db push 명령 실행으로 스키마가 성공적으로 적용되는지 확인</verify>
  <done>AuditLog와 SystemLog 모델이 스키마에 추가되고 데이터베이스 마이그레이션이 완료됨</done>
</task>

<task type="auto">
  <name>로그 기록 함수 생성</name>
  <files>src/lib/dal.ts</files>
  <action>
    src/lib/dal.ts 파일에 다음 두 함수를 추가합니다:

    ```typescript
    import { headers } from 'next/headers'

    /**
     * 감사 로그 기록 함수
     * 주요 설정 변경 시 호출하여 이력을 남깁니다
     */
    export async function logAuditAction(params: {
      action: string
      entityType: string
      entityId?: string
      changes?: Record<string, unknown>
    }) {
      const session = await verifySession()
      if (!session?.userId) return

      const headersList = await headers()
      const ipAddress = headersList.get('x-forwarded-for') ||
                       headersList.get('x-real-ip') ||
                       null
      const userAgent = headersList.get('user-agent') || null

      await db.auditLog.create({
        data: {
          teacherId: session.userId,
          action: params.action,
          entityType: params.entityType,
          entityId: params.entityId,
          changes: params.changes,
          ipAddress,
          userAgent,
        },
      })
    }

    /**
     * 시스템 로그 기록 함수
     * 애플리케이션 이벤트를 로그에 남깁니다
     */
    export async function logSystemAction(params: {
      level: 'ERROR' | 'WARN' | 'INFO' | 'DEBUG'
      message: string
      context?: Record<string, unknown>
    }) {
      await db.systemLog.create({
        data: {
          level: params.level,
          message: params.message,
          context: params.context,
        },
      })
    }
    ```

    제약사항:
    - 함수는 비동기로 동작하며 호출자에게 영향을 주지 않아야 합니다
    - 로그 기록 실패가 메인 로직에 영향을 주지 않도록 try-catch로 감싸지 않습니다 (나중에 필요시 추가)
    - IP 주소와 User-Agent를 자동으로 수집합니다
  </action>
  <verify>TypeScript 컴파일 에러가 없는지 확인 (npm run build 또는 tsc --noEmit)</verify>
  <done>logAuditAction과 logSystemAction 함수가 dal.ts에 추가되고 사용 가능한 상태가 됨</done>
</task>

<task type="auto">
  <name>/teachers/me 리다이렉트 페이지 생성</name>
  <files>src/app/(dashboard)/teachers/me/page.tsx</files>
  <action>
    src/app/(dashboard)/teachers/me 디렉토리와 page.tsx 파일을 생성합니다:

    ```typescript
    import { redirect } from 'next/navigation'
    import { verifySession } from '@/lib/dal'

    export const metadata = {
      title: '내 프로필 | AI AfterSchool',
      description: '본인 프로필 조회',
    }

    export default async function TeacherMePage() {
      const session = await verifySession()

      // 세션에서 현재 로그인한 선생님 ID를 가져와 리다이렉트
      redirect(`/teachers/${session.userId}`)
    }
    ```

    제약사항:
    - verifySession으로 인증된 사용자만 접근 가능합니다
    - 리다이렉트는 Next.js의 redirect 함수를 사용합니다
    - 별도의 UI 없이 즉시 리다이렉트합니다
  </action>
  <verify>브라우저에서 /teachers/me 접근 시 /teachers/[현재사용자ID]로 리다이렉트되는지 확인</verify>
  <done>/teachers/me 접근 시 현재 로그인한 선생님의 프로필 페이지로 자동 리다이렉트됨</done>
</task>

</tasks>

<verification>
1. Prisma 스키마에 AuditLog와 SystemLog 모델이 정의되어 있음
2. npx prisma db push로 데이터베이스 마이그레이션이 성공적으로 완료됨
3. logAuditAction과 logSystemAction 함수가 dal.ts에서 export되어 있음
4. /teachers/me 페이지 접근 시 리다이렉트가 동작함
</verification>

<success_criteria>
1. Prisma 모델이 추가되고 데이터베이스에 테이블이 생성됨
2. 로그 기록 함수가 다른 모듈에서 import하여 사용 가능함
3. /teachers/me 라우트가 존재하고 리다이렉트가 동작함
</success_criteria>

<output>
After completion, create `.planning/phases/24-missing-routes-creation/24-01-SUMMARY.md`
</output>
