---
phase: 24-missing-routes-creation
plan: 05
type: execute
wave: 3
depends_on: ["24-01"]
files_modified:
  - src/app/(dashboard)/admin/system-logs/page.tsx
  - src/lib/actions/system.ts
autonomous: true

must_haves:
  truths:
    - "/admin/system-logs 접근 시 시스템 로그가 테이블 형식으로 표시된다"
    - "로그 레벨별(ERROR/WARN/INFO) 필터링이 가능하다"
    - "로그가 시간순(최신 순)으로 정렬되어 표시된다"
    - "각 로그의 시간, 레벨, 메시지가 표시된다"
  artifacts:
    - path: src/app/(dashboard)/admin/system-logs/page.tsx
      provides: "시스템 로그 조회 페이지"
      min_lines: 100
    - path: src/lib/actions/system.ts
      provides: "시스템 로그 조회 Server Action"
      exports: ["getSystemLogs"]
  key_links:
    - from: src/app/(dashboard)/admin/system-logs/page.tsx
      to: src/lib/actions/system.ts
      via: getSystemLogs()
      pattern: "getSystemLogs\\(\\{.*level.*page.*\\}\\)"
    - from: src/lib/actions/system.ts
      to: prisma.schema.prisma
      via: Prisma Client
      pattern: "prisma\\.systemLog\\.findMany"
---

<objective>
시스템 로그 조회 페이지를 생성하여 애플리케이션 로그를 시간순으로 조회할 수 있습니다.

Purpose: 운영자가 시스템 로그를 확인하여 문제 발생 원인을 파악할 수 있습니다.
Output: /admin/system-logs 페이지와 getSystemLogs Server Action
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-missing-routes-creation/24-CONTEXT.md
@.planning/phases/24-missing-routes-creation/24-RESEARCH.md
@prisma/schema.prisma
@src/app/(dashboard)/admin/llm-settings/page.tsx
@src/components/ui/table.tsx
</context>

<tasks>

<task type="auto">
  <name>시스템 로그 조회 Server Action 생성</name>
  <files>src/lib/actions/system.ts</files>
  <action>
    src/lib/actions/system.ts 파일을 생성합니다:

    ```typescript
    'use server'

    import { verifySession } from '@/lib/dal'
    import { db } from '@/lib/db'
    import type { TeacherRole } from '@/lib/db/rbac'

    export interface SystemLogEntry {
      id: string
      level: string
      message: string
      context: Record<string, unknown> | null
      timestamp: Date
    }

    export interface SystemLogsResult {
      logs: SystemLogEntry[]
      total: number
      page: number
      pageSize: number
    }

    export async function getSystemLogs(params: {
      level?: string
      page?: number
      pageSize?: number
    }): Promise<SystemLogsResult> {
      const session = await verifySession()

      // 권한 검증: 원장만 접근 가능
      if (session.role !== 'DIRECTOR') {
        return { logs: [], total: 0, page: 1, pageSize: 20 }
      }

      const page = params.page || 1
      const pageSize = params.pageSize || 20
      const skip = (page - 1) * pageSize

      const where = params.level && params.level !== 'ALL'
        ? { level: params.level }
        : {}

      const [logs, total] = await Promise.all([
        db.systemLog.findMany({
          where,
          orderBy: { timestamp: 'desc' },
          skip,
          take: pageSize,
        }),
        db.systemLog.count({ where }),
      ])

      return {
        logs: logs.map((log) => ({
          id: log.id,
          level: log.level,
          message: log.message,
          context: log.context as Record<string, unknown> | null,
          timestamp: log.timestamp,
        })),
        total,
        page,
        pageSize,
      }
    }
    ```

    제약사항:
    - DIRECTOR 역할만 조회 가능합니다
    - 최신 순(내림차순) 정렬합니다
    - 페이지네이션을 지원합니다
  </action>
  <verify>TypeScript 컴파일 에러가 없는지 확인</verify>
  <done>getSystemLogs Server Action이 생성되고 로그 조회가 가능함</done>
</task>

<task type="auto">
  <name>시스템 로그 페이지 생성</name>
  <files>src/app/(dashboard)/admin/system-logs/page.tsx</files>
  <action>
    src/app/(dashboard)/admin/system-logs/page.tsx 파일을 생성합니다:

    ```typescript
    import { redirect } from 'next/navigation'
    import { verifySession } from '@/lib/dal'
    import { getSystemLogs } from '@/lib/actions/system'
    import Link from 'next/link'
    {
      /* Table, TableBody, TableCell, TableHead, TableHeader, TableRow */
    } from '@/components/ui/table'

    export const metadata = {
      title: '시스템 로그 | AI AfterSchool',
      description: '시스템 로그 조회',
    }

    function getLevelColor(level: string): string {
      switch (level) {
        case 'ERROR':
          return 'bg-red-100 text-red-800'
        case 'WARN':
          return 'bg-yellow-100 text-yellow-800'
        case 'INFO':
          return 'bg-blue-100 text-blue-800'
        case 'DEBUG':
          return 'bg-gray-100 text-gray-800'
        default:
          return 'bg-gray-100 text-gray-800'
      }
    }

    export default async function SystemLogsPage({
      searchParams,
    }: {
      searchParams: Promise<{ level?: string; page?: string }>
    }) {
      const session = await verifySession()
      if (!session || session.role !== 'DIRECTOR') {
        redirect('/dashboard')
      }

      const params = await searchParams
      const level = params.level || 'ALL'
      const page = parseInt(params.page || '1')

      const { logs, total } = await getSystemLogs({ level, page })

      const totalPages = Math.ceil(total / 20)

      return (
        <div className="container py-6 space-y-8">
          <div>
            <h1 className="text-2xl font-bold">시스템 로그</h1>
            <p className="text-gray-500">
              애플리케이션 로그를 시간순으로 조회합니다.
            </p>
          </div>

          {/* 레벨 필터 */}
          <div className="flex gap-2" data-testid="log-level-filter">
            <Link
              href="/admin/system-logs"
              className={`px-3 py-1 rounded ${level === 'ALL' ? 'bg-blue-600 text-white' : 'bg-gray-200'}`}
              data-testid="filter-all"
            >
              전체
            </Link>
            <Link
              href="/admin/system-logs?level=ERROR"
              className={`px-3 py-1 rounded ${level === 'ERROR' ? 'bg-red-600 text-white' : 'bg-gray-200'}`}
              data-testid="filter-error"
            >
              ERROR
            </Link>
            <Link
              href="/admin/system-logs?level=WARN"
              className={`px-3 py-1 rounded ${level === 'WARN' ? 'bg-yellow-600 text-white' : 'bg-gray-200'}`}
              data-testid="filter-warn"
            >
              WARN
            </Link>
            <Link
              href="/admin/system-logs?level=INFO"
              className={`px-3 py-1 rounded ${level === 'INFO' ? 'bg-blue-600 text-white' : 'bg-gray-200'}`}
              data-testid="filter-info"
            >
              INFO
            </Link>
          </div>

          {/* 로그 테이블 */}
          {logs.length === 0 ? (
            <div className="text-center py-12 text-gray-500" data-testid="no-logs">
              로그가 없습니다.
            </div>
          ) : (
            <div className="border rounded-lg" data-testid="system-logs-table">
              <Table>
                <TableHeader>
                  <TableRow>
                    <TableHead>시간</TableHead>
                    <TableHead>레벨</TableHead>
                    <TableHead>메시지</TableHead>
                  </TableRow>
                </TableHeader>
                <TableBody>
                  {logs.map((log) => (
                    <TableRow key={log.id} data-testid="log-row">
                      <TableCell data-testid="log-timestamp">
                        {new Date(log.timestamp).toLocaleString('ko-KR')}
                      </TableCell>
                      <TableCell>
                        <span
                          className={`px-2 py-1 rounded text-xs font-medium ${getLevelColor(log.level)}`}
                          data-testid="log-level"
                        >
                          {log.level}
                        </span>
                      </TableCell>
                      <TableCell data-testid="log-message">
                        {log.message}
                        {log.context && (
                          <pre className="mt-1 text-xs text-gray-500 overflow-auto">
                            {JSON.stringify(log.context, null, 2)}
                          </pre>
                        )}
                      </TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            </div>
          )}

          {/* 페이지네이션 */}
          {totalPages > 1 && (
            <div className="flex gap-2" data-testid="pagination">
              {page > 1 && (
                <Link
                  href={`/admin/system-logs?level=${level}&page=${page - 1}`}
                  className="px-3 py-1 border rounded"
                  data-testid="prev-page"
                >
                  이전
                </Link>
              )}
              <span className="px-3 py-1">
                {page} / {totalPages}
              </span>
              {page < totalPages && (
                <Link
                  href={`/admin/system-logs?level=${level}&page=${page + 1}`}
                  className="px-3 py-1 border rounded"
                  data-testid="next-page"
                >
                  다음
                </Link>
              )}
            </div>
          )}
        </div>
      )
    }
    ```

    제약사항:
    - DIRECTOR 역할만 접근 가능합니다
    - data-testid: log-level-filter, filter-all, filter-error, filter-warn, filter-info, system-logs-table, no-logs, log-row, log-timestamp, log-level, log-message, pagination, prev-page, next-page 추가
  </action>
  <verify>/admin/system-logs 접근 시 로그가 올바르게 표시되는지 확인</verify>
  <done>시스템 로그 페이지가 생성되고 필터링과 페이지네이션이 동작함</done>
</task>

</tasks>

<verification>
1. /admin/system-logs 접근 시 로그가 테이블 형식으로 표시됨
2. 레벨별 필터가 동작함
3. 로그가 최신 순으로 정렬됨
4. 페이지네이션이 동작함
5. DIRECTOR가 아닌 사용자 접근 시 리다이렉트됨
</verification>

<success_criteria>
1. 시스템 로그 페이지가 생성되고 로그가 표시됨
2. 필터링과 페이지네이션이 동작함
3. data-testid 속성이 추가되어 E2E 테스트 가능함
</success_criteria>

<output>
After completion, create `.planning/phases/24-missing-routes-creation/24-05-SUMMARY.md`
</output>
