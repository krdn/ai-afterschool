---
phase: 15-multi-llm-integration-smart-routing
plan: 08
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/lib/ai/providers/ollama.ts
  - src/lib/ai/providers/index.ts
  - docker-compose.yml
  - .env.example

autonomous: false

user_setup:
  - service: ollama
    why: "로컬 LLM 제공자 (비용 $0)"
    env_vars:
      - name: OLLAMA_BASE_URL
        source: "Ollama 서버 주소 (기본: http://192.168.0.5:11434/api)"
    dashboard_config:
      - task: "Ollama 서버 실행 확인"
        location: "http://192.168.0.5:11434 접속 시 'Ollama is running' 메시지"
      - task: "모델 다운로드"
        location: "ollama pull llama3.1:8b (서버에서 실행)"

must_haves:
  truths:
    - "Next.js 앱에서 Ollama 서버(192.168.0.5:11434)에 연결할 수 있다"
    - "Ollama를 통한 LLM 호출이 성공한다"
    - "Ollama 연결 실패 시 다른 제공자로 폴백된다"
  artifacts:
    - path: "src/lib/ai/providers/ollama.ts"
      provides: "Ollama 연결 테스트 유틸"
      exports: ["testOllamaConnection", "getOllamaModels"]
  key_links:
    - from: "src/lib/ai/providers/index.ts"
      to: "ollama-ai-provider-v2"
      via: "createOllama"
      pattern: "createOllama\\(\\{\\s*baseURL"
---

<objective>
Ollama Docker 네트워킹 설정 및 연결 테스트

Purpose: 로컬 Ollama 서버(192.168.0.5:11434)에 안정적으로 연결하여 무료 LLM 사용
Output: Ollama 연결 유틸리티, 환경 설정, 연결 테스트
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-multi-llm-integration-smart-routing/15-RESEARCH.md
@.planning/phases/15-multi-llm-integration-smart-routing/15-01-SUMMARY.md

@src/lib/ai/providers/index.ts
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Ollama connection utilities</name>
  <files>
    src/lib/ai/providers/ollama.ts
  </files>
  <action>
`src/lib/ai/providers/ollama.ts` 생성:

```typescript
import { createOllama } from 'ollama-ai-provider-v2';
import { generateText } from 'ai';

// ============================================================
// Configuration
// ============================================================

export function getOllamaBaseUrl(): string {
  return process.env.OLLAMA_BASE_URL || 'http://192.168.0.5:11434/api';
}

/**
 * Ollama 인스턴스를 생성합니다.
 */
export function createOllamaInstance() {
  return createOllama({
    baseURL: getOllamaBaseUrl(),
  });
}

// ============================================================
// Connection Testing
// ============================================================

interface OllamaConnectionResult {
  connected: boolean;
  baseUrl: string;
  error?: string;
  responseTimeMs?: number;
}

/**
 * Ollama 서버 연결을 테스트합니다.
 */
export async function testOllamaConnection(): Promise<OllamaConnectionResult> {
  const baseUrl = getOllamaBaseUrl();
  const startTime = Date.now();

  try {
    // Ollama API 버전 엔드포인트 호출 (가벼운 요청)
    const versionUrl = baseUrl.replace('/api', '/api/version');
    const controller = new AbortController();
    const timeout = setTimeout(() => controller.abort(), 5000);

    const response = await fetch(versionUrl, {
      signal: controller.signal,
    });

    clearTimeout(timeout);

    if (!response.ok) {
      return {
        connected: false,
        baseUrl,
        error: `HTTP ${response.status}: ${response.statusText}`,
        responseTimeMs: Date.now() - startTime,
      };
    }

    return {
      connected: true,
      baseUrl,
      responseTimeMs: Date.now() - startTime,
    };
  } catch (error) {
    return {
      connected: false,
      baseUrl,
      error: error instanceof Error ? error.message : 'Unknown error',
      responseTimeMs: Date.now() - startTime,
    };
  }
}

// ============================================================
// Model Management
// ============================================================

interface OllamaModel {
  name: string;
  size: number;
  digest: string;
  modified_at: string;
}

/**
 * Ollama 서버에서 사용 가능한 모델 목록을 조회합니다.
 */
export async function getOllamaModels(): Promise<OllamaModel[]> {
  const baseUrl = getOllamaBaseUrl();

  try {
    const tagsUrl = baseUrl.replace('/api', '/api/tags');
    const controller = new AbortController();
    const timeout = setTimeout(() => controller.abort(), 10000);

    const response = await fetch(tagsUrl, {
      signal: controller.signal,
    });

    clearTimeout(timeout);

    if (!response.ok) {
      console.error('Failed to fetch Ollama models:', response.statusText);
      return [];
    }

    const data = await response.json();
    return data.models || [];
  } catch (error) {
    console.error('Failed to fetch Ollama models:', error);
    return [];
  }
}

/**
 * Ollama로 간단한 테스트 요청을 보냅니다.
 */
export async function testOllamaGeneration(model = 'llama3.1:8b'): Promise<{
  success: boolean;
  response?: string;
  error?: string;
  responseTimeMs: number;
}> {
  const startTime = Date.now();

  try {
    const ollama = createOllamaInstance();
    const ollamaModel = ollama(model);

    const result = await generateText({
      model: ollamaModel,
      prompt: 'Say "Hello, Ollama is working!" in one short sentence.',
      maxTokens: 20,
    });

    return {
      success: true,
      response: result.text,
      responseTimeMs: Date.now() - startTime,
    };
  } catch (error) {
    return {
      success: false,
      error: error instanceof Error ? error.message : 'Unknown error',
      responseTimeMs: Date.now() - startTime,
    };
  }
}

// ============================================================
// Health Check
// ============================================================

/**
 * Ollama 전체 상태를 확인합니다.
 */
export async function checkOllamaHealth(): Promise<{
  status: 'healthy' | 'degraded' | 'unavailable';
  connection: OllamaConnectionResult;
  models: OllamaModel[];
  canGenerate: boolean;
}> {
  const connection = await testOllamaConnection();

  if (!connection.connected) {
    return {
      status: 'unavailable',
      connection,
      models: [],
      canGenerate: false,
    };
  }

  const models = await getOllamaModels();

  if (models.length === 0) {
    return {
      status: 'degraded',
      connection,
      models: [],
      canGenerate: false,
    };
  }

  // 첫 번째 모델로 생성 테스트
  const genTest = await testOllamaGeneration(models[0].name);

  return {
    status: genTest.success ? 'healthy' : 'degraded',
    connection,
    models,
    canGenerate: genTest.success,
  };
}
```
  </action>
  <verify>
- TypeScript 컴파일 통과
- testOllamaConnection이 Ollama API 호출
- getOllamaModels가 모델 목록 반환
  </verify>
  <done>
- testOllamaConnection: Ollama 서버 연결 테스트
- getOllamaModels: 사용 가능한 모델 목록 조회
- testOllamaGeneration: 실제 생성 테스트
- checkOllamaHealth: 전체 상태 확인
  </done>
</task>

<task type="auto">
  <name>Task 2: Update provider index and environment configuration</name>
  <files>
    src/lib/ai/providers/index.ts
    .env.example
    .env.development
  </files>
  <action>
1. `src/lib/ai/providers/index.ts` 업데이트 - Ollama 설정 개선:

```typescript
// 기존 ollama import를 새 모듈로 교체
import { createOllamaInstance, getOllamaBaseUrl } from './ollama';

// providers 객체의 ollama 항목 수정:
export const providers = {
  // ... anthropic, openai, google 유지
  
  ollama: (model?: string) => {
    const ollamaInstance = createOllamaInstance();
    return ollamaInstance(model || 'llama3.1:8b');
  },
} as const;
```

2. `.env.example`에 Ollama 설정 추가:

```env
# Ollama Configuration
# Local Ollama server URL (default: http://192.168.0.5:11434/api)
# For Docker: Use the host machine's IP, not localhost
OLLAMA_BASE_URL=http://192.168.0.5:11434/api
```

3. `.env.development`에 Ollama 설정 추가:

```env
# Ollama Configuration
OLLAMA_BASE_URL=http://192.168.0.5:11434/api
```

4. `src/lib/ai/providers/index.ts`에 export 추가:

```typescript
// 파일 끝에 추가
export { 
  testOllamaConnection, 
  getOllamaModels, 
  checkOllamaHealth 
} from './ollama';
```
  </action>
  <verify>
- OLLAMA_BASE_URL 환경변수가 .env.example에 문서화됨
- providers.ollama가 createOllamaInstance 사용
- Ollama 유틸 함수들이 export됨
  </verify>
  <done>
- 환경변수 OLLAMA_BASE_URL 설정 (기본값: 192.168.0.5:11434)
- Provider index가 Ollama 모듈 사용
- Ollama 유틸 함수들이 외부에서 접근 가능
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Ollama 연결 설정 및 유틸리티</what-built>
  <how-to-verify>
1. Ollama 서버 상태 확인:
   - 브라우저에서 http://192.168.0.5:11434 접속
   - "Ollama is running" 메시지 확인
   
2. 모델 확인 (서버에서 실행):
   ```bash
   ssh gon@192.168.0.5
   ollama list
   ```
   - llama3.1:8b 또는 다른 모델이 있는지 확인
   - 없으면: `ollama pull llama3.1:8b`
   
3. 연결 테스트 (개발 환경에서):
   ```bash
   # Next.js 앱에서 Ollama API 호출 테스트
   curl http://192.168.0.5:11434/api/version
   ```
   - 버전 정보가 반환되는지 확인
   
4. 방화벽 확인 (필요시):
   - 192.168.0.5에서 11434 포트가 열려있는지 확인
  </how-to-verify>
  <resume-signal>Type "approved" if Ollama server is accessible, or "skip" to continue without Ollama (will use fallback providers)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Add Ollama test endpoint for admin verification</name>
  <files>
    src/app/api/llm/test-ollama/route.ts
  </files>
  <action>
`src/app/api/llm/test-ollama/route.ts` 생성:

```typescript
import { NextResponse } from 'next/server';
import { verifySession } from '@/lib/session';
import { checkOllamaHealth, testOllamaConnection, getOllamaModels } from '@/lib/ai/providers';

/**
 * Ollama 상태 확인 엔드포인트 (DIRECTOR only)
 */
export async function GET() {
  try {
    const session = await verifySession();
    if (!session || session.role !== 'DIRECTOR') {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
    }

    const health = await checkOllamaHealth();

    return NextResponse.json({
      ...health,
      timestamp: new Date().toISOString(),
    });
  } catch (error) {
    return NextResponse.json({
      status: 'unavailable',
      error: error instanceof Error ? error.message : 'Unknown error',
    }, { status: 500 });
  }
}

/**
 * Ollama 연결 테스트 (POST)
 */
export async function POST(request: Request) {
  try {
    const session = await verifySession();
    if (!session || session.role !== 'DIRECTOR') {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
    }

    const body = await request.json().catch(() => ({}));
    const testType = body.type || 'connection';

    if (testType === 'connection') {
      const result = await testOllamaConnection();
      return NextResponse.json(result);
    }

    if (testType === 'models') {
      const models = await getOllamaModels();
      return NextResponse.json({ models });
    }

    if (testType === 'full') {
      const health = await checkOllamaHealth();
      return NextResponse.json(health);
    }

    return NextResponse.json({ error: 'Invalid test type' }, { status: 400 });
  } catch (error) {
    return NextResponse.json({
      success: false,
      error: error instanceof Error ? error.message : 'Unknown error',
    }, { status: 500 });
  }
}
```

Admin 설정 페이지의 Ollama 카드에 연결 테스트 버튼 추가 (기존 provider-card.tsx에서 Ollama 특별 처리):

```tsx
// Ollama 연결 테스트 추가 로직 (provider-card.tsx에 이미 포함됨)
// handleTest 함수가 Ollama일 때 /api/llm/test-ollama 호출
```
  </action>
  <verify>
- /api/llm/test-ollama GET 엔드포인트 동작
- DIRECTOR 권한 체크
- checkOllamaHealth 결과 반환
  </verify>
  <done>
- /api/llm/test-ollama 엔드포인트로 Ollama 상태 확인
- 연결 테스트, 모델 목록, 전체 상태 확인 지원
- Admin UI에서 Ollama 연결 테스트 가능
  </done>
</task>

</tasks>

<verification>
- [ ] ollama.ts 파일 존재 및 export 확인
- [ ] OLLAMA_BASE_URL이 .env.example에 문서화됨
- [ ] /api/llm/test-ollama 엔드포인트 응답
- [ ] Ollama 서버 연결 가능 (또는 폴백 제공자 사용)
</verification>

<success_criteria>
- Ollama 연결 유틸리티 (testOllamaConnection, getOllamaModels, checkOllamaHealth)
- 환경변수로 Ollama 서버 주소 설정 가능
- 연결 실패 시 다른 제공자로 폴백 (router.ts에서 처리)
- Admin UI에서 Ollama 연결 상태 확인 가능
</success_criteria>

<output>
After completion, create `.planning/phases/15-multi-llm-integration-smart-routing/15-08-SUMMARY.md`
</output>
