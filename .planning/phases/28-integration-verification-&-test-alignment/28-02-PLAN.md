# Phase 28-02: Selector Fixes for High-Priority Components

**Wave:** 2
**Autonomous:** Yes
**Depends on:** 28-01 (Test Environment Setup)

---

## Tasks

### Task 1: Fix Student Detail Tabs Selectors

**Action:** Add data-testid attributes to student detail page tabs.

**Components to modify:**
1. `src/app/(dashboard)/students/[id]/page.tsx` - Main tabs container
2. Student detail tab components

**Attributes to add:**
```tsx
// Tabs container
<Tabs data-testid="student-detail-tabs" defaultValue="info">

// Tab buttons
<TabsTrigger value="learning" data-tab="learning">학습</TabsTrigger>
<TabsTrigger value="analysis" data-tab="analysis">분석</TabsTrigger>
<TabsTrigger value="matching" data-tab="matching">매칭</TabsTrigger>
<TabsTrigger value="counseling" data-tab="counseling">상담</TabsTrigger>
```

**Files:**
- `src/components/students/tabs/learning-tab.tsx` (if exists)
- `src/components/students/tabs/analysis-tab.tsx`
- `src/components/students/tabs/matching-tab.tsx`
- `src/components/students/tabs/counseling-tab.tsx`

---

### Task 2: Fix Admin Page Selectors

**Action:** Add data-testid to all admin sub-pages created in Phase 24.

**Pages to modify:**
1. `/admin/llm-usage` - LLM usage statistics
2. `/admin/system-status` - System health monitoring
3. `/admin/system-logs` - System logs viewing
4. `/admin/backup` - Database backup management
5. `/admin/audit-logs` - Audit log viewing

**Attributes to add:**
```tsx
// Page containers
<div data-testid="admin-llm-usage-page">
<div data-testid="admin-system-status-page">
<div data-testid="admin-system-logs-page">
<div data-testid="admin-backup-page">
<div data-testid="admin-audit-logs-page">

// Key elements
<Button data-testid="run-backup-button">백업 실행</Button>
<Table data-testid="audit-logs-table">
```

---

### Task 3: Fix Counseling Component Selectors

**Action:** Add data-testid to counseling-related components.

**Components:**
1. Reservation calendar - `data-testid="counseling-calendar"`
2. Session modal - `data-testid="counseling-session-modal"`
3. Session card - `data-testid="counseling-session-card"`
4. Statistics cards - `data-testid="counseling-stat-card-{type}"`

**Files:**
- `src/components/counseling/ReservationCalendarView.tsx`
- `src/components/counseling/CounselingSessionModal.tsx`
- `src/components/counseling/CounselingSessionCard.tsx`
- `src/app/(dashboard)/counseling/page.tsx`

---

### Task 4: Fix Error Page Selectors

**Action:** Add data-testid to error pages created in Phase 27.

**Pages:**
1. 404 Not Found - `data-testid="not-found-page"`
2. 403 Forbidden - `data-testid="access-denied-page"`
3. 500 Server Error - `data-testid="server-error-page"`

**Files:**
- `src/app/not-found.tsx`
- `src/app/[locale]/not-found.tsx`
- Access denied page component

---

### Task 5: Fix Matching Component Selectors

**Action:** Add data-testid to matching/compatibility components.

**Components:**
1. Matching results - `data-testid="matching-results"`
2. Compatibility card - `data-testid="compatibility-card"`
3. Teacher-student match - `data-testid="teacher-match-{id}"`

---

## must_haves (Goal Verification)

1. All student detail tabs have data-tab attributes
2. All admin pages have page-level data-testid
3. All counseling components have appropriate test IDs
4. All error pages have identifying test IDs
5. No regressions in existing functionality

---

## Success Criteria

- [ ] Student detail tabs: data-tab added to all 4 tabs
- [ ] Admin pages: 5 pages with page-level test IDs
- [ ] Counseling: calendar, modal, cards have test IDs
- [ ] Error pages: 404, 403, 500 have test IDs
- [ ] Matching: results and cards have test IDs
- [ ] Component build succeeds: `npm run build`
