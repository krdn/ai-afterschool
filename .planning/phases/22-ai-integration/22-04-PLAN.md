---
phase: 22-ai-integration
plan: 04
type: execute
wave: 2
depends_on: ["22-02"]
files_modified:
  - src/components/counseling/PersonalitySummaryCard.tsx
  - src/components/counseling/CompatibilityScoreCard.tsx
  - src/components/counseling/AISummaryGenerator.tsx
autonomous: true

must_haves:
  truths:
    - "성향 요약 카드 컴포넌트가 성향 정보를 1-2문장으로 표시한다"
    - "궁합 점수 카드 컴포넌트가 점수와 해석을 함께 표시한다"
    - "궁합 점수 세부 항목을 펼쳐볼 수 있다"
    - "AI 요약 생성 버튼을 클릭하면 요약이 생성된다"
  artifacts:
    - path: "src/components/counseling/PersonalitySummaryCard.tsx"
      provides: "성향 요약 표시 컴포넌트"
      exports: ["PersonalitySummaryCard"]
    - path: "src/components/counseling/CompatibilityScoreCard.tsx"
      provides: "궁합 점수 표시 컴포넌트"
      exports: ["CompatibilityScoreCard"]
    - path: "src/components/counseling/AISummaryGenerator.tsx"
      provides: "AI 요약 생성 컴포넌트"
      exports: ["AISummaryGenerator"]
  key_links:
    - from: "src/components/counseling/CompatibilityScoreCard.tsx"
      to: "src/components/ui/collapsible.tsx"
      via: "Collapsible 컴포넌트 사용"
      pattern: "Collapsible"
---

<objective>
AI 지원 패널 내부 컴포넌트 구현

Purpose: 사이드 패널에 표시될 성향 요약, 궁합 점수, AI 요약 생성 컴포넌트를 구현한다
Output: 3개의 AI 지원 컴포넌트 구현
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-ai-integration/22-CONTEXT.md
@.planning/phases/22-ai-integration/22-RESEARCH.md
@src/components/ui/card.tsx
@src/components/ui/badge.tsx
@src/components/ui/button.tsx
@src/components/ui/collapsible.tsx
@src/components/ui/alert.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: PersonalitySummaryCard 컴포넌트 구현</name>
  <files>src/components/counseling/PersonalitySummaryCard.tsx</files>
  <action>
학생의 성향 요약을 표시하는 카드 컴포넌트를 구현한다.

Props:
```typescript
interface PersonalitySummaryCardProps {
  summary: string | null
  studentName: string
  hasAnalysisData: boolean
  onStartAnalysis?: () => void
}
```

UI 구조:
1. 성향 요약이 있는 경우:
   - Card 내부에 요약 문장 표시
   - 아이콘: Brain (lucide-react)

2. 성향 요약이 없는 경우:
   - hasAnalysisData가 false이면: "성향 분석이 아직 완료되지 않았습니다"
   - hasAnalysisData가 true이면: "성향 요약 생성하기" 버튼 표시
   - onStartAnalysis 콜백 호출

스타일:
- Card 컴포넌트 사용
- muted 배경색
- 간결한 텍스트
  </action>
  <verify>
`npm run build` 성공
PersonalitySummaryCard export 확인
  </verify>
  <done>
성향 요약 카드 컴포넌트 구현 완료
  </done>
</task>

<task type="auto">
  <name>Task 2: CompatibilityScoreCard 컴포넌트 구현</name>
  <files>src/components/counseling/CompatibilityScoreCard.tsx</files>
  <action>
선생님-학생 궁합 점수를 표시하는 카드 컴포넌트를 구현한다.

Props:
```typescript
interface CompatibilityScoreCardProps {
  score: {
    overallScore: number
    breakdown: Record<string, number>
    reasons: string[]
  } | null
  onCalculate?: () => void
  isCalculating?: boolean
}
```

UI 구조:
1. 궁합 점수가 있는 경우:
   - 큰 숫자로 점수 표시 (text-4xl font-bold)
   - Badge로 해석 텍스트 표시 (getScoreInterpretation)
   - Collapsible로 세부 항목 펼치기:
     - MBTI 궁합, 사주 궁합, 학습스타일 궁합 등
   - 점수가 낮을 때 Alert로 상담 팁 표시

2. 궁합 점수가 없는 경우:
   - "궁합 점수가 아직 계산되지 않았습니다" 메시지
   - "지금 계산하기" 버튼 (onCalculate 콜백)

해석 함수:
```typescript
function getScoreInterpretation(score: number): string {
  if (score >= 80) return "매우 좋은 궁합"
  if (score >= 70) return "좋은 궁합"
  if (score >= 60) return "보통 궁합"
  return "노력이 필요한 궁합"
}

function getTeachingTip(score: number): string | null {
  if (score < 60) return "이 학생과는 천천히 진행하며, 자주 피드백을 확인하세요."
  return null
}
```

스타일:
- Badge variant: score >= 70 ? "default" : "secondary"
- Alert: 팁이 있을 때만 표시
  </action>
  <verify>
`npm run build` 성공
CompatibilityScoreCard export 확인
  </verify>
  <done>
궁합 점수 카드 컴포넌트 구현 완료
  </done>
</task>

<task type="auto">
  <name>Task 3: AISummaryGenerator 컴포넌트 구현</name>
  <files>src/components/counseling/AISummaryGenerator.tsx</files>
  <action>
AI 상담 요약 생성 컴포넌트를 구현한다.

Props:
```typescript
interface AISummaryGeneratorProps {
  sessionId: string
  onSummaryGenerated: (summary: string) => void
  disabled?: boolean
}
```

상태 관리:
- isGenerating: boolean (생성 중 상태)
- generatedSummary: string | null (생성된 요약)
- showRegenerate: boolean (재생성 버튼 표시)

UI 구조:
1. 초기 상태 (요약 미생성):
   - "AI 요약 생성" 버튼
   - 아이콘: Sparkles (lucide-react)
   - 설명: "상담 내용과 학생 성향을 고려한 요약을 생성합니다"

2. 생성 중:
   - 버튼 disabled
   - "생성 중..." 텍스트
   - 로딩 스피너

3. 생성 완료:
   - 생성된 요약 텍스트 표시 (p-4 border rounded-md bg-muted/50)
   - "요약 적용" 버튼 (primary)
   - "다시 생성" 버튼 (outline)
   - 안내: "적용 후에도 직접 수정할 수 있습니다"

Server Action 호출:
```typescript
const handleGenerate = async () => {
  setIsGenerating(true)
  try {
    const result = await generateCounselingSummaryAction(sessionId)
    if (result.success) {
      setGeneratedSummary(result.data)
      setShowRegenerate(true)
      toast.success("AI 요약이 생성되었습니다")
    } else {
      toast.error(result.error || "요약 생성에 실패했습니다")
    }
  } finally {
    setIsGenerating(false)
  }
}
```

주의:
- generateCounselingSummaryAction import (동적 import 아님, 직접 import)
- toast는 sonner 사용
  </action>
  <verify>
`npm run build` 성공
AISummaryGenerator export 확인
  </verify>
  <done>
AI 요약 생성 컴포넌트 구현 완료
  </done>
</task>

</tasks>

<verification>
- [ ] `npm run build` 성공
- [ ] PersonalitySummaryCard 정상 export
- [ ] CompatibilityScoreCard 정상 export
- [ ] AISummaryGenerator 정상 export
- [ ] Collapsible 컴포넌트 정상 동작
- [ ] 타입 안전성 확보
</verification>

<success_criteria>
1. 3개의 AI 지원 컴포넌트가 구현됨
2. 각 컴포넌트가 props에 따라 올바른 UI를 렌더링함
3. Collapsible을 사용한 세부 항목 펼치기 동작
4. 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/22-ai-integration/22-04-SUMMARY.md`
</output>
