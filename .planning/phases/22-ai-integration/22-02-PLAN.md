---
phase: 22-ai-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/sheet.tsx
  - src/components/ui/collapsible.tsx
autonomous: true

must_haves:
  truths:
    - "shadcn/ui Sheet 컴포넌트가 설치되어 사용 가능하다"
    - "shadcn/ui Collapsible 컴포넌트가 설치되어 사용 가능하다"
  artifacts:
    - path: "src/components/ui/sheet.tsx"
      provides: "Sheet, SheetContent, SheetHeader, SheetTitle 컴포넌트"
      exports: ["Sheet", "SheetContent", "SheetHeader", "SheetTitle", "SheetDescription", "SheetTrigger"]
    - path: "src/components/ui/collapsible.tsx"
      provides: "Collapsible, CollapsibleTrigger, CollapsibleContent 컴포넌트"
      exports: ["Collapsible", "CollapsibleTrigger", "CollapsibleContent"]
  key_links:
    - from: "src/components/ui/sheet.tsx"
      to: "@radix-ui/react-dialog"
      via: "Radix primitive import"
      pattern: "@radix-ui/react-dialog"
---

<objective>
shadcn/ui 컴포넌트 설치 (Sheet, Collapsible)

Purpose: AI 지원 사이드 패널과 궁합 점수 세부 항목 펼치기를 위한 UI 컴포넌트를 준비한다
Output: Sheet, Collapsible 컴포넌트 설치 완료
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-ai-integration/22-CONTEXT.md
@.planning/phases/22-ai-integration/22-RESEARCH.md
@src/components/ui/dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn/ui Sheet 컴포넌트 설치</name>
  <files>src/components/ui/sheet.tsx</files>
  <action>
shadcn/ui CLI로 Sheet 컴포넌트를 설치한다.

```bash
npx shadcn@latest add sheet
```

설치 후 확인:
- Sheet 컴포넌트가 src/components/ui/sheet.tsx에 생성됨
- @radix-ui/react-dialog 의존성 확인 (이미 설치된 radix-ui 패키지에 포함)

Sheet 컴포넌트 구조:
- Sheet: 루트 컴포넌트
- SheetTrigger: 트리거 버튼
- SheetContent: 사이드 패널 내용 (side="right" 지원)
- SheetHeader: 헤더 영역
- SheetTitle: 제목
- SheetDescription: 설명
- SheetClose: 닫기 버튼

주의: CLI가 자동으로 필요한 의존성을 설치하지만, radix-ui 통합 패키지(1.4.3)가 이미 설치되어 있어 추가 설치 불필요
  </action>
  <verify>
`ls src/components/ui/sheet.tsx` 파일 존재 확인
`npm run build` 성공
  </verify>
  <done>
Sheet 컴포넌트가 설치되고 import 가능
  </done>
</task>

<task type="auto">
  <name>Task 2: shadcn/ui Collapsible 컴포넌트 설치</name>
  <files>src/components/ui/collapsible.tsx</files>
  <action>
shadcn/ui CLI로 Collapsible 컴포넌트를 설치한다.

```bash
npx shadcn@latest add collapsible
```

설치 후 확인:
- Collapsible 컴포넌트가 src/components/ui/collapsible.tsx에 생성됨
- @radix-ui/react-collapsible 의존성 확인 (이미 radix-ui 패키지에 포함 확인됨)

Collapsible 컴포넌트 구조:
- Collapsible: 루트 컴포넌트
- CollapsibleTrigger: 펼치기/접기 트리거
- CollapsibleContent: 펼쳐지는 내용

용도: 궁합 점수 세부 항목(MBTI, 사주, 학습스타일 등) 펼쳐보기에 사용
  </action>
  <verify>
`ls src/components/ui/collapsible.tsx` 파일 존재 확인
`npm run build` 성공
  </verify>
  <done>
Collapsible 컴포넌트가 설치되고 import 가능
  </done>
</task>

</tasks>

<verification>
- [ ] src/components/ui/sheet.tsx 파일 존재
- [ ] src/components/ui/collapsible.tsx 파일 존재
- [ ] `npm run build` 성공
- [ ] Sheet, SheetContent 등 모든 export 확인
- [ ] Collapsible, CollapsibleTrigger, CollapsibleContent export 확인
</verification>

<success_criteria>
1. Sheet 컴포넌트 설치 완료
2. Collapsible 컴포넌트 설치 완료
3. 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/22-ai-integration/22-02-SUMMARY.md`
</output>
