---
phase: 22-ai-integration
plan: 06
type: execute
wave: 3
depends_on: ["22-03", "22-05"]
files_modified:
  - src/components/counseling/CounselingSessionForm.tsx
  - src/components/counseling/NewCounselingClient.tsx
autonomous: true

must_haves:
  truths:
    - "상담 기록 폼에 AI 지원 패널 토글 버튼이 있다"
    - "버튼 클릭 시 사이드 패널이 열린다"
    - "AI 요약 적용 시 상담 내용 필드에 반영된다"
    - "기존 상담 폼 기능이 그대로 동작한다"
  artifacts:
    - path: "src/components/counseling/CounselingSessionForm.tsx"
      provides: "AI 지원 패널 통합된 상담 폼"
      contains: "AISupportPanel"
  key_links:
    - from: "src/components/counseling/CounselingSessionForm.tsx"
      to: "src/components/counseling/AISupportPanel.tsx"
      via: "AISupportPanel 렌더링"
      pattern: "AISupportPanel"
---

<objective>
상담 폼에 AI 지원 패널 통합

Purpose: 기존 상담 기록 폼에 AI 지원 사이드 패널을 연동하여 선생님이 상담 중 AI 지원을 받을 수 있게 한다
Output: CounselingSessionForm에 AI 지원 패널 토글 및 연동 완료
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-ai-integration/22-CONTEXT.md
@.planning/phases/22-ai-integration/22-RESEARCH.md
@src/components/counseling/CounselingSessionForm.tsx
@src/components/counseling/NewCounselingClient.tsx
@src/components/counseling/AISupportPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: CounselingSessionForm에 AI 지원 패널 버튼 추가</name>
  <files>src/components/counseling/CounselingSessionForm.tsx</files>
  <action>
기존 CounselingSessionForm에 AI 지원 패널 토글 버튼을 추가하고 패널을 연동한다.

Props 확장:
```typescript
interface CounselingSessionFormProps {
  studentId: string
  studentName: string  // 추가: 패널에 표시용
  sessionId?: string   // 추가: 기존 상담 수정 시
  onSuccess?: () => void
}
```

상태 추가:
```typescript
const [isPanelOpen, setIsPanelOpen] = useState(true)  // CONTEXT.md: 기본 열림
```

UI 변경:
1. CardHeader에 AI 지원 버튼 추가:
```tsx
<CardHeader className="flex flex-row items-center justify-between">
  <CardTitle>상담 기록</CardTitle>
  <Button
    type="button"
    variant="outline"
    size="sm"
    onClick={() => setIsPanelOpen(true)}
    className="gap-2"
  >
    <Sparkles className="h-4 w-4" />
    AI 지원
  </Button>
</CardHeader>
```

2. AISupportPanel 렌더링 추가:
```tsx
<AISupportPanel
  studentId={studentId}
  studentName={studentName}
  sessionId={sessionId}
  isOpen={isPanelOpen}
  onOpenChange={setIsPanelOpen}
  onAISummaryApply={handleAISummaryApply}
/>
```

3. handleAISummaryApply 구현:
```typescript
const handleAISummaryApply = (aiSummary: string) => {
  // 기존 summary 필드에 AI 요약 추가 (덮어쓰기 아님, 앞에 추가)
  const currentSummary = form.getValues("summary")
  const newSummary = aiSummary + (currentSummary ? `\n\n---\n\n${currentSummary}` : "")
  form.setValue("summary", newSummary, { shouldDirty: true })
  toast.success("AI 요약이 적용되었습니다. 필요시 수정하세요.")
}
```

레이아웃 조정:
- Card를 flex 컨테이너로 변경하지 않음 (Sheet가 overlay로 표시됨)
- 기존 레이아웃 유지

주의:
- 기존 폼 기능(제출, 유효성 검사 등) 손상 없이 추가
- Button에 type="button" 필수 (form 제출 방지)
  </action>
  <verify>
`npm run build` 성공
AI 지원 버튼이 CardHeader에 표시됨
  </verify>
  <done>
상담 폼에 AI 지원 패널 토글 버튼 추가 완료
  </done>
</task>

<task type="auto">
  <name>Task 2: AI 요약 저장 로직 추가</name>
  <files>src/components/counseling/CounselingSessionForm.tsx</files>
  <action>
상담 기록 저장 시 AI 요약도 함께 저장되도록 수정한다.

1. 상태 추가:
```typescript
const [appliedAISummary, setAppliedAISummary] = useState<string | null>(null)
```

2. handleAISummaryApply 수정:
```typescript
const handleAISummaryApply = (aiSummary: string) => {
  setAppliedAISummary(aiSummary)  // AI 요약 원본 저장
  // ... 기존 summary 필드 업데이트 로직
}
```

3. onSubmit 수정:
```typescript
const onSubmit = async (data: CounselingFormData) => {
  // ... 기존 로직

  // AI 요약이 적용되었으면 별도 필드에 저장
  if (appliedAISummary) {
    formData.append("aiSummary", appliedAISummary)
  }

  const result = await recordCounselingAction(undefined, formData)
  // ...
}
```

4. recordCounselingAction 수정 필요 여부 확인:
   - src/lib/actions/performance.ts의 recordCounselingAction이 aiSummary를 처리하는지 확인
   - 필요시 aiSummary 필드 처리 추가

주의:
- AI 요약은 aiSummary 필드에 별도 저장 (CONTEXT.md 결정사항)
- summary 필드에는 선생님이 수정한 최종 내용만 저장
  </action>
  <verify>
`npm run build` 성공
상담 저장 시 aiSummary 필드가 전송됨
  </verify>
  <done>
AI 요약 저장 로직 추가 완료
  </done>
</task>

<task type="auto">
  <name>Task 3: recordCounselingAction에 aiSummary 처리 추가</name>
  <files>src/lib/actions/performance.ts</files>
  <action>
recordCounselingAction에 aiSummary 필드 처리를 추가한다.

1. FormData에서 aiSummary 추출:
```typescript
const aiSummary = formData.get("aiSummary") as string | null
```

2. CounselingSession 생성 시 aiSummary 포함:
```typescript
await db.counselingSession.create({
  data: {
    // ... 기존 필드들
    aiSummary: aiSummary || null,
  },
})
```

주의:
- aiSummary는 선택적 필드 (null 허용)
- 기존 로직에 영향 없이 추가
  </action>
  <verify>
`npm run build` 성공
recordCounselingAction이 aiSummary를 DB에 저장
  </verify>
  <done>
recordCounselingAction에 aiSummary 처리 추가 완료
  </done>
</task>

</tasks>

<verification>
- [ ] `npm run build` 성공
- [ ] AI 지원 버튼이 상담 폼에 표시됨
- [ ] 버튼 클릭 시 패널이 열림
- [ ] AI 요약 적용 시 summary 필드에 반영됨
- [ ] 상담 저장 시 aiSummary가 DB에 저장됨
- [ ] 기존 상담 폼 기능이 정상 동작
</verification>

<success_criteria>
1. 상담 폼에 AI 지원 버튼 추가됨
2. AI 요약 적용 및 저장 연동됨
3. 기존 폼 기능 유지
4. 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/22-ai-integration/22-06-SUMMARY.md`
</output>
