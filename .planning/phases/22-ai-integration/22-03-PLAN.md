---
phase: 22-ai-integration
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/lib/actions/counseling-ai.ts
autonomous: true

must_haves:
  truths:
    - "학생 AI 지원 데이터(성향, 궁합)를 조회하는 Server Action이 존재한다"
    - "AI 상담 요약을 생성하는 Server Action이 존재한다"
    - "AI 요약을 저장하는 Server Action이 존재한다"
    - "성향 요약을 사전 생성하는 Server Action이 존재한다"
  artifacts:
    - path: "src/lib/actions/counseling-ai.ts"
      provides: "AI 상담 지원 Server Actions"
      exports: ["getStudentAISupportDataAction", "generateCounselingSummaryAction", "saveAISummaryAction", "generatePersonalitySummaryAction"]
  key_links:
    - from: "src/lib/actions/counseling-ai.ts"
      to: "src/lib/ai/router.ts"
      via: "generateWithProvider 호출"
      pattern: "generateWithProvider"
    - from: "src/lib/actions/counseling-ai.ts"
      to: "src/lib/db/compatibility-result.ts"
      via: "getCompatibilityResult 호출"
      pattern: "getCompatibilityResult"
---

<objective>
AI 상담 지원 Server Actions 구현

Purpose: 성향/궁합 데이터 조회, AI 요약 생성/저장을 위한 Server Actions를 구현한다
Output: counseling-ai.ts에 4개의 Server Actions 구현
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ai-integration/22-CONTEXT.md
@.planning/phases/22-ai-integration/22-RESEARCH.md
@src/lib/ai/router.ts
@src/lib/db/compatibility-result.ts
@src/lib/db/personality-summary.ts
@src/lib/ai/counseling-prompts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: getStudentAISupportDataAction 구현</name>
  <files>src/lib/actions/counseling-ai.ts</files>
  <action>
학생의 AI 지원 데이터(성향 요약, 궁합 점수)를 조회하는 Server Action을 구현한다.

```typescript
"use server"

import { z } from "zod"
import { verifySession } from "@/lib/auth/session"
import { getUnifiedPersonalityData } from "@/lib/db/personality-summary"
import { getCompatibilityResult } from "@/lib/db/compatibility-result"
import { db } from "@/lib/db"

const studentIdSchema = z.string().cuid()

export async function getStudentAISupportDataAction(studentId: string) {
  // 1. 인증 확인
  // 2. 학생 조회 (teacherId로 권한 확인)
  // 3. personalitySummary 필드 조회
  // 4. 궁합 점수 조회 (getCompatibilityResult)
  // 5. 궁합 점수가 없고 성향 데이터가 있으면 자동 계산 트리거 가능성 표시
  // 반환: { studentName, personalitySummary, compatibility, canCalculateCompatibility }
}
```

반환 타입:
```typescript
type AISupportData = {
  studentName: string
  personalitySummary: string | null
  compatibility: {
    overallScore: number
    breakdown: Record<string, number>
    reasons: string[]
  } | null
  canCalculateCompatibility: boolean
  hasAnalysisData: boolean
}
```

주의:
- teacherId로 권한 확인 필수 (다른 팀 학생 접근 방지)
- 궁합 점수가 null이어도 에러가 아님 (UI에서 "지금 계산하기" 버튼 표시)
  </action>
  <verify>
`npm run build` 성공
getStudentAISupportDataAction export 확인
  </verify>
  <done>
학생 AI 지원 데이터 조회 Server Action 구현 완료
  </done>
</task>

<task type="auto">
  <name>Task 2: generateCounselingSummaryAction 및 saveAISummaryAction 구현</name>
  <files>src/lib/actions/counseling-ai.ts</files>
  <action>
AI 상담 요약 생성 및 저장 Server Actions를 구현한다.

1. `generateCounselingSummaryAction(sessionId: string)`:
   - 상담 세션 조회 (teacherId로 권한 확인)
   - 학생 성향 정보 조회 (getUnifiedPersonalityData)
   - 이전 상담 이력 조회 (최근 3개)
   - 프롬프트 빌더로 프롬프트 생성 (buildCounselingSummaryPrompt)
   - generateWithProvider로 요약 생성
   - featureType: 'counseling_suggest'
   - 반환: { success: true, data: generatedSummary }

2. `saveAISummaryAction(sessionId: string, aiSummary: string)`:
   - 상담 세션 조회 (teacherId로 권한 확인)
   - CounselingSession.aiSummary 필드 업데이트
   - 반환: { success: true }

3. `generatePersonalitySummaryAction(studentId: string)`:
   - 학생 성향 데이터 조회 (getUnifiedPersonalityData)
   - 프롬프트 빌더로 프롬프트 생성 (buildPersonalitySummaryPrompt)
   - generateWithProvider로 요약 생성
   - featureType: 'personality_summary'
   - Student.personalitySummary 필드 업데이트
   - 반환: { success: true, data: summary }

주의:
- generateWithProvider 호출 시 teacherId 전달 (사용량 추적용)
- temperature: 0.3 (낮은 온도로 일관된 요약)
- maxOutputTokens: 500 (요약은 짧게)
  </action>
  <verify>
`npm run build` 성공
generateCounselingSummaryAction export 확인
saveAISummaryAction export 확인
generatePersonalitySummaryAction export 확인
  </verify>
  <done>
AI 요약 생성/저장 Server Actions 구현 완료
  </done>
</task>

</tasks>

<verification>
- [ ] `npm run build` 성공
- [ ] getStudentAISupportDataAction 정상 export
- [ ] generateCounselingSummaryAction 정상 export
- [ ] saveAISummaryAction 정상 export
- [ ] generatePersonalitySummaryAction 정상 export
- [ ] 권한 확인 로직 포함 (teacherId 검증)
</verification>

<success_criteria>
1. 4개의 Server Actions가 타입 안전하게 구현됨
2. 기존 함수들(getUnifiedPersonalityData, getCompatibilityResult, generateWithProvider) 재사용
3. 권한 확인 로직 포함
4. 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/22-ai-integration/22-03-SUMMARY.md`
</output>
