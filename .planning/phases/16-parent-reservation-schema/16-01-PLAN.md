---
phase: 16-parent-reservation-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma

autonomous: true

must_haves:
  truths:
    - "학생별로 복수의 학부모 정보(이름, 연락처, 관계)를 등록할 수 있다"
    - "학부모 중 한 명을 주 연락처로 지정할 수 있다"
    - "상담 예약 데이터(날짜, 시간, 학부모, 주제)가 저장된다"
    - "예약 상태(SCHEDULED/COMPLETED/CANCELLED/NO_SHOW)가 관리된다"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Parent, ParentCounselingReservation 모델 및 관련 enum"
      contains: "model Parent"
    - path: "prisma/schema.prisma"
      provides: "ParentRelation, ReservationStatus enum"
      contains: "enum ParentRelation"
    - path: "prisma/schema.prisma"
      provides: "Student.primaryParentId FK"
      contains: "primaryParentId"
  key_links:
    - from: "Parent"
      to: "Student"
      via: "studentId FK with onDelete: Cascade"
      pattern: "student.*Student.*@relation.*onDelete: Cascade"
    - from: "Student"
      to: "Parent"
      via: "primaryParentId FK with onDelete: SetNull"
      pattern: "primaryParent.*Parent.*@relation.*onDelete: SetNull"
    - from: "ParentCounselingReservation"
      to: "CounselingSession"
      via: "counselingSessionId FK with onDelete: SetNull"
      pattern: "counselingSession.*CounselingSession.*@relation.*onDelete: SetNull"
---

<objective>
Parent 및 ParentCounselingReservation Prisma 모델을 정의하고 DB에 반영

Purpose: 학부모 상담 예약 시스템의 데이터 기반 구축. Student FK를 통한 간접 팀 격리로 기존 RBAC 패턴 재사용
Output: prisma/schema.prisma에 새 모델 추가, DB 테이블 생성 완료
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-parent-reservation-schema/16-CONTEXT.md
@.planning/phases/16-parent-reservation-schema/16-RESEARCH.md

@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Parent and ParentCounselingReservation models to Prisma schema</name>
  <files>prisma/schema.prisma</files>
  <action>
prisma/schema.prisma 파일 끝에 다음 내용 추가:

1. **ParentRelation enum** 추가 (BloodType enum 근처):
```prisma
enum ParentRelation {
  FATHER
  MOTHER
  GRANDFATHER
  GRANDMOTHER
  OTHER
}
```

2. **ReservationStatus enum** 추가 (CounselingType enum 근처):
```prisma
enum ReservationStatus {
  SCHEDULED
  COMPLETED
  CANCELLED
  NO_SHOW
}
```

3. **Parent 모델** 추가 (StudentSatisfaction 모델 뒤):
```prisma
model Parent {
  id            String         @id @default(cuid())
  studentId     String
  name          String
  phone         String
  email         String?
  relation      ParentRelation
  relationOther String?
  note          String?
  isPrimary     Boolean        @default(false)
  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt

  student                Student                      @relation("StudentParents", fields: [studentId], references: [id], onDelete: Cascade)
  studentAsPrimary       Student[]                    @relation("StudentPrimaryParent")
  counselingReservations ParentCounselingReservation[]

  @@index([studentId])
  @@index([studentId, isPrimary])
}
```

4. **ParentCounselingReservation 모델** 추가 (Parent 모델 뒤):
```prisma
model ParentCounselingReservation {
  id                  String            @id @default(cuid())
  scheduledAt         DateTime
  studentId           String
  teacherId           String
  parentId            String
  topic               String
  status              ReservationStatus @default(SCHEDULED)
  counselingSessionId String?           @unique
  createdAt           DateTime          @default(now())
  updatedAt           DateTime          @updatedAt

  student           Student            @relation(fields: [studentId], references: [id], onDelete: Cascade)
  teacher           Teacher            @relation(fields: [teacherId], references: [id], onDelete: Cascade)
  parent            Parent             @relation(fields: [parentId], references: [id], onDelete: Cascade)
  counselingSession CounselingSession? @relation(fields: [counselingSessionId], references: [id], onDelete: SetNull)

  @@index([studentId])
  @@index([teacherId])
  @@index([parentId])
  @@index([scheduledAt])
  @@index([studentId, scheduledAt])
  @@index([teacherId, scheduledAt])
}
```

5. **Student 모델 확장** - 기존 Student 모델에 다음 필드/관계 추가:
- `primaryParentId String?` 필드 추가 (id 필드 근처)
- `primaryParent Parent? @relation("StudentPrimaryParent", fields: [primaryParentId], references: [id], onDelete: SetNull)` 관계 추가
- `parents Parent[] @relation("StudentParents")` 관계 추가
- `counselingReservations ParentCounselingReservation[]` 관계 추가
- `@@index([primaryParentId])` 인덱스 추가

6. **Teacher 모델 확장** - 기존 Teacher 모델에 다음 관계 추가:
- `counselingReservations ParentCounselingReservation[]` 관계 추가

7. **CounselingSession 모델 확장** - 기존 CounselingSession 모델에 다음 관계 추가:
- `reservation ParentCounselingReservation?` 역참조 관계 추가

**중요 규칙:**
- 기존 모델 필드 순서 유지
- 새 관계는 기존 관계 목록 끝에 추가
- onDelete 정책: Student FK는 Cascade (학생 삭제 시 학부모/예약도 삭제), primaryParentId는 SetNull (학부모 삭제 시 null로), counselingSessionId는 SetNull (상담 기록 삭제 시 null로)
  </action>
  <verify>
- `npx prisma validate` 실행하여 스키마 문법 오류 없음 확인
- Parent 모델에 studentId, name, phone, relation, isPrimary 필드 존재 확인
- ParentCounselingReservation 모델에 scheduledAt, studentId, teacherId, parentId, topic, status 필드 존재 확인
- Student 모델에 primaryParentId, parents, counselingReservations 필드/관계 존재 확인
  </verify>
  <done>
- ParentRelation enum (FATHER, MOTHER, GRANDFATHER, GRANDMOTHER, OTHER) 정의됨
- ReservationStatus enum (SCHEDULED, COMPLETED, CANCELLED, NO_SHOW) 정의됨
- Parent 모델이 Student와 1:N 관계로 연결됨
- ParentCounselingReservation 모델이 Student, Teacher, Parent, CounselingSession과 연결됨
- Student.primaryParentId FK로 주 연락처 빠른 조회 가능
- 모든 FK에 적절한 인덱스 추가됨
  </done>
</task>

<task type="auto">
  <name>Task 2: Run database migration with prisma db push</name>
  <files>-</files>
  <action>
1. DB 마이그레이션 실행:
```bash
cd /home/gon/projects/ai/ai-afterschool
npx prisma db push
```

**참고:**
- Shadow DB 이슈(7회 발생 이력)로 `prisma migrate dev` 대신 `db push` 사용
- 프로덕션 배포 시에는 `prisma migrate deploy` 사용

2. Prisma Client 재생성 (db push가 자동으로 실행하지만 명시적으로):
```bash
npx prisma generate
```

3. 테이블 생성 확인:
```bash
npx prisma db execute --stdin <<< "SELECT table_name FROM information_schema.tables WHERE table_schema = 'public' AND table_name IN ('Parent', 'ParentCounselingReservation');"
```

또는 Prisma Studio로 확인:
```bash
npx prisma studio
```
  </action>
  <verify>
- `npx prisma db push` 명령이 에러 없이 완료
- Prisma Studio 또는 psql에서 Parent, ParentCounselingReservation 테이블 존재 확인
- Student 테이블에 primaryParentId 컬럼 추가 확인
- 모든 인덱스 생성 확인 (pg_indexes 조회)
  </verify>
  <done>
- Parent 테이블이 PostgreSQL에 생성됨
- ParentCounselingReservation 테이블이 PostgreSQL에 생성됨
- Student.primaryParentId 컬럼이 추가됨 (nullable)
- 모든 외래 키 제약조건과 인덱스가 적용됨
- Prisma Client에 새 모델 타입이 포함됨
  </done>
</task>

</tasks>

<verification>
- [ ] `npx prisma validate` 통과
- [ ] `npx prisma db push` 성공
- [ ] Prisma Studio에서 Parent, ParentCounselingReservation 테이블 확인
- [ ] Student 테이블에 primaryParentId 컬럼 존재
- [ ] `npx prisma generate` 후 Prisma Client에 Parent, ParentCounselingReservation 타입 존재
</verification>

<success_criteria>
- Parent 모델: id, studentId, name, phone, email?, relation, relationOther?, note?, isPrimary, createdAt, updatedAt 필드와 Student 관계
- ParentCounselingReservation 모델: id, scheduledAt, studentId, teacherId, parentId, topic, status, counselingSessionId?, createdAt, updatedAt 필드와 4개 모델 관계
- Student 모델에 primaryParentId FK, parents, counselingReservations 관계 추가
- Teacher 모델에 counselingReservations 관계 추가
- CounselingSession 모델에 reservation 역참조 추가
- 모든 테이블/인덱스가 PostgreSQL에 생성됨
</success_criteria>

<output>
After completion, create `.planning/phases/16-parent-reservation-schema/16-01-SUMMARY.md`
</output>
