---
phase: 30-issue-ui-screenshot
plan: 03
type: execute
wave: 3
depends_on: []
files_modified:
  - src/components/issues/issue-form.tsx
  - src/components/issues/issue-report-modal.tsx
  - src/lib/actions/issues.ts
  - src/lib/github/services.ts
autonomous: true

must_haves:
  truths:
    - "사용자가 이슈 제목, 설명, 카테고리를 입력할 수 있다"
    - "카테고리는 버그/기능수정/기능추가/UI개선/기타 중 선택 가능하다"
    - "이슈 폼이 유효성 검사를 수행한다"
    - "이슈 보고 모달이 Radix Dialog로 구현되어 있다"
    - "모달에서 이슈 폼과 스크린샷 캡처가 함께 표시된다"
  artifacts:
    - path: "src/components/issues/issue-form.tsx"
      provides: "이슈 입력 폼"
      exports: ["IssueForm"]
    - path: "src/components/issues/issue-report-modal.tsx"
      provides: "이슈 보고 모달"
      exports: ["IssueReportModal"]
  key_links:
    - from: "issue-form.tsx"
      to: "src/lib/validations/issues.ts"
      via: "import { IssueSchema }"
    - from: "issue-report-modal.tsx"
      to: "issue-form.tsx"
      via: "import { IssueForm }"
    - from: "issue-report-modal.tsx"
      to: "screenshot-capture.tsx"
      via: "import { ScreenshotCapture }"
---

<objective>
이슈 입력 폼과 모달 컴포넌트 구현

Purpose: 사용자가 이슈 정보를 입력하고 스크린샷과 함께 제출할 수 있는 UI
Output: IssueForm (폼), IssueReportModal (모달 래퍼)
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/validations/issues.ts
@src/lib/actions/issues.ts
@src/lib/github/services.ts
@src/components/ui/dialog.tsx
@src/components/ui/select.tsx
@src/components/ui/button.tsx
@src/components/ui/label.tsx
@src/components/ui/input.tsx
@src/components/ui/textarea.tsx

## Category Mapping (한국어)
- BUG: 버그
- FEATURE: 기능추가
- IMPROVEMENT: 기능수정
- UI_UX: UI개선
- DOCUMENTATION: 문서
- PERFORMANCE: 성능
- SECURITY: 보안
- 기타는 없음 - DOCUMENTATION 등으로 대체

## Existing Patterns
- IssueSchema에 category enum 정의됨
- createIssue Server Action 있음 (screenshotUrl, userContext 미지원 - 수정 필요)
- generateIssueBody에 screenshot URL 추가 필요
</context>

<tasks>

<task type="auto">
  <name>Task 1: IssueForm 컴포넌트 생성</name>
  <files>src/components/issues/issue-form.tsx</files>
  <action>
    이슈 정보 입력 폼 컴포넌트 구현.
    
    파일: src/components/issues/issue-form.tsx
    
    Props 인터페이스:
    ```typescript
    interface IssueFormProps {
      onSubmit: (data: IssueFormData) => void;
      isSubmitting?: boolean;
      screenshotUrl?: string;  // 선택적 스크린샷 URL
    }
    
    interface IssueFormData {
      title: string;
      description: string;
      category: IssueCategory;
      screenshotUrl?: string;
    }
    ```
    
    폼 필드:
    1. 제목 (Input)
       - placeholder: "이슈 제목을 입력하세요"
       - maxLength: 200
       - 필수
    2. 카테고리 (Select)
       - 옵션: 버그(BUG), 기능추가(FEATURE), 기능수정(IMPROVEMENT), UI개선(UI_UX), 기타(DOCUMENTATION)
       - 기본값: BUG
       - 필수
    3. 설명 (Textarea)
       - placeholder: "이슈에 대한 상세 설명을 입력하세요"
       - rows: 5
       - maxLength: 5000
       - 선택
    4. 스크린샷 미리보기 (있을 경우)
       - screenshotUrl prop이 있으면 작은 미리보기 표시
       - "스크린샷 첨부됨" 텍스트 + 삭제 버튼
    
    유효성 검사:
    - IssueSchema 사용 (src/lib/validations/issues.ts)
    - 제목 3자 이상
    - 카테고리 필수
    
    UI 구성:
    - Label + Input/Select/Textarea 쌍
    - 에러 메시지 표시 (빨간색 텍스트)
    - 제출 버튼 ("이슈 등록하기")
    
    상태 관리:
    - useState로 폼 상태 관리
    - useActionState 또는 직접 onSubmit 호출
  </action>
  <verify>
    cat src/components/issues/issue-form.tsx
    head -60 src/components/issues/issue-form.tsx | grep -E "(use client|interface|Props|IssueSchema)"
    npx tsc --noEmit src/components/issues/issue-form.tsx 2>&1 | head -20
  </verify>
  <done>
    issue-form.tsx 파일이 존재하고:
    - 'use client' 선언
    - IssueFormProps, IssueFormData 인터페이스
    - title, category, description 입력 필드
    - IssueSchema로 유효성 검사
    - screenshotUrl 미리보기 표시 (있을 경우)
    - onSubmit 콜백으로 데이터 전달
  </done>
</task>

<task type="auto">
  <name>Task 2: 이슈 생성 액션 및 GitHub 서비스 수정</name>
  <files>
    src/lib/actions/issues.ts
    src/lib/github/services.ts
  </files>
  <action>
    기존 createIssue Server Action과 generateIssueBody를 수정하여 스크린샷과 사용자 컨텍스트 지원.
    
    ## src/lib/actions/issues.ts 수정:
    
    createIssue 함수 수정:
    1. formData에서 screenshotUrl, userContext 추출
    2. DB 저장 시 screenshotUrl, userContext 필드 포함
    3. generateIssueBody 호출 시 screenshotUrl, userContext 전달
    
    변경된 시그니처 (기존과 호환되도록):
    ```typescript
    // formData에 추가 필드:
    // - screenshotUrl: string (optional)
    // - userContext: JSON string (optional)
    ```
    
    ## src/lib/github/services.ts 수정:
    
    generateIssueBody 함수 수정:
    ```typescript
    export function generateIssueBody(params: {
      title: string
      description?: string
      category: IssueCategory
      priority: string
      creatorName: string
      screenshotUrl?: string      // 추가
      userContext?: {             // 추가
        role: string
        url: string
        timestamp: string
      }
    }): string
    ```
    
    본문 템플릿 수정:
    - screenshotUrl이 있으면: `![스크린샷](${screenshotUrl})` 추가
    - userContext가 있으면 "사용자 정보" 섹션 추가:
      ```
      **사용자 정보:**
      - 역할: {role}
      - 페이지: {url}
      - 발생 시각: {timestamp}
      ```
    
    주의: 기존 IssueEvent, AuditLog 기록 로직은 그대로 유지
  </action>
  <verify>
    grep -n "screenshotUrl" src/lib/actions/issues.ts
    grep -n "userContext" src/lib/actions/issues.ts
    grep -n "screenshotUrl" src/lib/github/services.ts
    grep -n "userContext" src/lib/github/services.ts
    npx tsc --noEmit src/lib/actions/issues.ts src/lib/github/services.ts 2>&1 | head -20
  </verify>
  <done>
    issues.ts 수정됨:
    - createIssue에서 screenshotUrl, userContext formData 추출
    - db.issue.create에 screenshotUrl, userContext 포함
    
    services.ts 수정됨:
    - generateIssueBody에 screenshotUrl, userContext 파라미터 추가
    - 본문에 스크린샷 이미지 마크다운 추가
    - 본문에 사용자 정보 섹션 추가
  </done>
</task>

<task type="auto">
  <name>Task 3: IssueReportModal 컴포넌트 생성</name>
  <files>src/components/issues/issue-report-modal.tsx</files>
  <action>
    이슈 보고 모달 컴포넌트 구현. IssueForm과 ScreenshotCapture를 통합.
    
    파일: src/components/issues/issue-report-modal.tsx
    
    Props 인터페이스:
    ```typescript
    interface IssueReportModalProps {
      isOpen: boolean;
      onClose: () => void;
      userRole: string;  // 현재 사용자 역할 (userContext용)
    }
    ```
    
    낮은 수준 구성:
    1. Radix Dialog (shadcn/ui Dialog 컴포넌트 사용)
       - Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription
       - max-width: 600px (lg)
    2. 모달 헤더
       - 제목: "이슈 보고하기"
       - 설명: "발견하신 문제나 개선사항을 알려주세요"
    3. 모달 본문 (세션션 레이아웃)
       - 상단: ScreenshotCapture 컴포넌트 (스크린샷 섹션)
       - 하단: IssueForm 컴포넌트 (이슈 정보 입력)
    4. 상태 관리
       - screenshotUrl: string | null (캡처 완료된 이미지 URL)
       - isSubmitting: boolean (제출 중)
       - submitResult: 성공/실패 결과
    
    데이터 흐름:
    1. ScreenshotCapture의 onCapture → screenshotUrl 상태 설정
    2. IssueForm의 onSubmit → createIssue Server Action 호출
    3. createIssue 호출 시 formData에 screenshotUrl, userContext(JSON) 추가
    
    userContext 생성:
    ```typescript
    const userContext = {
      role: props.userRole,
      url: typeof window !== 'undefined' ? window.location.href : '',
      timestamp: new Date().toISOString(),
    };
    ```
    
    성공/실패 처리:
    - 성공: "이슈가 등록되었습니다" 메시지 + 모달 닫기 (또는 1초 후 닫기)
    - 실패: 에러 메시지 표시 (IssueForm 내부에서 처리)
    
    스타일:
    - DialogContent className: "max-w-2xl max-h-[90vh] overflow-y-auto"
    - 섹션 구분선: border-t pt-4 mt-4
  </action>
  <verify>
    cat src/components/issues/issue-report-modal.tsx
    head -80 src/components/issues/issue-report-modal.tsx | grep -E "(use client|interface|Props|Dialog|IssueForm|ScreenshotCapture)"
    npx tsc --noEmit src/components/issues/issue-report-modal.tsx 2>&1 | head -20
  </verify>
  <done>
    issue-report-modal.tsx 파일이 존재하고:
    - 'use client' 선언
    - IssueReportModalProps 인터페이스 (isOpen, onClose, userRole)
    - Dialog, DialogContent 등 shadcn/ui 컴포넌트 사용
    - ScreenshotCapture 컴포넌트 통합
    - IssueForm 컴포넌트 통합
    - screenshotUrl 상태 관리
    - userContext 생성 및 전달
  </done>
</task>

</tasks>

<verification>
- src/components/issues/issue-form.tsx 존재 및 타입 체크 통과
- src/components/issues/issue-report-modal.tsx 존재 및 타입 체크 통과
- src/lib/actions/issues.ts 수정 확인 (screenshotUrl, userContext 처리)
- src/lib/github/services.ts 수정 확인 (generateIssueBody에 이미지/컨텍스트 추가)
</verification>

<success_criteria>
- IssueForm: title, category, description 입력 + 유효성 검사
- IssueReportModal: Dialog + IssueForm + ScreenshotCapture 통합
- createIssue 액션: screenshotUrl, userContext 저장
- generateIssueBody: 스크린샷 이미지 + 사용자 정보 포함
- 타입스크립트 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/30-issue-ui-screenshot/30-03-SUMMARY.md`
</output>
