---
phase: 30-issue-ui-screenshot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/screenshot/capture.ts
  - src/lib/storage/image-storage.ts
  - package.json
autonomous: true

must_haves:
  truths:
    - "modern-screenshot 라이브러리가 설치되어 있다"
    - "화면 캡처 유틸리티가 DOM에서 요소를 캡처할 수 있다"
    - "이미지 스토리지가 MinIO에 이미지를 업로드하고 공개 URL을 반환한다"
  artifacts:
    - path: "src/lib/screenshot/capture.ts"
      provides: "화면 캡처 기능"
      exports: ["captureScreenshot", "captureElement"]
    - path: "src/lib/storage/image-storage.ts"
      provides: "이미지 업로드 기능"
      exports: ["uploadImage", "getImageUrl"]
  key_links:
    - from: "capture.ts"
      to: "modern-screenshot"
      via: "import { toBlob } from 'modern-screenshot'"
    - from: "image-storage.ts"
      to: "S3PDFStorage"
      via: "extends existing S3 storage pattern for images"
---

<objective>
화면 캡처 및 이미지 업로드 인프라 구축

Purpose: 이슈 보고 시 스크린샷 캡처와 MinIO 업로드를 위한 기반 인프라
Output: capture.ts (캡처 유틸리티), image-storage.ts (이미지 업로드), modern-screenshot 패키지
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/storage/s3-storage.ts
@src/lib/storage/storage-interface.ts

## Phase 29 Assets
- Issue Prisma model에 screenshotUrl, userContext 필드 있음
- MinIO S3-compatible storage 설정 완료 (Phase 9)
- S3PDFStorage 클래스 존재 - 이미지용으로 확장 필요

## Decisions
- Screenshot library: modern-screenshot (20KB, 3x faster than html2canvas)
- Image storage: 기존 S3PDFStorage 패턴 재사용하되 이미지 특화 인터페이스 추가
</context>

<tasks>

<task type="auto">
  <name>Task 1: modern-screenshot 라이브러리 설치</name>
  <files>package.json</files>
  <action>
    npm install modern-screenshot
    
    TypeScript 타입 정의 확인 - modern-screenshot은 기본적으로 타입 포함
  </action>
  <verify>
    npm list modern-screenshot
    node -e "const m = require('modern-screenshot'); console.log('exports:', Object.keys(m))"
  </verify>
  <done>modern-screenshot가 package.json dependencies에 추가되고 import 가능함</done>
</task>

<task type="auto">
  <name>Task 2: 스크린샷 캡처 유틸리티 생성</name>
  <files>src/lib/screenshot/capture.ts</files>
  <action>
    modern-screenshot의 toBlob, toPng 함수를 사용하여 화면 캡처 유틸리티 구현.
    
    함수 시그니처:
    - captureScreenshot(): Promise<Blob> - 전체 화면 캡처 (document.body)
    - captureElement(selector: string): Promise<Blob> - 특정 요소 캡처
    
    고려사항:
    - modern-screenshot은 기본적으로 DOM 요소를 Canvas로 변환
    - 이미지 태그, CSS transform 등은 자동 처리됨
    - CORS 이슈 있는 이미지는 캡처 실패 가능 - 에러 핸들링 필요
    
    에러 처리:
    - 캡처 실패 시 사용자 친화적 에러 메시지 반환
    - 선택자로 요소를 찾을 수 없을 때 예외 발생
  </action>
  <verify>
    cat src/lib/screenshot/capture.ts
    npx tsc --noEmit src/lib/screenshot/capture.ts 2>&1 | head -20
  </verify>
  <done>
    capture.ts 파일이 존재하고:
    - toBlob/toPng을 modern-screenshot에서 import
    - captureScreenshot 함수: document.body 캡처
    - captureElement 함수: CSS selector로 특정 요소 캡처
    - 두 함수 모두 Promise<Blob> 반환
  </done>
</task>

<task type="auto">
  <name>Task 3: 이미지 스토리지 서비스 생성</name>
  <files>src/lib/storage/image-storage.ts</files>
  <action>
    S3PDFStorage 패턴을 참고하여 이미지 업로드 전용 스토리지 서비스 구현.
    
    파일: src/lib/storage/image-storage.ts
    
    구현 내용:
    1. S3ImageStorage 클래스 (S3PDFStorage와 유사하지만 이미지용)
    2. 환경 변수에서 MinIO 설정 로드 (PDF와 동일한 설정 사용 가능)
    3. uploadImage(blob: Blob, filename: string): Promise<string>
       - Blob을 Buffer로 변환하여 S3에 업로드
       - 성공 시 공개 URL 반환
    4. getImageUrl(filename: string): string
       - MinIO public URL 생성
    
    환경 변수 (기존 PDF와 동일):
    - MINIO_ENDPOINT
    - MINIO_BUCKET (또는 별도 MINIO_IMAGE_BUCKET)
    - MINIO_ACCESS_KEY
    - MINIO_SECRET_KEY
    - MINIO_REGION
    
    Content-Type: image/png 고정 (스크린샷은 PNG)
    
    싱글톤 인스턴스 export: imageStorage
  </action>
  <verify>
    cat src/lib/storage/image-storage.ts
    npx tsc --noEmit src/lib/storage/image-storage.ts 2>&1 | head -20
  </verify>
  <done>
    image-storage.ts 파일이 존재하고:
    - S3ImageStorage 클래스 구현
    - uploadImage 메서드: Blob → S3 업로드 → URL 반환
    - getImageUrl 메서드: public URL 생성
    - imageStorage 싱글톤 export
  </done>
</task>

</tasks>

<verification>
- npm list modern-screenshot 확인
- src/lib/screenshot/capture.ts 존재 및 타입 체크 통과
- src/lib/storage/image-storage.ts 존재 및 타입 체크 통과
- 두 파일의 함수 시그니처가 상호 운용 가능 (Blob 기반)
</verification>

<success_criteria>
- modern-screenshot 패키지 설치 완료
- capture.ts: 캡처 유틸리티 2개 함수 export
- image-storage.ts: 이미지 업로드/URL 생성 기능 export
- 타입스크립트 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/30-issue-ui-screenshot/30-01-SUMMARY.md`
</output>
