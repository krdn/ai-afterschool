---
phase: 19-calendar-view
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/counseling/ReservationCalendarWeek.tsx
autonomous: true

must_haves:
  truths:
    - "주간 뷰에서 요일별 헤더와 시간대별 행이 표시된다"
    - "각 시간 슬롯(30분 단위)에 해당 시간의 예약 정보가 표시된다"
    - "예약이 있는 슬롯이 배경색으로 강조 표시된다"
    - "예약이 없는 슬롯은 '-'로 표시된다"
  artifacts:
    - path: "src/components/counseling/ReservationCalendarWeek.tsx"
      provides: "주간 캘린더 뷰 (시간대별 예약 현황)"
      min_lines: 100
      exports: ["ReservationCalendarWeek"]
  key_links:
    - from: "src/components/counseling/ReservationCalendarWeek.tsx"
      to: "date-fns"
      via: "startOfWeek, endOfWeek, format, isSameDay"
      pattern: "from\\s+['\"]date-fns['\"]"
    - from: "src/components/counseling/ReservationCalendarWeek.tsx"
      to: "react-day-picker"
      via: "주 선택 패턴 참조 (직접 구현)"
      pattern: "TIME_SLOTS.*const"
---

<objective>
주간 캘린더 뷰에서 시간대별 예약 현황을 시각화하여, 사용자가 주 단위로 예약 스케줄을 한눈에 파악할 수 있게 합니다.

Purpose: 시간대별 예약 분포를 직관적으로 표시하여 선생님이 주간 스케줄을 효율적으로 관리할 수 있도록 지원합니다.

Output: ReservationCalendarWeek 컴포넌트 (시간 슬롯 그리드)
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-calendar-view/19-RESEARCH.md

# Phase 18에서 구축한 기반 활용
@src/components/counseling/TimeSlotGrid.tsx         # 기존 시간 슬롯 그리드 패턴 참조
@src/components/counseling/ReservationCalendar.tsx  # 한국어 로케일 패턴 재사용
@src/lib/db/reservations.ts                         # getReservations 함수 활용
</context>

<tasks>

<task type="auto">
  <name>Task 1: 주간 캘린더 뷰 컴포넌트 구현</name>
  <files>src/components/counseling/ReservationCalendarWeek.tsx</files>
  <action>
    시간 슬롯 그리드 형태의 주간 캘린더 뷰를 구현합니다:

    1. **영업시간 상수 정의**
       - 09:00 ~ 17:30, 30분 단위
       - TIME_SLOTS 배열: ["09:00", "09:30", ..., "17:30"]

    2. **주간 날짜 계산**
       - startOfWeek, endOfWeek으로 주간 범위 계산 (월요일 시작: weekStartsOn: 1)
       - 한국어 로케일로 요일/날짜 포맷팅

    3. **시간 슬롯 그리드 렌더링**
       - 8열 그리드: 1열(시간) + 7열(월~일)
       - 헤더: 요일 한글 표시 (월, 화, 수, 목, 금, 토, 일)
       - 각 행: 시간 슬롯별 요일별 예약 표시

    4. **예약 매칭 로직**
       - scheduledAt과 슬롯 시간 비교
       - isSameDay로 날짜 확인
       - getHours로 시간대 일치 확인

    **구현 패턴:**
    ```typescript
    "use client"

    import { startOfWeek, endOfWeek, format, isSameDay } from "date-fns"
    import { ko } from "date-fns/locale"
    import { cn } from "@/lib/utils"
    import type { ReservationWithRelations } from "./ReservationCard"

    // 영업시간 (9:00 ~ 18:00, 30분 단위)
    const TIME_SLOTS = [
      "09:00", "09:30", "10:00", "10:30", "11:00", "11:30",
      "12:00", "12:30", "13:00", "13:30", "14:00", "14:30",
      "15:00", "15:30", "16:00", "16:30", "17:00", "17:30",
    ]

    interface ReservationCalendarWeekProps {
      weekStart: Date // 주간 뷰 기준 날짜
      reservations: ReservationWithRelations[]
      className?: string
    }

    export function ReservationCalendarWeek({
      weekStart,
      reservations,
      className,
    }: ReservationCalendarWeekProps) {
      const weekEnd = endOfWeek(weekStart, { weekStartsOn: 1 })

      // 주간 날짜 배열 생성 (월~일)
      const weekDates: Date[] = []
      let current = startOfWeek(weekStart, { weekStartsOn: 1 })

      while (current <= weekEnd) {
        weekDates.push(new Date(current))
        current = new Date(current.getTime() + 86400000) // +1 day
      }

      return (
        <div className={cn("border rounded-lg overflow-hidden", className)}>
          {/* 요일 헤더 */}
          <div className="grid grid-cols-8 border-b bg-muted">
            <div className="p-2 text-sm font-medium text-center">시간</div>
            {weekDates.map((date) => (
              <div key={date.toISOString()} className="p-2 text-center border-l">
                <div className="text-xs text-muted-foreground">
                  {format(date, "E", { locale: ko })}
                </div>
                <div className="text-sm font-medium">
                  {format(date, "M/d", { locale: ko })}
                </div>
              </div>
            ))}
          </div>

          {/* 시간 슬롯 그리드 */}
          <div className="max-h-96 overflow-y-auto">
            {TIME_SLOTS.map((time) => (
              <div key={time} className="grid grid-cols-8 border-b last:border-b-0">
                {/* 시간 라벨 */}
                <div className="p-2 text-xs text-muted-foreground text-center border-r">
                  {time}
                </div>

                {/* 요일별 슬롯 */}
                {weekDates.map((date) => {
                  // 해당 날짜와 시간의 예약 찾기
                  const slotDateTime = new Date(
                    `${format(date, "yyyy-MM-dd")}T${time}:00`
                  )

                  const reservation = reservations.find((r) => {
                    const reservationDate = new Date(r.scheduledAt)
                    return (
                      isSameDay(reservationDate, slotDateTime) &&
                      reservationDate.getHours() === slotDateTime.getHours() &&
                      reservationDate.getMinutes() === slotDateTime.getMinutes()
                    )
                  })

                  return (
                    <div
                      key={date.toISOString()}
                      className={cn(
                        "p-2 border-l text-xs text-center min-h-[40px] flex items-center justify-center",
                        reservation && "bg-primary/10 font-medium"
                      )}
                    >
                      {reservation ? (
                        <span className="truncate" title={reservation.student.name}>
                          {reservation.student.name}
                        </span>
                      ) : (
                        <span className="text-muted-foreground">-</span>
                      )}
                    </div>
                  )
                })}
              </div>
            ))}
          </div>
        </div>
      )
    }

    export default ReservationCalendarWeek
    ```

    **중요:**
    - 주간 뷰는 react-day-picker의 week selection 패턴을 참조하되 별도 시간 슬롯 그리드로 직접 구현 (Pattern 3)
    - 시간대별 예약 매칭 시 Pitfall 4 방지: isSameDay로 날짜 확인 후 시간/분까지 정확히 비교
    - overflow-y-auto로 스크롤 가능하도록 하여 모든 시간 슬롯 표시
    - 예약이 있는 슬롯은 bg-primary/10으로 강조
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>
    주간 캘린더 뷰가 요일별 헤더와 시간대별 행을 표시하고, 각 슬롯에 예약 정보 또는 '-'를 표시한다
  </done>
</task>

</tasks>

<verification>
1. **주간 뷰 렌더링**: 8열 그리드(시간 + 요일 7일)가 렌더링된다
2. **요일 헤더**: 한글 요일(월~일)과 날짜(M/d)가 표시된다
3. **시간 슬롯**: 09:00~17:30까지 30분 단위로 18개 슬롯이 표시된다
4. **예약 표시**: 예약이 있는 슬롯이 bg-primary/10 배경색으로 강조되고 학생 이름이 표시된다
5. **빈 슬롯**: 예약이 없는 슬롯이 '-'로 표시된다
</verification>

<success_criteria>
1. ReservationCalendarWeek 컴포넌트가 주간 캘린더를 렌더링한다
2. 8열 그리드(1열 시간 + 7열 요일)가 표시된다
3. 18개 시간 슬롯(09:00~17:30, 30분 단위)이 표시된다
4. 예약이 있는 슬롯이 배경색으로 강조되고 학생 이름이 표시된다
5. 예약이 없는 슬롯이 '-'로 표시된다
</success_criteria>

<output>
After completion, create `.planning/phases/19-calendar-view/19-02-SUMMARY.md`
</output>
