---
phase: 19-calendar-view
plan: 03
type: execute
wave: 2
depends_on: [19-01, 19-02]
files_modified:
  - src/components/counseling/CounselingPageTabs.tsx
  - src/components/counseling/ReservationCalendarView.tsx
autonomous: false

must_haves:
  truths:
    - "캘린더 뷰 전환(월간/주간)이 가능하다"
    - "월간 뷰에서 날짜 클릭 시 예약 목록이 필터링된다"
    - "주간 뷰에서 주간 범위 내 예약이 시간대별로 표시된다"
    - "뷰 전환 시 현재 선택된 날짜/주간이 유지된다"
  artifacts:
    - path: "src/components/counseling/ReservationCalendarView.tsx"
      provides: "캘린더 뷰 전환 및 데이터 연결 컴포넌트"
      min_lines: 80
      exports: ["ReservationCalendarView"]
    - path: "src/components/counseling/CounselingPageTabs.tsx"
      provides: "캘린더 뷰 탭 통합"
      min_lines: 150  # 기존 파일 수정으로 라인 증가 예상
      modifies: "캘린더 뷰 탭 추가"
  key_links:
    - from: "src/components/counseling/ReservationCalendarView.tsx"
      to: "src/components/counseling/ReservationCalendarMonth.tsx"
      via: "import 및 props 전달"
      pattern: "ReservationCalendarMonth"
    - from: "src/components/counseling/ReservationCalendarView.tsx"
      to: "src/components/counseling/ReservationCalendarWeek.tsx"
      via: "import 및 props 전달"
      pattern: "ReservationCalendarWeek"
    - from: "src/components/counseling/CounselingPageTabs.tsx"
      to: "src/lib/actions/reservations.ts"
      via: "getReservationsAction"
      pattern: "getReservationsAction"
---

<objective>
월간/주간 캘린더 뷰 전환 기능을 구현하고, 기존 상담 페이지에 캘린더 뷰를 통합합니다.

Purpose: 사용자가 월간/주간 뷰를 자유롭게 전환하며 예약 현황을 확인할 수 있도록 단일 진입점을 제공합니다.

Output: ReservationCalendarView 컴포넌트 (뷰 전환), CounselingPageTabs 수정 (캘린더 뷰 탭 추가)
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-calendar-view/19-RESEARCH.md

# Phase 18에서 구축한 기반 활용
@src/components/counseling/CounselingPageTabs.tsx     # 기존 탭 컴포넌트 확장
@src/components/counseling/ReservationList.tsx        # dateFilter prop 패턴 재사용
@src/lib/actions/reservations.ts                      # getReservationsAction 활용

# Phase 19 Plan 01-02에서 구축한 컴포넌트
@src/components/counseling/ReservationCalendarMonth.tsx  # 월간 뷰
@src/components/counseling/ReservationCalendarWeek.tsx   # 주간 뷰
@src/lib/utils/calendar.ts                               # 캘린더 유틸리티
</context>

<tasks>

<task type="auto">
  <name>Task 1: 캘린더 뷰 전환 컴포넌트 구현</name>
  <files>src/components/counseling/ReservationCalendarView.tsx</files>
  <action>
    월간/주간 뷰 전환과 데이터 연결을 담당하는 컴포넌트를 구현합니다:

    1. **뷰 타입 정의**
       - type CalendarViewType = "month" | "week"

    2. **상태 관리**
       - viewType: 현재 뷰 타입 (month/week)
       - selectedDate: 현재 선택된 날짜 (월간 뷰 기준)
       - dateFilter: 부모로부터 전달받은 필터 (예약 목록용)

    3. **데이터 페칭**
       - getReservationsAction으로 예약 목록 조회
       - 월간 뷰: 현재 월 기준 전체 예약
       - 주간 뷰: selectedDate 기준 주간 범위 예약

    4. **뷰 전환 UI**
       - Segmented Control 또는 Tabs로 월간/주간 전환
       - 전환 시 selectedDate 유지

    **구현 패턴:**
    ```typescript
    "use client"

    import { useState, useEffect } from "react"
    import { getReservationsAction } from "@/lib/actions/reservations"
    import { ReservationCalendarMonth } from "./ReservationCalendarMonth"
    import { ReservationCalendarWeek } from "./ReservationCalendarWeek"
    import { Button } from "@/components/ui/button"
    import { cn } from "@/lib/utils"
    import type { ReservationWithRelations } from "./ReservationCard"

    type CalendarViewType = "month" | "week"

    interface ReservationCalendarViewProps {
      initialDate?: Date
      onDateSelect?: (date: Date | undefined) => void
    }

    export function ReservationCalendarView({
      initialDate,
      onDateSelect,
    }: ReservationCalendarViewProps) {
      const [viewType, setViewType] = useState<CalendarViewType>("month")
      const [selectedDate, setSelectedDate] = useState<Date>(initialDate || new Date())
      const [reservations, setReservations] = useState<ReservationWithRelations[]>([])
      const [loading, setLoading] = useState(true)

      // 예약 데이터 페칭
      useEffect(() => {
        async function fetchReservations() {
          setLoading(true)
          try {
            // 월간/주간 모두 현재 월 기준으로 페칭 (주간 뷰에서 필터링)
            const monthStart = new Date(selectedDate.getFullYear(), selectedDate.getMonth(), 1)
            const monthEnd = new Date(selectedDate.getFullYear(), selectedDate.getMonth() + 1, 0, 23, 59, 59)

            const result = await getReservationsAction({
              dateFrom: monthStart,
              dateTo: monthEnd,
            })

            if (result.success && result.data) {
              setReservations(result.data)
            }
          } catch (error) {
            console.error("Failed to fetch reservations:", error)
          } finally {
            setLoading(false)
          }
        }

        fetchReservations()
      }, [selectedDate])

      // 날짜 선택 핸들러
      const handleDateSelect = (date: Date | undefined) => {
        if (date) {
          setSelectedDate(date)
          onDateSelect?.(date)
        }
      }

      // 월 변경 핸들러
      const handleMonthChange = (month: Date) => {
        setSelectedDate(month)
      }

      if (loading) {
        return (
          <div className="flex items-center justify-center py-12">
            <div className="text-muted-foreground">로딩 중...</div>
          </div>
        )
      }

      return (
        <div className="space-y-4">
          {/* 뷰 전환 버튼 */}
          <div className="flex items-center justify-center">
            <div className="inline-flex items-center rounded-lg bg-muted p-1">
              <Button
                variant={viewType === "month" ? "default" : "ghost"}
                size="sm"
                onClick={() => setViewType("month")}
                className={cn(
                  "rounded-md",
                  viewType === "month" && "shadow-sm"
                )}
              >
                월간
              </Button>
              <Button
                variant={viewType === "week" ? "default" : "ghost"}
                size="sm"
                onClick={() => setViewType("week")}
                className={cn(
                  "rounded-md",
                  viewType === "week" && "shadow-sm"
                )}
              >
                주간
              </Button>
            </div>
          </div>

          {/* 캘린더 뷰 렌더링 */}
          {viewType === "month" ? (
            <ReservationCalendarMonth
              reservations={reservations}
              selected={selectedDate}
              onSelect={handleDateSelect}
              onMonthChange={handleMonthChange}
            />
          ) : (
            <ReservationCalendarWeek
              weekStart={selectedDate}
              reservations={reservations}
            />
          )}
        </div>
      )
    }

    export default ReservationCalendarView
    ```

    **중요:**
    - 뷰 전환 시 selectedDate 상태 유지 (월간 → 주간 전환 시 현재 날짜 기준 주간 표시)
    - getReservationsAction을 한 번만 호출하고 두 뷰에서 공유 (불필요한 API 호출 방지)
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>
    캘린더 뷰 전환 컴포넌트가 월간/주간 뷰를 렌더링하고, 뷰 전환 시 selectedDate가 유지되며, 날짜 선택 시 onDateSelect 콜백이 호출된다
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>캘린더 뷰 전환 컴포넌트 (ReservationCalendarView)가 월간/주간 뷰 전환과 데이터 연결을 구현했습니다</what-built>
  <how-to-verify>
    1. /counseling 페이지 접속
    2. "예약 관리" 탭 클릭
    3. 월간/주간 전환 버튼 클릭하여 뷰 전환 확인
    4. 월간 뷰에서 날짜 클릭 시 예약 목록 필터링 확인
    5. 주간 뷰에서 시간대별 예약 표시 확인
  </how-to-verify>
  <resume-signal>뷰 전환과 예약 표시가 정상 작동하면 "approved" 입력</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 상담 페이지에 캘린더 뷰 탭 추가</name>
  <files>src/components/counseling/CounselingPageTabs.tsx</files>
  <action>
    기존 CounselingPageTabs 컴포넌트에 캘린더 뷰 탭을 추가합니다:

    1. **탭 타입 확장**
       - 기존: "history" | "reservations"
       - 수정: "history" | "reservations" | "calendar"

    2. **캘린더 뷰 상태 관리**
       - calendarViewDate: 캘린더 뷰 기준 날짜
       - calendarDateFilter: 캘린더에서 선택한 날짜 필터

    3. **탭 콘텐츠 렌더링**
       - calendar 탭: ReservationCalendarView 렌더링
       - onDateSelect: calendarDateFilter 상태 업데이트

    4. **예약 목록과 캘린더 연동**
       - 캘린더에서 날짜 선택 시 ReservationList의 dateFilter 업데이트
       - "필터 초기화" 버튼 클릭 시 calendarDateFilter 초기화

    **수정 패턴 (기존 CounselingPageTabs.tsx 확장):**
    ```typescript
    // 기존 탭 타입 수정
    type TabType = "history" | "reservations" | "calendar"  // "calendar" 추가

    // ... (기존 코드)

    // 캘린더 관련 상태 추가
    const [calendarViewDate, setCalendarViewDate] = useState<Date>(new Date())
    const [calendarDateFilter, setCalendarDateFilter] = useState<Date | undefined>(undefined)

    // 캘린더 날짜 선택 핸들러
    const handleCalendarDateSelect = (date: Date | undefined) => {
      setCalendarDateFilter(date)
      // 예약 목록 탭으로 자동 전환 (선택 사항)
      // setActiveTab("reservations")
    }

    // 탭 콘텐츠 렌더링 함수 수정
    const renderTabContent = () => {
      switch (activeTab) {
        case "history":
          return <CounselingHistoryContent sessions={sessions} />
        case "reservations":
          return (
            <ReservationContent
              reservations={reservations}
              onRefresh={refetchReservations}
              dateFilter={calendarDateFilter}  // 캘린더 필터 적용
              onClearDateFilter={() => setCalendarDateFilter(undefined)}
            />
          )
        case "calendar":
          return (
            <ReservationCalendarView
              initialDate={calendarViewDate}
              onDateSelect={handleCalendarDateSelect}
            />
          )
        default:
          return null
      }
    }

    // 탭 버튼 UI에 "캘린더" 탭 추가
    // ...
    <Button
      variant={activeTab === "calendar" ? "default" : "ghost"}
      onClick={() => setActiveTab("calendar")}
    >
      캘린더
    </Button>
    // ...
    ```

    **중요:**
    - 기존 "예약 관리" 탭 기능 유지 (회귀 방지)
    - 캘린더 뷰와 예약 목록 간 날짜 필터 연동
    - ReservationContent 컴포넌트에 onClearDateFilter prop 추가하여 필터 초기화 버튼 지원
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>
    상담 페이지에 "캘린더" 탭이 추가되고, 캘린더 뷰가 렌더링되며, 날짜 선택 시 예약 목록이 필터링된다
  </done>
</task>

</tasks>

<verification>
1. **탭 전환**: "상담 기록" | "예약 관리" | "캘린더" 3개 탭이 표시되고 전환된다
2. **캘린더 뷰**: "캘린더" 탭에서 월간/주간 뷰 전환 버튼이 표시된다
3. **월간 뷰**: 월간 캘린더에서 예약 건수가 dots로 표시된다
4. **주간 뷰**: 주간 캘린더에서 시간대별 예약이 표시된다
5. **날짜 필터 연동**: 캘린더에서 날짜 선택 후 "예약 관리" 탭 이동 시 해당 날짜 예약만 표시된다
6. **기존 기능 유지**: "상담 기록"과 "예약 관리" 탭이 기존과 동일하게 작동한다 (회귀 없음)
</verification>

<success_criteria>
1. CounselingPageTabs 컴포넌트에 "캘린더" 탭이 추가된다
2. ReservationCalendarView 컴포넌트가 월간/주간 뷰 전환을 지원한다
3. 월간 뷰에서 예약 건수가 dots로 표시된다
4. 주간 뷰에서 시간대별 예약이 표시된다
5. 캘린더에서 날짜 선택 시 예약 목록이 필터링된다
6. 기존 "상담 기록"과 "예약 관리" 탭 기능이 유지된다
</success_criteria>

<output>
After completion, create `.planning/phases/19-calendar-view/19-03-SUMMARY.md`
</output>
