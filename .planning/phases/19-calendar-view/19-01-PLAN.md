---
phase: 19-calendar-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/counseling/ReservationCalendarMonth.tsx
  - src/lib/utils/calendar.ts
autonomous: true

must_haves:
  truths:
    - "월간 캘린더에서 각 날짜에 예약 건수가 dot indicators로 표시된다"
    - "예약이 있는 날짜가 배경색으로 시각적으로 구분된다"
    - "날짜 클릭 시 해당 날짜의 예약 목록으로 필터링된다"
    - "최대 3건까지 표시하고 그 이상은 '+N' 표시 또는 dots로 처리한다"
  artifacts:
    - path: "src/components/counseling/ReservationCalendarMonth.tsx"
      provides: "월간 캘린더 뷰 (예약 건수 시각화)"
      min_lines: 80
      exports: ["ReservationCalendarMonth"]
    - path: "src/lib/utils/calendar.ts"
      provides: "캘린더 유틸리티 함수 (날짜별 예약 건수 계산)"
      min_lines: 30
      exports: ["groupReservationsByDate", "getReservationCountByDate"]
  key_links:
    - from: "src/components/counseling/ReservationCalendarMonth.tsx"
      to: "react-day-picker DayPicker"
      via: "Custom DayButton components prop"
      pattern: "components=\\{\\{\\s*DayButton:\\s*CustomDayButton\\s*\\}\\}"
    - from: "src/components/counseling/ReservationCalendarMonth.tsx"
      to: "/api/reservations"
      via: "getReservationsAction with date range"
      pattern: "getReservationsAction"
    - from: "src/lib/utils/calendar.ts"
      to: "date-fns"
      via: "startOfMonth, endOfMonth, format, isSameDay"
      pattern: "from\\s+['\"]date-fns['\"]"
---

<objective>
월간 캘린더 뷰에서 날짜별 예약 건수를 시각화하고, 예약된 날짜를 시각적으로 구분합니다.

Purpose: 사용자가 한눈에 월간 예약 현황을 파악할 수 있도록 캘린더 UI에 예약 정보를 통합 표시합니다.

Output: ReservationCalendarMonth 컴포넌트 (Custom DayButton + Modifiers), 캘린더 유틸리티 함수
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-calendar-view/19-RESEARCH.md

# Phase 18에서 구축한 기반 활용
@src/components/counseling/ReservationCalendar.tsx  # 기존 DatePicker 패턴 참조
@src/lib/db/reservations.ts                         # getReservations 함수 활용
@src/components/counseling/ReservationList.tsx      # dateFilter prop 패턴 재사용
</context>

<tasks>

<task type="auto">
  <name>Task 1: 캘린더 유틸리티 함수 작성</name>
  <files>src/lib/utils/calendar.ts</files>
  <action>
    캘린더 관련 유틸리티 함수들을 새 파일에 작성합니다:

    1. **groupReservationsByDate**: 예약 목록을 날짜별로 그룹화
       - 입력: ReservationWithRelations[], 기준 Date (월)
       - 출력: Map&lt;string, ReservationWithRelations[]&gt; (key: "YYYY-MM-DD")
       - date-fns의 startOfMonth, endOfMonth로 월 범위 계산
       - isSameDay로 날짜 비교

    2. **getReservationCountByDate**: 날짜별 예약 건수 계산
       - 입력: ReservationWithRelations[], 기준 Date (월)
       - 출력: Map&lt;string, number&gt; (key: "YYYY-MM-DD", value: count)
       - groupReservationsByDate 활용

    **구현 패턴:**
    ```typescript
    import { startOfMonth, endOfMonth, isSameDay, format } from "date-fns"
    import type { ReservationWithRelations } from "@/components/counseling/ReservationCard"

    export function groupReservationsByDate(
      reservations: ReservationWithRelations[],
      month: Date
    ): Map<string, ReservationWithRelations[]> {
      const monthStart = startOfMonth(month)
      const monthEnd = endOfMonth(month)

      const grouped = new Map<string, ReservationWithRelations[]>()

      for (const reservation of reservations) {
        const reservationDate = new Date(reservation.scheduledAt)

        // 월 범위 내인지 확인
        if (reservationDate >= monthStart && reservationDate <= monthEnd) {
          const dateKey = format(reservationDate, "yyyy-MM-dd")

          if (!grouped.has(dateKey)) {
            grouped.set(dateKey, [])
          }

          grouped.get(dateKey)!.push(reservation)
        }
      }

      return grouped
    }

    export function getReservationCountByDate(
      reservations: ReservationWithRelations[],
      month: Date
    ): Map<string, number> {
      const grouped = groupReservationsByDate(reservations, month)

      const counts = new Map<string, number>()

      for (const [dateKey, reservations] of grouped) {
        counts.set(dateKey, reservations.length)
      }

      return counts
    }
    ```

    **중요:** CANCELLED/NO_SHOW 상태도 포함하여 전체 예약 현황 표시 (상태 필터링은 UI 단에서 처리)
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>캘린더 유틸리티 함수가 TypeScript 타입 체크를 통과하고 날짜별 예약 건수를 정확히 계산한다</done>
</task>

<task type="auto">
  <name>Task 2: 월간 캘린더 뷰 컴포넌트 구현</name>
  <files>src/components/counseling/ReservationCalendarMonth.tsx</files>
  <action>
    Custom DayButton과 modifiers를 사용하여 월간 캘린더 뷰를 구현합니다:

    1. **Custom DayButton 컴포넌트**
       - react-day-picker v9의 Custom Components API 활용
       - useDayPicker() hook으로 기본 DayButton 래핑
       - reservationCount prop으로 예약 건수 표시
       - dot indicators: 최대 3개까지 표시 (3개 이상은 3개 dots)
       - Pattern 1 (Research 문서 참조)

    2. **ReservationCalendarMonth 컴포넌트**
       - 기존 ReservationCalendar 패턴 확장 (DayPicker + 한국어 로케일)
       - props: reservations, selected, onSelect, onMonthChange (선택)
       - getReservationCountByDate로 예약 건수 계산
       - modifiers로 예약된 날짜에 CSS 클래스 적용 (Pattern 2)
       - Custom DayButton을 components prop으로 전달

    **구현 패턴:**
    ```typescript
    "use client"

    import { DayPicker, DayButtonProps, UI, useDayPicker } from "react-day-picker"
    import "react-day-picker/style.css"
    import { ko } from "date-fns/locale"
    import { cn } from "@/lib/utils"
    import { getReservationCountByDate } from "@/lib/utils/calendar"
    import type { ReservationWithRelations } from "./ReservationCard"

    interface CustomDayButtonProps extends DayButtonProps {
      reservationCount?: number
    }

    function CustomDayButton({ day, reservationCount = 0, ...buttonProps }: CustomDayButtonProps) {
      const { components, classNames } = useDayPicker()

      return (
        <components.DayButton
          {...buttonProps}
          day={day}
          className={cn(
            classNames?.[UI.DayButton],
            "relative h-14 w-14 flex flex-col items-center justify-center"
          )}
        >
          <span>{day.date.getDate()}</span>
          {reservationCount > 0 && (
            <div className="absolute bottom-1 left-1/2 -translate-x-1/2 flex gap-0.5">
              {Array.from({ length: Math.min(reservationCount, 3) }).map((_, i) => (
                <span
                  key={i}
                  className="h-1 w-1 rounded-full bg-primary"
                  aria-hidden="true"
                />
              ))}
            </div>
          )}
        </components.DayButton>
      )
    }

    interface ReservationCalendarMonthProps {
      reservations: ReservationWithRelations[]
      selected?: Date
      onSelect?: (date: Date | undefined) => void
      onMonthChange?: (month: Date) => void
      className?: string
    }

    export function ReservationCalendarMonth({
      reservations,
      selected,
      onSelect,
      onMonthChange,
      className,
    }: ReservationCalendarMonthProps) {
      const reservationCounts = getReservationCountByDate(reservations, selected || new Date())
      const reservedDates = reservations.map((r) => new Date(r.scheduledAt))

      return (
        <div className={cn("w-full", className)}>
          <DayPicker
            mode="single"
            selected={selected}
            onSelect={onSelect}
            onMonthChange={onMonthChange}
            locale={ko}
            components={{
              DayButton: (props) => {
                const dateKey = props.day.date.toISOString().split("T")[0]
                const count = reservationCounts.get(dateKey) || 0

                return <CustomDayButton {...props} reservationCount={count} />
              },
            }}
            modifiers={{
              reserved: reservedDates,
            }}
            modifiersClassNames={{
              reserved: "bg-primary/10 border-primary/50",
            }}
            classNames={{
              // 기존 ReservationCalendar와 동일한 classNames
              nav: "flex items-center space-x-1",
              month: "space-y-4",
              // ... (나머지는 ReservationCalendar.tsx 참조)
              day: cn(
                "h-14 w-14 p-0 font-normal flex items-center justify-center rounded-md",
                "hover:bg-accent hover:text-accent-foreground",
                "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2",
                "transition-colors relative"
              ),
            }}
          />
        </div>
      )
    }

    export default ReservationCalendarMonth
    ```

    **중요:**
    - Custom DayButton에서 반드시 useDayPicker()의 components.DayButton을 래핑하여 접근성 유지 (Pitfall 2 방지)
    - modifiers와 Custom Components 중복 사용 피하기 (Pitfall 3 방지): modifiers는 배경색, Custom DayButton은 dot indicators만 담당
    - 날짜 높이를 h-14로 늘려서 dot indicators 공간 확보
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>
    월간 캘린더 뷰가 예약 건수를 dot indicators로 표시하고, 예약된 날짜가 배경색으로 구분되며, 날짜 클릭 시 onSelect가 호출된다
  </done>
</task>

</tasks>

<verification>
1. **월간 캘린더 렌더링**: 캘린더가 한국어 로케일로 표시되고 각 날짜에 예약 건수가 dots로 표시된다
2. **예약된 날짜 구분**: 예약이 있는 날짜가 bg-primary/10 배경색으로 강조 표시된다
3. **날짜 선택 동작**: 날짜 클릭 시 onSelect 콜백이 호출되어 부모 컴포넌트가 필터링을 수행할 수 있다
4. **3건 이상 처리**: 예약이 4건 이상인 날짜도 3개 dots로 표시된다 (초과 표시는 향후 개선)
5. **타입 안전성**: TypeScript 컴파일 에러가 없다
</verification>

<success_criteria>
1. ReservationCalendarMonth 컴포넌트가 월간 캘린더를 렌더링한다
2. 각 날짜 셀에 예약 건수가 dot indicators(최대 3개)로 표시된다
3. 예약이 있는 날짜가 bg-primary/10 배경색으로 시각적으로 구분된다
4. 날짜 클릭 시 onSelect prop이 호출된다
5. 캘린더 유틸리티 함수로 날짜별 예약 건수를 정확히 계산한다
</success_criteria>

<output>
After completion, create `.planning/phases/19-calendar-view/19-01-SUMMARY.md`
</output>
