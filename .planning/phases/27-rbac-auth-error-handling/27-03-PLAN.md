---
phase: 27-rbac-auth-error-handling
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/students/student-image-uploader.tsx
  - src/lib/actions/student-images.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "프로필 사진 업로드 시 용량 초과 에러 메시지가 Toast로 표시된다"
    - "파일 크기 제한(10MB) 초과 시 명확한 에러 메시지가 제공된다"
    - "업로드 실패 시 사용자 친화적인 에러 메시지가 표시된다"
    - "업로드 진행 중 로딩 상태가 표시된다"
  artifacts:
    - path: "src/components/students/student-image-uploader.tsx"
      provides: "이미지 업로드 컴포넌트 (수정됨)"
      contains: "toast.error|CldUploadWidget|maxFileSize"
  key_links:
    - from: "src/components/students/student-image-uploader.tsx"
      to: "src/lib/actions/student-images.ts"
      via: "setStudentImage call"
      pattern: "setStudentImage"
    - from: "src/components/students/student-image-uploader.tsx"
      to: "sonner"
      via: "toast import"
      pattern: "from ['\"]sonner['\"]"
---

<objective>
파일 업로드 에러 처리 개선

Purpose: 프로필 사진 업로드 시 용량 초과 및 기본 에러를 사용자 친화적으로 처리하여 사용자 경험 개선
Output: 에러 처리가 강화된 이미지 업로더 컴포넌트
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-rbac-auth-error-handling/27-RESEARCH.md

@src/lib/actions/student-images.ts
@src/lib/actions/students.ts
@src/components/ui/sonner.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 이미지 업로더 컴포넌트 에러 처리 강화</name>
  <files>src/components/students/student-image-uploader.tsx</files>
  <action>
이미지 업로더 컴포넌트의 에러 처리를 강화합니다:

1. 기존 컴포넌트 찾기 (이름이 다를 수 있음):
```bash
find src/components -name "*upload*" -o -name "*image*"
```

2. CldUploadWidget의 옵션 수정:
- maxFileSize: 10 * 1024 * 1024 (10MB) 명시적 설정
- maxFiles: 1 (단일 파일)
- sources: ['local', 'camera', 'url'] (허용된 소스)

3. onError 핸들러 강화:
```typescript
onError={(error) => {
  console.error('Upload error:', error)

  // 파일 크기 초과 에러
  if (error.message.includes('File too large') ||
      error.message.includes('exceeds')) {
    toast.error('파일 크기 초과', {
      description: '파일은 최대 10MB까지 업로드할 수 있어요',
      id: 'file-size-error',
    })
    return
  }

  // 형식 불일치 에러
  if (error.message.includes('Invalid') ||
      error.message.includes('format')) {
    toast.error('파일 형식 오류', {
      description: '이미지 파일만 업로드할 수 있어요 (JPG, PNG)',
      id: 'file-format-error',
    })
    return
  }

  // 기타 에러
  toast.error('업로드 실패', {
    description: '이미지 업로드 중 오류가 발생했어요. 다시 시도해주세요.',
    id: 'upload-error',
  })
}}
```

4. onUpload 핸들러에서 로딩 상태 표시:
- 업로드 시작 시 toast.loading 표시
- 성공 시 toast.success로 교체
- 실패 시 toast.error로 교체

5. Client Component 확인:
- 'use client' 지시자 확인
- toast import 확인
  </action>
  <verify>
1. 컴포넌트가 toast를 import함: `grep "from.*sonner" src/components/students/student-image-uploader.tsx`
2. onError 핸들러가 파일 크기 에러를 처리함: `grep -A5 "onError" src/components/students/student-image-uploader.tsx | grep -E "too large|10MB"`
3. TypeScript 컴파일 에러 없음: `npm run lint`
  </verify>
  <done>
이미지 업로더에서:
- 10MB 초과 파일 업로드 시도 시 "파일 크기 초과" Toast 표시됨
- 잘못된 형식 파일 시 "파일 형식 오류" Toast 표시됨
- 업로드 실패 시 일반적인 에러 메시지 표시됨
- 업로드 중 로딩 상태가 표시됨
  </done>
</task>

<task type="auto">
  <name>Task 2: Server Action 에러 처리 개선</name>
  <files>src/lib/actions/student-images.ts</files>
  <action>
student-images.ts Server Action의 에러 처리를 개선합니다:

1. setStudentImage 함수의 에러 처리 강화:
- Cloudinary 업로드 에러를 명확한 메시지로 변환
- StudentImageSchema 검증 에러 처리
- 파일 크기/형식 검증 추가

2. 에러 메시지 한국어화:
```typescript
try {
  const validatedPayload = StudentImageSchema.parse(payload)
  // ... 기존 로직
} catch (error) {
  if (error instanceof z.ZodError) {
    console.error('Validation error:', error.errors)
    throw new Error('이미지 정보 형식이 올바르지 않아요')
  }
  throw error
}
```

3. Cloudinary 관련 에러 처리:
- 네트워크 에러: "이미지 업로드 중 연결 오류가 발생했어요"
- API 에러: "이미지 서비스 오류가 발생했어요. 잠시 후 다시 시도해주세요"

4. 반환 타입 변경 고려:
- void 대신 { success: boolean, error?: string } 반환
- Client에서 toast 표시를 위한 명확한 에러 메시지 제공

주의: 이 변경은 해당 액션을 사용하는 모든 컴포넌트에 영향을 줄 수 있으니 주의합니다.
  </action>
  <verify>
1. 함수가 에러를 catch함: `grep -A5 "catch" src/lib/actions/student-images.ts | grep -E "error|Error"`
2. 한국어 에러 메시지 포함: `grep -E "올바르지 않아요|오류가 발생했어요" src/lib/actions/student-images.ts`
3. TypeScript 컴파일 에러 없음: `npm run lint`
  </verify>
  <done>
Server Action에서:
- 이미지 검증 에러 시 명확한 한국어 메시지 반환됨
- Cloudinary 에러를 적절하게 처리함
- 클라이언트에서 Toast로 표시할 수 있는 에러 형식 제공됨
  </done>
</task>

<task type="auto">
  <name>Task 3: 클라이언트 측 파일 검증 추가</name>
  <files>src/components/students/student-image-uploader.tsx</files>
<action>
CldUploadWidget 호출 전에 클라이언트 측 파일 검증을 추가합니다:

1. 파일 선택 전 검증 (onOpen 콜백 또는 custom upload button):
```typescript
const validateFileBeforeUpload = (file: File) => {
  // 파일 크기 검증 (10MB)
  const MAX_FILE_SIZE = 10 * 1024 * 1024
  if (file.size > MAX_FILE_SIZE) {
    toast.error('파일 크기 초과', {
      description: '파일은 최대 10MB까지 업로드할 수 있어요',
      id: 'file-size-error',
    })
    return false
  }

  // 파일 형식 검증
  const ALLOWED_TYPES = ['image/jpeg', 'image/png', 'image/webp']
  if (!ALLOWED_TYPES.includes(file.type)) {
    toast.error('파일 형식 오류', {
      description: 'JPG, PNG, WebP 형식만 지원해요',
      id: 'file-type-error',
    })
    return false
  }

  return true
}
```

2. uploadPreset이 존재하는지 확인:
- Cloudinary 콘솔에서 upload preset 설정 확인
- unsigned preset인지 signed preset인지 확인

3. CldUploadWidget signatureEndpoint 확인:
- 서버 사이드签名 엔드포인트가 필요한 경우 설정
- 개발 환경에서 unsigned preset 사용 가능

주의: CldUploadWidget이 파일 크기 제한을 자체적으로 처리하지만, 이중 검증을 위해 클라이언트 측에서도 체크합니다.
  </action>
  <verify>
1. validateFileBeforeUpload 함수가 존재함: `grep "validateFileBeforeUpload" src/components/students/student-image-uploader.tsx`
2. 파일 크기 검증 로직 포함: `grep -A3 "MAX_FILE_SIZE" src/components/students/student-image-uploader.tsx | grep "10.*1024.*1024"`
3. TypeScript 컴파일 에러 없음: `npm run lint`
  </verify>
  <done>
클라이언트 측에서:
- 파일 선택 시 즉시 크기와 형식 검증 수행함
- 10MB 초과 시 Toast 에러 표시됨
- 지원되지 않는 형식 시 Toast 에러 표시됨
- 유효한 파일만 업로드됨
  </done>
</task>

</tasks>

<verification>
1. 학생 상세 페이지에서 프로필 사진 업로드 시도
2. 10MB 초과 파일 업로드 시도
3. "파일 크기 초과" Toast가 표시되는지 확인
4. 잘못된 형식 파일 업로드 시도
5. "파일 형식 오류" Toast가 표시되는지 확인
6. 정상적인 이미지 파일 업로드
7. 업로드가 성공하고 Toast 성공 메시지가 표시되는지 확인
</verification>

<success_criteria>
1. 10MB 초과 파일 업로드 시 "파일 크기 초과" Toast 표시됨
2. 지원되지 않는 형식 시 "파일 형식 오류" Toast 표시됨
3. 업로드 실패 시 사용자 친화적인 에러 메시지 제공됨
4. 업로드 성공 시 Toast 성공 메시지 표시됨
5. 업로드 중 로딩 상태가 시각적으로 표시됨
6. 모든 에러 메시지가 한국어로 제공됨
</success_criteria>

<output>
After completion, create `.planning/phases/27-rbac-auth-error-handling/27-03-SUMMARY.md`
</output>
