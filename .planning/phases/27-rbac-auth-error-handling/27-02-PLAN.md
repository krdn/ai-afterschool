---
phase: 27-rbac-auth-error-handling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/teachers/[id]/page.tsx
  - src/app/(dashboard)/teachers/[id]/not-found.tsx
  - src/app/(dashboard)/admin/page.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "존재하지 않는 선생님 ID 접근 시 404 에러 페이지가 표시된다"
    - "404 페이지에 요청한 리소스 정보와 제안 URL이 포함된다"
    - "팀장 역할이 Admin 페이지의 모든 탭에 접근할 수 있다"
    - "팀장은 자신의 팀 데이터만 접근 가능하며 타 팀 데이터는 완전 차단된다"
    - "Admin 탭 컴포넌트들이 getRBACPrisma()를 사용하여 자동 팀 필터링을 수행한다"
  artifacts:
    - path: "src/app/(dashboard)/teachers/[id]/not-found.tsx"
      provides: "선생님 404 페이지"
      min_lines: 40
    - path: "src/components/errors/not-found-page.tsx"
      provides: "공통 404 페이지 컴포넌트"
      min_lines: 50
  key_links:
    - from: "src/app/(dashboard)/teachers/[id]/page.tsx"
      to: "src/app/(dashboard)/teachers/[id]/not-found.tsx"
      via: "notFound() call"
      pattern: "notFound\\(\\)"
    - from: "src/app/(dashboard)/admin/page.tsx"
      to: "src/lib/dal.ts"
      via: "verifySession with TEAM_LEADER check"
      pattern: "TEAM_LEADER"
    - from: "src/app/(dashboard)/admin/page.tsx"
      to: "src/lib/db/rbac.ts"
      via: "getRBACPrisma import and usage"
      pattern: "getRBACPrisma"
    - from: "src/components/admin/StatusTab.tsx"
      to: "src/lib/db/rbac.ts"
      via: "getRBACPrisma usage for team filtering"
      pattern: "getRBACPrisma"
    - from: "src/components/admin/LogsTab.tsx"
      to: "src/lib/db/rbac.ts"
      via: "getRBACPrisma usage for team filtering"
      pattern: "getRBACPrisma"
---

<objective>
404 에러 페이지 구현 및 팀장 Admin 접근 제어 수정

Purpose: 존재하지 않는 리소스 접근 시 친화적인 404 페이지를 제공하고, 팀장 역할의 Admin 페이지 접근을 허용하되 팀 데이터 접근을 제한
Output: 선생님/공통 404 페이지와 팀장 접근이 가능한 Admin 페이지
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-rbac-auth-error-handling/27-RESEARCH.md

@src/app/(dashboard)/teachers/[id]/page.tsx
@src/app/(dashboard)/admin/page.tsx
@src/lib/db/rbac.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 공통 404 에러 페이지 컴포넌트 생성</name>
  <files>src/components/errors/not-found-page.tsx</files>
  <action>
NotFoundPage 컴포넌트를 생성합니다:

1. src/components/errors/ 디렉터리 확인 (Task 01에서 생성)
2. not-found-page.tsx 파일 생성

컴포넌트 사양:
- 인터페이스: {
    resourceType?: string (리소스 유형, 예: "선생님", "학생")
    suggestions?: Array<{label: string, href: string}> (제안 URL)
  } props
- 아이콘: Search (lucide-react)를 회색 원형 배경에 표시
- 제목: "{resourceType}을(를) 찾을 수 없어요"
- 설명: "요청하신 {resourceType} 정보를 찾을 수 없어요. 삭제되었거나 잘못된 주소일 수 있어요."
- 제안 URL: suggestions prop으로 전달된 링크를 버튼으로 표시
- 기본 제안: suggestions가 없으면 ["목록으로", "대시보드"] 사용
- 스타일: shadcn/ui Card, CardHeader, CardContent, CardFooter 사용
- 중앙 정렬: min-h-[400px] flex items-center justify-center

범위 명확화:
- Phase 27에서는 404 에러만 구현합니다 (Search 아이콘, 회색)
- 향후 Phase에서 403, 401, 500 확장 가능성을 고려하여 컴포넌트 구조는 유연하게 유지
- CONTEXT.md의 "에러 타입별 다른 아이콘/메시지/색상"은 향후 확장을 위한 가이드로 해석

사용 예시:
```typescript
<NotFoundPage
  resourceType="선생님"
  suggestions={[
    {label: "선생님 목록", href: "/teachers"},
    {label: "대시보드", href: "/dashboard"}
  ]}
/>
```
  </action>
  <verify>
1. 컴포넌트 파일이 존재하고 50줄 이상: `wc -l src/components/errors/not-found-page.tsx`
2. Search 아이콘이 포함됨: `grep "Search" src/components/errors/not-found-page.tsx`
3. TypeScript 컴파일 에러 없음: `npm run lint`
  </verify>
  <done>
NotFoundPage 컴포넌트가 다음 조건을 충족:
- Search 아이콘이 회색 배경에 표시됨
- "{resourceType}을(를) 찾을 수 없어요" 제목 표시됨
- 제안 URL 버튼이 렌더링됨
- 기본 제안이 작동함
- 향후 403/401/500 확장 가능한 유연한 구조
  </done>
</task>

<task type="auto">
  <name>Task 2: 선생님 상세 404 페이지 생성</name>
  <files>src/app/(dashboard)/teachers/[id]/not-found.tsx</files>
  <action>
선생님 상세 페이지 전용 404 페이지를 생성합니다:

1. src/app/(dashboard)/teachers/[id]/not-found.tsx 파일 생성

페이지 사양:
- NotFoundPage 컴포넌트 활용 (props로 resourceType="선생님" 전달)
- 제안 URL: [
    {label: "선생님 목록", href: "/teachers"},
    {label: "대시보드", href: "/dashboard"},
    {label: "학생 목록", href: "/students"}
  ]
- UserX 아이콘도 대안으로 고려 (선생님 특화 아이콘)

선생님 상세 페이지(page.tsx) 수정:
- teacher 조회 결과가 null일 때만 notFound() 호출 (현재 구조 유지)
- not-found.tsx가 자동으로 렌더링되도록 함
  </action>
  <verify>
1. not-found.tsx 파일이 존재함: `ls -la src/app/\(dashboard\)/teachers/\[id\]/not-found.tsx`
2. NotFoundPage를 import함: `grep "NotFoundPage" src/app/\(dashboard\)/teachers/\[id\]/not-found.tsx`
3. TypeScript 컴파일 에러 없음: `npm run lint`
  </verify>
  <done>
존재하지 않는 선생님 ID로 접근 시:
- 404 에러 페이지가 올바른 상태 코드로 반환됨
- "선생님을 찾을 수 없어요" 메시지가 표시됨
- 선생님 목록, 대시보드, 학생 목록 버튼이 작동함
  </done>
</task>

<task type="auto">
  <name>Task 3: Admin 페이지 TEAM_LEADER 접근 허용 및 팀 데이터 필터링 강화</name>
  <files>src/app/(dashboard)/admin/page.tsx</files>
  <action>
Admin 페이지의 접근 제어를 수정하여 팀장도 접근 가능하게 하고, 팀 데이터 필터링을 강화합니다:

1. 기존 역할 체크 수정 (line ~139):
```typescript
// 기존: if (!session || session.role !== 'DIRECTOR') {
// 수정: if (!session || (session.role !== 'DIRECTOR' && session.role !== 'TEAM_LEADER')) {
if (!session || (session.role !== 'DIRECTOR' && session.role !== 'TEAM_LEADER')) {
  redirect('/dashboard')
}
```

2. getRBACPrisma() import 및 사용:
```typescript
import { getRBACPrisma } from '@/lib/db/rbac'

// Admin 페이지에서 팀 데이터 필터링을 위한 RBAC Prisma 인스턴스 사용
const prisma = await getRBACPrisma()
```

3. 각 Admin 탭 컴포넌트에서 팀 데이터 필터링 구현:
- StatusTab: getRBACPrisma()로 시스템 상태 데이터 조회 시 팀 필터링 적용
- LogsTab: getRBACPrisma()로 로그 데이터 조회 시 팀 필터링 적용
- DatabaseTab: getRBACPrisma()로 DB 상태 조회 시 팀 필터링 적용
- AuditTab: getRBACPrisma()로 감사 로그 조회 시 팀 필터링 적용

4. 탭 컴포넌트 수정 패턴:
```typescript
// 기존: const prisma = new PrismaClient()
// 수정: const prisma = await getRBACPrisma()
```

5. Admin 페이지 진입 시 팀장에게 안내 메시지:
- 팀장에게는 전체 시스템 설정이 아닌 팀 관련 설정만 표시
- 모든 탭 접근 허용하지만 데이터는 getRBACPrisma()로 자동 팀 필터링 적용

참고: CONTEXT.md 결정에 따라 "모든 Admin 탭 허용" + "자신의 팀 데이터만 접근 가능, 타 팀 데이터 완전 차단"으로 구현
  </action>
  <verify>
1. TEAM_LEADER 역할 체크가 추가됨: `grep -B2 -A2 "TEAM_LEADER" src/app/\(dashboard\)/admin/page.tsx | grep "role"`
2. DIRECTOR와 TEAM_LEADER 모두 접근 가능한 조건: `grep -E "DIRECTOR.*TEAM_LEADER|TEAM_LEADER.*DIRECTOR" src/app/\(dashboard\)/admin/page.tsx`
3. getRBACPrisma import 및 사용: `grep "getRBACPrisma" src/app/\(dashboard\)/admin/page.tsx`
4. StatusTab에서 getRBACPrisma 사용: `grep "getRBACPrisma" src/components/admin/StatusTab.tsx`
5. LogsTab에서 getRBACPrisma 사용: `grep "getRBACPrisma" src/components/admin/LogsTab.tsx`
6. TypeScript 컴파일 에러 없음: `npm run lint`
  </verify>
  <done>
팀장 역할로 Admin 페이지 접근 시:
- 페이지가 정상적으로 로드됨
- 모든 Admin 탭에 접근 가능함
- 자신의 팀 데이터만 표시됨 (getRBACPrisma()에 의해 자동 필터링)
- 타 팀 데이터는 완전히 차단됨 (CONTEXT.md 결정 준수)
  </done>
</task>

</tasks>

<verification>
1. 존재하지 않는 선생님 ID로 /teachers/invalid-id 접근
2. 404 에러 페이지가 올바르게 표시되는지 확인
3. 제안 URL 버튼이 작동하는지 확인
4. 팀장 계정으로 /admin 페이지 접근
5. Admin 페이지가 로드되는지 확인
6. 팀장에게 자신의 팀 데이터만 표시되는지 확인
</verification>

<success_criteria>
1. 존재하지 않는 선생님 ID 접근 시 404 에러 페이지 표시됨
2. 404 페이지에 "선생님을 찾을 수 없어요" 메시지 표시됨
3. 3개 이상의 복귀 경로 버튼 제공됨
4. 팀장이 Admin 페이지에 접근 가능함
5. 팀장에게 자신의 팀 데이터만 표시되고 타 팀 데이터는 완전 차단됨 (CONTEXT.md 결정 준수)
6. Admin 탭 컴포넌트들이 getRBACPrisma()를 사용하여 자동 팀 필터링 수행
7. 모든 에러 메시지가 한국어로 친화적임
</success_criteria>

<output>
After completion, create `.planning/phases/27-rbac-auth-error-handling/27-02-SUMMARY.md`
</output>
