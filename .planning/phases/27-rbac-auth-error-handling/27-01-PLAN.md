---
phase: 27-rbac-auth-error-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/teachers/page.tsx
  - src/lib/actions/teachers.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "일반 선생님이 /teachers 관리 페이지 접근 시 접근 거부 UI가 표시된다"
    - "접근 거부 UI에 Toast 알림과 Dashboard로 이동 버튼이 포함된다"
    - "선생님 삭제 액션에서 RBAC 검증이 수행된다"
    - "본인 계정 삭제 시도가 차단된다"
  artifacts:
    - path: "src/components/errors/access-denied-page.tsx"
      provides: "접근 거부 UI 컴포넌트"
      min_lines: 50
    - path: "src/lib/actions/teachers.ts"
      provides: "RBAC 검증이 포함된 선생님 액션"
      contains: "deleteTeacher|updateTeacher|createTeacher"
  key_links:
    - from: "src/app/(dashboard)/teachers/page.tsx"
      to: "src/components/errors/access-denied-page.tsx"
      via: "import and render"
      pattern: "AccessDeniedPage"
    - from: "src/lib/actions/teachers.ts"
      to: "src/lib/dal.ts"
      via: "verifySession call"
      pattern: "verifySession"
---

<objective>
접근 제어 강화 - 일반 선생님의 선생님 관리 페이지 접근 제한 및 RBAC 검증 강화

Purpose: 권한 없는 사용자의 관리 페이지 접근을 차단하고, Server Action 레벨에서 일관된 RBAC 검증을 적용하여 시스템 보안 강화
Output: 접근 거부 UI 컴포넌트와 RBAC 검증이 강화된 선생님 관리 액션
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-rbac-auth-error-handling/27-RESEARCH.md

@src/lib/dal.ts
@src/lib/db/rbac.ts
@src/app/(dashboard)/teachers/page.tsx
@src/lib/actions/teachers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 접근 거부 UI 컴포넌트 생성</name>
  <files>src/components/errors/access-denied-page.tsx</files>
  <action>
AccessDeniedPage 컴포넌트를 생성합니다:

1. src/components/errors/ 디렉터리 생성 (없는 경우)
2. access-denied-page.tsx 파일 생성

컴포넌트 사양:
- 인터페이스: { resource?: string, action?: string } props
- 아이콘: ShieldX (lucide-react)를 빨간색 원형 배경에 표시
- 제목: "접근 권한이 없어요"
- 설명: "{resource}에 {action}할 권한이 없어요" + "필요한 권한이 있는지 확인하거나 관리자에게 문의해주세요"
- 버튼 2개:
  1. "대시보드로 이동" (Primary 버튼, /dashboard 링크)
  2. "학생 목록" (Outline 버튼, /students 링크)
- 스타일: shadcn/ui Card, CardHeader, CardContent, CardFooter 사용
- 중앙 정렬: min-h-[400px] flex items-center justify-center

Toast 통합 (Client Component wrapper 필요):
- 'use client' 지시자 추가
- useEffect로 마운트 시 toast.error 호출
- Toast 메시지: "이 페이지에 접근할 권한이 없습니다"
- Toast id: "access-denied" (중복 방지)
  </action>
  <verify>
1. 컴포넌트 파일이 존재하고 50줄 이상: `wc -l src/components/errors/access-denied-page.tsx`
2. TypeScript 컴파일 에러 없음: `npm run lint`
3. 컴포넌트가 ShieldX 아이콘과 2개 버튼을 포함: `grep -E "ShieldX|대시보드로 이동|학생 목록" src/components/errors/access-denied-page.tsx`
  </verify>
  <done>
AccessDeniedPage 컴포넌트가 다음 조건을 충족:
- ShieldX 아이콘이 빨간색 배경에 표시됨
- "접근 권한이 없어요" 제목 표시됨
- Toast 알림이 마운트 시 표시됨
- Dashboard와 학생 목록 버튼이 작동함
  </done>
</task>

<task type="auto">
  <name>Task 2: 선생님 페이지 RBAC 강화 및 접근 거부 UI 통합</name>
  <files>src/app/(dashboard)/teachers/page.tsx</files>
  <action>
선생님 관리 페이지의 접근 제어를 강화합니다:

1. 기존 권한 검증 로직 제거 (lines 13-22)
2. AccessDeniedPage 컴포넌트로 교체:

```typescript
import { AccessDeniedPage } from '@/components/errors/access-denied-page'

// 기존 조건부 렌더링 제거
if (session.role === 'MANAGER' || session.role === 'TEACHER') {
  return <AccessDeniedPage resource="선생님 관리" action="접근" />
}
```

3. TEAM_LEADER는 접근 허용 (기존 로직 유지)
4. 선생님 등록 버튼의 역할 체크 유지 (DIRECTOR만 표시)

변경 사항:
- 간단한 텍스트 메시지 대신 AccessDeniedPage 컴포넌트 사용
- Toast 알림이 자동으로 표시됨
- Dashboard와 학생 목록으로의 복귀 경로 제공
  </action>
  <verify>
1. 페이지가 AccessDeniedPage를 import함: `grep "AccessDeniedPage" src/app/\(dashboard\)/teachers/page.tsx`
2. MANAGER/TEACHER 역할 시 접근 거부 UI 반환: `grep -A2 "MANAGER.*TEACHER" src/app/\(dashboard\)/teachers/page.tsx | grep "AccessDeniedPage"`
3. TypeScript 컴파일 에러 없음: `npm run lint`
  </verify>
  <done>
일반 선생님이 /teachers 접근 시:
- AccessDeniedPage 컴포넌트가 렌더링됨
- Toast 알림 "이 페이지에 접근할 권한이 없습니다" 표시됨
- Dashboard로 이동 버튼이 작동함
  </done>
</task>

<task type="auto">
  <name>Task 3: 선생님 Server Action RBAC 검증 강화</name>
  <files>src/lib/actions/teachers.ts</files>
  <action>
선생님 관련 Server Action에 RBAC 검증을 추가합니다:

1. deleteTeacher 함수 수정:
   - verifySession() 호출 후 역할 검증 추가
   - DIRECTOR만 삭제 가능하도록 체크
   - 본인 계정 삭제 방지
   - 에러 발생 시 명확한 메시지 반환

2. updateTeacher 함수 수정 (있는 경우):
   - 역할 검증 추가 (DIRECTOR 또는 TEAM_LEADER)
   - TEAM_LEADER는 같은 팀 선생님만 수정 가능

3. createTeacher 함수 수정:
   - DIRECTOR만 생성 가능하도록 체크

검증 패턴:
```typescript
export async function deleteTeacher(id: string): Promise<{ success?: boolean; error?: string }> {
  const session = await verifySession()

  // 권한 검증: 원장만 삭제 가능
  if (session.role !== 'DIRECTOR') {
    return { error: '선생님을 삭제할 권한이 없어요' }
  }

  // 본인 삭제 방지
  if (session.userId === id) {
    return { error: '본인 계정은 삭제할 수 없어요' }
  }

  // 삭제 로직...
}
```

주의: 기존 로직을 유지하며 검증만 추가합니다. 반환 타입이 기존과 호환되도록 주의합니다.
  </action>
  <verify>
1. deleteTeacher가 역할 검증을 포함함: `grep -A5 "deleteTeacher" src/lib/actions/teachers.ts | grep -E "DIRECTOR|role"`
2. 본인 삭제 방지 로직 존재: `grep -A10 "deleteTeacher" src/lib/actions/teachers.ts | grep "userId"`
3. TypeScript 컴파일 에러 없음: `npm run lint`
  </verify>
  <done>
Server Action 레벨에서:
- deleteTeacher가 DIRECTOR 역할만 실행 가능함
- 본인 계정 삭제 시도가 차단됨
- 권한 없는 요청에 명확한 에러 메시지 반환됨
  </done>
</task>

</tasks>

<verification>
1. 일반 선생님 계정으로 /teachers 페이지 접근 시도
2. 접근 거부 UI가 올바르게 표시되는지 확인
3. Toast 알림이 표시되는지 확인
4. Dashboard 버튼 클릭 시 정상적으로 이동하는지 확인
5. 테스트 계정으로 선생님 삭제 시도 (권한 없는 경우)
6. 적절한 에러 메시지가 반환되는지 확인
</verification>

<success_criteria>
1. 일반 선생님이 /teachers 접근 시 AccessDeniedPage가 렌더링됨
2. Toast 알림 "이 페이지에 접근할 권한이 없습니다"가 표시됨
3. Dashboard와 학생 목록 복귀 버튼이 작동함
4. deleteTeacher Server Action에서 RBAC 검증이 수행됨
5. 본인 계정 삭제 시도가 차단됨
6. 모든 에러 메시지가 한국어로 사용자 친화적임
</success_criteria>

<output>
After completion, create `.planning/phases/27-rbac-auth-error-handling/27-01-SUMMARY.md`
</output>
