---
phase: 40-llmquerybar-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/llm-query-bar.tsx
  - src/app/[locale]/(dashboard)/chat/page.tsx
  - src/components/chat/chat-page.tsx
autonomous: true
requirements:
  - UI-04

must_haves:
  truths:
    - "대시보드 LLMQueryBar에서 @를 입력하면 자동완성 드롭다운이 표시된다"
    - "LLMQueryBar에서 멘션 포함 메시지를 전송하면 /chat 페이지로 이동하여 서버 파이프라인이 처리한다"
    - "LLMQueryBar에서 멘션 없는 일반 질문은 기존과 동일하게 /chat?q=... URL로 라우팅된다"
    - "LLMQueryBar는 단일 행(single-row) UI를 유지하며 높이가 팽창하지 않는다"
  artifacts:
    - path: "src/components/layout/llm-query-bar.tsx"
      provides: "MentionsInput 기반 LLMQueryBar 컴포넌트"
      contains: "MentionsInput"
    - path: "src/app/[locale]/(dashboard)/chat/page.tsx"
      provides: "mentions URL 파라미터 파싱"
      contains: "initialMentions"
    - path: "src/components/chat/chat-page.tsx"
      provides: "initialMentions prop 수신 및 handleSend 연결"
      contains: "initialMentions"
  key_links:
    - from: "src/components/layout/llm-query-bar.tsx"
      to: "/chat?q=...&mentions=..."
      via: "router.push with URL-serialized mentions"
      pattern: "router\\.push.*mentions"
    - from: "src/app/[locale]/(dashboard)/chat/page.tsx"
      to: "src/components/chat/chat-page.tsx"
      via: "initialMentions prop"
      pattern: "initialMentions.*MentionItem"
    - from: "src/components/chat/chat-page.tsx"
      to: "handleSend"
      via: "useEffect with initialMentions"
      pattern: "handleSend.*initialMentions"
---

<objective>
LLMQueryBar의 `<input type="text">`를 `<MentionsInput>`으로 교체하고, submit 시 멘션 데이터를 URL 파라미터로 직렬화하여 채팅 페이지로 전달한다. 채팅 페이지는 URL에서 mentions를 파싱해 `handleSend`에 전달함으로써 Phase 36 서버 파이프라인을 동일하게 활용한다.

Purpose: 교사가 대시보드 어디서든 @멘션으로 컨텍스트 인식 AI 질문을 빠르게 시작할 수 있도록 한다.
Output: MentionsInput이 통합된 llm-query-bar.tsx, mentions 파라미터를 파싱하는 chat/page.tsx, initialMentions를 수신하는 chat-page.tsx
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-llmquerybar-extension/40-RESEARCH.md

# 핵심 참조 파일
@src/components/layout/llm-query-bar.tsx
@src/components/chat/chat-input.tsx
@src/hooks/use-mention.ts
@src/lib/chat/mention-types.ts
@src/app/[locale]/(dashboard)/chat/page.tsx
@src/components/chat/chat-page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: LLMQueryBar에 MentionsInput 통합 + submit 로직 확장</name>
  <files>src/components/layout/llm-query-bar.tsx</files>
  <action>
  기존 `llm-query-bar.tsx`의 `<input type="text">`를 react-mentions-ts `<MentionsInput>`으로 교체한다. 기존 chat-input.tsx 패턴을 참조하되, QueryBar 고유의 단일 행 제약을 적용한다.

  **Import 변경:**
  - 제거: `useRef` (inputRef 불필요)
  - 추가: `useRef` (prevTypeRef용), `MentionsInput`, `Mention` from 'react-mentions-ts', `MentionsInputChangeEvent`, `MentionDataItem` from 'react-mentions-ts', `useMention`, `occurrencesToMentionItems`, `MentionExtra` from '@/hooks/use-mention'

  **State 변경:**
  - 제거: `prompt` (string state), `inputRef` (HTMLInputElement ref)
  - 추가: `mentionMarkup` (string state, 기본값 ""), `activeMentions` (배열 state, 기본값 []), `prevTypeRef` (useRef<string | null>(null))
  - 추가: `const { fetchMentions } = useMention()`

  **handleMentionsChange 콜백 추가:**
  ```typescript
  const handleMentionsChange = useCallback((change: MentionsInputChangeEvent<MentionExtra>) => {
    setMentionMarkup(change.value)
    setActiveMentions(change.mentions)
  }, [])
  ```

  **handleSubmit 수정:**
  - plain text 추출: `mentionMarkup.replace(/@\[([^\]]+)\]\([^)]+\)/g, '@$1').trim()`
  - 빈 텍스트 시 기존 toast.error 유지
  - `occurrencesToMentionItems(activeMentions)` 호출하여 mentionItems 생성
  - URL 파라미터 구성: `const params = new URLSearchParams({ q: plainText })`
  - mentionItems.length > 0 일 때만: `params.set('mentions', JSON.stringify(mentionItems))`
  - `router.push('/chat?' + params.toString())`로 네비게이션
  - 상태 초기화: `setMentionMarkup("")`, `setActiveMentions([])`

  **handleKeyDown 수정:**
  - 타입을 `React.KeyboardEvent`로 변경 (HTMLInputElement 제거)
  - `Enter && !e.shiftKey` 조건 추가 (Shift+Enter는 무시 또는 방지)

  **renderSuggestion 콜백 추가 (chat-input.tsx에서 복붙):**
  - `prevTypeRef`로 그룹 헤더 감지
  - `groupLabels` 상수도 동일하게 컴포넌트 외부에 선언: `{ student: '학생', teacher: '선생님', team: '학급' }`
  - chat-input.tsx의 renderSuggestion과 완전 동일한 JSX

  **JSX 변경:**
  - `<input ref={inputRef} type="text" ...>` 삭제
  - `<MentionsInput>` 교체:
    - `value={mentionMarkup}`
    - `onMentionsChange={handleMentionsChange}`
    - `onKeyDown={handleKeyDown}`
    - `placeholder={t("placeholder")}`
    - `a11ySuggestionsListLabel="멘션 검색 결과"`
    - `suggestionsPlacement="below"` (QueryBar는 화면 상단 → 드롭다운 아래쪽)
    - `className="flex-1 h-[38px] max-h-[38px] text-sm"` (단일 행 유지)
    - `autoResize` 미사용 (prop 추가하지 않음)
  - `<Mention>` 자식 요소:
    - `trigger="@"`
    - `data={fetchMentions as ...}` (chat-input.tsx 패턴 동일)
    - `renderSuggestion={renderSuggestion}`
    - `displayTransform={(_id, display) => '@' + (display ?? '')}`
    - `appendSpaceOnAdd`
    - `markup="@[__display__](__id__)"`
  - Button disabled 조건: `!plainTextForDisabled` (기존 `!prompt.trim()` 대체)

  **추가 CSS 고려:**
  - MentionsInput wrapper에 `overflow-hidden` 적용하여 단일 행 UI 보장
  - 기존 input의 `px-3 border rounded-md focus:outline-none focus:ring-2 focus:ring-purple-500/20 focus:border-purple-500` 스타일은 MentionsInput의 className 또는 classNames prop으로 이관

  **주의사항:**
  - `suggestionsPlacement="below"` (not "auto" or "above") — QueryBar는 화면 상단
  - `autoResize` 사용하지 않음 — 단일 행 UI
  - `prevTypeRef`는 컴포넌트 내부 `useRef`로 선언 (모듈 수준 공유 금지)
  - teacher session props 전달 불필요: useMention API가 자체적으로 verifySession() 호출
  </action>
  <verify>
  1. TypeScript 컴파일 확인: `npx tsc --noEmit --project tsconfig.json 2>&1 | grep llm-query-bar` 에러 없음
  2. 브라우저에서 대시보드 접속 → LLMQueryBar에 "@" 입력 → 자동완성 드롭다운 표시 확인
  3. 멘션 선택 후 Submit → URL에 `?q=...&mentions=...` 파라미터 포함 확인
  4. 멘션 없이 일반 텍스트 Submit → URL에 `?q=...`만 포함 (기존 동작)
  5. LLMQueryBar 높이가 38px로 유지되고 팽창하지 않음
  </verify>
  <done>
  - LLMQueryBar에 MentionsInput이 통합되어 @를 입력하면 자동완성 드롭다운이 below 방향으로 표시됨
  - 멘션 포함 submit 시 /chat?q=...&mentions=[...] URL로 네비게이션됨
  - 멘션 없는 submit 시 기존 /chat?q=... URL로 네비게이션됨 (하위 호환)
  - QueryBar는 단일 행 UI를 유지
  </done>
</task>

<task type="auto">
  <name>Task 2: ChatPage에서 URL mentions 파라미터 수신 + handleSend 연결</name>
  <files>
    src/app/[locale]/(dashboard)/chat/page.tsx
    src/components/chat/chat-page.tsx
  </files>
  <action>
  LLMQueryBar가 URL에 직렬화한 mentions 파라미터를 chat/page.tsx에서 파싱하고, ChatPage에 initialMentions prop으로 전달하여 handleSend가 멘션을 포함해 호출되도록 한다.

  **chat/page.tsx 수정:**
  1. import 추가: `import type { MentionItem } from "@/lib/chat/mention-types"`
  2. `Props` 타입의 `searchParams` 확장:
     ```typescript
     type Props = {
       searchParams: Promise<{ q?: string; mentions?: string }>
     }
     ```
  3. mentions 파싱 로직 추가 (params 할당 후):
     ```typescript
     let initialMentions: MentionItem[] | undefined
     if (params.mentions) {
       try {
         const parsed = JSON.parse(params.mentions)
         if (Array.isArray(parsed) && parsed.every(
           (m): m is MentionItem => typeof m === 'object' && m !== null && 'type' in m && 'id' in m
         )) {
           initialMentions = parsed
         }
       } catch {
         // 파싱 실패 시 무시 — 멘션 없이 전송
       }
     }
     ```
  4. `<ChatPage>` JSX에 `initialMentions` prop 추가:
     ```typescript
     <ChatPage
       initialSessions={sessions}
       initialQuery={params.q}
       initialMentions={initialMentions}
     />
     ```

  **chat-page.tsx 수정:**
  1. `ChatPageProps` 타입에 `initialMentions` 추가:
     ```typescript
     type ChatPageProps = {
       initialSessions: SessionSummary[]
       sessionId?: string
       initialMessages?: Message[]
       initialQuery?: string
       initialMentions?: MentionItem[]  // NEW
     }
     ```
  2. 컴포넌트 함수 매개변수에 `initialMentions` 추가 (destructuring)
  3. initialQuery useEffect 수정 — initialMentions를 handleSend에 전달:
     ```typescript
     useEffect(() => {
       if (initialQuery && !initialSessionId) {
         handleSend(initialQuery, initialMentions ?? [], undefined)
       }
       // eslint-disable-next-line react-hooks/exhaustive-deps
     }, [])
     ```
     기존 `handleSend(initialQuery, [], undefined)` → `handleSend(initialQuery, initialMentions ?? [], undefined)`로 변경

  **주의사항:**
  - initialMentions를 useEffect 의존성 배열에 추가하지 않음 (기존 패턴: 빈 배열 `[]` + eslint-disable)
  - JSON.parse 실패 시 조용히 무시 (멘션 없이 전송)
  - MentionItem 타입 가드: `type`과 `id` 필드 존재 확인으로 안전한 파싱
  - 기존 initialQuery만 있는 경우: initialMentions가 undefined이므로 `initialMentions ?? []` → 빈 배열 (하위 호환)
  </action>
  <verify>
  1. TypeScript 컴파일 확인: `npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "chat/page|chat-page"` 에러 없음
  2. LLMQueryBar에서 멘션 포함 submit → 채팅 페이지 이동 → AI 응답에 멘션된 엔티티 컨텍스트가 반영됨
  3. LLMQueryBar에서 멘션 없는 submit → 채팅 페이지 이동 → 기존과 동일하게 일반 질문 처리
  4. 직접 URL 입력 /chat?q=test → 기존과 동일하게 동작 (initialMentions undefined)
  5. 잘못된 mentions 파라미터 /chat?q=test&mentions=invalid → 에러 없이 일반 질문으로 처리
  </verify>
  <done>
  - chat/page.tsx가 URL에서 mentions 파라미터를 안전하게 파싱하여 ChatPage에 전달
  - ChatPage가 initialMentions를 받아 handleSend에 전달하여 Phase 36 서버 파이프라인으로 멘션 처리
  - initialMentions가 없을 때 기존 동작과 완전 동일 (하위 호환)
  </done>
</task>

</tasks>

<verification>
1. **자동완성 동작**: 대시보드 LLMQueryBar에서 "@"를 입력하면 아래쪽 드롭다운에 학생/선생님/학급 검색 결과가 표시된다
2. **멘션 submit**: 멘션 선택 후 submit → /chat?q=...&mentions=[...] URL로 이동 → AI가 멘션된 엔티티 데이터를 참조해 응답
3. **일반 submit**: 멘션 없이 submit → /chat?q=... URL (기존 동작)
4. **하위 호환**: chat/page.tsx에 mentions 파라미터가 없어도 정상 동작
5. **단일 행 UI**: LLMQueryBar 높이가 38px로 유지, 긴 텍스트 입력 시에도 팽창하지 않음
6. **TypeScript**: `npx tsc --noEmit` 컴파일 에러 없음
</verification>

<success_criteria>
- LLMQueryBar에서 @멘션 자동완성이 동작한다 (Phase 38 드롭다운과 동일)
- 멘션 포함 메시지가 Phase 36 서버 파이프라인으로 처리된다
- 기존 멘션 없는 질문이 하위 호환성을 유지한다
- 3개 파일만 수정되고 새 파일 생성 없음
</success_criteria>

<output>
After completion, create `.planning/phases/40-llmquerybar-extension/40-01-SUMMARY.md`
</output>
