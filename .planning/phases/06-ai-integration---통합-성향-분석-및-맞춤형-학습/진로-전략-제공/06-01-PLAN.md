---
phase: 06-ai-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/db/personality-summary.ts
  - src/lib/db/student-analysis.ts
autonomous: true

must_haves:
  truths:
    - "PersonalitySummary 모델이 생성되고 데이터베이스에 테이블이 존재한다"
    - "getUnifiedPersonalityData() 함수가 모든 분석 타입을 조회하여 통합된 데이터를 반환한다"
    - "upsertPersonalitySummary() 함수가 성격 요약 데이터를 저장/갱신할 수 있다"
    - "통합 데이터 조회 함수는 일부 분석이 누락되어도 null 처리로 정상 동작한다"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "PersonalitySummary 모델 및 관계 정의"
      contains: "model PersonalitySummary"
    - path: "src/lib/db/personality-summary.ts"
      provides: "통합 성향 요약 CRUD 함수"
      exports: ["getUnifiedPersonalityData", "upsertPersonalitySummary", "getPersonalitySummary"]
    - path: "src/lib/db/student-analysis.ts"
      provides: "기존 분석 조회 함수 (수정됨)"
      exports: ["getStudentCalculationStatus"]
  key_links:
    - from: "src/lib/db/personality-summary.ts"
      to: "prisma.schema.prisma"
      via: "Prisma Client Student 모델 include 관계"
      pattern: "db.student.findFirst.*include.*sajuAnalysis.*nameAnalysis.*mbtiAnalysis.*faceAnalysis.*palmAnalysis"
---

<objective>
통합 성향 분석을 위한 데이터베이스 스키마 및 데이터 접근 계층 구축

Purpose: 모든 분석 결과(사주, 성명학, MBTI, 관상, 손금)를 통합하여 저장하고 조회할 수 있는 기반 마련
Output: PersonalitySummary 모델, 통합 데이터 조회 함수, 요약 저장 함수
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai-integration---통합-성향-분석-및-맞춤형-학습/진로-전략-제공/06-RESEARCH.md
@/mnt/data/projects/ai/ai-afterschool/prisma/schema.prisma
@/mnt/data/projects/ai/ai-afterschool/src/lib/db/student-analysis.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: PersonalitySummary 모델 생성</name>
  <files>prisma/schema.prisma</files>
  <action>
    prisma/schema.prisma에 PersonalitySummary 모델 추가:

    1. Student 모델에 personalitySummary 관계 추가 (기존 Student 모델 수정)
    2. PersonalitySummary 모델 생성:
       - id (String, @id, @default(cuid()))
       - studentId (String, @unique)
       - coreTraits (String?, nullable)
       - learningStrategy (Json?, nullable)
       - careerGuidance (Json?, nullable)
       - status (String, @default("none"))
       - errorMessage (String?, nullable)
       - version (Int, @default(1))
       - generatedAt (DateTime?, nullable)
       - createdAt (DateTime, @default(now()))
       - updatedAt (DateTime, @updatedAt)
       - student (Relation, onDelete: Cascade)
       - @@index([studentId])

    3. status 필드는 "none", "pending", "complete", "failed" 값 사용

    주의: 기존 Student 모델의 마지막에 personalitySummary 관계만 추가
  </action>
  <verify>
    grep -n "model PersonalitySummary" prisma/schema.prisma
    npx prisma db push
    grep -n "personalitySummary" prisma/schema.prisma | grep Student
  </verify>
  <done>
    PersonalitySummary 모델이 스키마에 정의되고 데이터베이스에 테이블이 생성된다
    Student 모델과 cascade delete 관계가 설정된다
  </done>
</task>

<task type="auto">
  <name>Task 2: 통합 데이터 조회 함수 생성</name>
  <files>src/lib/db/personality-summary.ts</files>
  <action>
    src/lib/db/personality-summary.ts 파일 생성:

    1. UnifiedPersonalityData 타입 정의 (RESEARCH.md Pattern 1 참조)
    2. getUnifiedPersonalityData() 함수 구현:
       - studentId, teacherId 파라미터
       - db.student.findFirst()로 모든 분석 관계 include
       - 각 분석의 result, calculatedAt/analyzedAt, interpretation 반환
       - null 처리로 일부 분석 누락에 대응
       - 학생을 찾지 못하면 null 반환

    3. getPersonalitySummary() 함수 구현:
       - studentId로 PersonalitySummary 조회
       - null 반환 허용

    src/lib/db/student-analysis.ts에 calculateStatus 관련 코드는 유지 (이미 존재)
  </action>
  <verify>
    grep -n "getUnifiedPersonalityData" src/lib/db/personality-summary.ts
    grep -n "UnifiedPersonalityData" src/lib/db/personality-summary.ts
    npx tsc --noEmit
  </verify>
  <done>
    getUnifiedPersonalityData() 함수가 5개 분석의 통합 데이터를 반환한다
    일부 분석이 없어도 에러 없이 null 값을 포함하여 반환한다
  </done>
</task>

<task type="auto">
  <name>Task 3: 요약 저장 함수 생성</name>
  <files>src/lib/db/personality-summary.ts</files>
  <action>
    src/lib/db/personality-summary.ts에 upsertPersonalitySummary() 함수 추가:

    1. PersonalitySummaryUpsertPayload 타입 정의:
       - studentId (string)
       - coreTraits?: string | null
       - learningStrategy?: Prisma.InputJsonValue | null
       - careerGuidance?: Prisma.InputJsonValue | null
       - status?: string
       - errorMessage?: string | null
       - generatedAt?: Date

    2. upsertPersonalitySummary() 함수 구현:
       - db.personalitySummary.upsert() 사용
       - where: { studentId }
       - create/update에 데이터 매핑
       - Prisma.InputJsonValue로 JSON 타입 캐스팅
       - 기존 레코드가 있으면 version 자동 증가 로직 추가

    기존 student-analysis.ts의 upsertSajuAnalysis, upsertNameAnalysis 패턴 참조
  </action>
  <verify>
    grep -n "upsertPersonalitySummary" src/lib/db/personality-summary.ts
    npx tsc --noEmit
  </verify>
  <done>
    upsertPersonalitySummary() 함수가 PersonalitySummary를 생성/갱신한다
    version이 자동으로 증가한다
  </done>
</task>

</tasks>

<verification>
1. npx tsc --noEmit으로 TypeScript 타입 검증 통과
2. Prisma Client가 PersonalitySummary 모델을 포함하여 재생성되었다
3. 데이터베이스에 personality_summary 테이블이 존재한다
4. 모든 함수가 올바른 타입으로 export 된다
</verification>

<success_criteria>
1. PersonalitySummary 모델이 Prisma 스키마에 정의되고 데이터베이스에 테이블이 생성된다
2. getUnifiedPersonalityData()가 5개 분석의 통합 데이터를 반환한다
3. upsertPersonalitySummary()가 요약 데이터를 저장하고 version을 관리한다
4. 일부 분석이 누락되어도 에러 없이 동작한다 (partial data 지원)
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-integration---통합-성향-분석-및-맞춤형-학습/진로-전략-제공/06-01-SUMMARY.md`
</output>
