---
phase: 06-ai-integration
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/students/personality-summary-card.tsx
autonomous: true

must_haves:
  truths:
    - "통합 성향 요약 카드가 학생 상세 페이지에 표시된다"
    - "5개 분석의 완료 상태가 아이콘으로 시각화된다"
    - "최소 3개 분석 완료 시 AI 생성 버튼이 표시된다"
    - "AI 생성 완료 시 핵심 성향 요약이 표시된다"
    - "데이터 부족 시 안내 메시지가 표시된다"
  artifacts:
    - path: "src/components/students/personality-summary-card.tsx"
      provides: "통합 성향 요약 카드 컴포넌트"
      exports: ["PersonalitySummaryCard"]
  key_links:
    - from: "src/components/students/personality-summary-card.tsx"
      to: "src/lib/db/personality-summary.ts"
      via: "Server Component에서 getUnifiedPersonalityData 호출"
      pattern: "getUnifiedPersonalityData.*await"
    - from: "src/components/students/personality-summary-card.tsx"
      to: "src/lib/actions/personality-integration.ts"
      via: "generateLearningStrategy 액션 호출"
      pattern: "generateLearningStrategy.*server action"
---

<objective>
통합 성향 요약 카드 컴포넌트 구현

Purpose: 학생의 모든 분석 완료 상태를 한눈에 보여주고 AI 통합 분석 생성 진입점 제공
Output: 분석 완료 상태 표시, AI 생성 버튼, 핵심 성향 요약 표시 카드
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai-integration---통합-성향-분석-및-맞춤형-학습/진로-전략-제공/06-RESEARCH.md
@.planning/phases/06-ai-integration---통합-성향-분석-및-맞춤형-학습/진로-전략-제공/06-01-SUMMARY.md
@/mnt/data/projects/ai/ai-afterschool/src/components/students/saju-analysis-panel.tsx
@/mnt/data/projects/ai/ai-afterschool/src/components/students/mbti-analysis-panel.tsx
@/mnt/data/projects/ai/ai-afterschool/src/lib/db/personality-summary.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 요약 카드 컴포넌트 구조 생성</name>
  <files>src/components/students/personality-summary-card.tsx</files>
  <action>
    src/components/students/personality-summary-card.tsx 파일 생성 (Server Component):

    1. 컴포넌트 Props:
       - studentId: string
       - teacherId: string

    2. 데이터 로딩:
       - getUnifiedPersonalityData(studentId, teacherId) 호출
       - getPersonalitySummary(studentId) 호출
       - null 처리

    3. UI 구조 (Card, CardHeader, CardTitle, CardContent 사용):
       - 헤더: "통합 성향 분석" + 완료 카운트 (예: "3/5 완료")
       - 분석 상태 그리드: 5개 분석 아이콘 (CheckCircle2/Circle)
       - 본문: 데이터 상태에 따른 조건부 렌더링

    4. 분석 상태 컴포넌트:
       - AnalysisStatus 하위 컴포넌트
       - label, available props
       - available이면 CheckCircle2 (초록), 아니면 Circle (회색)

    기존 saju-analysis-panel.tsx의 Card 패턴 참조
  </action>
  <verify>
    grep -n "PersonalitySummaryCard" src/components/students/personality-summary-card.tsx
    grep -n "getUnifiedPersonalityData" src/components/students/personality-summary-card.tsx
    grep -n "CheckCircle2\|Circle" src/components/students/personality-summary-card.tsx
    npx tsc --noEmit
  </verify>
  <done>
    PersonalitySummaryCard 컴포넌트가 정의되고 export 된다
    5개 분석 상태가 아이콘으로 표시된다
  </done>
</task>

<task type="auto">
  <name>Task 2: 상태별 UI 조건부 렌더링 구현</name>
  <files>src/components/students/personality-summary-card.tsx</files>
  <action>
    personality-summary-card.tsx에 조건부 렌더링 로직 추가:

    1. availableCount 계산:
       - sajuAvailable, nameAvailable, mbtiAvailable, faceAvailable, palmAvailable
       - true인 개수 집계

    2. 상태별 렌더링:
       a) summary?.coreTraits가 있으면:
          - 파란색 배경 박스 (bg-blue-50)
          - "핵심 성향" 라벨
          - coreTraits 내용 표시

       b) availableCount >= 3이고 요약 없으면:
          - "충분한 데이터가 모였어요. AI 분석을 시작할까요?" 메시지
          - "AI 통합 분석 생성" 버튼 (파란색 bg-blue-600)
          - 버튼 클릭 시 generateLearningStrategy 액션 호출

       c) availableCount < 3이면:
          - AlertCircle 아이콘 (회색)
          - "최소 3개 이상의 분석이 필요해요." 메시지

    기존 face-analysis-panel.tsx의 상태 처리 패턴 참조
  </action>
  <verify>
    grep -n "availableCount" src/components/students/personality-summary-card.tsx
    grep -n "coreTraits" src/components/students/personality-summary-card.tsx
    grep -n "AI 통합 분석 생성\|최소 3개" src/components/students/personality-summary-card.tsx
  </verify>
  <done>
    데이터 상태에 따른 UI가 올바르게 렌더링된다
    3개 이상 완료 시 생성 버튼이 표시된다
    요약 완료 시 핵심 성향이 표시된다
  </done>
</task>

<task type="auto">
  <name>Task 3: 클라이언트 버튼 컴포넌트 분리</name>
  <files>src/components/students/personality-summary-card.tsx</files>
  <action>
    personality-summary-card.tsx에 클라이언트 컴포넌트 추가:

    1. GenerateActionButton 클라이언트 컴포넌트:
       - "use client" 지시자
       - studentId prop
       - generateLearningAction 생성:
         - generateLearningStrategy(studentId) 호출
         - loading/error 상태 관리
         - 성공 시 페이지 새로고침 (router.refresh())
         - 에러 시 toast 표시 (sonner 사용)

    2. 메인 컴포넌트에서 분리:
       - Server Component는 데이터만 전달
       - GenerateActionButton은 조건부로 렌더링

    기존 컴포넌트의 server/client 분리 패턴 참조
  </action>
  <verify>
    grep -n "use client" src/components/students/personality-summary-card.tsx
    grep -n "GenerateActionButton" src/components/students/personality-summary-card.tsx
    grep -n "router.refresh\|sonner" src/components/students/personality-summary-card.tsx
    npx tsc --noEmit
  </verify>
  <done>
    클라이언트 버튼 컴포넌트가 정의된다
    버튼 클릭 시 Server Action이 호출된다
    완료 후 페이지가 갱신된다
  </done>
</task>

</tasks>

<verification>
1. npx tsc --noEmit으로 TypeScript 검증 통과
2. 컴포넌트가 Server/Client가 올바르게 분리되었다
3. 5개 분석 상태가 모두 표시된다
4. 상태별 조건부 렌더링이 동작한다
5. 버튼 클릭 시 Server Action이 호출된다
</verification>

<success_criteria>
1. 통합 성향 요약 카드가 학생 상세 페이지에 표시된다
2. 5개 분석(사주, 성명, MBTI, 관상, 손금) 완료 상태가 아이콘으로 표시된다
3. 최소 3개 완료 시 "AI 통합 분석 생성" 버튼이 표시된다
4. AI 생성 완료 후 핵심 성향 요약이 카드에 표시된다
5. 데이터 부족(3개 미만) 시 안내 메시지가 표시된다
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-integration---통합-성향-분석-및-맞춤형-학습/진로-전략-제공/06-03-SUMMARY.md`
</output>
