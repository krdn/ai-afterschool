---
phase: 06-ai-integration
plan: 05
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03, 06-04]
files_modified:
  - src/app/students/[id]/page.tsx
autonomous: false

must_haves:
  truths:
    - "학생 상세 페이지에 통합 성향 요약 카드가 표시된다"
    - "학생 상세 페이지에 학습 전략 패널이 표시된다"
    - "학생 상세 페이지에 진로 가이드 패널이 표시된다"
    - "모든 분석 완료 시 AI 제안이 자동으로 생성될 수 있다"
    - "페이지 레이아웃이 일관되고 사용자 친화적이다"
  artifacts:
    - path: "src/app/students/[id]/page.tsx"
      provides: "학생 상세 페이지 (수정됨)"
      contains: "PersonalitySummaryCard, LearningStrategyPanel, CareerGuidancePanel"
  key_links:
    - from: "src/app/students/[id]/page.tsx"
      to: "src/components/students/personality-summary-card.tsx"
      via: "컴포넌트 import 및 렌더링"
      pattern: "PersonalitySummaryCard.*studentId.*teacherId"
    - from: "src/app/students/[id]/page.tsx"
      to: "src/components/students/learning-strategy-panel.tsx"
      via: "컴포넌트 import 및 렌더링"
      pattern: "LearningStrategyPanel.*studentId.*teacherId"
    - from: "src/app/students/[id]/page.tsx"
      to: "src/components/students/career-guidance-panel.tsx"
      via: "컴포넌트 import 및 렌더링"
      pattern: "CareerGuidancePanel.*studentId.*teacherId"
---

<objective>
학생 상세 페이지에 AI 통합 분석 컴포넌트 통합

Purpose: 요약 카드, 학습 전략, 진로 가이드 패널을 학생 상세 페이지에 통합하여 전체 흐름 완성
Output: 업데이트된 학생 상세 페이지, 전체 기능 검증
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai-integration---통합-성향-분석-및-맞춤형-학습/진로-전략-제공/06-RESEARCH.md
@.planning/phases/06-ai-integration---통합-성향-분석-및-맞춤형-학습/진로-전략-제공/06-01-SUMMARY.md
@.planning/phases/06-ai-integration---통합-성향-분석-및-맞춤형-학습/진로-전략-제공/06-02-SUMMARY.md
@.planning/phases/06-ai-integration---통합-성향-분석-및-맞춤형-학습/진로-전략-제공/06-03-SUMMARY.md
@.planning/phases/06-ai-integration---통합-성향-분석-및-맞춤형-학습/진로-전략-제공/06-04-SUMMARY.md
@/mnt/data/projects/ai/ai-afterschool/src/app/students/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 통합 성향 섹션 추가</name>
  <files>src/app/students/[id]/page.tsx</files>
  <action>
    src/app/students/[id]/page.tsx에 통합 성향 섹션 추가:

    1. 컴포넌트 import:
       - PersonalitySummaryCard 추가
       - LearningStrategyPanel 추가
       - CareerGuidancePanel 추가

    2. 섹션 추가:
       - 기존 분석 패널(사주, 성명, MBTI, 관상, 손금) 다음에
       - "통합 성향 분석" 섹션 추가
       - 섹션 내부에 PersonalitySummaryCard 배치

    3. 레이아웃 구조:
       - Section 태그 또는 div로 구분
       - 섹션 헤더 (h2 또는 similar)
       - 그리드 또는 플렉스 레이아웃으로 여러 패널 배치

    기존 페이지의 섹션 구조 패턴 따르기
  </action>
  <verify>
    grep -n "PersonalitySummaryCard" src/app/students/[id]/page.tsx
    grep -n "통합 성향 분석\|AI 통합" src/app/students/[id]/page.tsx
    npx tsc --noEmit
  </verify>
  <done>
    학생 상세 페이지에 통합 성향 요약 카드가 표시된다
    섹션 구조가 기존 패널과 일관된다
  </done>
</task>

<task type="auto">
  <name>Task 2: 학습 전략 및 진로 가이드 섹션 추가</name>
  <files>src/app/students/[id]/page.tsx</files>
  <action>
    src/app/students/[id]/page.tsx에 AI 제안 섹션 추가:

    1. "AI 맞춤형 제안" 섹션 추가:
       - 통합 성향 섹션 다음에 배치
       - 섹션 헤더

    2. 그리드 레이아웃 (2열):
       - 왼쪽: LearningStrategyPanel
       - 오른쪽: CareerGuidancePanel
       - 반응형 (모바일에서는 1열)

    3. Tailwind 클래스:
       - grid grid-cols-1 lg:grid-cols-2 gap-6

    기존 페이지의 그리드 레이아웃 패턴 참조
  </action>
  <verify>
    grep -n "LearningStrategyPanel" src/app/students/[id]/page.tsx
    grep -n "CareerGuidancePanel" src/app/students/[id]/page.tsx
    grep -n "grid-cols-1 lg:grid-cols-2" src/app/students/[id]/page.tsx
    npx tsc --noEmit
  </verify>
  <done>
    학습 전략과 진로 가이드 패널이 2열 그리드로 표시된다
    반응형 레이아웃이 올바르게 동작한다
  </done>
</task>

<task type="auto">
  <name>Task 3: teacherId prop 전달 확인</name>
  <files>src/app/students/[id]/page.tsx</files>
  <action>
    src/app/students/[id]/page.tsx에서 teacherId 전달 확인:

    1. 페이지 컴포넌트의 teacherId 접근:
       - session.userId 사용 (기존 패턴)
       - 또는 student.teacherId 사용

    2. 모든 새 컴포넌트에 props 전달:
       - PersonalitySummaryCard studentId={student.id} teacherId={session.userId}
       - LearningStrategyPanel studentId={student.id} teacherId={session.userId}
       - CareerGuidancePanel studentId={student.id} teacherId={session.userId}

    3. 일관성 확인:
       - 기존 패널들도 동일한 패턴 사용
       - prop 순서 일관성 (studentId, teacherId)

    기존 페이지의 prop 전달 패턴 확인
  </action>
  <verify>
    grep -n "studentId.*teacherId" src/app/students/[id]/page.tsx | grep "PersonalitySummaryCard\|LearningStrategyPanel\|CareerGuidancePanel"
    npx tsc --noEmit
  </verify>
  <done>
    모든 새 컴포넌트에 studentId와 teacherId가 전달된다
    prop 순서가 일관된다
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    학생 상세 페이지에 통합 성향 분석 기능 통합 완료:
    - PersonalitySummaryCard: 5개 분석 완료 상태 표시, AI 생성 진입점
    - LearningStrategyPanel: 학습 스타일, 과목별 전략, 효율화 팁 표시
    - CareerGuidancePanel: 적합 학과, 진로 경로, 개발 제안 표시
    - 모든 컴포넌트가 학생 상세 페이지에 통합됨
  </what-built>
  <how-to-verify>
    1. 학생 상세 페이지 접속:
       - 개발 서버 실행: npm run dev
       - 브라우저에서 http://localhost:3000/students/[학생ID] 접속
       - 또는 학생 목록에서 특정 학생 클릭

    2. 통합 성향 요약 카드 확인:
       - 페이지 하단에 "통합 성향 분석" 섹션 확인
       - 5개 분석(사주, 성명, MBTI, 관상, 손금) 상태 아이콘 확인
       - 완료된 분석은 초록색 체크, 미완료는 회색 원
       - 완료 카운트 표시 (예: "3/5 완료")

    3. AI 통합 분석 생성 (최소 3개 완료 시):
       - "AI 통합 분석 생성" 버튼 클릭
       - "생성 중" 메시지 확인
       - 10-20초 대기 후 페이지 새로고침
       - 핵심 성향 요약이 카드에 표시되는지 확인

    4. 학습 전략 패널 확인:
       - "AI 맞춤형 제안" 섹션 확인
       - 왼쪽 "맞춤형 학습 전략" 패널 확인
       - 학습 스타일, 과목별 전략(국어, 수학, 영어, 과학, 사회), 효율화 팁 표시 확인

    5. 진로 가이드 패널 확인:
       - 오른쪽 "AI 진로 가이드" 패널 확인
       - 적합 학과, 진로 경로, 개발 제안 표시 확인

    6. 반응형 레이아웃 확인:
       - 브라우저 폭 조절 (모바일/태블릿/데스크톱)
       - 2열 레이아웃이 모바일에서 1열로 변경되는지 확인

    7. 에러 처리 확인:
       - 3개 미만 분석 완료 시 안내 메시지 확인
       - AI 생성 실패 시 에러 메시지 및 재시도 버튼 확인

    Expected results:
    - 모든 섹션이 순서대로 표시됨
    - 아이콘과 텍스트가 명확하게 구분됨
    - AI 생성 후 결과가 올바르게 표시됨
    - 반응형 레이아웃이 정상 동작함
    - 에러 상태가 적절히 처리됨
  </how-to-verify>
  <resume-signal>
    검증 완료 후 "approved" 또는 문제점 설명
  </resume-signal>
</task>

</tasks>

<verification>
1. npx tsc --noEmit으로 TypeScript 검증 통과
2. npm run build로 빌드 성공
3. 모든 컴포넌트가 올바르게 import 되었다
4. props가 올바르게 전달되었다
5. 레이아웃이 기존 페이지와 일관된다
6. 사용자 검증으로 전체 흐름 확인
</verification>

<success_criteria>
1. 학생 상세 페이지에 3개 섹션이 추가된다 (요약, 학습 전략, 진로 가이드)
2. 5개 분석 완료 상태가 시각화된다
3. AI 생성 버튼이 동작하고 결과가 표시된다
4. 학습 전략과 진로 가이드가 구조화되어 표시된다
5. 반응형 레이아웃이 모든 화면 크기에서 동작한다
6. 사용자가 전체 흐름을 문제엽이 사용할 수 있다
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-integration---통합-성향-분석-및-맞춤형-학습/진로-전략-제공/06-05-SUMMARY.md`
</output>
