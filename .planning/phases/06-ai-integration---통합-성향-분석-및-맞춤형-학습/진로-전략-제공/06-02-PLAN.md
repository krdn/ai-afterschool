---
phase: 06-ai-integration
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/lib/ai/integration-prompts.ts
  - src/lib/actions/personality-integration.ts
  - src/lib/validations/personality.ts
autonomous: true

must_haves:
  truths:
    - "buildLearningStrategyPrompt()가 학생 데이터를 기반으로 학습 전략 생성 프롬프트를 반환한다"
    - "buildCareerGuidancePrompt()가 학생 데이터를 기반으로 진로 가이드 생성 프롬프트를 반환한다"
    - "generateLearningStrategy() Server Action이 AI를 호출하여 학습 전략을 생성한다"
    - "generateCareerGuidance() Server Action이 AI를 호출하여 진로 가이드를 생성한다"
    - "AI 응답은 Zod 스키마로 검증된 후 저장된다"
  artifacts:
    - path: "src/lib/ai/integration-prompts.ts"
      provides: "통합 분석 프롬프트 생성 함수"
      exports: ["buildLearningStrategyPrompt", "buildCareerGuidancePrompt"]
    - path: "src/lib/actions/personality-integration.ts"
      provides: "AI 생성 Server Actions"
      exports: ["generateLearningStrategy", "generateCareerGuidance"]
    - path: "src/lib/validations/personality.ts"
      provides: "AI 응답 검증 스키마"
      exports: ["LearningStrategySchema", "CareerGuidanceSchema"]
  key_links:
    - from: "src/lib/actions/personality-integration.ts"
      to: "src/lib/ai/claude.ts"
      via: "anthropic.messages.create 호출"
      pattern: "anthropic\\.messages\\.create.*model.*claude-3-5-sonnet"
    - from: "src/lib/actions/personality-integration.ts"
      to: "src/lib/db/personality-summary.ts"
      via: "upsertPersonalitySummary 호출"
      pattern: "upsertPersonalitySummary.*learningStrategy|careerGuidance"
    - from: "src/lib/actions/personality-integration.ts"
      to: "src/lib/validations/personality.ts"
      via: "Zod 스키마 검증"
      pattern: "LearningStrategySchema\\.parse|CareerGuidanceSchema\\.parse"
---

<objective>
AI 기반 학습 전략 및 진로 가이드 생성 프롬프트와 Server Actions 구현

Purpose: 통합 성향 데이터를 Claude AI에 전달하여 맞춤형 학습 전략과 진로 가이드 생성
Output: 프롬프트 템플릿, Zod 검증 스키마, 비동기 AI 생성 Server Actions
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai-integration---통합-성향-분석-및-맞춤형-학습/진로-전략-제공/06-RESEARCH.md
@.planning/phases/06-ai-integration---통합-성향-분석-및-맞춤형-학습/진로-전략-제공/06-01-SUMMARY.md
@/mnt/data/projects/ai/ai-afterschool/src/lib/ai/prompts.ts
@/mnt/data/projects/ai/ai-afterschool/src/lib/actions/ai-image-analysis.ts
@/mnt/data/projects/ai/ai-afterschool/src/lib/db/personality-summary.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zod 검증 스키마 생성</name>
  <files>src/lib/validations/personality.ts</files>
  <action>
    src/lib/validations/personality.ts 파일 생성:

    1. LearningStrategySchema Zod 스키마:
       - coreTraits: string().min(50).max(500)
       - learningStyle: object({
           type: z.enum(['시각', '청각', '운동', '혼합']),
           description: string(),
           focusMethod: string()
         })
       - subjectStrategies: object({
           korean: string(),
           math: string(),
           english: string(),
           science: string(),
           social: string()
         })
       - efficiencyTips: array(z.string()).min(3).max(7)
       - motivationApproach: string()

    2. CareerGuidanceSchema Zod 스키마:
       - coreTraits: string().min(50).max(500)
       - suitableMajors: array(object({
           name: string(),
           reason: string(),
           matchScore: z.number().min(1).max(100)
         })).min(3).max(10)
       - careerPaths: array(object({
           field: string(),
           roles: array(z.string()),
           reasoning: string()
         })).min(3).max(7)
       - developmentSuggestions: array(z.string()).min(3).max(7)

    zod가 이미 설치되어 있음 (package.json 확인)
  </action>
  <verify>
    grep -n "LearningStrategySchema" src/lib/validations/personality.ts
    grep -n "CareerGuidanceSchema" src/lib/validations/personality.ts
    npx tsc --noEmit
  </verify>
  <done>
    LearningStrategySchema와 CareerGuidanceSchema가 정의되고 export 된다
    모든 필수 필드와 제약조건이 포함된다
  </done>
</task>

<task type="auto">
  <name>Task 2: 통합 분석 프롬프트 생성 함수 구현</name>
  <files>src/lib/ai/integration-prompts.ts</files>
  <action>
    src/lib/ai/integration-prompts.ts 파일 생성:

    1. buildLearningStrategyPrompt() 함수 (RESEARCH.md Pattern 2 참조):
       - UnifiedPersonalityData와 학생 기본 정보 파라미터
       - 사용 가능한 분석 타입 동적 감지
       - 각 분석의 해석/결과를 포맷팅
       - 한국어 프롬프트로 학습 전략 요청
       - JSON 출력 형식 명시 (LearningStrategySchema와 일치)
       - 긍정적/격려 톤 유지 지침
       - 부족한 데이터는 무시하고 사용 가능한 데이터만 활용 지침

    2. buildCareerGuidancePrompt() 함수:
       - 유사한 구조지만 진로/학과 추천에 초점
       - 학생의 targetMajor/목표 대학 고려
       - 적성 학과 및 진로 경로 추천
       - JSON 출력 형식 명시 (CareerGuidanceSchema와 일치)

    기존 prompts.ts의 FACE_READING_PROMPT, PALM_READING_PROMPT 스타일 참조
  </action>
  <verify>
    grep -n "buildLearningStrategyPrompt" src/lib/ai/integration-prompts.ts
    grep -n "buildCareerGuidancePrompt" src/lib/ai/integration-prompts.ts
    grep -n "출력 형식.*JSON" src/lib/ai/integration-prompts.ts
  </verify>
  <done>
    두 프롬프트 함수가 정의되고 export 된다
    프롬프트에 JSON 출력 형식이 명시된다
    부분 데이터 처리 지침이 포함된다
  </done>
</task>

<task type="auto">
  <name>Task 3: AI 생성 Server Actions 구현</name>
  <files>src/lib/actions/personality-integration.ts</files>
  <action>
    src/lib/actions/personality-integration.ts 파일 생성:

    1. generateLearningStrategy() Server Action:
       - "use server" 지시자
       - studentId 파라미터
       - verifySession()으로 선생님 인증
       - 학생 소속 검증 (teacherId 일치)
       - getUnifiedPersonalityData()로 분석 데이터 조회
       - 최소 3개 분석 있는지 확인 (아니면 에러)
       - 기존 'pending' 상태 확인 (중복 생성 방지)
       - upsertPersonalitySummary()로 status='pending' 저장
       - after()로 비동기 AI 호출:
         - buildLearningStrategyPrompt()로 프롬프트 생성
         - anthropic.messages.create()로 Claude API 호출
         - 응답 텍스트 JSON.parse()
         - LearningStrategySchema.parse()로 검증
         - upsertPersonalitySummary()로 결과 저장 (status='complete')
         - revalidatePath()로 페이지 갱신
       - 에러 시 status='failed', errorMessage 저장
       - 반환: { success, message }

    2. generateCareerGuidance() Server Action:
       - 유사한 구조 but buildCareerGuidancePrompt() 사용
       - CareerGuidanceSchema로 검증
       - careerGuidance 필드에 저장

    기존 ai-image-analysis.ts의 analyzeFaceImage 패턴 참조
  </action>
  <verify>
    grep -n "generateLearningStrategy" src/lib/actions/personality-integration.ts
    grep -n "generateCareerGuidance" src/lib/actions/personality-integration.ts
    grep -n "after(" src/lib/actions/personality-integration.ts
    grep -n "LearningStrategySchema.parse" src/lib/actions/personality-integration.ts
    grep -n "revalidatePath" src/lib/actions/personality-integration.ts
    npx tsc --noEmit
  </verify>
  <done>
    두 Server Action이 정의되고 export 된다
    after()로 비동기 처리가 구현된다
    Zod 검증으로 응답이 검증된다
    pending 상태 체크로 중복 생성이 방지된다
  </done>
</task>

</tasks>

<verification>
1. npx tsc --noEmit으로 TypeScript 검증 통과
2. Zod 스키마가 올바른 제약조건을 가진다
3. 프롬프트 함수가 올바른 파라미터를 받는다
4. Server Actions가 "use server" 지시자를 가진다
5. after() 내에서 anthropic API가 호출된다
6. 에러 처리가 구현되어 있다 (status='failed')
</verification>

<success_criteria>
1. buildLearningStrategyPrompt()와 buildCareerGuidancePrompt()가 프롬프트를 생성한다
2. generateLearningStrategy()와 generateCareerGuidance()가 AI를 비동기로 호출한다
3. AI 응답이 Zod 스키마로 검증된 후 저장된다
4. 중복 생성이 방지된다 (pending 체크)
5. 에러 발생 시 failed 상태와 에러 메시지가 저장된다
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-integration---통합-성향-분석-및-맞춤형-학습/진로-전략-제공/06-02-SUMMARY.md`
</output>
