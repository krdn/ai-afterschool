---
phase: 06-ai-integration
plan: 04
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/students/learning-strategy-panel.tsx
  - src/components/students/career-guidance-panel.tsx
autonomous: true

must_haves:
  truths:
    - "학습 전략 패널이 AI 생성 결과를 표시한다"
    - "진로 가이드 패널이 AI 생성 결과를 표시한다"
    - "학습 스타일, 과목별 접근법, 효율화 팁이 구조화되어 표시된다"
    - "적합 학과, 진로 경로, 개발 제안이 구조화되어 표시된다"
    - "loading/empty/error 상태가 올바르게 처리된다"
  artifacts:
    - path: "src/components/students/learning-strategy-panel.tsx"
      provides: "학습 전략 표시 패널"
      exports: ["LearningStrategyPanel"]
    - path: "src/components/students/career-guidance-panel.tsx"
      provides: "진로 가이드 표시 패널"
      exports: ["CareerGuidancePanel"]
  key_links:
    - from: "src/components/students/learning-strategy-panel.tsx"
      to: "src/lib/db/personality-summary.ts"
      via: "getPersonalitySummary 호출"
      pattern: "getPersonalitySummary.*await"
    - from: "src/components/students/career-guidance-panel.tsx"
      to: "src/lib/actions/personality-integration.ts"
      via: "generateCareerGuidance 액션 호출"
      pattern: "generateCareerGuidance.*server action"
    - from: "src/components/students/learning-strategy-panel.tsx"
      to: "src/lib/db/personality-summary.ts"
      via: "learningStrategy JSON 타입 단언"
      pattern: "as LearningStrategyResult"
---

<objective>
학습 전략 및 진로 가이드 표시 패널 구현

Purpose: AI 생성된 맞춤형 학습 전략과 진로 가이드를 구조화하여 표시
Output: 학습 스타일/과목별 전략 패널, 적성 학과/진로 경로 패널
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai-integration---통합-성향-분석-및-맞춤형-학습/진로-전략-제공/06-RESEARCH.md
@.planning/phases/06-ai-integration---통합-성향-분석-및-맞춤형-학습/진로-전략-제공/06-01-SUMMARY.md
@.planning/phases/06-ai-integration---통합-성향-분석-및-맞춤형-학습/진로-전략-제공/06-02-SUMMARY.md
@/mnt/data/projects/ai/ai-afterschool/src/components/students/face-analysis-panel.tsx
@/mnt/data/projects/ai/ai-afterschool/src/components/students/mbti-analysis-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 학습 전략 패널 구조 구현</name>
  <files>src/components/students/learning-strategy-panel.tsx</files>
  <action>
    src/components/students/learning-strategy-panel.tsx 파일 생성:

    1. 컴포넌트 Props (Server Component):
       - studentId: string
       - teacherId: string

    2. 데이터 로딩:
       - getPersonalitySummary(studentId) 호출
       - learningStrategy 추출 및 타입 단언

    3. LearningStrategyResult 타입 정의 (컴포넌트 내부):
       - coreTraits: string
       - learningStyle: { type, description, focusMethod }
       - subjectStrategies: { korean, math, english, science, social }
       - efficiencyTips: string[]
       - motivationApproach: string

    4. UI 구조 (Card 기반):
       - 헤더: "맞춤형 학습 전략" + 생성일시
       - 상태 처리: pending/complete/failed/empty
       - complete 시 각 섹션 구조화 표시

    5. 섹션별 레이아웃:
       - 핵심 성향: 파란색 박스 (bg-blue-50)
       - 학습 스타일: 배지 + 설명
       - 과목별 전략: 5개 과목 카드 그리드
       - 효율화 팁: 리스트 아이템
       - 동기 부여: 구분된 섹션

    기존 face-analysis-panel.tsx의 상태 처리 패턴 참조
  </action>
  <verify>
    grep -n "LearningStrategyPanel" src/components/students/learning-strategy-panel.tsx
    grep -n "LearningStrategyResult" src/components/students/learning-strategy-panel.tsx
    grep -n "학습 스타일\|과목별 전략" src/components/students/learning-strategy-panel.tsx
    npx tsc --noEmit
  </verify>
  <done>
    LearningStrategyPanel 컴포넌트가 정의된다
    learningStrategy 데이터가 구조화되어 표시된다
  </done>
</task>

<task type="auto">
  <name>Task 2: 진로 가이드 패널 구조 구현</name>
  <files>src/components/students/career-guidance-panel.tsx</files>
  <action>
    src/components/students/career-guidance-panel.tsx 파일 생성:

    1. 컴포넌트 Props (Server Component):
       - studentId: string
       - teacherId: string

    2. 데이터 로딩:
       - getPersonalitySummary(studentId) 호출
       - careerGuidance 추출 및 타입 단언

    3. CareerGuidanceResult 타입 정의:
       - coreTraits: string
       - suitableMajors: { name, reason, matchScore }[]
       - careerPaths: { field, roles[], reasoning }[]
       - developmentSuggestions: string[]

    4. UI 구조:
       - 헤더: "AI 진로 가이드" + 생성일시
       - 상태 처리
       - 적합 학과: 카드 리스트 (matchScore 배지)
       - 진로 경로: 분야별 역할 및 이유
       - 개발 제안: 리스트

    5. 클라이언트 컴포넌트 (생성 버튼):
       - "use client"
       - generateCareerGuidance 액션 호출
       - 완료 후 router.refresh()

    기존 mbti-analysis-panel.tsx의 결과 표시 패턴 참조
  </action>
  <verify>
    grep -n "CareerGuidancePanel" src/components/students/career-guidance-panel.tsx
    grep -n "CareerGuidanceResult" src/components/students/career-guidance-panel.tsx
    grep -n "적합 학과\|진로 경로" src/components/students/career-guidance-panel.tsx
    npx tsc --noEmit
  </verify>
  <done>
    CareerGuidancePanel 컴포넌트가 정의된다
    careerGuidance 데이터가 구조화되어 표시된다
  </done>
</task>

<task type="auto">
  <name>Task 3: 상태 처리 및 에러 UI 구현</name>
  <files>src/components/students/learning-strategy-panel.tsx, src/components/students/career-guidance-panel.tsx</files>
  <action>
    두 패널에 상태별 UI 구현:

    1. 상태별 조건부 렌더링:
       a) status === 'complete' && result 있으면:
          - 구조화된 데이터 표시

       b) status === 'pending'이면:
          - 로딩 스피너/메시지
          - "AI가 분석 중이에요... (10-20초 소요)"

       c) status === 'failed'이면:
          - 에러 메시지 표시
          - errorMessage 내용
          - "재시도" 버튼

       d) 데이터 없으면:
          - empty state (안내 메시지)
          - personality-summary-card에서 생성 유도

    2. 공통 UI 패턴:
       - 라딩 애니메이션 (기존 Loader 컴포넌트 활용)
       - 에러 아이콘 (AlertCircle)
       - 버튼 스타일 일관성

    기존 face-analysis-panel.tsx의 status 처리 참조
  </action>
  <verify>
    grep -n "status.*pending\|status.*complete\|status.*failed" src/components/students/learning-strategy-panel.tsx
    grep -n "status.*pending\|status.*complete\|status.*failed" src/components/students/career-guidance-panel.tsx
    grep -n "재시도\|AI가 분석 중" src/components/students/learning-strategy-panel.tsx
  </verify>
  <done>
    모든 상태(pending/complete/failed/empty)에 대한 UI가 구현된다
    에러 발생 시 재시도 버튼이 표시된다
    로딩 상태가 명확하게 표시된다
  </done>
</task>

</tasks>

<verification>
1. npx tsc --noEmit으로 TypeScript 검증 통과
2. 두 패널 모두 데이터를 올바르게 렌더링한다
3. 상태별 UI가 올바르게 표시된다
4. 타입 단언이 컴포넌트 레벨에서 이루어진다
5. 클라이언트 컴포넌트가 액션을 올바르게 호출한다
</verification>

<success_criteria>
1. 학습 전략 패널이 학습 스타일, 과목별 전략, 효율화 팁을 표시한다
2. 진로 가이드 패널이 적합 학과, 진로 경로, 개발 제안을 표시한다
3. pending/complete/failed/empty 상태가 올바르게 처리된다
4. 에러 발생 시 재시도 기능이 제공된다
5. AI 생성 완료 후 결과가 즉시 표시된다
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-integration---통합-성향-분석-및-맞춤형-학습/진로-전략-제공/06-04-SUMMARY.md`
</output>
