---
phase: 03-calculation-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/actions/calculation-analysis.ts
  - src/lib/actions/students.ts
  - src/lib/db/student-analysis.ts
  - src/components/students/student-analysis-status.tsx
autonomous: true
must_haves:
  truths:
    - "Student has persisted calculation analysis records for 사주 and 성명학 with latest-only storage."
    - "Student profile shows analysis status badges (latest vs recalculation needed)."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Tables and relations for calculation analysis storage"
    - path: "src/lib/actions/calculation-analysis.ts"
      provides: "Server actions to read/write analysis results"
    - path: "src/components/students/student-analysis-status.tsx"
      provides: "UI badge component for analysis freshness"
  key_links:
    - from: "src/lib/actions/calculation-analysis.ts"
      to: "prisma/schema.prisma"
      via: "Prisma client models"
      pattern: "SajuAnalysis|NameAnalysis|CalculationAnalysis"
    - from: "src/components/students/student-analysis-status.tsx"
      to: "src/lib/actions/calculation-analysis.ts"
      via: "status query"
      pattern: "getCalculationStatus"
---

<objective>
Establish storage and status tracking for Phase 3 calculation analysis results so later plans can compute and render outputs.

Purpose: Create durable DB models and actions for saju/naming analysis and surface freshness badges on student profiles.
Output: Prisma schema updates, analysis actions, and UI status component.
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-calculation-analysis/03-CONTEXT.md
@.planning/phases/03-calculation-analysis/03-RESEARCH.md
@prisma/schema.prisma
@src/lib/actions/students.ts
@src/app/(dashboard)/students/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add calculation analysis models to Prisma schema</name>
  <files>
    prisma/schema.prisma
  </files>
  <action>
    Extend the schema to store one latest analysis per student:
    - Add models for `SajuAnalysis` and `NameAnalysis` (or a single `CalculationAnalysis` with JSON fields) linked to Student with unique constraint.
    - Store input snapshots (birth date/time, timeKnown, selectedHanja) and output JSON + interpretation text.
    - Include `status`/`version`, `calculatedAt`, and `updatedAt` fields.
    Run Prisma migration in the normal workflow for the repo.
  </action>
  <verify>`npm run lint`</verify>
  <done>Prisma schema compiles with new analysis models and constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Add server actions for analysis persistence and status</name>
  <files>
    src/lib/actions/calculation-analysis.ts
    src/lib/db/student-analysis.ts
    src/lib/actions/students.ts
  </files>
  <action>
    Create a dedicated action module to:
    - `getCalculationStatus(studentId)` returning latest analysis timestamps and recalc-needed flag.
    - `saveSajuAnalysis(studentId, input, result)` and `saveNameAnalysis(...)` that upsert latest records.
    - `markRecalculationNeeded(studentId, reason)` when birth/name data changes.
    Wire into student update flows to flag recalculation when relevant fields change.
  </action>
  <verify>`npm run lint`</verify>
  <done>Analysis records can be saved and status can be queried from UI.</done>
</task>

<task type="auto">
  <name>Task 3: Add analysis status badge component and detail wiring</name>
  <files>
    src/components/students/student-analysis-status.tsx
    src/app/(dashboard)/students/[id]/page.tsx
  </files>
  <action>
    Build a small UI component that shows two badges:
    - "최신 분석" when calculatedAt exists and no recalc-needed flag.
    - "재계산 필요" when data changed after last analysis.
    Render it on the student detail page near the analysis section header.
  </action>
  <verify>`npm run lint`</verify>
  <done>Student detail page shows analysis freshness badges driven by persisted status.</done>
</task>

</tasks>

<verification>
`npm run lint` passes.
</verification>

<success_criteria>
Student profiles have persisted analysis data models and visible status badges that reflect freshness or required recalculation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-calculation-analysis/03-01-SUMMARY.md`
</output>
