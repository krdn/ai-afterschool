---
phase: 03-calculation-analysis
plan: 04
type: execute
wave: 1
depends_on:
  - "03-02"
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/<timestamp>_add_student_birth_time/migration.sql
  - src/lib/validations/students.ts
  - src/lib/actions/students.ts
  - src/components/students/student-form.tsx
  - src/lib/actions/calculation-analysis.ts
  - src/components/students/saju-analysis-panel.tsx
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "학생의 생년월일시 입력값으로 사주팔자(시주 포함)가 계산된다."
    - "출생 시간이 없으면 시주는 비워지고 화면에 미상으로 표시된다."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Student 출생 시간(시/분) 저장 필드"
    - path: "src/components/students/student-form.tsx"
      provides: "출생 시간 입력 UI (선택)"
    - path: "src/lib/actions/calculation-analysis.ts"
      provides: "runSajuAnalysis 시간 전달 및 timeKnown 처리"
    - path: "src/components/students/saju-analysis-panel.tsx"
      provides: "출생 시간 표시 및 시주 계산 표기"
  key_links:
    - from: "src/components/students/student-form.tsx"
      to: "src/lib/actions/students.ts"
      via: "birthTimeHour/birthTimeMinute form fields"
      pattern: "birthTimeHour|birthTimeMinute"
    - from: "src/lib/actions/calculation-analysis.ts"
      to: "src/lib/analysis/saju.ts"
      via: "calculateSaju time param"
      pattern: "calculateSaju\(\{[\s\S]*time"
    - from: "src/components/students/saju-analysis-panel.tsx"
      to: "src/lib/actions/calculation-analysis.ts"
      via: "runSajuAnalysisAction"
      pattern: "runSajuAnalysisAction"
---

<objective>
Close the Phase 3 verification gap by capturing birth time, persisting it, and using it to compute hour pillars (시주) in saju analysis.

Purpose: Satisfy CALC-01 truth for birth time-aware saju calculations.
Output: Student birth time fields, input UI, and saju execution wired with timeKnown handling.
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-calculation-analysis/03-calculation-analysis-VERIFICATION.md
@.planning/phases/03-calculation-analysis/03-CONTEXT.md
@src/lib/analysis/saju.ts
@prisma/schema.prisma
@src/lib/actions/students.ts
@src/lib/validations/students.ts
@src/components/students/student-form.tsx
@src/lib/actions/calculation-analysis.ts
@src/components/students/saju-analysis-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist optional birth time on students</name>
  <files>
    prisma/schema.prisma
    prisma/migrations/<timestamp>_add_student_birth_time/migration.sql
    src/lib/validations/students.ts
    src/lib/actions/students.ts
  </files>
  <action>
    Add optional birth time fields to Student:
    - `birthTimeHour Int?` and `birthTimeMinute Int?` (nullable, default null).
    - Create a migration named `add_student_birth_time`.

    Update validation and actions:
    - Extend `CreateStudentSchema`/`UpdateStudentSchema` with `birthTimeHour` (0-23) and `birthTimeMinute` (0-59) using `z.coerce.number()` and preprocessing to treat empty strings as undefined.
    - Reject minute-only input (minute provided when hour missing) with a clear error.
    - In `createStudent`/`updateStudent`, parse form fields, store the values, and default minute to 0 when hour is present but minute is missing.
    - Add time-change detection in `updateStudent` and mark recalculation needed when birth time changes (use a distinct reason like "학생 출생 시간 변경").
  </action>
  <verify>`npm run lint`</verify>
  <done>학생 기록에 출생 시간(시/분)이 선택적으로 저장되고, 시간 변경 시 재계산 필요 상태가 표시된다.</done>
</task>

<task type="auto">
  <name>Task 2: Wire birth time into saju analysis and UI</name>
  <files>
    src/components/students/student-form.tsx
    src/lib/actions/calculation-analysis.ts
    src/components/students/saju-analysis-panel.tsx
  </files>
  <action>
    Add birth time inputs to StudentForm:
    - Place a "출생 시간 (선택)" section near the birth date field.
    - Use two number inputs (hour/minute) consistent with existing Input/Label patterns.
    - Bind to `birthTimeHour`/`birthTimeMinute`, show validation errors, and set defaults from existing student values.
    - Add helper text: leave blank if unknown.

    Update saju execution and display:
    - In `runSajuAnalysis`, fetch `birthTimeHour`/`birthTimeMinute` and build `time` only when hour is present.
    - Store `inputSnapshot` with `timeKnown` and `time` values, and pass `time` to `calculateSaju`.
    - In `SajuAnalysisPanel`, show the actual birth time when available (HH:mm) and keep "미상" when missing.
  </action>
  <verify>`npm run test -- saju`</verify>
  <done>출생 시간을 입력한 학생은 시주가 포함된 사주가 계산되며, 시간 미입력 시 시주가 비어 있고 UI에 미상으로 표시된다.</done>
</task>

</tasks>

<verification>
`npm run lint`
`npm run test -- saju`
</verification>

<success_criteria>
학생의 출생 시간 입력 여부에 따라 시주 포함/미포함 사주 계산이 정확히 동작하고 저장된다.
</success_criteria>

<output>
After completion, create `.planning/phases/03-calculation-analysis/03-04-SUMMARY.md`
</output>
