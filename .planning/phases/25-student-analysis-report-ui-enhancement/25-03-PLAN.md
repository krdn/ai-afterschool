---
wave: 2
depends_on: [25-02]
files_modified:
  - src/lib/actions/analysis.ts
  - src/components/students/tabs/analysis-tab.tsx
  - prisma/schema.prisma
autonomous: true
---

# Plan 25-03: 분석 이력 조회 UI

## Overview

이전 분석 결과 목록과 상세 보기 모달을 추가합니다.

## Tasks

### Task 1: Prisma 스키마에 이력 필드 확인

**파일:** `prisma/schema.prisma`

1. 기존 분석 모델에 이력 필드가 있는지 확인
2. 없으면 간단한 해결책: 기존 `calculatedAt` 필드와 재계산으로 이력 조회
3. 또는 별도 이력 테이블 생성 (필요시)

### Task 2: 이력 조회 Server Action

**파일:** `src/lib/actions/analysis.ts` (또는 새 파일)

1. `getAnalysisHistory` 액션 생성:
   ```tsx
   "use server"

   export async function getAnalysisHistory(studentId: string, type: 'saju' | 'face' | 'palm' | 'mbti') {
     // 최근 5개 이력 반환
     // calculatedAt 기준 내림차순 정렬
   }
   ```

### Task 3: 이력 목록 UI

**파일:** `src/components/students/tabs/analysis-tab.tsx`

1. 각 서브탭에 이력 버튼 추가:
   ```tsx
   <div className="flex items-center justify-between">
     <h2>{tabTitle} 분석</h2>
     <Button variant="ghost" size="sm" onClick={() => setShowHistory(true)}>
       <History className="w-4 h-4" />
       이력 보기
     </Button>
   </div>
   ```

2. Dialog로 이력 목록 표시:
   ```tsx
   <Dialog open={showHistory} onOpenChange={setShowHistory}>
     <DialogContent>
       <DialogHeader>
         <DialogTitle>분석 이력</DialogTitle>
       </DialogHeader>
       <div className="space-y-2">
         {history.map((h) => (
           <div key={h.id} className="p-3 border rounded hover:bg-gray-50 cursor-pointer">
             <p className="text-sm font-medium">{formatDate(h.calculatedAt)}</p>
             <p className="text-xs text-gray-500">{h.summary}</p>
           </div>
         ))}
       </div>
     </DialogContent>
   </Dialog>
   ```

### Task 4: 이력 상세 보기

1. 이력 항목 클릭 시 상세 보기 모달 표시
2. 해당 시점의 분석 결과 표시

## Verification

- [ ] 이력 버튼 클릭 시 이력 목록 모달 표시
- [ ] 최근 5개 이력 표시
- [ ] 이력 항목 클릭 시 상세 보기 표시

## Notes

- 범위 제한: 최근 5개 (성능 고려)
- 복잡한 이력 테이블 대신 기존 필드 활용 우선
- 데이터베이스 마이그레이션이 필요한 경우 간단한 구조 선택
