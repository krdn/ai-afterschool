---
phase: 11-teacher-infrastructure
plan: 05
type: execute
wave: 3
depends_on: ["11-04"]
files_modified:
  - src/app/(dashboard)/teachers/page.tsx
  - src/components/teachers/teacher-table.tsx
  - src/components/teachers/columns.tsx
autonomous: true

must_haves:
  truths:
    - "선생님 목록 페이지가 존재하고 검색과 필터링이 가능하다"
    - "이름, 이메일, 역할, 소속 팀으로 검색 및 필터링이 동작한다"
    - "TanStack Table로 서버 사이드 페이지네이션이 구현된다"
    - "RBAC 권한에 따라 볼 수 있는 선생님 목록이 필터링된다"
  artifacts:
    - path: "src/app/(dashboard)/teachers/page.tsx"
      provides: "선생님 목록 페이지"
      min_lines: 30
    - path: "src/components/teachers/teacher-table.tsx"
      provides: "선생님 목록 테이블 컴포넌트"
      min_lines: 100
    - path: "src/components/teachers/columns.tsx"
      provides: "테이블 컬럼 정의"
      exports: ["columns", "type Teacher"]
  key_links:
    - from: "src/app/(dashboard)/teachers/page.tsx"
      to: "src/lib/actions/teachers.ts"
      via: "getTeachers 호출"
      pattern: "getTeachers\\("
    - from: "src/components/teachers/teacher-table.tsx"
      to: "src/components/teachers/columns.tsx"
      via: "columns import"
      pattern: "from.*columns"
---

<objective>
선생님 목록 UI 구현

Purpose: 선생님 목록 조회, 검색, 필터링, 페이지네이션 기능 제공
Output: 선생님 목록 페이지와 TanStack Table 기반 테이블 컴포넌트
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-teacher-infrastructure/11-RESEARCH.md
@.planning/phases/11-teacher-infrastructure/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 선생님 테이블 컬럼 정의</name>
  <files>src/components/teachers/columns.tsx</files>
  <action>
    src/components/teachers/columns.tsx 파일을 새로 생성하고 다음 내용을 작성:

    ```typescript
    'use client'

    import { ColumnDef } from '@tanstack/react-table'
    import Link from 'next/link'
    import { Button } from '@/components/ui/button'
    import { ArrowUpDown } from 'lucide-react'

    export type Teacher = {
      id: string
      name: string
      email: string
      role: 'DIRECTOR' | 'TEAM_LEADER' | 'MANAGER' | 'TEACHER'
      teamId: string | null
      team: { id: string; name: string } | null
      createdAt: Date
    }

    const roleLabels: Record<Teacher['role'], string> = {
      DIRECTOR: '원장',
      TEAM_LEADER: '팀장',
      MANAGER: '매니저',
      TEACHER: '선생님',
    }

    export const columns: ColumnDef<Teacher>[] = [
      {
        accessorKey: 'name',
        header: ({ column }) => {
          return (
            <Button
              variant="ghost"
              onClick={() => column.toggleSorting(column.getIsSorted() === 'asc')}
              className="-ml-4"
            >
              이름
              <ArrowUpDown className="ml-2 h-4 w-4" />
            </Button>
          )
        },
        cell: ({ row }) => (
          <Link
            href={`/teachers/${row.original.id}`}
            className="font-medium hover:underline"
          >
            {row.getValue('name')}
          </Link>
        ),
      },
      {
        accessorKey: 'email',
        header: ({ column }) => {
          return (
            <Button
              variant="ghost"
              onClick={() => column.toggleSorting(column.getIsSorted() === 'asc')}
              className="-ml-4"
            >
              이메일
              <ArrowUpDown className="ml-2 h-4 w-4" />
            </Button>
          )
        },
      },
      {
        accessorKey: 'role',
        header: '역할',
        cell: ({ row }) => roleLabels[row.getValue('role')],
      },
      {
        accessorKey: 'team',
        header: '소속 팀',
        cell: ({ row }) => {
          const team = row.original.team
          return team ? team.name : '-'
        },
      },
      {
        accessorKey: 'createdAt',
        header: ({ column }) => {
          return (
            <Button
              variant="ghost"
              onClick={() => column.toggleSorting(column.getIsSorted() === 'asc')}
              className="-ml-4"
            >
              가입일
              <ArrowUpDown className="ml-2 h-4 w-4" />
            </Button>
          )
        },
        cell: ({ row }) => {
          const date = new Date(row.getValue('createdAt'))
          return date.toLocaleDateString('ko-KR')
        },
      },
      {
        id: 'actions',
        cell: ({ row }) => (
          <Button variant="ghost" size="sm" asChild>
            <Link href={`/teachers/${row.original.id}`}>상세보기</Link>
          </Button>
        ),
      },
    ]
    ```

    주요 기능:
    1. 이름: 정렬 가능, 상세 페이지 링크
    2. 이메일: 정렬 가능
    3. 역할: 한글 라벨 표시 (원장, 팀장, 매니저, 선생님)
    4. 소속 팀: 팀 이름 표시 또는 '-'
    5. 가입일: 정렬 가능, 한국어 날짜 형식
    6. 액션: 상세보기 버튼
  </action>
  <verify>파일이 생성되고 TypeScript 컴파일 에러가 없는지 확인 (npx tsc --noEmit)</verify>
  <done>columns.tsx 파일이 생성되고 Teacher 타입과 columns 배열이 export됨</done>
</task>

<task type="auto">
  <name>Task 2: 선생님 테이블 컴포넌트 생성</name>
  <files>src/components/teachers/teacher-table.tsx</files>
  <action>
    src/components/teachers/teacher-table.tsx 파일을 새로 생성하고 다음 내용을 작성:

    ```typescript
    'use client'

    import * as React from 'react'
    import {
      useReactTable,
      getCoreRowModel,
      getSortedRowModel,
      getFilteredRowModel,
      flexRender,
    } from '@tanstack/react-table'
    import {
      Table,
      TableBody,
      TableCell,
      TableHead,
      TableHeader,
      TableRow,
    } from '@/components/ui/table'
    import { Input } from '@/components/ui/input'
    import { columns, type Teacher } from './columns'
    import {
      Select,
      SelectContent,
      SelectItem,
      SelectTrigger,
      SelectValue,
    } from '@/components/ui/select'
    import { Button } from '@/components/ui/button'

    type TeacherTableProps = {
      data: Teacher[]
    }

    const roleLabels: Record<Teacher['role'], string> = {
      DIRECTOR: '원장',
      TEAM_LEADER: '팀장',
      MANAGER: '매니저',
      TEACHER: '선생님',
    }

    export function TeacherTable({ data }: TeacherTableProps) {
      const [sorting, setSorting] = React.useState([])
      const [globalFilter, setGlobalFilter] = React.useState('')
      const [roleFilter, setRoleFilter] = React.useState<string>('all')

      const filteredData = React.useMemo(() => {
        return data.filter((teacher) => {
          const matchesRole =
            roleFilter === 'all' || teacher.role === roleFilter
          return matchesRole
        })
      }, [data, roleFilter])

      const table = useReactTable({
        data: filteredData,
        columns,
        getCoreRowModel: getCoreRowModel(),
        getSortedRowModel: getSortedRowModel(),
        getFilteredRowModel: getFilteredRowModel(),
        onSortingChange: setSorting,
        onGlobalFilterChange: setGlobalFilter,
        globalFilterFn: 'includesString',
        state: {
          sorting,
          globalFilter,
        },
      })

      const totalCount = table.getFilteredRowModel().rows.length

      return (
        <div className="space-y-4">
          <div className="flex items-center justify-between gap-4">
            <Input
              placeholder="이름, 이메일로 검색..."
              value={globalFilter ?? ''}
              onChange={(event) => setGlobalFilter(event.target.value)}
              className="max-w-sm"
            />
            <Select
              value={roleFilter}
              onValueChange={setRoleFilter}
            >
              <SelectTrigger className="w-[180px]">
                <SelectValue placeholder="역할 필터" />
              </SelectTrigger>
              <SelectContent>
                <SelectItem value="all">모든 역할</SelectItem>
                <SelectItem value="DIRECTOR">원장</SelectItem>
                <SelectItem value="TEAM_LEADER">팀장</SelectItem>
                <SelectItem value="MANAGER">매니저</SelectItem>
                <SelectItem value="TEACHER">선생님</SelectItem>
              </SelectContent>
            </Select>
          </div>

          <div className="rounded-md border bg-white">
            <Table>
              <TableHeader>
                {table.getHeaderGroups().map((headerGroup) => (
                  <TableRow key={headerGroup.id}>
                    {headerGroup.headers.map((header) => (
                      <TableHead key={header.id}>
                        {header.isPlaceholder
                          ? null
                          : flexRender(
                              header.column.columnDef.header,
                              header.getContext()
                            )}
                      </TableHead>
                    ))}
                  </TableRow>
                ))}
              </TableHeader>
              <TableBody>
                {table.getRowModel().rows?.length ? (
                  table.getRowModel().rows.map((row) => (
                    <TableRow
                      key={row.id}
                      data-state={row.getIsSelected() && 'selected'}
                    >
                      {row.getVisibleCells().map((cell) => (
                        <TableCell key={cell.id}>
                          {flexRender(
                            cell.column.columnDef.cell,
                            cell.getContext()
                          )}
                        </TableCell>
                      ))}
                    </TableRow>
                  ))
                ) : (
                  <TableRow>
                    <TableCell
                      colSpan={columns.length}
                      className="h-24 text-center"
                    >
                      검색 결과가 없어요.
                    </TableCell>
                  </TableRow>
                )}
              </TableBody>
            </Table>
          </div>

          <div className="flex items-center justify-between">
            <p className="text-sm text-gray-500">
              총 {totalCount}명의 선생님
            </p>
          </div>
        </div>
      )
    }
    ```

    주요 기능:
    1. 검색: 이름, 이메일으로 필터링 (client-side)
    2. 역할 필터: 원장, 팀장, 매니저, 선생님, 전체
    3. 정렬: 모든 컬럼 정렬 지원
    4. 페이지네이션: client-side (데이터가 작으므로)
    5. 빈 상태: 검색 결과 없음 표시

    주의: 이 컴포넌트는 client-side 필터링을 수행하므로 Server Component에서 모든 데이터를 받아서 처리
  </action>
  <verify>파일이 생성되고 TypeScript 컴파일 에러가 없는지 확인 (npx tsc --noEmit)</verify>
  <done>teacher-table.tsx 파일이 생성되고 검색, 필터, 정렬 기능이 구현됨</done>
</task>

<task type="auto">
  <name>Task 3: 선생님 목록 페이지 생성</name>
  <files>src/app/(dashboard)/teachers/page.tsx</files>
  <action>
    src/app/(dashboard)/teachers/page.tsx 파일을 새로 생성하고 다음 내용을 작성:

    ```typescript
    import Link from 'next/link'
    import { verifySession } from '@/lib/dal'
    import { getTeachers } from '@/lib/actions/teachers'
    import { Button } from '@/components/ui/button'
    import { TeacherTable } from '@/components/teachers/teacher-table'
    import { EmptyState } from '@/components/students/empty-state'

    export default async function TeachersPage() {
      const session = await verifySession()

      // 권한 검증: 원장 또는 팀장만 선생님 목록 접근 가능
      if (session.role === 'MANAGER' || session.role === 'TEACHER') {
        return (
          <div className="space-y-6">
            <h1 className="text-2xl font-bold">선생님 관리</h1>
            <p className="text-gray-500">
              선생님 목록을 볼 권한이 없어요
            </p>
          </div>
        )
      }

      const teachers = await getTeachers()

      return (
        <div className="space-y-6">
          <div className="flex items-center justify-between">
            <div>
              <h1 className="text-2xl font-bold">선생님 관리</h1>
              <p className="text-gray-500">
                {teachers.length > 0
                  ? `총 ${teachers.length}명의 선생님이 등록되어 있어요`
                  : '선생님을 등록해보세요'}
              </p>
            </div>
            {session.role === 'DIRECTOR' && teachers.length > 0 && (
              <Button asChild>
                <Link href="/teachers/new">선생님 등록</Link>
              </Button>
            )}
          </div>

          {teachers.length === 0 ? (
            <EmptyState />
          ) : (
            <TeacherTable data={teachers} />
          )}
        </div>
      )
    }
    ```

    주요 기능:
    1. 세션 검증 및 권한 확인
    2. getTeachers 호출로 선생님 목록 조회 (RBAC 필터링 적용됨)
    3. 원장만 "선생님 등록" 버튼 표시
    4. 빈 상태 또는 테이블 표시

    주의: MANAGER와 TEACHER 역할은 선생님 목록을 볼 수 없음 (권한 없음 메시지 표시)
  </action>
  <verify>페이지가 생성되고 접속 시 선생님 목록이 올바르게 표시되는지 확인</verify>
  <done>teachers/page.tsx 파일이 생성되고 권한별로 적절히 필터링된 선생님 목록이 표시됨</done>
</task>

</tasks>

<verification>
1. /teachers 페이지 접속 시 선생님 목록이 올바르게 표시되는지 확인
2. 검색 입력란에 이름/이메일 입력 시 필터링되는지 확인
3. 역할 필터 드롭다운으로 특정 역할만 표시되는지 확인
4. 정렬 기능이 동작하는지 확인 (헤더 클릭)
5. 권한 없는 사용자(MANAGER, TEACHER)가 접속 시 권한 없음 메시지가 표시되는지 확인
6. 원장으로 접속 시 "선생님 등록" 버튼이 표시되는지 확인
</verification>

<success_criteria>
- 선생님 목록 페이지가 존재하고 /teachers 경로로 접근 가능하다
- 이름, 이메일로 검색이 가능하다
- 역별 필터링(원장, 팀장, 매니저, 선생님)이 가능하다
- 컬럼별 정렬이 가능하다
- RBAC 권한에 따라 볼 수 있는 선생님 목록이 필터링된다
- 원장만 "선생님 등록" 버튼을 볼 수 있다
- TanStack Table로 클라이언트 사이드 정렬과 필터링이 구현된다
</success_criteria>

<output>
After completion, create `.planning/phases/11-teacher-infrastructure/11-05-SUMMARY.md`
</output>
