---
phase: 11-teacher-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/db.ts
autonomous: true

must_haves:
  truths:
    - "Prisma Client Extensions로 팀 기반 자동 필터링이 구현된다"
    - "PostgreSQL RLS 정책이 테이블별로 생성된다"
    - "setRLSSessionContext 함수로 세션 변수 설정이 가능하다"
    - "원장은 모든 데이터에 접근 가능하고 팀장은 자신의 팀 데이터만 접근 가능하다"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "RLS 정책 정의"
      contains: "ALTER TABLE", "CREATE POLICY"
    - path: "src/lib/db/rbac.ts"
      provides: "Prisma Client Extensions 생성기"
      exports: ["createTeamFilteredPrisma", "setRLSSessionContext"]
    - path: "src/lib/db.ts"
      provides: "기본 db export"
      exports: ["db"]
  key_links:
    - from: "src/lib/db/rbac.ts"
      to: "prisma/schema.prisma"
      via: "Prisma Client Extensions"
      pattern: "\\$extends|\\$executeRaw"
    - from: "src/lib/db/rbac.ts"
      to: "PostgreSQL"
      via: "SET LOCAL rls."
      pattern: "current_setting\\('rls\\."
---

<objective>
팀 기반 RBAC 구현 (Prisma Extensions + PostgreSQL RLS)

Purpose: 애플리케이션 레이어와 데이터베이스 레이어 모두에서 팀 기반 접근 제어 강제 (Defense in Depth)
Output: Prisma Client Extensions, RLS 정책, 세션 컨텍스트 설정 함수
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-teacher-infrastructure/11-RESEARCH.md
@.planning/phases/11-teacher-infrastructure/11-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RLS 정책 SQL 스크립트 작성</name>
  <files>prisma/schema.prisma</files>
  <action>
    prisma/schema.prisma 파일 끝에 RLS 정책을 추가 (주석으로 SQL 스크립트 작성):

    ```prisma
    // RLS (Row-Level Security) Policies
    // Execute in psql after migration:
    //
    // -- Enable RLS on Student and Teacher tables
    // ALTER TABLE "Student" ENABLE ROW LEVEL SECURITY;
    // ALTER TABLE "Teacher" ENABLE ROW LEVEL SECURITY;
    //
    // -- Student table policies
    // CREATE POLICY "Student_director_all_access"
    // ON "Student"
    // FOR ALL
    // USING (current_setting('rls.teacher_role', TRUE) = 'DIRECTOR');
    //
    // CREATE POLICY "Student_team_isolation"
    // ON "Student"
    // FOR ALL
    // USING (
    //   current_setting('rls.teacher_role', TRUE) = 'TEAM_LEADER' AND
    //   teamId = current_setting('rls.team_id', TRUE)
    // );
    //
    // -- Teacher table policies
    // CREATE POLICY "Teacher_director_all_access"
    // ON "Teacher"
    // FOR ALL
    // USING (current_setting('rls.teacher_role', TRUE) = 'DIRECTOR');
    //
    // CREATE POLICY "Teacher_team_isolation"
    // ON "Teacher"
    // FOR ALL
    // USING (
    //   current_setting('rls.teacher_role', TRUE) = 'TEAM_LEADER' AND
    //   teamId = current_setting('rls.team_id', TRUE)
    // );
    ```

    이 SQL 스크립트는 Task 3에서 수동 실행할 것이므로 주석으로만 저장
  </action>
  <verify>schema.prisma 파일에 RLS 정책 주석이 존재하는지 확인</verify>
  <done>RLS 정책 SQL이 schema.prisma에 주석으로 작성됨</done>
</task>

<task type="auto">
  <name>Task 2: Prisma Client Extensions RBAC 모듈 생성</name>
  <files>src/lib/db/rbac.ts</files>
  <action>
    src/lib/db/rbac.ts 파일을 새로 생성하고 다음 내용을 작성:

    ```typescript
    import 'server-only'
    import { PrismaClient } from '@prisma/client'
    import { db } from '@/lib/db'

    export type TeacherRole = 'DIRECTOR' | 'TEAM_LEADER' | 'MANAGER' | 'TEACHER'

    export interface RLSSessionContext {
      teacherId: string
      role: TeacherRole
      teamId: string | null
    }

    /**
     * PostgreSQL RLS 세션 변수 설정
     * 모든 DB 쿼리 전에 호출해야 함
     */
    export async function setRLSSessionContext({
      teacherId,
      role,
      teamId,
    }: RLSSessionContext): Promise<void> {
      await db.$executeRaw`SET LOCAL rls.teacher_id = ${teacherId}`
      await db.$executeRaw`SET LOCAL rls.teacher_role = ${role}`
      if (teamId) {
        await db.$executeRaw`SET LOCAL rls.team_id = ${teamId}`
      } else {
        await db.$executeRaw`SET LOCAL rls.team_id = NULL`
      }
    }

    /**
     * 팀 필터링이 적용된 Prisma Client Extensions 생성
     * 애플리케이션 레이어에서 추가 보안 계층 제공
     */
    export function createTeamFilteredPrisma(
      teamId: string | null,
      role: TeacherRole
    ) {
      // 원장은 필터링 없이 전체 Prisma Client 반환
      if (role === 'DIRECTOR') {
        return db
      }

      // 팀장/매니저/선생님은 자신의 팀 데이터만 접근 가능
      return db.$extends({
        query: {
          $allOperations({ model, args, query }) {
            // Student와 Teacher 모델에 teamId 필터 적용
            if ((model === 'Teacher' || model === 'Student') && teamId) {
              args.where = {
                ...args.where,
                teamId,
              }
            }
            return query(args)
          },
        },
      })
    }

    /**
     * 현재 세션의 RBAC 컨텍스트로 Prisma Client 생성
     * 세션 정보가 없으면 기본 db 반환
     */
    export function getRBACPrisma(session: {
      userId: string
      role?: TeacherRole
      teamId?: string | null
    }) {
      const role = session.role || 'TEACHER'
      const teamId = session.teamId || null

      return createTeamFilteredPrisma(teamId, role)
    }
    ```

    주요 기능:
    1. setRLSSessionContext: PostgreSQL 세션 변수 설정 (RLS용)
    2. createTeamFilteredPrisma: Prisma Extensions로 팀 필터링
    3. getRBACPrisma: 세션 정보로 자동 Prisma Client 생성
  </action>
  <verify>파일이 생성되고 TypeScript 컴파일 에러가 없는지 확인 (npx tsc --noEmit)</verify>
  <done>rbac.ts 파일이 생성되고 setRLSSessionContext, createTeamFilteredPrisma, getRBACPrisma 함수가 export됨</done>
</task>

<task type="auto">
  <name>Task 3: RLS 정책 데이터베이스에 적용</name>
  <files>prisma/schema.prisma</files>
  <action>
    PostgreSQL 데이터베이스에 직접 연결하여 RLS 정책을 적용:

    1. DATABASE_URL로 psql 접속:
       ```bash
       psql $DATABASE_URL
       ```

    2. RLS 활성화 및 정책 생성 (순서대로 실행):
       ```sql
       -- Enable RLS
       ALTER TABLE "Student" ENABLE ROW LEVEL SECURITY;
       ALTER TABLE "Teacher" ENABLE ROW LEVEL SECURITY;

       -- Student table policies
       CREATE POLICY "Student_director_all_access"
       ON "Student"
       FOR ALL
       USING (current_setting('rls.teacher_role', TRUE) = 'DIRECTOR');

       CREATE POLICY "Student_team_isolation"
       ON "Student"
       FOR ALL
       USING (
         current_setting('rls.teacher_role', TRUE) = 'TEAM_LEADER' AND
         teamId = current_setting('rls.team_id', TRUE)
       );

       -- Teacher table policies
       CREATE POLICY "Teacher_director_all_access"
       ON "Teacher"
       FOR ALL
       USING (current_setting('rls.teacher_role', TRUE) = 'DIRECTOR');

       CREATE POLICY "Teacher_team_isolation"
       ON "Teacher"
       FOR ALL
       USING (
         current_setting('rls.teacher_role', TRUE) = 'TEAM_LEADER' AND
         teamId = current_setting('rls.team_id', TRUE)
       );
       ```

    3. 정책 확인:
       ```sql
       \d "Student"
       \d "Teacher"
       SELECT * FROM pg_policies WHERE tablename IN ('Student', 'Teacher');
       ```

    주의: 이 SQL은 psql에서 직접 실행해야 하며 Prisma 마이그레이션으로는 자동화되지 않음
  </action>
  <verify>psql에서 SELECT * FROM pg_policies 실행하여 4개 정책이 생성되었는지 확인</verify>
  <done>PostgreSQL에 RLS가 활성화되고 4개 정책(director_all_access 2개, team_isolation 2개)이 생성됨</done>
</task>

</tasks>

<verification>
1. 단위 테스트로 setRLSSessionContext 함수가 세션 변수를 올바르게 설정하는지 확인
2. Prisma Extensions가 teamId 필터를 올바르게 적용하는지 로그로 확인
3. psql에서 RLS 정책이 활성화되어 있는지 확인 (SELECT * FROM pg_policies)
4. test용 팀 ID로 RLS를 테스트하여 다른 팀 데이터가 필터링되는지 확인
</verification>

<success_criteria>
- Prisma Client Extensions가 생성되고 원장/팀장 역할에 따라 적절히 필터링한다
- PostgreSQL RLS 정책 4개가 생성되고 활성화된다
- setRLSSessionContext 함수가 세션 변수를 올바르게 설정한다
- 원장(DIRECTOR)은 모든 데이터에 접근 가능하고 팀장(TEAM_LEADER)은 자신의 팀 데이터만 접근 가능하다
- 데이터베이스 레이어에서 RLS가 데이터 유출을 방지한다
</success_criteria>

<output>
After completion, create `.planning/phases/11-teacher-infrastructure/11-02-SUMMARY.md`
</output>
