---
phase: 11-teacher-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/lib/session.ts
  - src/lib/actions/auth.ts
  - src/lib/dal.ts
autonomous: true

must_haves:
  truths:
    - "JWT 세션 페이로드에 role과 teamId가 포함된다"
    - "로그인 시 Teacher 테이블에서 role과 teamId를 조회하여 세션에 저장한다"
    - "verifySession 함수가 확장된 세션 정보를 반환한다"
    - "RBAC 모듈과 세션이 통합되어 각 요청마다 RLS 컨텍스트가 설정된다"
  artifacts:
    - path: "src/lib/session.ts"
      provides: "JWT 세션 생성 및 검증"
      exports: ["SessionPayload", "encrypt", "decrypt", "createSession", "verifySession"]
    - path: "src/lib/actions/auth.ts"
      provides: "로그인 Server Actions"
      exports: ["login"]
    - path: "src/lib/dal.ts"
      provides: "세션 검증 및 RLS 통합"
      exports: ["verifySession", "getCurrentTeacher", "getRBACDB"]
  key_links:
    - from: "src/lib/actions/auth.ts"
      to: "src/lib/session.ts"
      via: "createSession 호출"
      pattern: "createSession\\("
    - from: "src/lib/dal.ts"
      to: "src/lib/session.ts"
      via: "decrypt 호출"
      pattern: "decrypt\\("
    - from: "src/lib/dal.ts"
      to: "src/lib/db/rbac.ts"
      via: "setRLSSessionContext 호출"
      pattern: "setRLSSessionContext\\("
---

<objective>
세션 확장 (role, teamId JWT 포함)

Purpose: 인증된 사용자의 역할과 팀 정보를 세션에 포함하여 RBAC 적용 기반 마련
Output: 확장된 SessionPayload 타입, 수정된 login 함수, 통합된 verifySession 함수
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-teacher-infrastructure/11-RESEARCH.md
@.planning/phases/11-teacher-infrastructure/11-01-SUMMARY.md
@.planning/phases/11-teacher-infrastructure/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SessionPayload 타입 확장</name>
  <files>src/lib/session.ts</files>
  <action>
    src/lib/session.ts 파일의 SessionPayload 타입을 확장:

    기존 타입:
    ```typescript
    export type SessionPayload = {
      userId: string
      expiresAt: Date
    }
    ```

    변경 후:
    ```typescript
    export type SessionPayload = {
      userId: string
      role: 'DIRECTOR' | 'TEAM_LEADER' | 'MANAGER' | 'TEACHER'
      teamId: string | null
      expiresAt: Date
    }
    ```

    encrypt 함수에서 payload를 확장된 형식으로 변환:
    ```typescript
    export async function encrypt(payload: SessionPayload): Promise<string> {
      return new SignJWT({
        ...payload,
        expiresAt: payload.expiresAt.toISOString(),
      })
        .setProtectedHeader({ alg: 'HS256' })
        .setIssuedAt()
        .setExpirationTime('7d')
        .sign(encodedKey)
    }
    ```

    decrypt 함수에서 role과 teamId를 추출:
    ```typescript
    export async function decrypt(
      session: string | undefined
    ): Promise<SessionPayload | null> {
      if (!session) return null

      try {
        const { payload } = await jwtVerify(session, encodedKey, {
          algorithms: ['HS256'],
        })
        return {
          userId: payload.userId as string,
          role: (payload.role || 'TEACHER') as SessionPayload['role'],
          teamId: (payload.teamId || null) as string | null,
          expiresAt: new Date(payload.expiresAt as string),
        }
      } catch (error) {
        console.error('Session decryption failed:', error)
        return null
      }
    }
    ```

    주의: 기존 세션과의 호환성을 위해 role 기본값은 'TEACHER', teamId 기본값은 null
  </action>
  <verify>TypeScript 컴파일 에러 없는지 확인 (npx tsc --noEmit)</verify>
  <done>SessionPayload 타입에 role과 teamId가 추가되고 encrypt/decrypt 함수가 이를 처리함</done>
</task>

<task type="auto">
  <name>Task 2: login 함수에서 Teacher role/teamId 조회</name>
  <files>src/lib/actions/auth.ts</files>
  <action>
    src/lib/actions/auth.ts 파일의 login 함수를 수정하여 Teacher의 role과 teamId를 조회:

    기존 코드:
    ```typescript
    const teacher = await db.teacher.findUnique({
      where: { email },
    })

    if (!teacher) {
      return {
        errors: {
          _form: ["이메일 또는 비밀번호가 일치하지 않아요. 다시 확인해주세요."],
        },
      }
    }

    const passwordMatch = await argon2.verify(teacher.password, password)

    if (!passwordMatch) {
      return {
        errors: {
          _form: ["이메일 또는 비밀번호가 일치하지 않아요. 다시 확인해주세요."],
        },
      }
    }

    await createSession(teacher.id)
    ```

    변경 후:
    ```typescript
    const teacher = await db.teacher.findUnique({
      where: { email },
      select: {
        id: true,
        email: true,
        password: true,
        name: true,
        role: true,
        teamId: true,
      },
    })

    if (!teacher) {
      return {
        errors: {
          _form: ["이메일 또는 비밀번호가 일치하지 않아요. 다시 확인해주세요."],
        },
      }
    }

    const passwordMatch = await argon2.verify(teacher.password, password)

    if (!passwordMatch) {
      return {
        errors: {
          _form: ["이메일 또는 비밀번호가 일치하지 않아요. 다시 확인해주세요."],
        },
      }
    }

    await createSession(teacher.id, teacher.role, teacher.teamId)
    ```

    createSession 함수 호출 시 role과 teamId를 전달하도록 수정 (Task 3에서 createSession 함수를 수정)
  </action>
  <verify>login 함수가 teacher의 role과 teamId를 select 하고 createSession에 전달하는지 확인</verify>
  <done>login 함수가 Teacher의 role과 teamId를 조회하여 세션 생성 시 전달함</done>
</task>

<task type="auto">
  <name>Task 3: createSession 함수 시그니처 변경</name>
  <files>src/lib/session.ts</files>
  <action>
    src/lib/session.ts 파일의 createSession 함수를 수정하여 role과 teamId를 받도록 변경:

    기존 함수:
    ```typescript
    export async function createSession(userId: string): Promise<void> {
      const expiresAt = new Date(Date.now() + 7 * 24 * 60 * 60 * 1000)
      const session = await encrypt({ userId, expiresAt })
      const cookieStore = await cookies()

      cookieStore.set('session', session, {
        httpOnly: true,
        secure: process.env.NODE_ENV === 'production',
        expires: expiresAt,
        sameSite: 'lax',
        path: '/',
      })
    }
    ```

    변경 후:
    ```typescript
    export async function createSession(
      userId: string,
      role: SessionPayload['role'] = 'TEACHER',
      teamId: string | null = null
    ): Promise<void> {
      const expiresAt = new Date(Date.now() + 7 * 24 * 60 * 60 * 1000)
      const session = await encrypt({ userId, role, teamId, expiresAt })
      const cookieStore = await cookies()

      cookieStore.set('session', session, {
        httpOnly: true,
        secure: process.env.NODE_ENV === 'production',
        expires: expiresAt,
        sameSite: 'lax',
        path: '/',
      })
    }
    ```

    updateSession 함수도 마찬가지로 role과 teamId를 유지하도록 수정:
    ```typescript
    export async function updateSession(
      userId: string,
      role: SessionPayload['role'],
      teamId: string | null
    ): Promise<void> {
      const cookieStore = await cookies()
      const session = cookieStore.get('session')?.value
      const payload = await decrypt(session)

      if (!payload) return

      const oneDayFromNow = new Date(Date.now() + 24 * 60 * 60 * 1000)
      if (payload.expiresAt < oneDayFromNow) {
        await createSession(userId, role, teamId)
      }
    }
    ```

    주의: 기존 코드와의 호환성을 위해 role과 teamId 매개변수는 선택사항으로 설정 (기본값 제공)
  </action>
  <verify>createSession과 updateSession 함수가 role과 teamId를 받아서 JWT에 포함하는지 확인</verify>
  <done>createSession과 updateSession 함수가 role과 teamId를 받아서 세션에 저장함</done>
</task>

<task type="auto">
  <name>Task 4: verifySession 함수에서 RLS 컨텍스트 설정</name>
  <files>src/lib/dal.ts</files>
  <action>
    src/lib/dal.ts 파일을 수정하여 RLS 컨텍스트 설정을 통합:

    기존 코드:
    ```typescript
    export const verifySession = cache(async () => {
      const cookieStore = await cookies()
      const session = cookieStore.get('session')?.value
      const payload = await decrypt(session)

      if (!payload?.userId) {
        redirect('/login')
      }

      await updateSession()

      return { isAuth: true, userId: payload.userId }
    })
    ```

    변경 후:
    ```typescript
    import 'server-only'
    import { cookies } from 'next/headers'
    import { redirect } from 'next/navigation'
    import { cache } from 'react'
    import { decrypt, updateSession } from '@/lib/session'
    import { db } from '@/lib/db'
    import { setRLSSessionContext, getRBACPrisma } from '@/lib/db/rbac'

    export type VerifiedSession = {
      isAuth: true
      userId: string
      role: 'DIRECTOR' | 'TEAM_LEADER' | 'MANAGER' | 'TEACHER'
      teamId: string | null
    }

    export const verifySession = cache(async (): Promise<VerifiedSession> => {
      const cookieStore = await cookies()
      const session = cookieStore.get('session')?.value
      const payload = await decrypt(session)

      if (!payload?.userId) {
        redirect('/login')
      }

      await updateSession(payload.userId, payload.role, payload.teamId)

      // PostgreSQL RLS 세션 컨텍스트 설정
      await setRLSSessionContext({
        teacherId: payload.userId,
        role: payload.role,
        teamId: payload.teamId,
      })

      return {
        isAuth: true,
        userId: payload.userId,
        role: payload.role,
        teamId: payload.teamId,
      }
    })
    ```

    getRBACDB 헬퍼 함수 추가:
    ```typescript
    export const getRBACDB = cache(async () => {
      const session = await verifySession()
      return getRBACPrisma(session)
    })
    ```

    주의: verifySession은 모든 Server Action과 Server Component에서 호출되므로 RLS 컨텍스트 설정의 진입점
  </action>
  <verify>verifySession 함수가 role과 teamId를 반환하고 RLS 컨텍스트를 설정하는지 확인</verify>
  <done>verifySession 함수가 확장된 세션 정보를 반환하고 setRLSSessionContext를 호출하여 RLS 컨텍스트 설정함</done>
</task>

<task type="auto">
  <name>Task 5: getCurrentTeacher 함수에서 team 정보 조회</name>
  <files>src/lib/dal.ts</files>
  <action>
    src/lib/dal.ts 파일의 getCurrentTeacher 함수를 수정하여 team 정보도 조회:

    기존 코드:
    ```typescript
    export const getCurrentTeacher = cache(async () => {
      const session = await verifySession()

      const teacher = await db.teacher.findUnique({
        where: { id: session.userId },
        select: {
          id: true,
          email: true,
          name: true,
        },
      })

      if (!teacher) {
        redirect('/login')
      }

      return teacher
    })
    ```

    변경 후:
    ```typescript
    export const getCurrentTeacher = cache(async () => {
      const session = await verifySession()

      const teacher = await db.teacher.findUnique({
        where: { id: session.userId },
        select: {
          id: true,
          email: true,
          name: true,
          role: true,
          teamId: true,
          team: {
            select: {
              id: true,
              name: true,
            },
          },
        },
      })

      if (!teacher) {
        redirect('/login')
      }

      return teacher
    })
    ```

    주의: team 정보는 조인으로 조회하며 teamId가 NULL인 경우 team은 null이 됨
  </action>
  <verify>getCurrentTeacher 함수가 teacher의 role과 team 정보를 조회하는지 확인</verify>
  <done>getCurrentTeacher 함수가 teacher의 role과 team 정보를 포함하여 반환함</done>
</task>

</tasks>

<verification>
1. 로그인 후 JWT 토큰을 디코딩하여 role과 teamId가 포함되어 있는지 확인
2. verifySession 함수 호출 후 PostgreSQL 세션 변수(rsl.teacher_role, rls.team_id)가 설정되어 있는지 psql로 확인
3. 로그인한 사용자의 role에 따라 적절한 데이터가 필터링되는지 테스트
4. 기존 세션(이전 형식)과의 호환성이 유지되는지 확인 (롤백 시나리오)
</verification>

<success_criteria>
- JWT 세션 페이로드에 role과 teamId가 포함된다
- 로그인 시 Teacher 테이블에서 role과 teamId를 조회하여 세션에 저장한다
- verifySession 함수가 확장된 세션 정보(userId, role, teamId)를 반환한다
- 모든 요청마다 setRLSSessionContext가 호출되어 RLS 컨텍스트가 설정된다
- RBAC 모듈과 세션이 통합되어 각 요청마다 적절한 데이터 필터링이 적용된다
</success_criteria>

<output>
After completion, create `.planning/phases/11-teacher-infrastructure/11-03-SUMMARY.md`
</output>
