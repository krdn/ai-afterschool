# Plan 08-07: Environment Variable Management

```yaml
wave: 1
depends_on: []
files_modified:
  - .env.example
  - .env.development
  - .env.production
  - .dockerignore
  - README.md
autonomous: true
```

## Objective

환경별 (dev/staging/prod) 설정 파일을 분리해서 관리합니다. `.dockerignore`로 환경변수 파일을 Docker 이미지에서 제외하고, 각 환경에 맞는 설정 템플릿을 제공합니다.

## Tasks

<tasks>
<task>
<action>Create environment-specific configuration files</action>
<file>.env.development</file>
<details>
Create `.env.development`:

```bash
# Development Environment Configuration
# Copy this file to .env.local for local development

# Application
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ai_afterschool_dev?connection_limit=10"

# NextAuth
NEXTAUTH_SECRET="development-secret-change-in-production"
NEXTAUTH_URL="http://localhost:3000"

# PDF Storage (local filesystem for development)
PDF_STORAGE_TYPE=local
PDF_STORAGE_PATH="./public/reports"

# MinIO (for development testing with S3 storage)
# Uncomment to test S3 storage locally
# MINIO_ENDPOINT=localhost:9000
# MINIO_REGION=us-east-1
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin
# MINIO_BUCKET=reports

# Cloudinary (image upload)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Claude AI
ANTHROPIC_API_KEY=your-anthropic-api-key

# Logging
LOG_LEVEL=debug
```
</details>
</task>

<task>
<action>Create production environment template</action>
<file>.env.production</file>
<details>
Create `.env.production`:

```bash
# Production Environment Configuration
# DO NOT commit this file to version control
# Copy .env.production.example and fill in actual values

# Application
NODE_ENV=production
NEXT_PUBLIC_APP_URL=https://ai-afterschool.example.com

# Database (Docker Compose networking)
DATABASE_URL="postgresql://postgres:CHANGE_ME_PASSWORD@postgres:5432/ai_afterschool?connection_limit=10"

# NextAuth
NEXTAUTH_SECRET="CHANGE_ME_generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="https://ai-afterschool.example.com"

# PDF Storage (MinIO/S3 for production)
PDF_STORAGE_TYPE=s3
PDF_STORAGE_PATH="./public/reports"
MINIO_ENDPOINT=minio:9000
MINIO_REGION=us-east-1
MINIO_ACCESS_KEY=CHANGE_ME_minioadmin
MINIO_SECRET_KEY=CHANGE_ME_generate-strong-password
MINIO_BUCKET=reports

# Cloudinary (image upload)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Claude AI
ANTHROPIC_API_KEY=your-anthropic-api-key

# Caddy (SSL)
CADDY_EMAIL=admin@example.com

# Logging
LOG_LEVEL=info

# Security
# Set these to strong values in production!
# Generate passwords: openssl rand -base64 32
```
</details>
</task>

<task>
<action>Create staging environment template</action>
<file>.env.staging</file>
<details>
Create `.env.staging`:

```bash
# Staging Environment Configuration
# Used for pre-production testing

# Application
NODE_ENV=production
NEXT_PUBLIC_APP_URL=https://staging.ai-afterschool.example.com

# Database
DATABASE_URL="postgresql://postgres:staging_password@postgres:5432/ai_afterschool_staging?connection_limit=10"

# NextAuth
NEXTAUTH_SECRET="staging-secret-generate-with-openssl"
NEXTAUTH_URL="https://staging.ai-afterschool.example.com"

# PDF Storage (MinIO for staging)
PDF_STORAGE_TYPE=s3
MINIO_ENDPOINT=minio:9000
MINIO_REGION=us-east-1
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin-staging
MINIO_BUCKET=reports-staging

# Cloudinary
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Claude AI
ANTHROPIC_API_KEY=your-anthropic-api-key

# Caddy
CADDY_EMAIL=admin@example.com

# Logging
LOG_LEVEL=debug

# Security
# Change these values for actual staging deployment
```
</details>
</task>

<task>
<action>Update .env.example with all variables</action>
<file>.env.example</file>
<details>
Update `.env.example` with complete variable list:

```bash
# Environment Configuration Template
# Copy this file to .env.local (development) or .env.production (production)

# =============================================================================
# Application
# =============================================================================
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# Database
# =============================================================================
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database?options
# Production: Use 'postgres' as host (Docker service name)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ai_afterschool?connection_limit=10"

# =============================================================================
# NextAuth.js Authentication
# =============================================================================
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"

# =============================================================================
# PDF Storage
# =============================================================================
# Storage type: 'local' (filesystem) or 's3' (MinIO/S3)
PDF_STORAGE_TYPE=local

# Local storage path (used when PDF_STORAGE_TYPE=local)
PDF_STORAGE_PATH="./public/reports"

# MinIO/S3 configuration (used when PDF_STORAGE_TYPE=s3)
MINIO_ENDPOINT=minio:9000
MINIO_REGION=us-east-1
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=reports

# =============================================================================
# Cloudinary (Image Upload)
# =============================================================================
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# =============================================================================
# Claude AI Integration
# =============================================================================
ANTHROPIC_API_KEY=your-anthropic-api-key

# =============================================================================
# Caddy (Reverse Proxy & SSL)
# =============================================================================
# Email for Let's Encrypt certificate expiration notices
CADDY_EMAIL=admin@example.com

# =============================================================================
# Logging & Monitoring
# =============================================================================
# Log level: debug, info, warn, error
LOG_LEVEL=info

# =============================================================================
# Docker Compose Overrides (production)
# =============================================================================
# PostgreSQL credentials (for docker-compose.prod.yml)
POSTGRES_DB=ai_afterschool
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# MinIO credentials (for docker-compose.prod.yml)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
```
</details>
</task>

<task>
<action>Update .dockerignore to exclude all environment files</action>
<file>.dockerignore</file>
<details>
Update `.dockerignore` with comprehensive exclusions:

```dockerignore
# =============================================================================
# Environment Files (CRITICAL - Prevents secret leakage)
# =============================================================================
.env
.env.*
.env.local
.env.*.local
!.env.example

# =============================================================================
# Dependencies
# =============================================================================
node_modules
npm-debug.log
yarn-error.log
package-lock.json
yarn.lock

# =============================================================================
# Next.js
# =============================================================================
.next/
out/
build/
dist/

# =============================================================================
# Testing
# =============================================================================
coverage/
.nyc_output/
*.test.ts
*.test.tsx
__tests__/
tests/

# =============================================================================
# Git & CI/CD
# =============================================================================
.git/
.gitignore
.github/
.gitlab-ci.yml

# =============================================================================
# Documentation
# =============================================================================
README.md
CHANGELOG.md
LICENSE
*.md
docs/

# =============================================================================
# Planning & Meta
# =============================================================================
.planning/
.vscode/
.idea/
*.swp
*.swo

# =============================================================================
# Backup & Temp Files
# =============================================================================
backups/
*.backup
*.bak
*.tmp
*.temp

# =============================================================================
# Security & Secrets
# =============================================================================
*.pem
*.key
*.crt
*.cer
*.p12
*.pfx
secrets/

# =============================================================================
# OS Files
# =============================================================================
.DS_Store
Thumbs.db
desktop.ini

# =============================================================================
# Development Tools
# =============================================================================
.eslintrc.js
.prettierrc
.editorconfig
```
</details>
</task>

<task>
<action>Create environment setup documentation</action>
<file>README.md</file>
<details>
Add "Environment Configuration" section to README.md:

```markdown
## Environment Configuration

### Quick Start

1. **For development:**
   ```bash
   cp .env.development .env.local
   # Edit .env.local with your values
   npm run dev
   ```

2. **For production:**
   ```bash
   cp .env.production .env
   # Edit .env with production values
   docker-compose -f docker-compose.prod.yml up -d
   ```

### Environment Files

| File | Purpose | Committed |
|------|---------|-----------|
| `.env.example` | Template with all variables | ✅ Yes |
| `.env.development` | Development settings | ✅ Yes |
| `.env.production` | Production settings | ✅ Yes |
| `.env.staging` | Staging settings | ✅ Yes |
| `.env.local` | Your local overrides | ❌ No |
| `.env` | Production runtime | ❌ No |

### Required Variables

**Minimum for development:**
```bash
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ai_afterschool"
NEXTAUTH_SECRET="any-secret-for-dev"
ANTHROPIC_API_KEY="your-key"
```

**Minimum for production:**
```bash
DATABASE_URL="postgresql://user:pass@postgres:5432/db"
NEXTAUTH_SECRET="generated-with-openssl"
NEXTAUTH_URL="https://yourdomain.com"
MINIO_ACCESS_KEY="strong-password"
MINIO_SECRET_KEY="strong-password"
```

### Security Best Practices

1. **Never commit secrets:**
   - `.env` and `.env.local` are in `.gitignore`
   - Only `.env.example` and templates are committed

2. **Generate strong secrets:**
   ```bash
   # NextAuth secret
   openssl rand -base64 32

   # Database password
   openssl rand -base64 24

   # MinIO credentials
   openssl rand -base64 16
   ```

3. **Verify .dockerignore:**
   ```bash
   # Check that .env files are excluded
   docker build --progress=plain -f Dockerfile.prod . | grep -i "\.env"
   # Should return no results
   ```

4. **Use Docker secrets (production):**
   ```yaml
   # docker-compose.prod.yml
   services:
     app:
       secrets:
         - database_password
         - minio_secret_key

   secrets:
     database_password:
       external: true
     minio_secret_key:
       external: true
   ```

### Variable Reference

**Application:**
- `NODE_ENV`: development | production
- `NEXT_PUBLIC_APP_URL`: Public URL (must be https in production)

**Database:**
- `DATABASE_URL`: PostgreSQL connection string
- `connection_limit=10`: Recommended for Docker

**Storage:**
- `PDF_STORAGE_TYPE`: local | s3
- `MINIO_ENDPOINT`: S3 endpoint URL
- `MINIO_BUCKET`: Default bucket name

**NextAuth:**
- `NEXTAUTH_SECRET`: Required, generate with openssl
- `NEXTAUTH_URL`: Must match public URL

**AI:**
- `ANTHROPIC_API_KEY`: Required for AI features
```
</details>
</task>

<task>
<action>Create environment validation script</action>
<file>scripts/validate-env.ts</file>
<details>
Create `scripts/validate-env.ts`:

```typescript
#!/usr/bin/env tsx
/**
 * Validate environment variables before starting application
 *
 * Usage:
 *   npm run validate:env
 *   npm run validate:env -- --production
 */

const requiredVars = {
  development: [
    'DATABASE_URL',
    'NEXTAUTH_SECRET',
  ],
  production: [
    'DATABASE_URL',
    'NEXTAUTH_SECRET',
    'NEXTAUTH_URL',
    'MINIO_ENDPOINT',
    'MINIO_ACCESS_KEY',
    'MINIO_SECRET_KEY',
    'ANTHROPIC_API_KEY',
  ],
}

const isProduction = process.argv.includes('--production')
const env = isProduction ? 'production' : 'development'
const required = requiredVars[env as keyof typeof requiredVars]

let missing: string[] = []
let invalid: string[] = []

console.log(`🔍 Validating ${env} environment...\n`)

// Check for missing variables
for (const variable of required) {
  const value = process.env[variable]
  if (!value) {
    missing.push(variable)
  } else if (value.includes('CHANGE_ME') || value === 'your-secret-key-here') {
    invalid.push(variable)
  }
}

// Print results
if (missing.length > 0) {
  console.error('❌ Missing required variables:')
  missing.forEach(v => console.error(`   - ${v}`))
  console.error('')
}

if (invalid.length > 0) {
  console.error('⚠️  Variables with placeholder values:')
  invalid.forEach(v => console.error(`   - ${v}`))
  console.error('')
}

if (missing.length === 0 && invalid.length === 0) {
  console.log('✅ All required variables are set!')
  console.log(`\nChecked ${required.length} variables`)
  process.exit(0)
} else {
  console.error('❌ Environment validation failed')
  console.error('\nFix the issues above before starting the application.')
  process.exit(1)
}
```

Add to package.json:
```json
{
  "scripts": {
    "validate:env": "tsx scripts/validate-env.ts",
    "validate:env:prod": "tsx scripts/validate-env.ts --production"
  }
}
```
</details>
</task>
</tasks>

## Verification

After completion, verify:

- [ ] `.env.development` created with development configuration
- [ ] `.env.production` created with production configuration
- [ ] `.env.staging` created with staging configuration
- [ ] `.env.example` updated with all variables and descriptions
- [ ] `.dockerignore` excludes all .env files except .env.example
- [ ] README.md includes environment configuration section
- [ ] `scripts/validate-env.ts` created and functional
- [ ] `npm run validate:env` works for development
- [ ] `npm run validate:env:prod` works for production
- [ ] Environment files clearly document required vs optional variables
- [ ] Security best practices documented

## must_haves

For Phase 8 success criteria to be met:

1. **DEPLOY-04:** `.dockerignore`로 환경변수 파일을 Docker 이미지에서 제외할 수 있다
2. **DEPLOY-05:** 환경별 (dev/staging/prod) 설정 파일을 분리해서 관리할 수 있다
3. **Success Criteria #7:** 환경변수가 적절히 격리됨 (Docker 이미지에 포함되지 않음)

## Notes

- `.env.local` is for local overrides and should never be committed
- `.env` is created in production from `.env.production` template
- `.dockerignore` prevents accidental secret inclusion in Docker images
- Validation script helps catch configuration errors before deployment
- Environment variables should be the only source of secrets - no hardcoded values
