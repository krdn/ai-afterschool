---
phase: 38-autocomplete-ui-chatinput-integration
plan: 03
type: execute
wave: 3
depends_on: [38-02]
files_modified:
  - src/hooks/use-chat-stream.ts
  - src/components/chat/chat-page.tsx
autonomous: true
requirements: [MENT-01, MENT-02]
must_haves:
  truths:
    - "useChatStream.sendMessage가 mentions[] 파라미터를 POST body에 포함하여 서버에 전달한다"
    - "ChatPage.handleSend가 ChatInput에서 받은 mentions[]를 useChatStream에 전달한다"
    - "멘션 없는 기존 채팅 동작이 하위 호환성을 유지한다"
  artifacts:
    - path: "src/hooks/use-chat-stream.ts"
      provides: "mentions 파라미터가 확장된 채팅 스트리밍 훅"
      exports: ["useChatStream"]
    - path: "src/components/chat/chat-page.tsx"
      provides: "mentions[] 전달이 통합된 ChatPage"
      contains: "mentions"
  key_links:
    - from: "src/components/chat/chat-page.tsx"
      to: "src/hooks/use-chat-stream.ts"
      via: "sendMessage({ mentions })"
      pattern: "sendMessage.*mentions"
    - from: "src/hooks/use-chat-stream.ts"
      to: "/api/chat"
      via: "fetch POST body.mentions"
      pattern: "mentions"
    - from: "src/components/chat/chat-page.tsx"
      to: "src/components/chat/chat-input.tsx"
      via: "handleSend(prompt, mentions, providerId)"
      pattern: "handleSend"
---

<objective>
useChatStream 훅과 ChatPage 컴포넌트에 mentions[] 전달 파이프라인을 연결한다

Purpose: ChatInput에서 수집된 mentions[] 배열이 useChatStream을 거쳐 POST /api/chat body에 포함되도록 하여, Phase 36에서 구현한 서버 사이드 멘션 처리 파이프라인(mention-resolver → context-builder → dynamic system prompt)과 연결한다.
Output: mentions[]가 클라이언트에서 서버까지 완전히 전달되며, 기존 멘션 없는 채팅이 하위 호환성을 유지함
</objective>

<execution_context>
@/home/gon/.claude/get-shit-done/workflows/execute-plan.md
@/home/gon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-autocomplete-ui-chatinput-integration/38-02-SUMMARY.md
@src/hooks/use-chat-stream.ts
@src/components/chat/chat-page.tsx
@src/components/chat/chat-input.tsx
@src/lib/chat/mention-types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useChatStream SendMessageOptions에 mentions 추가 + POST body 전달</name>
  <files>src/hooks/use-chat-stream.ts</files>
  <action>
`src/hooks/use-chat-stream.ts`를 수정하여 mentions[] 파라미터를 지원한다.

**1. MentionItem 타입 import:**
```typescript
import type { MentionItem } from '@/lib/chat/mention-types'
```

**2. SendMessageOptions 타입 확장:**
```typescript
type SendMessageOptions = {
  prompt: string
  providerId?: string
  sessionId?: string
  messages?: Array<{ role: "user" | "assistant"; content: string }>
  mentions?: MentionItem[]   // 추가
}
```

**3. sendMessage 함수 내 body에 mentions 추가:**

기존 destructuring에 `mentions` 추가:
```typescript
const { prompt, providerId, sessionId, messages, mentions } = options
```

fetch body에 mentions 포함:
```typescript
body: JSON.stringify({
  prompt,
  providerId,
  sessionId,
  messages,
  mentions,  // 추가 — undefined이면 JSON 직렬화에서 자동 제외
}),
```

**하위 호환성:** `mentions`가 undefined이면 `JSON.stringify`가 해당 키를 제외하므로 기존 동작과 동일. 서버(POST /api/chat)는 이미 `mentions?: MentionItem[]` optional 필드로 body를 파싱함 (Phase 36-04에서 구현 완료).
  </action>
  <verify>`pnpm tsc --noEmit` — 타입 에러 없음. SendMessageOptions에 mentions 필드 확인.</verify>
  <done>useChatStream.sendMessage가 mentions[]를 POST body에 포함하여 서버에 전달하며, 멘션 없으면 기존 동작 유지</done>
</task>

<task type="auto">
  <name>Task 2: ChatPage handleSend 시그니처 변경 + mentions[] 전달</name>
  <files>src/components/chat/chat-page.tsx</files>
  <action>
`src/components/chat/chat-page.tsx`를 수정하여 ChatInput의 새 onSend 시그니처와 맞추고, mentions[]를 useChatStream에 전달한다.

**1. MentionItem 타입 import:**
```typescript
import type { MentionItem } from '@/lib/chat/mention-types'
```

**2. handleSend 시그니처 변경:**
```typescript
// 기존: async (prompt: string, providerId?: string)
// 변경:
const handleSend = useCallback(
  async (prompt: string, mentions: MentionItem[], providerId?: string) => {
    ...
  },
  [messages, sendMessage, sessionId, router, refreshSessions]
)
```

**3. sendMessage 호출에 mentions 추가:**
```typescript
const result = await sendMessage({
  prompt,
  providerId,
  sessionId,
  messages: contextMessages.length > 0 ? contextMessages : undefined,
  mentions: mentions.length > 0 ? mentions : undefined,
})
```
- `mentions.length > 0`일 때만 전달하여 불필요한 빈 배열 전송 방지.

**4. handleSuggestionClick 수정:**
```typescript
const handleSuggestionClick = useCallback(
  (text: string) => {
    handleSend(text, [])  // 제안 클릭은 멘션 없음 → 빈 배열
  },
  [handleSend]
)
```

**5. initialQuery 처리 수정:**
```typescript
useEffect(() => {
  if (initialQuery && !initialSessionId) {
    handleSend(initialQuery, [])  // 초기 쿼리는 멘션 없음
  }
}, [])
```

**하위 호환성 보장:**
- `mentions: []` → `mentions.length > 0`이 false → sendMessage에 mentions 미전달 → 서버에서 기존 동작
- ChatInput의 `onSend` 시그니처가 `(prompt: string, mentions: MentionItem[], providerId?: string)`로 변경되었으므로 ChatPage도 동일하게 맞춤
  </action>
  <verify>
1. `pnpm tsc --noEmit` — 타입 에러 없음
2. `pnpm build` — 빌드 성공
3. ChatPage.handleSend 시그니처에 mentions: MentionItem[] 확인
4. sendMessage 호출에 mentions 전달 확인
5. handleSuggestionClick이 빈 배열로 handleSend 호출 확인
  </verify>
  <done>ChatPage가 ChatInput의 mentions[]를 수신하여 useChatStream에 전달하고, 멘션 없는 기존 동작이 하위 호환됨</done>
</task>

</tasks>

<verification>
1. `pnpm tsc --noEmit` — 타입 에러 없음
2. `pnpm build` — 빌드 성공
3. 전체 파이프라인 확인:
   - ChatInput @멘션 선택 → onSend(plainText, mentionItems, providerId)
   - ChatPage handleSend(prompt, mentions, providerId)
   - useChatStream sendMessage({ prompt, mentions, providerId, ... })
   - fetch POST body에 mentions 포함
   - 서버 /api/chat에서 mentions 수신 (Phase 36-04 구현 완료)
4. 하위 호환성: 멘션 없는 일반 메시지 전송 시 mentions 필드 미포함
</verification>

<success_criteria>
- useChatStream.sendMessage의 SendMessageOptions에 mentions?: MentionItem[] 포함
- ChatPage.handleSend가 mentions[]를 수신하여 sendMessage에 전달
- POST /api/chat body에 mentions 배열이 포함됨
- 멘션 없는 기존 채팅이 정상 동작함 (하위 호환)
- TypeScript 빌드 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/38-autocomplete-ui-chatinput-integration/38-03-SUMMARY.md`
</output>
